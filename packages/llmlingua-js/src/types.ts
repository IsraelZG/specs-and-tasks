export interface LLMLinguaConfig { modelName?: string; targetRatio?: number; headSize?: number; tailSize?: number; device?: 'cpu' | 'wasm'; } export interface TokenInfo { text: string; perplexity: number; kept: boolean; } export interface CompressResult { compressed: string; originalLength: number; compressedLength: number; ratio: number; tokensRemoved: number; tokens: TokenInfo[]; }