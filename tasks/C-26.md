---
id: C-26
title: "Cleanup: estaleiro-ui tests (4 nao-bloqueantes)"
status: ready
complexity: 2
target_agent: deepseek
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: []
blocks: []
capacity_target: haiku
test_profile: backend
---

# C-26 · Cleanup: estaleiro-ui tests (4 nao-bloqueantes)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo — use `pnpm --filter @plataforma/estaleiro-ui`
- **Test Runner:** `vitest` via `@plataforma/estaleiro-ui` — JSDOM + React Testing Library
- **Capacidade-alvo:** haiku (mecânico — 4 achados de cobertura, sem novidade algorítmica)

## 1. Objetivo
Resolver 4 achados não-bloqueantes de cobertura de testes em `apps/estaleiro/ui/`.

## 2. Contexto RAG (Spec-Driven Development)
- [ ] `apps/estaleiro/ui/tests/ws-client.test.ts` — teste WS com `freePort()` duplicando WSS (achado EST-14a)
- [ ] `apps/estaleiro/ui/src/views/cost/CostView.test.tsx` — mocka `useCost` inteiro, cobertura ZERO do hook (achado EST-14e)
- [ ] `apps/estaleiro/ui/src/views/cost/hooks.ts` — hook real cujo teste é inexistente
- [ ] `apps/estaleiro/ui/src/views/terminal/AgentTerminal.tsx` — componente sem teste próprio (achado DMM-08)
- [ ] `apps/estaleiro/ui/src/views/fleet/hooks.ts` — `useAgentStream` usado pelo AgentTerminal
- [ ] `apps/estaleiro/ui/tests/smoke.test.ts` — só testa importabilidade (achado DMM-09: não renderiza ExecutionView/FlowGrid)
- [ ] `apps/estaleiro/ui/tests/BoardView.test.tsx` — padrão de teste existente (referência de estilo)

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[UPDATE]** `apps/estaleiro/ui/tests/ws-client.test.ts` — achado [i3]: freePort() cria 2 WSS separados no caso 1; refatorar para reaproveitar a instância de freePort como WSS do teste
- **[UPDATE]** `apps/estaleiro/ui/src/views/cost/CostView.test.tsx` — achado [i4]: `vi.mock('./hooks.js')` mocka o hook inteiro; trocar por `vi.spyOn(await import('./hooks.js'), 'useCost')` ou mock seletivo, mantendo os casos existentes verdes
- **[CREATE]** `apps/estaleiro/ui/src/views/cost/__tests__/hooks.test.ts` — 1-2 casos do hook `useCost` real (sem mock), exercitando o caminho do `costStore`
- **[CREATE]** `apps/estaleiro/ui/src/views/terminal/__tests__/AgentTerminal.test.tsx` — 1-2 testes de smoke do componente AgentTerminal (mockando `useAgentStream` do `../../fleet/hooks.js`)
- **[CREATE]** `apps/estaleiro/ui/tests/execution/WorkflowTree.test.tsx` — ⚠ divergente: `WorkflowTree.tsx` não existe mais na source (coverage reflete estado anterior). O ExecutionView atual usa `FlowGrid` de `@plataforma/ui-engines`. Alternativa: testar o smoke de renderização do ExecutionView com FlowGrid. Este path e abordagem precisam de decisão do arquiteto (ver Seção 6).

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Framework:** Vitest + React Testing Library + JSDOM (padrão do pacote `@plataforma/estaleiro-ui`)
- [ ] **Métricas/Cobertura:** Testar os 4 achados sem regredir os testes existentes
- [ ] **Casos enumerados:**
  1. `ws-client.test.ts`: freePort() retorna uma porta; o mesmo WSS é usado como servidor do teste (vs. criar 2 WSS). Verificar que todos os 5 casos existentes continuam passando.
  2. `CostView.test.tsx`: `useCost` é spyado (não mockado inteiro). Verificar que os 2 casos existentes passam.
  3. `hooks.test.ts`: 1 caso — `useCost` retorna estado inicial (loading=true, costs=[]) sem store populado. 2º caso opcional — `useCost` reflete dados inseridos no costStore.
  4. `AgentTerminal.test.tsx`: 1 caso — renderiza placeholder "Digite um taskId" quando taskId vazio. 2º caso — renderiza linhas de log mockadas via `useAgentStream`.
  5. `WorkflowTree.test.tsx` OU execução: **decisão pendente (Seção 6)** — criar smoke que renderiza ExecutionView com FlowGrid mockado similar a `ExecutionView.test.tsx`.
- [ ] **Ambiente do Teste:** Node puro (JSDOM, sem browser)
- [ ] **Fora de Escopo:** Testes end-to-end (Playwright), testes de integração cross-package, refatoração de componentes
> **Se a task afeta UI** (frontend_agent ou escopo em `apps/*-frontend/**`): unit/JSDOM NÃO
> basta. Especifique aqui um **smoke Playwright** (browser real) OU exija a verificação manual
> do revisor (subir o app e exercitar o fluxo) — o `agile-reviewer` trata isso como BLOCKER de
> processo. Marque `ui: true` no frontmatter para deixar explícito.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - NÃO refatore além dos achados listados (escopo fechado)
> - NÃO adicione dependências novas

> **Regra: Disposição obrigatória por achado.** Cada achado abaixo DEVE ser fechado com um
> destino da taxonomia: `fixed` (ref arquivo/commit), `no-op` (justificativa em 1 linha),
> ou `defer→T-YYY` (task futura nomeada).

1. **[i3][EST-14a][estaleiro/ui]** `freePort()` no `ws-client.test.ts` cria 2 `WebSocketServer` separados no caso 1. Track: reaproveitar instância.
2. **[i4][EST-14e][estaleiro/ui]** Cobertura de teste do hook `useCost` é ZERO — `CostView.test.tsx:8` faz `vi.mock('./hooks.js')` mockando o hook inteiro. Track: criar `hooks.test.ts` com 1-2 casos.
3. **[i1][DMM-08][estaleiro-ui/test]** Sem teste próprio do `AgentTerminal`. Track: adicionar `__tests__/AgentTerminal.test.tsx` com 1-2 testes de smoke.
4. **[i3][DMM-09][estaleiro-ui/test]** Smoke test cobre só "pacote é importável" — não renderiza WorkflowTree. Track: `tests/execution/WorkflowTree.test.tsx`.

## 6. Feedback de Especificação (Spec Feedback Loop)
> **DECIDIDO (arquiteto, 2026-07-27):** Opção 1 — Criar `tests/execution/ExecutionView.smoke.test.tsx` (reaproveitando a fixture de grafo) exercitando o `ExecutionView` com o `FlowGrid` refatorado para garantir zero crash.

## 7. Definition of Done (DoD) & Reviewer Checklist
- [ ] Código segue estritamente os arquivos de Output da §3 (sem criar arquivos extras)
- [ ] `pnpm --filter @plataforma/estaleiro-ui test` roda verde, sem regressão
- [ ] `pnpm --filter @plataforma/estaleiro-ui lint` ZERO erros novos (rode baseline ANTES de tocar)
- [ ] A implementação respeita a Regra do Que Não Fazer (sem refatorar além dos achados, sem dependências novas)
- [ ] Testes não usam `vi.mock()` para hooks de domínio do mesmo pacote (exceto para mockar `useAgentStream` em AgentTerminal, que é hook de integração WS)
- [ ] Export público esperado: N/A — task de cobertura de testes, sem novos exports de barrel

### Verificação automática *(comandos exatos — worker E reviewer rodam e COLAM a saída)*
```bash
pnpm --filter @plataforma/estaleiro-ui test       # vitest — precisa ficar verde
pnpm --filter @plataforma/estaleiro-ui lint       # eslint — ZERO erros novos
```
> **GATE DE EVIDÊNCIA:** nem o `finish` (worker) nem o veredito (reviewer) são válidos sem a
> saída literal desses comandos colada na seção 8. Marcar `[x]` sem evidência é violação.
> 
> **Nota:** `pnpm gate` do estaleiro é `pnpm --filter @plataforma/estaleiro-ui` build/test/lint. Como esta task não altera source (só test), build já está verde de antemão — mas `test` e `lint` são obrigatórios.

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
**Disposição por achado (obrigatória):** liste CADA achado da §5 com seu destino:
- `- [id] → fixed (ref) | no-op (justificativa) | defer→T-YYY`
Handover sem disposição per-item é inválido e será rejeitado pelo reviewer.

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória — colar saída de build/tsc + test + lint):**
```
(cole aqui a saída real de pnpm build, pnpm test e pnpm lint)
```
- **Comentários de Revisão:**

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.

| Data | Ator | Ação | Detalhes |
|------|------|------|----------|
| 2026-07-24 | deepseek | endurecer | Editou frontmatter p/ `draft:pending_decision`. Corrigiu paths divergentes (AgentTerminal em terminal/ não fleet/; WorkflowTree inexistente). Adicionou Seção 6 com 3 opções p/ arquiteto. |
- **[2026-07-27T14:09]** - *gemini* - `[Decidido]`: decisão: Opção 1 (ExecutionView.smoke.test.tsx)
- **[2026-07-27T14:13]** - *gemini* - `[Promovida p/ ready]`: promover C-26 para ready
