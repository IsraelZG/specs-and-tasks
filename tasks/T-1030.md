---
id: T-1030
title: "Migracao do eixo spec_status para draft:<sub> + consumidores (ledger/hardening/generate)"
status: ready
complexity: 4
target_agent: logic_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-1028", "T-1029"]
blocks: ["T-1031"]
spec_status: hardened
capacity_target: sonnet
hardened_at: "2026-07-01T20:30:00-03:00"
hardened_by: minimax-m3
decisions: []
---

# T-1030 · Migração spec_status → draft:<sub> + consumidores (ledger/hardening/generate)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Repo:** Docs (controle) — scripts em `tools/scripts/`. O `nexus-backend` já está com a máquina de estados nova (T-1028/T-1029).
- **Test Runner:** os scripts `.mjs` têm self-check `--dry-run`; sem vitest aqui.
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Fechar a transição do eixo `spec_status` para o `status: draft:<sub>`:
1. **Script de migração** `tools/scripts/migrate-status-axis.mjs` — converte as ~203 tasks `draft` para o sub-status equivalente, remove `spec_status`/`hardened_at`, e semeia a entrada de ledger + linha de Log que o gate anti-drift exige.
2. **Consumidores** — reescreve `nextAction()` em `ledger.mjs`, adapta `hardening.mjs` (lê sub-status do `status` + staleness do Log) e `generate-task.mjs` (default `draft:placeholder`, sem `spec_status`).

Depende de T-1028 (status/verbos existem) e T-1029 (auto-promote existe — a migração reavalia os `draft:hardened` elegíveis).

## 2. Contexto RAG (Spec-Driven Development)
- [ ] [T-1028](./T-1028.md) — `TaskStatus` novos, helper `baseStatus()`, verbos.
- [ ] [T-1029](./T-1029.md) — auto-promote (a migração usa o serviço p/ promover os hardened elegíveis).
- [ ] `tools/scripts/ledger.mjs` §159-176 (`nextAction`, `depsOk`) e §189-204 (coletor JSON que lê `spec_status`).
- [ ] `tools/scripts/hardening.mjs` §51-106 (lê `spec_status`/`hardened_at`).
- [ ] `tools/scripts/generate-task.mjs` §27-30 (template `spec_status: draft` + comentários) e §118-125.
- [ ] `apps/nexus-backend/src/services/task.service.ts` — usar `TaskService` via `dist/` para semear ledger/promover (mesmo padrão do `manage-task.mjs`), OU escrever a entrada de ledger direto (ver §3).

### Mapa de migração (status atual `draft` + `spec_status` → novo `status`)
| spec_status atual | nº tasks | novo status |
|---|---|---|
| (ausente) / `draft` | 131 | `draft:placeholder` |
| `triaged` | 13 | `draft:triaged` |
| `hardened` | 43 | `draft:hardened` → **reavaliar**: se deps todas done → `ready` |
| `blocked-decision` | 11 | `draft:pending_decision` |
| `decomposed` | 5 | `draft:decomposed` |

Tasks com `status` ≠ `draft` (ready/in_progress/review/rework/done/blocked): **mantêm o status**, só removem `spec_status`/`hardened_at`. Total draft = 203 (131+13+43+11+5).

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[CREATE]** `tools/scripts/migrate-status-axis.mjs` — migração idempotente, com `--dry-run` (default) e `--apply`.
- **[UPDATE]** `tools/scripts/ledger.mjs` — `nextAction()` sem `specStatus`; coletor sem `spec_status`.
- **[UPDATE]** `tools/scripts/hardening.mjs` — sub-status do `status`; staleness do Log `[Endurecido]`.
- **[UPDATE]** `tools/scripts/generate-task.mjs` — default `status: draft:placeholder`; remove linhas de template `spec_status`/`hardened_at` (mantém `decisions` se usado pela Seção 6).

### Contratos exatos

```js
// --- ledger.mjs: nextAction reescrito (specStatus REMOVIDO) ---
function nextAction(task) {
  const { status, depsOk } = task;
  if (status === 'in_progress')            return 'busy';
  if (status === 'in_review')              return 'busy';   // ← claimed: orquestrador NÃO despacha
  if (status === 'review')                 return 'review';
  if (status === 'rework')                 return 'rework';
  if (status === 'ready')                  return 'work';
  if (status === 'draft:pending_decision') return 'decide';
  if (status === 'draft:decomposed')       return null;     // container — nunca despacha
  if (status === 'draft:hardened')         return depsOk ? 'promote' : null;
  if (status === 'draft:triaged')          return depsOk ? 'harden' : null;
  if (status === 'draft:placeholder' || status === 'draft') return 'triage';
  return null;
}
```

```
migrate-status-axis.mjs — algoritmo:
  para cada arquivo em tasks/ e meta-tasks/ (exceto _*, INDEX.md, LEDGER.md):
    lê status + spec_status
    se status !== 'draft' e não tem spec_status/hardened_at → skip (nada a fazer)
    se status === 'draft':
       novo = mapa[spec_status || 'draft']
       reescreve linha `status:` para `novo`
       remove linhas `spec_status:` e `hardened_at:` (e `hardened_by:` se houver)
       --apply: append no ledger `.nexus/transitions.jsonl`:
                { ts, id, from: statusAntigo, to: novo, action: 'migrate', agent: 'system' }
                (só se o topo do ledger daquele id ≠ novo — idempotente)
       --apply: append no Log §9: `[Migrado]` "spec_status:<x> → status:<novo>"
    regenera INDEX ao final (via TaskService.rebuildIndexes ou rebuild-index.mjs)
  --apply, passo final: para cada task agora em `draft:hardened`, se deps todas done,
       chama TaskService.transition(id, 'promote', 'system', 'migração: deps done') → ready
       (reusa o auto-promote/serviço da T-1029; NÃO reimplemente a checagem de deps)
  imprime contagem por bucket (131/13/43/11/5) e quantas promoveram.
```

**`hardening.mjs`:** trocar `get('spec_status')` por derivar de `baseStatus(status)`/o sub-status; a coluna "hardened_at" passa a ler o timestamp da última linha `[Endurecido]` no Log §9 da task (regex simples), já que o campo foi removido.

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Self-check:** `migrate-status-axis.mjs --dry-run` imprime o plano (contagens por bucket) sem escrever nada; rodar ANTES do `--apply`.
- [x] **Idempotência:** rodar `--apply` duas vezes seguidas — a segunda não faz nada (todas já em `draft:*`), sem duplicar ledger/Log.
- [x] **Pós-migração:** `node tools/scripts/ledger.mjs --json --idle` roda sem erro e classifica os sub-status corretamente; `node tools/scripts/hardening.mjs` roda sem erro.
- [x] **Fora de escopo:** vitest do backend (T-1028/T-1029 já cobriram a máquina).

### Gate específico (colar saída):
```bash
node tools/scripts/migrate-status-axis.mjs --dry-run     # plano: contagens por bucket
node tools/scripts/migrate-status-axis.mjs --apply       # aplica
node tools/scripts/migrate-status-axis.mjs --dry-run     # 2ª vez: "nada a migrar"
node tools/scripts/ledger.mjs --json --idle | node -e "process.stdin.pipe(process.stdout)" | head -c 400
node tools/scripts/hardening.mjs | head -20
```

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** edite o markdown das tasks à mão para migrar — é o script que faz, uniformemente, com entrada de ledger. Editar `status` na mão sem ledger dispara `StatusDriftError` na próxima transição.
> - **NÃO** reimplemente a checagem de deps/auto-promote — reuse o `TaskService` da T-1029.
> - **NÃO** rode `--apply` sem antes conferir o `--dry-run` (203 arquivos).
> - Este é o repo de controle: **NÃO** rode git. Enfileire via `fila.mjs add` ao final.

### Pegadinhas conhecidas
- **Gate anti-drift:** o serviço compara o topo do `.nexus/transitions.jsonl` daquele id com o `status:` do arquivo. Por isso a migração PRECISA semear a entrada `migrate` no ledger — senão a primeira transição pós-migração de cada task quebra com `StatusDriftError`.
- **CRLF:** os arquivos podem ter `\r\n` (Windows). Normalize na leitura (`.replace(/\r\n/g,'\n')`) mas preserve o EOL ao reescrever, ou o diff fica gigante.
- **`ledger.mjs` coletor:** remova a leitura de `spec_status` (§196) e o campo `specStatus` do objeto — mas confira se algo mais no arquivo usa (o markdown-mode e o `--json`).
- **203 arquivos:** o script é I/O pesado — rode local, não via tool que trava (ver PITFALLS/ memória do usuário sobre comandos longos). Se demorar, o worker roda no terminal e cola a saída.

1. Escreva `migrate-status-axis.mjs` com `--dry-run`/`--apply` e o mapa.
2. Reescreva `nextAction()` e o coletor em `ledger.mjs`.
3. Adapte `hardening.mjs` e `generate-task.mjs`.
4. `--dry-run` → confira 131/13/43/11/5 → `--apply` → `--dry-run` de novo (idempotência).
5. Rode `ledger.mjs --json --idle` e `hardening.mjs` para confirmar consumidores ok. Cole tudo na Seção 8.

## 6. Feedback de Especificação (Spec Feedback Loop)
- *[Nenhum problema identificado. Se a contagem por bucket divergir de 131/13/43/11/5 no dry-run, PARE e registre — o backlog mudou desde o endurecimento (2026-07-01) e o mapa precisa reconferência, não é bug do script.]*

## 7. Definition of Done (DoD) & Reviewer Checklist
- [ ] `migrate-status-axis.mjs` idempotente; `--dry-run` mostra 131/13/43/11/5; `--apply` migra e semeia ledger+Log.
- [ ] Nenhuma task com `spec_status`/`hardened_at` restante após `--apply` (`grep -rl spec_status tasks/ meta-tasks/` vazio).
- [ ] `draft:hardened` com deps done viram `ready` na migração.
- [ ] `ledger.mjs nextAction` reescrito; `in_review`→busy; `--json --idle` roda.
- [ ] `hardening.mjs` e `generate-task.mjs` adaptados; `generate-task` cria `draft:placeholder`.
- [ ] Zero `StatusDriftError` numa transição de smoke pós-migração (ex.: `manage-task.mjs triage <alguma>`).

### Verificação automática
```bash
node tools/scripts/migrate-status-axis.mjs --dry-run
node tools/scripts/migrate-status-axis.mjs --apply
grep -rl "spec_status" tasks/ meta-tasks/ || echo "OK: nenhum spec_status restante"
node tools/scripts/ledger.mjs --json --idle >/dev/null && echo "ledger OK"
node tools/scripts/hardening.mjs >/dev/null && echo "hardening OK"
```
> **GATE DE EVIDÊNCIA:** saída literal na Seção 8.

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
-

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória):**
```
(cole aqui a saída real de --dry-run, --apply, e os greps de verificação)
```
- **Comentários de Revisão:**

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-07-01T19:30]** - *deepseek* - `[Promovida p/ ready]`: spec_status hardened — flip draft→ready
