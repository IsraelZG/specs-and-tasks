---
id: T-206
title: "Heartbeat implícito/explícito + evicção"
status: ready
spec_status: hardened
capacity_target: sonnet
decisions: []
hardened_at: "2026-06-29T19:30:00-03:00"
complexity: 3
target_agent: core_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-205", "T-003"]
blocks: []
---

# T-206 · Heartbeat implícito/explícito + evicção

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro, VirtualClock)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Estender `SwarmRegistry` (T-205) com heartbeat implícito (tráfego reseta timer), explícito (PING 8B após 15s ocioso), evicção (3 falhas/45s → cool-off 5min), modo economia (bateria <15% desativa PING explícito). Tudo sob VirtualClock
*(derivado de `docs/plano-de-implementacao.md` M2 — T-206; `docs/conceitos/swarm-registry.md`)*.

### Contratos exatos
```ts
// --- packages/transport/src/SwarmRegistry.ts (extensão) ---
import { PeerId, ClockPort } from '@plataforma/protocol';

export interface HeartbeatOptions {
  pingIntervalMs?: number;      // default 15000 (15s)
  evictionThresholdMs?: number; // default 45000 (45s, ou seja, 3× ping)
  coolOffMs?: number;           // default 300000 (5min)
  batteryPort?: { level: number }; // <15% → desativa ping explícito
}

export class SwarmRegistry {
  // Construtor estendido para aceitar clock e opções de heartbeat
  constructor(clock: ClockPort, opts?: HeartbeatOptions);

  // ... API existente (T-205)

  /** Registra tráfego recebido (heartbeat implícito). */
  recordTraffic(peerId: PeerId): void;

  /** Dispara heartbeat explícito se ocioso > pingInterval. Retorna peers que precisam de PING. */
  checkHeartbeats(): PeerId[];

  /** Processa timeout: remove e retorna peers para evicção. Coloca-os em cool-off. */
  evictStale(): PeerId[];
}
```
- **Cool-off Logic:** Ao evitar um peer em `evictStale()`, a data de término de cool-off é calculada como `clock.now() + coolOffMs`. Durante esse período, qualquer chamada para `upsert()` tentando definir o estado do peer como `'connected'` ou `'connecting'` é ignorada, mantendo o estado como `'disconnected'`.
- **Battery Port Mode:** Se `batteryPort` for informado e `batteryPort.level < 15`, `checkHeartbeats()` não emitirá solicitações de PING explícitas (sendo mantido o comportamento de evicção se o peer ultrapassar o timeout de qualquer forma).

## 2. Contexto RAG
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (M2 — T-206)
- [T-205 · SwarmRegistry](./T-205.md)
- [swarm-registry.md](../docs/conceitos/swarm-registry.md)

## 3. Escopo de Arquivos
- **[READ]** `packages/transport/src/SwarmRegistry.ts` (T-205)
- **[UPDATE]** `packages/transport/src/SwarmRegistry.ts`
- **[CREATE]** `packages/transport/tests/heartbeat.test.ts`

## 4. Estratégia de Testes
- [x] **Framework:** Vitest + VirtualClock (T-003).
- [x] **Ambiente:** `pnpm --filter @plataforma/transport test`.

Casos de teste:
1. Tráfego recebido → `lastSeenAt` atualizado, sem PING.
2. 15s ocioso → `checkHeartbeats` retorna peer para PING.
3. 3 PINGs sem resposta (45s) → `evictStale` retorna peer.
4. Cool-off 5min → peer não reconecta antes do tempo (upsert para connected é ignorado).
5. Bateria <15% → `checkHeartbeats` não retorna PING.
6. Evicção remove peer de candidatos a líder de sync.

## 5. Instruções de Execução
1. Escreva testes com VirtualClock.
2. Implemente heartbeat implícito/explícito.
3. Implemente evicção + cool-off + modo economia.
4. Rode build + test.

## 6. Feedback de Especificação
- *(Nenhuma pendência)*

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/transport build
pnpm --filter @plataforma/transport test
```

### Checklist
- [ ] Heartbeat implícito (tráfego zera timer)?
- [ ] Evicção 3 falhas → cool-off 5min?
- [ ] Modo economia desativa PING?
- [ ] 6 casos de teste passando?
- [ ] `pnpm --filter @plataforma/transport build` e `test` verdes?

## 8. Log de Handover e Revisão Agile
### Handover do Executor:
-

### Parecer do Agente Revisor:
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução:**
```
(cole aqui a saída real de pnpm build e pnpm test)
```

## 9. Log de Execução
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-06-29T23:21]** - *Antigravity* - `[Promovida p/ ready]`: spec_status hardened — flip draft→ready
