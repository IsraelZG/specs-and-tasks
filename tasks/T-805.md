---
id: T-805
title: "Reidratação no browser"
status: draft
spec_status: triaged
capacity_target: sonnet
complexity: 4
target_agent: frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-801", "T-804"]
blocks: []
---

# T-805 · Pipeline de reidratação no browser

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `playwright` (E2E browser)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar pipeline de reidratação no browser: Service Worker intercepta `/blobs/{infohash}` → MessageChannel → Sync Worker → adapter → Crypto Worker (zero-copy transferables) → ReadableStream/SourceBuffer; tag_region buscada primeiro; backpressure 20/5 MB; chunks decifrados nunca expostos ao JS da página
*(derivado de `docs/plano-de-implementacao.md` §11 T-805)*.

### Contratos exatos
```ts
// --- packages/workers/src/rehydration/pipeline.ts ---

export interface RehydrationRequest {
  infoHash: string;
  range?: { start: number; end?: number };
  priority: 'streaming' | 'prefetch';
}

export interface RehydrationConfig {
  backpressureHighWaterMark: number;  // default 20 MB
  backpressureLowWaterMark: number;   // default 5 MB
  maxConcurrentChunks: number;        // default 4
}

export interface RehydrationPipeline {
  /** Inicia reidratação: intercepta /blobs/{infohash} no Service Worker. */
  intercept(request: Request): Promise<Response>;

  /** Pipeline: Service Worker → MessageChannel → Sync Worker → Crypto Worker. */
  startPipeline(request: RehydrationRequest): Promise<ReadableStream<Uint8Array>>;

  /** Busca tag_region primeiro (metadados de decifração). */
  fetchTagRegion(infoHash: string): Promise<Uint8Array>;

  /** Aplica backpressure (pausa quando buffer > 20 MB). */
  applyBackpressure(bufferSize: number): boolean;

  /** Cria SourceBuffer para <video>/<audio>. */
  createMediaSource(infoHash: string): Promise<MediaSource>;
}
```
- Service Worker intercepta `GET /blobs/{infohash}` → MessageChannel.
- Fluxo: Service Worker → MessageChannel → Sync Worker (seleciona adapter) → Crypto Worker (decifra) via zero-copy transferables.
- `tag_region` buscada primeiro — contém nonces e checksums para decifração.
- Backpressure: pausa quando buffer > 20 MB, retoma quando < 5 MB.
- Chunks decifrados **nunca** expostos ao JS da página principal (isolamento via Worker).
- `<video>` toca MP4 cifrado via P2P/WebSeed.

## 2. Contexto RAG
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (§11 T-805)
- [T-801a · Chunking](./T-801a.md) — InfoHash, chunks
- [T-804 · WebSeed](./T-804.md) — fallback cloud

## 3. Escopo de Arquivos
- **[READ]** `packages/core/src/media/chunking.ts` (chunks — T-801a)
- **[READ]** `apps/system-peer/src/media/webSeed.ts` (T-804)
- **[CREATE]** `packages/workers/src/rehydration/pipeline.ts`
- **[CREATE]** `apps/web/public/sw.js` (Service Worker — reidratação)
- **[CREATE]** `apps/bancada/e2e/rehydration.test.ts` (Playwright E2E)
- **[UPDATE]** `packages/workers/src/index.ts` — re-export

## 4. Estratégia de Testes
- [x] **Framework:** Playwright (E2E browser).
- [x] **Ambiente:** `pnpm --filter bancada test:e2e` (Anel 3).

Casos de teste (Playwright):
1. `<video>` toca MP4 cifrado via P2P/WebSeed.
2. `tag_region` buscada antes dos chunks de dados.
3. Backpressure: pausa quando buffer > 20 MB.
4. Backpressure: retoma quando buffer < 5 MB.
5. Chunks decifrados não expostos ao JS da página (asserção de escopo).
6. PAUSE/RESUME observados sob throttling do Playwright.
7. Fallback para WebSeed quando zero seeders P2P.

## 5. Instruções de Execução
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** exponha chunks decifrados ao JS da página principal.
> - **NÃO** pule a busca da tag_region antes dos chunks.

1. Implemente Service Worker com interceptação de `/blobs/{infohash}`.
2. Implemente pipeline: SW → Sync Worker → Crypto Worker com transferables.
3. Implemente backpressure (20/5 MB).
4. Escreva teste Playwright E2E com vídeo cifrado.
5. Rode build + test.

## 6. Feedback de Especificação
- [Nenhum problema identificado]

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/workers build
pnpm --filter bancada test:e2e
```

### Checklist
- [ ] Service Worker intercepta `/blobs/{infohash}`?
- [ ] Pipeline: SW → Sync Worker → Crypto Worker?
- [ ] tag_region buscada primeiro?
- [ ] Backpressure 20/5 MB?
- [ ] Chunks decifrados nunca expostos ao JS da página?
- [ ] 7 casos de teste E2E?
- [ ] `pnpm --filter @plataforma/workers build` verde?
