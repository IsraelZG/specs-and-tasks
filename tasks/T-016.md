---
id: T-016
title: "Implementar RFC de Tema Multi-Nível no Design System"
status: review
complexity: 3
capacity_target: sonnet
target_agent: frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-011", "T-013"]
blocks: []
---

# T-016 · Implementar RFC de Tema Multi-Nível no Design System

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest`
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar a [RFC de Temas Multi-Nível](../docs/caderno-3-sdk/09-hierarchical-theme-customization.md) na lib `@plataforma/design-system`:
1. Habilitar `outputReferences: true` no Style Dictionary (RFC §2)
2. Adicionar `compileScopedOverrides` e `overridesToStyleObject` ao `themeEngine.ts` (RFC §4–5)
3. Criar `themeOverrideKeys.ts` com validação de chaves canônicas (RFC §5)
4. Opcional: `ThemeScope.tsx` componente wrapper React (RFC §1)

*(Derivado de: `docs/caderno-3-sdk/09-hierarchical-theme-customization.md`; `docs/caderno-3-sdk/04-theme-and-i18n-data-structures.md`)*

### Contratos exatos (assinaturas TS fixadas)

```ts
// --- packages/design-system/src/lib/themeEngine.ts (novas exports) ---

/** Flat override map: "theme.intent.primary.fill" → "#7c3aed".
 *  Derivado de caderno-3-sdk/09 §5 "Formato de Declaração". */
export type ThemeOverrideMap = Record<string, string>;

/** Nível de escopo para compileScopedOverrides.
 *  Derivado de caderno-3-sdk/09 §1: data-ds-module | data-ds-page. */
export type ScopedLevel = 'module' | 'page';

/** Nome do atributo DOM por nível.
 *  Derivado de caderno-3-sdk/09 §1 invariante DOM. */
export const SCOPE_SELECTORS: Record<ScopedLevel, string>;

/**
 * Compila overrides em CSS escopado para injeção via <style>.
 * - Chaves prefixadas "theme." → --ds-theme-*
 * - Demais chaves → --ds-component-*
 * - Referências {theme.xxx} → var(--ds-theme-xxx)
 * - Valores literais → mantidos como literais
 * Derivado de caderno-3-sdk/09 §4 "Bloco global <style>" + §5 "Formato".
 */
export function compileScopedOverrides(
  overrides: ThemeOverrideMap,
  scope: ScopedLevel,
  scopeId: string
): string;

/**
 * Converte overrides para objeto de estilo inline React.
 * { "button.primary.bg": "#333" } → { "--ds-component-button-primary-bg": "#333" }
 * Derivado de caderno-3-sdk/09 §4 "Componente/Instância — inline".
 */
export function overridesToStyleObject(
  overrides: ThemeOverrideMap
): Record<string, string>;

// --- packages/design-system/src/lib/themeOverrideKeys.ts (CREATE) ---

/** Conjunto canônico de chaves de override válidas.
 *  Derivado de caderno-3-sdk/09 §5 "dicionário canônico". */
export const VALID_OVERRIDE_KEYS: ReadonlySet<string>;

/** Valida se uma chave pertence ao dicionário canônico.
 *  Derivado de caderno-3-sdk/09 §5 "chave não documentada gera erro". */
export function isValidOverrideKey(key: string): boolean;

// --- packages/design-system/src/lib/ThemeScope.tsx (CREATE, opcional) ---

/** Wrapper React que aplica atributo data-ds-module ou data-ds-page.
 *  Derivado de caderno-3-sdk/09 §1. */
export function ThemeScope(props: {
  level: ScopedLevel;
  scopeId: string;
  children: React.ReactNode;
}): JSX.Element;
```

### Regras de conversão de chave → variável CSS (derivado de caderno-3-sdk/09 §5)
- Prefixo `theme.` → `--ds-theme-` (ex: `theme.intent.primary.fill` → `--ds-theme-intent-primary-fill`)
- Sem prefixo → `--ds-component-` (ex: `card.radius` → `--ds-component-card-radius`)
- Referências `{theme.xxx}` → `var(--ds-theme-xxx)`; `{component.xxx}` → `var(--ds-component-xxx)`
- Separação de tokens por `.` → kebab-case via `toKebabCase` existente

## 2. Contexto RAG (Spec-Driven Development)
- [ ] [09-hierarchical-theme-customization.md](../docs/caderno-3-sdk/09-hierarchical-theme-customization.md) — O spec completo.
- [ ] [04-theme-and-i18n-data-structures.md](../docs/caderno-3-sdk/04-theme-and-i18n-data-structures.md) — Dicionários e validações.
- [ ] [T-011](./T-011.md) e [T-013](./T-013.md) — Configurações base. **Pré-requisitos.**

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[READ]** `packages/design-system/src/lib/themeEngine.ts` — funções existentes (`toKebabCase`, `pathToVariable`, `resolveTokenValue`)
- **[READ]** `packages/design-system/style-dictionary.config.js` — builds atuais
- **[UPDATE]** `packages/design-system/style-dictionary.config.js` — `outputReferences: false` → `true` nos builds CSS (tokens-global + theme-light/dark)
- **[UPDATE]** `packages/design-system/src/lib/themeEngine.ts` — novas exports: `ThemeOverrideMap`, `ScopedLevel`, `SCOPE_SELECTORS`, `compileScopedOverrides`, `overridesToStyleObject`
- **[CREATE]** `packages/design-system/src/lib/themeOverrideKeys.ts` — `VALID_OVERRIDE_KEYS`, `isValidOverrideKey`
- **[UPDATE]** `packages/design-system/src/index.ts` — re-export dos novos símbolos
- **[CREATE]** `packages/design-system/tests/theme-overrides.test.ts`
- **[CREATE]** `packages/design-system/src/lib/ThemeScope.tsx` — opcional

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Framework:** `vitest` (Node puro, sem JSDOM para os testes unitários de compilação de CSS)
- [x] **Ambiente do Teste:** `pnpm --filter @plataforma/design-system test`

Casos de teste (numerados) — todos derivados de caderno-3-sdk/09:

1. **Scoped CSS module:** `compileScopedOverrides({"theme.intent.primary.fill": "#333"}, "module", "chat")` → CSS contém `[data-ds-module="chat"]` e `--ds-theme-intent-primary-fill: #333`.
2. **Scoped CSS page:** `compileScopedOverrides({"card.radius": "8px"}, "page", "settings")` → CSS contém `[data-ds-page="settings"]` e `--ds-component-card-radius: 8px`.
3. **Reference resolution:** `compileScopedOverrides({"button.primary.bg": "{theme.intent.primary.fill}"}, "module", "x")` → CSS contém `var(--ds-theme-intent-primary-fill)` não valor estático.
4. **Inline style object:** `overridesToStyleObject({"button.primary.bg": "#333"})` → `{"--ds-component-button-primary-bg": "#333"}`.
5. **Kebab conversion:** `overridesToStyleObject({"theme.intent.primary.fillHover": "red"})` → chave contém `--ds-theme-intent-primary-fill-hover`.
6. **Valid key:** `isValidOverrideKey("theme.intent.primary.fill")` → `true`.
7. **Invalid key:** `isValidOverrideKey("garbage.x")` → `false`.
8. **Empty overrides:** `compileScopedOverrides({}, "module", "x")` → string CSS válida (bloco vazio).
9. **Multiple overrides:** `compileScopedOverrides` com 3+ entradas → todas aparecem no bloco CSS.
10. **No XSS:** valor contendo `</style><script>` → escapa/valida adequadamente.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** altere a lógica dos componentes visuais existentes. Foco: engine de tema.
> - **NÃO** instancie React Components complexos de teste. Foque nos utilitários exportados.
> - **NÃO** implemente validação de contraste WCAG (fora de escopo, vai em tarefa futura).

1. **[TDD]** Crie `packages/design-system/tests/theme-overrides.test.ts` com os 10 casos da Seção 4.
2. Altere `style-dictionary.config.js`: `outputReferences: false` → `true` nas linhas 52 e 77.
3. Rode `pnpm --filter @plataforma/design-system build:tokens` e inspecione o CSS de saída (`build/web/`).
4. Implemente `compileScopedOverrides` e `overridesToStyleObject` em `themeEngine.ts`:
   - `keyToCssVariable(flatKey: string): string` — converte "theme.x.y" → "--ds-theme-x-y" e "a.b" → "--ds-component-a-b"
   - `resolveReference(val: string): string` — `{theme.x}` → `var(--ds-theme-x)`, literais → literais
   - `compileScopedOverrides`: gera bloco `<style>` com seletor `[data-ds-{scope}="{scopeId}"]`
5. Crie `themeOverrideKeys.ts`: derive `VALID_OVERRIDE_KEYS` dos tokens existentes (`tokens/themes/light/theme.json`, `tokens/semantic/components.json`).
6. Re-exporte em `packages/design-system/src/index.ts`.
7. (Opcional) Crie `ThemeScope.tsx`.
8. Rode build + test (Seção 7) e cole saída.

## 6. Feedback de Especificação (Spec Feedback Loop)
- *[Nenhum problema identificado]*

## 7. Definition of Done (DoD) & Reviewer Checklist

### Verificação automática (Gate de Evidência)
```bash
pnpm --filter @plataforma/design-system build:tokens
pnpm --filter @plataforma/design-system build
pnpm --filter @plataforma/design-system test
```
> **GATE DE EVIDÊNCIA:** Worker cola saída literal na Seção 8.

### Checklist do Reviewer
- [ ] `compileScopedOverrides` gera CSS com seletor correto (`[data-ds-module|page]`)?
- [ ] `overridesToStyleObject` converte flat keys → `--ds-*` corretamente?
- [ ] Referências `{theme.xxx}` → `var(--ds-theme-xxx)`?
- [ ] `isValidOverrideKey` rejeita chaves não canônicas?
- [ ] `outputReferences: true` emite `var(--ds-...)` no CSS de tokens?
- [ ] 10 casos de teste passando?
- [ ] `pnpm --filter @plataforma/design-system build:tokens`, `build` e `test` verdes?

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- `style-dictionary.config.js`: `outputReferences: false` → `true` (2 locais)
- `themeEngine.ts`: + `compileScopedOverrides`, `overridesToStyleObject`, `SCOPE_SELECTORS`, tipos `ThemeOverrideMap`/`ScopedLevel`
- `themeOverrideKeys.ts`: `VALID_OVERRIDE_KEYS`, `isValidOverrideKey` (derivado de tokens)
- `ThemeScope.tsx`: wrapper React (`data-ds-module` / `data-ds-page`)
- `index.ts`: re-exports
- 10/10 testes new passando, 35/35 total

### Evidência de Execução (Gate):
```bash
$ pnpm --filter @plataforma/design-system build:tokens
✔ build/web/tokens-global.css
✔ build/web/theme-light.css
✔ build/web/theme-dark.css
✓ Built tokens-global.css + light/dark themes

$ pnpm --filter @plataforma/design-system build
vite v6.4.3 building for production...
✓ 150 modules transformed.
build/react/index.css  61.09 kB │ gzip: 9.72 kB
build/react/index.js  465.98 kB │ gzip: 103.13 kB
✓ built in 2.85s

$ pnpm --filter @plataforma/design-system test
$ vitest run

 ✓ tests/theme-overrides.test.ts (10 tests) 5ms
 ✓ tests/components/Tabs.test.tsx (2 tests)
 ✓ tests/components/Switch.test.tsx (3 tests)
 ✓ tests/components/Input.test.tsx (5 tests)
 ✓ tests/components/Button.test.tsx (5 tests)
 ✓ tests/components/Modal.test.tsx (5 tests)
 ✓ tests/components/Checkbox.test.tsx (5 tests)

 Test Files  7 passed (7)
      Tests  35 passed (35)
```

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Comentários de Revisão:**

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-06-22T14:30]** - *DeepSeek* - `[Iniciado]`: iniciando Tema Multi-Nível: outputReferences + compileScopedOverrides + overridesToStyleObject + themeOverrideKeys
- **[2026-06-22T14:33]** - *DeepSeek* - `[Finalizado]`: Tema Multi-Nível: outputReferences true, compileScopedOverrides, overridesToStyleObject, themeOverrideKeys, ThemeScope. 10/10 testes novos. 35/35 total. build:tokens+build+test verdes
