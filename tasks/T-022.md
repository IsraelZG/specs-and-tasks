---
id: T-022
title: "Ativar regressao visual Playwright toHaveScreenshot no design-system (gate ADR-0006)"
status: ready
complexity: 3
target_agent: frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
ui: true
dependencies: [T-021, T-019, T-012, T-015, T-018]
blocks: []
---

# T-022 · Ativar regressão visual Playwright toHaveScreenshot no design-system (gate ADR-0006)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (`pnpm build`, `pnpm test`, `pnpm lint` na raiz afetam todos os pacotes)
- **Test Runner:** `playwright` (E2E/visual) — esta task é **exclusivamente Playwright**
- **Capacidade-alvo:** sonnet *(decisões de ferramenta já fechadas pela ADR-0006; implementação é mecânica mas envolve configuração cross-arch e estratégia de baselines)*

## 1. Objetivo

Ativar regressão visual com **Playwright `toHaveScreenshot`** no `packages/design-system`,
rodando no CI. A ADR [0006](../docs/adr/0006-gate-regressao-visual.md) (T-020) decidiu esta
ferramenta e descartou o Lookout — esta task implementa essa decisão.

O gate visual é: screenshot do componente renderizado → diff contra baseline → Pass/Fail
automático no CI. Se o design muda legitimamente, aceitar nova baseline via `--update-snapshots`.

## 2. Contexto RAG (Spec-Driven Development)

A ADR-0006 é a fonte da verdade para esta task — **todas** as decisões já estão tomadas lá.
Leia-a antes de qualquer implementação.

- [x] `docs/adr/0006-gate-regressao-visual.md` — ADR de decisão (Lookout descartado;
  `toHaveScreenshot` aceito; trade-offs; pegadinhas; recorte desta task)
- [x] `tasks/T-021.md` — Preset compartilhado `createPlaywrightConfig` em `@plataforma/testkit`;
  showcase como 1º consumidor; Playwright+Chromium no CI; `webServer` com boot via
  `pnpm --filter design-system-showcase dev`
- [x] `tasks/T-019.md` — Matriz de CI: `ubuntu-latest` (x64) + `ubuntu-24.04-arm` (arm64)
- [x] `tasks/T-012.md` — Incorporação do showcase (`apps/design-system-showcase`)
- [x] `tasks/T-015.md` — Quality gate do design-system no CI (harness compartilhado)
- [x] `tasks/T-018.md` — Inventário de componentes por tier (A/B/C); **Tier B** = portais
  (`Popover`, `Tooltip`, `DropdownMenu`) — renderizam fora do container raiz

## 3. Escopo de Arquivos (Inputs e Outputs)

> **REUSO OBRIGATÓRIO — não criar infra local:** Todo o config Playwright mora no
> `@plataforma/testkit` (preset `createPlaywrightConfig`) e no showcase (consumidor). **NÃO**
> criar `playwright.config.ts` dentro de `packages/design-system`.

- **[READ]** `packages/testkit/src/playwright/index.ts` — preset `createPlaywrightConfig`
  (parâmetros `baseURL`, `webServer`, retries, timeout, etc.). Reusar sem duplicar.
- **[READ]** `apps/design-system-showcase/playwright.config.ts` — config existente do showcase
  (gerada pela T-021). Pode ser o ponto de adição das opções de screenshot.
- **[READ]** `apps/design-system-showcase/e2e/` — specs E2E existentes (funcionais). Adicionar
  asserções visuais **aos specs existentes** ou criar novos specs focados em screenshot.
- **[CREATE]** `apps/design-system-showcase/e2e/visual/` — (opcional) diretório dedicado para specs
  de regressão visual, se a equipe preferir separá-las dos E2E funcionais.
- **[CREATE]** Baselines de screenshot (gerados por Playwright — geralmente em
  `apps/design-system-showcase/test-results/` ou caminho configurado pelo snapshotDir).
  **Versionar** no git (são a "verdade" do gate).
- **[UPDATE]** `apps/design-system-showcase/playwright.config.ts` — adicionar
  `snapshotDir`/`snapshotPathTemplate` se necessário para suportar baselines por-arch (ver
  Seção 5, passo 3).

**FORA DE ESCOPO (não criar, não editar):**
- `packages/design-system/**` — não criar config/baseline/infra de gate visual no próprio
  design-system; a infra mora no consumidor (showcase) e no preset (testkit).
- `.github/workflows/ci.yml` — não criar step de CI novo; o gate visual entra pelo mesmo
  `test:e2e` da T-021.
- `lookout.config.yaml`, binário Lookout, `packages/plugins/qa-lookout/` — ADR-0006 descartou.

## 4. Estratégia de Testes Estrita (Test-Driven Development)

- [x] **Framework:** Playwright (`toHaveScreenshot`) — esta **é** a estratégia de teste.
- [x] **Métricas/Cobertura:** Cada spec deve fotografar um componente renderizado e comparar
  contra a baseline. Começar com **1 componente de cada tier** (A, B, C) para validar a
  estratégia; expandir em task futura.
- [x] **Ambiente do Teste:** Headless Chromium (Playwright CI mode). Roda nos dois runners da
  T-019 (x64 + arm64).
- [x] **Fora de Escopo:** Testes unit (Vitest/JSDOM) de componentes — isso já é coberto pela
  T-018 e task predecessoras. Esta task foca **exclusivamente** em regressão visual.
- `ui: true` — marcar no frontmatter (já marcado).

> **Smoke Playwright obrigatório:** o `agile_reviewer` trata ausência de screenshot
> funcional como BLOCKER. A evidence gate (Seção 7) exige saída literal de
> `pnpm --filter design-system-showcase build && pnpm --filter design-system-showcase test:e2e`.

## 5. Instruções de Execução (Step-by-Step)

> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** criar `playwright.config.ts` dentro de `packages/design-system` — o config mora no
>   showcase e no preset (testkit). Design-system não é consumidor direto de Playwright.
> - **NÃO** instalar/configurar o Lookout — ADR-0006 descartou.
> - **NÃO** criar step de CI novo — o gate visual entra pelo mesmo `test:e2e` da T-021.
> - **NÃO** usar `expect(...).toMatchScreenshot()` (nome antigo/deprecado) — use
>   `expect(...).toHaveScreenshot()` (API atual do Playwright).
> - **NÃO** fotografar só o container do componente para componentes com **portal**
>   (Tier B: `Popover`, `Tooltip`, `DropdownMenu`) — o portal renderiza em `document.body`,
>   fora do container. Fotografar `document.body` inteiro ou `fullPage: true`.
> - **NÃO** aceitar baselines novas sem reviewer — `--update-snapshots` requer PR + review
>   dos diffs.

### Pegadinhas conhecidas *(preencher pelo Task Architect)*

- **Antialiasing cross-arch (x64 vs arm64).** O diff de pixel pode divergir entre
  `ubuntu-latest` (x64) e `ubuntu-24.04-arm` (arm64) por renderização diferente de fontes
  (subpixel rendering). **Mitigação:** usar `maxDiffPixelRatio` (tolerância) ou baselines
  **por arquitetura** (snapshotPathTemplate com `{project.name}` que inclua `runner.arch`).
  Decidir com PoC nos dois runners (rodar `test:e2e` em ambos e comparar).
- **Portais (Tier B) fora do frame.** `Popover`, `Tooltip`, `DropdownMenu` renderizam o overlay
  em `document.body`, não dentro do container do componente. `toHaveScreenshot()` no container
  corta o portal. **Fix:** fotografar `await page.goto('/component/popover')` e capturar o
  `page` inteiro (ou `document.body`) — não o container isolado.
- **Baselines desatualizadas = falso-positivo permanente.** Toda mudança legítima de design
  (ex.: troca de tokens pela T-013) invalida as baselines. Sem processo de re-baseline, o CI
  quebra para sempre. **Processo:** `npx playwright test --update-snapshots` para aceitar,
  commit das novas baselines, review dos diffs no PR.
- **`webServer` do preset da T-021.** O showcase sobe via `pnpm --filter design-system-showcase dev`
  (não `build`). O `webServer` do `createPlaywrightConfig` faz isso automaticamente. **Não**
  subir um segundo dev server ou build separado para o screenshot.
- **Tamanho do snapshotDir.** Os baselines são imagens PNG (centenas de KB cada). Versioná-las
  no git é correto (são a verdade do gate), mas o repo cresce. Não por em `.gitignore`.

1. **[READ]** Leia a ADR-0006 completa. Entenda o recorte, as pegadinhas e os trade-offs.
2. **[READ]** Leia o preset `createPlaywrightConfig` em `@plataforma/testkit` e o
   `playwright.config.ts` do showcase. Entenda o `webServer` e como os E2E funcionais já são
   executados.
3. **[DECISÃO DE ESTRATÉGIA CROSS-ARCH]** — antes de escrever o primeiro spec, rode um PoC
   rápido: adicione `toHaveScreenshot()` a **1 componente simples** (Tier A, sem portal),
   capture baselines no runner x64, e tente rodar no runner arm64.
   - Se o diff for **zero** (mesmo pixel) → `maxDiffPixelRatio` default basta; baselines únicos.
   - Se o diff for **não-zero** (antialiasing) → decida: (a) `maxDiffPixelRatio` com tolerância
     aceitável, **ou** (b) baselines por-arch via `snapshotPathTemplate` usando
     `{project.name}`.
   - Registre a decisão na Seção 8 (handover).
4. **[IMPLEMENT]** Adicione asserções `toHaveScreenshot(...)` aos specs E2E do showcase:
   - Comece com **1 componente de cada tier** (ex.: `Button` = A, `Popover` = B, `Icon` = C).
   - Para Tier B (portais): use `page` inteiro ou `document.body`, não o container isolado.
   - Nomes de snapshot descritivos (ex.: `button-primary-default-light.png`).
5. **[BASELINES]** Capture as baselines iniciais:
   ```bash
   npx playwright test --update-snapshots
   ```
   Versione as baselines geradas (commit + push junto com o spec).
6. **[VERIFY]** Execute o gate:
   ```bash
   pnpm --filter design-system-showcase build
   pnpm --filter design-system-showcase test:e2e
   ```
   Ambos devem passar sem erro. Cole a saída na Seção 8.

## 6. Feedback de Especificação (Spec Feedback Loop)

> **ATENÇÃO:** Se a spec (RAG) for ambígua, contraditória ou o design pattern imposto for
> impossível, **PARE**. Mude o status para `blocked` e escreva o motivo abaixo. Não alucine
> uma abstração não documentada.

- *[Nenhum problema identificado — ADR-0006 fechou todas as decisões.]*

## 7. Definition of Done (DoD) & Reviewer Checklist

O agente `agile_reviewer` usará esta checklist para aprovar ou rejeitar o PR:

- [ ] Pelo menos 1 asserção `toHaveScreenshot()` por tier (A, B, C) — ou justificativa de por
  que algum tier foi excluído.
- [ ] Baselines versionadas no git (não em `.gitignore`).
- [ ] Componentes Tier B (portais) fotografam `document.body`/`fullPage`, não o container isolado.
- [ ] Estratégia cross-arch decidida e documentada (maxDiffPixelRatio **ou** baselines por-arch).
- [ ] Processo de re-baseline documentado (como aceitar nova baseline quando o design muda).
- [ ] **Nenhum** `playwright.config.ts` criado dentro de `packages/design-system`.
- [ ] **Nenhum** artefato do Lookout criado.
- [ ] `pnpm --filter design-system-showcase build` — sem erro.
- [ ] `pnpm --filter design-system-showcase test:e2e` — sem erro, sem regressão.

### Verificação automática *(comandos exatos — worker E reviewer rodam e COLAM a saída)*
```bash
pnpm --filter design-system-showcase build        # tsc/Vite — precisa terminar sem erro
pnpm --filter design-system-showcase test:e2e      # Playwright (funcional + visual) — precisa ficar verde
```
> **GATE DE EVIDÊNCIA:** nem o `finish` (worker) nem o veredito (reviewer) são válidos sem a
> saída literal desses comandos colada na seção 8. Marcar `[x]` sem evidência é violação.

## 8. Log de Handover e Revisão Agile (Code Review)

### Handover do Executor:
-

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória — colar saída de build/tsc + test):**
```
(cole aqui a saída real de pnpm --filter design-system-showcase build e pnpm --filter design-system-showcase test:e2e)
```
- **Comentários de Revisão:**

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
