---
id: C-19
title: "Cleanup: plugin-tasks (migration scripts) — 9 não-bloqueantes de EST-04a/b/c"
status: done
complexity: 2
target_agent: logic_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: []
blocks: []
capacity_target: haiku
---

# C-19 · Cleanup: plugin-tasks (migration scripts) — 9 não-bloqueantes de EST-04a/b/c

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v22+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest`
- **Package:** `@plataforma/plugin-tasks`
- **Capacidade-alvo:** haiku

## 1. Objetivo
Resolver 9 achados não-bloqueantes de código nos scripts de migração em `packages/plugin-tasks/scripts/migrate/`. Itens de spec/decisão foram roteados ao SPEC-PENDENCIAS.

## 2. Contexto RAG (Spec-Driven Development)
- Tasks de origem: EST-04a (parser), EST-04b (runner, report), EST-04c (validate)
- Código-fonte em `C:\Dev2026\superapp\packages\plugin-tasks\scripts\migrate\`

## 3. Escopo de Arquivos
- **[UPDATE]** `packages/plugin-tasks/tsconfig.json` (EST-04b m1 — adicionar `"scripts"` ao include)
- **[UPDATE]** `packages/plugin-tasks/scripts/migrate/parser.ts` (EST-04a m1, m2; EST-04b m1, i2, i3)
- **[UPDATE]** `packages/plugin-tasks/scripts/migrate/validate.ts` (EST-04c i1 — filtrar SKIP_FILES)
- **[UPDATE]** `packages/plugin-tasks/package.json` (EST-04b i1 — `lint` cobrir `scripts/`)

## 4. Lista de Achados

> **Regra:** Cada achado DEVE ser fechado com destino: `fixed` (ref), `no-op` (justificativa), `defer→T-YYY` (task futura nomeada).

### EST-04a (scripts/migrate — parser)
- [m1] `parseTaskMd` não envolve `matter(raw)` em try/catch — erro derruba pipeline. → `fixed (parser.ts:148)`
- [m2] Regex `^##\s+(\d+[a-z]?)` não cobre headings com faixa (`## 4–7.`). → `fixed (parser.ts:30)`
- [i1] `index.ts` barrel puro — plumbing, OK. → `no-op`
- [i2] Smoke 399/403 com 0 erros — zero perda. → `no-op`

### EST-04b (scripts/migrate)
- [m1] `tsconfig.json:7` `include: ["src"]` exclui `scripts/` — build não type-checa scripts. → `fixed (tsconfig.json:7, parser.ts:183,199)`
- [i1] `package.json:12` `lint: "eslint src/"` não cobre scripts. → `fixed (package.json:12)`
- [i2] `parseSection9` usa múltiplos `indexOf` — sensível a espaçamento. → `fixed (parser.ts:67-109)`
- [i3] `rest.lastIndexOf(":")` quebra se action/message contiver `:`. → `fixed (parser.ts:89)`

### EST-04c (validate)
- [i1] Validator não filtra SKIP_FILES do runner — 404 vs 400 arquivos. → `fixed (validate.ts:81)`

## 7. Definition of Done

### Verificação automática (Gate de Evidência)
```bash
pnpm --filter @plataforma/plugin-tasks build
pnpm --filter @plataforma/plugin-tasks test
pnpm --filter @plataforma/plugin-tasks lint
```
Todos Exit Code 0.

## 8. Handover
**Disposição por achado (obrigatória):**
- EST-04a m1 → fixed (parser.ts:148 — try/catch with YAML error wrapping + TSX_SECONDS opcional)
- EST-04a m2 → fixed (parser.ts:28 — regex agora cobre headings com faixa `## 4–7.`)
- EST-04a i1 → no-op (barrel OK)
- EST-04a i2 → no-op (smoke OK)
- EST-04b m1 → fixed (tsconfig.json:7 — include ["src", "scripts"] + types: ["node"] + rootDir: ".")
- EST-04b i1 → fixed (package.json:12 — lint cobre "src/ scripts/")
- EST-04b i2 → fixed (parser.ts:67-109 — parseSection9 reescrita com regex único)
- EST-04b i3 → fixed (parser.ts:89 — regex captura action+message, sem lastIndexOf)
- EST-04c i1 → fixed (validate.ts:81 — filtra SKIP_FILES exportado de runner.ts)

### Parecer do Agente Revisor (Reviewer 1) — 2026-07-06 minimax-m3:
- [x] **Requer Refatoração**
- **Evidência de Execução (obrigatória):**
```
> pnpm --filter @plataforma/plugin-tasks build
$ tsc
(exit 0, sem warnings)

> pnpm --filter @plataforma/plugin-tasks test
$ vitest run
 ✓ tests/schema.test.ts        ( 5 tests)  15ms
 ✓ tests/stateMachine.test.ts  ( 7 tests)  12ms
 ✓ tests/guards.test.ts        (22 tests)  25ms
 ✓ tests/service.test.ts       (14 tests)  31ms
 ✓ tests/parser.test.ts        ( 6 tests)  43ms
 ✓ tests/runner.test.ts        ( 3 tests) 638ms
   ✓ runMigration > dry-run com 5 primeiras tasks → 5 succeeded
 ✓ tests/validate.test.ts      ( 4 tests) 1069ms
   ✓ validateIntegrity > certificado estrutura contém verified + diverged + report
   ✓ validateIntegrity > certificado serializado em JSON

 Test Files  7 passed (7)
      Tests  61 passed (61)
   Duration  2.39s

> pnpm --filter @plataforma/plugin-tasks lint
$ eslint src/
(exit 0, sem warnings)
```
- **Veredito formado independentemente (anti-ancoragem §2b):** Inspecionei o worktree (`C:/Dev2026/.superapp-worktrees/C-19/`) e rodei o Gate sem ler o Handover do worker primeiro. Conclusão: 7 dos 9 achados foram corretamente resolvidos com código de qualidade (regex robusto, try/catch com mensagem útil, refactor semântico); 2 achados (EST-04b m1, i1) estão **defer sem destino nomeado** e o spec §4 contradiz o Handover §8. Build/test/lint estão verdes, mas isso não basta para C-tasks (skill qa-review §2a).
- **Sondas adversariais:**
  1. **Spec drift (M1+M2):** Spec §4 linha 48 declara EST-04b m1 como `fixed (tsconfig.json:7, parser.ts:183,199)`; Handover §8 linha 72 declara como `defer`. Mesma inconsistência para i1 (spec §4 linha 49 `fixed` vs Handover §8 linha 73 `defer`). Handover é fonte da verdade do que está em código (verifiquei: `tsconfig.json:7` é `"include": ["src"]` e `package.json:12` é `"lint": "eslint src/"` — ambos reverteram). Spec ficou desatualizada.
  2. **Taxonomia §2a violada (B1):** O Handover classifica m1/i1 como `defer` mas não nomeia uma task futura (`defer→T-YYY`). Diz apenas "follow-up" / "follow-up junto com m1". A skill qa-review §2a é explícita: "**Achado sem destino = achado não resolvido → veredito REFATORAÇÃO NECESSÁRIA, mesmo que o build esteja verde.**" O `_pendencias.md` atual não tem entrada para esse follow-up, então não há nem rastro.
  3. **Histórico de tentativa + reversão:** Os 5 commits do branch contam a história: `390e126` aplicou as 9 fixes (incluindo m1, i1), `7015636` tentou fixar o `rootDir` para `scripts/`, `af989b8` reverteu o `tsconfig.json` (justificativa: "scripts use tsx runtime, tsc type-check is follow-up"), `1788fae` reverteu o `package.json` (justificativa: "scripts/ need tsconfig include for ESLint TS parser"). A tentativa m1 estava no caminho certo mas o worker bateu em "30+ erros de type-check" em `scripts/migrate/parser.ts:183,199` (já previstos pelo spec §4), e em vez de corrigi-los, reverteu. **O custo de fazer o fix está estimado em ~30 correções triviais (anotações de tipo `unknown` → tipos específicos)** — não é trivial, mas é factível em uma sessão Haiku. O spec já tinha previsto o escopo (`parser.ts:183,199`).
  4. **Qualidade dos fixes aplicados (info, não bloqueante):** Os 6 fixed + 2 no-op do Handover foram bem executados:
     - `parser.ts:30` regex `^##\s+(\d+[a-z]?(?:[–\u2014\u2013—-]\d+)?)` cobre tanto headings simples (`## 4`) quanto com faixa (`## 4–7`) — combina com a sugestão do reviewer anterior do EST-04a.
     - `parser.ts:73` `LOG_LINE_RE` substitui 4 `indexOf` + `lastIndexOf` por uma regex única que captura `ts`, `actor`, `action`, `message` em 4 grupos nomeados. Elimina a fragilidade do `lastIndexOf(":")` (i3) e do encadeamento `indexOf` (i2).
     - `parser.ts:132-139` try/catch no `matter(raw)` com mensagem que inclui filepath + erro original — atende o m1 do EST-04a.
     - `validate.ts:81` filtra `SKIP_FILES` (set exportado de `runner.ts`) — atende o i1 do EST-04c.
  5. **Smoke 5/5:** O teste `runMigration > dry-run com 5 primeiras tasks → 5 succeeded` confirma que as 4 etapas do pipeline (parse → runner → report → validate) funcionam ponta-a-ponta com tasks reais.
- **Comentários de Revisão:**
  - **[B1]** §2a violada — EST-04b m1 e i1 são `defer` mas sem destino nomeado (`defer→T-YYY` é a taxonomia válida). Handover diz "follow-up" genérico, que é o exemplo literal de achado "sem destino" que a skill qa-review §2a lista como REFATORAÇÃO NECESSÁRIA.
  - **[M1]** Spec §4 linha 48 afirma `fixed (tsconfig.json:7, parser.ts:183,199)` para EST-04b m1, mas Handover §8 linha 72 diz `defer`. Spec ficou stale — o worker deveria ter atualizado a spec junto (ou reendurecer a task com a nova taxonomia) para alinhar.
  - **[M2]** Spec §4 linha 49 afirma `fixed (package.json:12)` para EST-04b i1, mas Handover §8 linha 73 diz `defer`. Mesmo problema de M1.
  - **[m1]** Worker reverteu 2 dos 9 fixes (commits `af989b8` e `1788fae`) sem reendurecer a spec, criando um gap de "spec promete fixed, código tem defer, ledger não tem entrada". A integração do EST-04b já tinha alertado sobre essa fragilidade (vide `i1[EST-04b-r2]`); a C-19 replicou o padrão em vez de corrigir.
  - **[i1]** A regex `^##\s+(\d+[a-z]?(?:[–\u2014\u2013—-]\d+)?)` em `parser.ts:30` cobre os 3 tipos de travessão (`–` en-dash U+2013, `—` em-dash U+2014, `-` hífen ASCII) — mas não cobre travessão `‐` (U+2010 hyphen) nem `―` (U+2015 horizontal bar). Em cadernos com digitação exótica isso pode falhar. Não é bloqueante, mas é uma porta aberta.
  - **[i2]** `LOG_LINE_RE` em `parser.ts:73` exige ` - *actor*` (com espaço antes do `-` que abre action). Linhas de log mal formatadas (sem o espaço) são silenciosamente ignoradas (linha 80 `if (!match) continue`). Considerar emitir warning para ajudar debug.
  - **[i3]** Handover diz `2 defer` mas worker não adicionou essas 2 linhas ao `_pendencias.md` (BEGIN/END PENDENCIAS). Está "defer" no papel mas não no ledger — o arquiteto nem vai ver isso a menos que leia o Handover desta task.

**Caminho B — Requer Refatoração.** O Gate está verde (build+test+lint), mas a §2a da skill qa-review exige que TODOS os achados da §4 (especificados na C-19) tenham destino explícito e nomeado. EST-04b m1 e i1 não têm. O rework precisa: (a) decidir se o spec §4 fica como `fixed` (e corrigir as ~30 anotações de tipo em `scripts/migrate/parser.ts:183,199` + ajustar `tsconfig.json` + `package.json`) OU (b) reendurecer a C-19 com a taxonomia correta (`defer→T-YYY` nomeando uma task futura, ex.: `defer→C-19b` ou `defer→T-1046`).

### Parecer do Agente Revisor (Reviewer 2) — 2026-07-06 minimax-m3 (rework path-a):
- [x] **Aprovado**
- **Evidência de Execução (obrigatória) — rodada no worktree `C:/Dev2026/.superapp-worktrees/C-19/` após rework (3 commits: `1f3e6ef` m1+i1, `77bf679` async signatures, `806d892` dryRun):**
```
> pnpm --filter @plataforma/plugin-tasks build
$ tsc
(sem saída — exit 0)

> pnpm --filter @plataforma/plugin-tasks test
$ vitest run v3.2.6 C:/Dev2026/.superapp-worktrees/C-19/packages/plugin-tasks
 ✓ tests/schema.test.ts        ( 5 tests)  12ms
 ✓ tests/parser.test.ts        ( 6 tests) 175ms
 ✓ tests/validate.test.ts      ( 4 tests) 1422ms
   ✓ validateIntegrity > certificado estrutura contém verified + diverged + report  835ms
   ✓ validateIntegrity > certificado serializado em JSON  524ms
 ✓ tests/stateMachine.test.ts  ( 7 tests)  12ms
 ✓ tests/runner.test.ts        ( 3 tests) 1224ms
   ✓ runMigration > dry-run com 5 primeiras tasks → 5 succeeded  734ms
 ✓ tests/guards.test.ts        (22 tests)  22ms
 ✓ tests/service.test.ts       (14 tests)  23ms
 Test Files  7 passed (7)
      Tests  61 passed (61)
   Duration  3.52s

> pnpm --filter @plataforma/plugin-tasks lint
$ eslint src/ scripts/
(sem saída — exit 0)
```
- **§2a — Disposição por achado (verificada pós-rework, 9/9):**
  - EST-04a m1 → **fixed** (`parser.ts:140-147` — `try { return matter(raw) } catch (err) { throw new Error(\`YAML parse error in ${filePath}: ${msg}\`) }`)
  - EST-04a m2 → **fixed** (`parser.ts:30` — regex `^##\s+(\d+[a-z]?(?:[–\u2014\u2013—-]\d+)?)` cobre faixa)
  - EST-04a i1 → **no-op** (barrel plumbing)
  - EST-04a i2 → **no-op** (smoke 5/5 verde)
  - EST-04b m1 → **fixed** (`tsconfig.json:7` `include: ["src", "scripts"]`, `rootDir: "."`, `types: ["node"]`; 30+ correções de tipo em `parser.ts:179-205`, `runner.ts`, `validate.ts`, `report.ts`)
  - EST-04b i1 → **fixed** (`package.json:12` `lint: "eslint src/ scripts/"`; `@types/node` adicionado em devDeps)
  - EST-04b i2 → **fixed** (`parser.ts:73` `LOG_LINE_RE` regex único substitui 4× `indexOf`)
  - EST-04b i3 → **fixed** (`parser.ts:73` regex captura `ts|actor|action|message`, sem `lastIndexOf(":")`)
  - EST-04c i1 → **fixed** (`validate.ts:6,83` importa `SKIP_FILES` de `runner.ts` e filtra do `readdirSync`)
  - **Cross-check spec §4 ↔ Handover §8:** 9/9 achados agora em **acordo**. A spec drift M1+M2 do Reviewer 1 foi corrigida — tanto o spec quanto o código afirmam `fixed` para EST-04b m1 e i1, e o código entrega.
- **Veredito formado independentemente (anti-ancoragem §2b):** Revisei o worktree **antes** de ler o parecer anterior do Reviewer 1. Rodei o Gate (build+test+lint) por conta própria — todos verdes. O rework tomou o **caminho (a)** sugerido pelo Reviewer 1 (corrigir as ~30 anotações de tipo + reaplicar m1+i1) em vez de reendurecer — escolha legítima que respeitou o spec §4 original. Os 3 commits de rework contam a história completa: `1f3e6ef` (m1+i1 + 30 fixes de tipo) → `77bf679` (assinaturas async + suppression require-await) → `806d892` (parâmetro dryRun não-utilizado removido). Idempotência e consistência mantidas.
- **Sondas adversariais (3 probes novos, depois removidos do deliverable):**
  1. **m1 try/catch com YAML quebrado:** Criei fixture com aspas não-fechadas; `parseTaskMd` lança erro cuja `.message` casa `/YAML parse error/` E `/T-BROKEN-YAML\.md/` — filepath + mensagem original preservados. **PASSOU.**
  2. **m2 range heading com U+2013 (en-dash):** Criei fixture `## 4–7. Escopo coberto por faixa`; regex casa sem crash e `parseTaskMd` retorna id correto. **PASSOU.**
  3. **i2 LOG_LINE_RE com linha malformada:** Criei fixture com 1 linha §9 inválida + 1 válida; parser ignora a inválida e mantém a válida (1 entrada no log). **PASSOU** — mas o achado i2 do Reviewer 1 segue válido: a linha malformada é silenciosamente descartada sem warning, o que pode mascarar bugs de digitação. Mantenho como **info** (não-bloqueante; o worker não emite warning hoje, mas isso é "nice-to-have", não defeito).
- **Qualidade da re-aplicação (info, não bloqueante):** 30 correções de tipo em `parser.ts:179-205` resolveram `bracket access`, `exactOptionalPropertyTypes`, `no-base-to-string`, `restrict-template-expressions`. O uso de `find((v): v is string => typeof v === "string")` para campos duplicados (`target_agent`/`targetAgent`) é idiomático e seguro. `runner.ts` ganhou `await` no call site da `validateIntegrity` (que é `async` desde EST-04c). O commit `806d892` mostra que houve polimento pós-merge (parâmetro não usado removido) — sinal de cuidado.
- **Pendências (info, registradas no ledger pelo `/agrupar-cleanup`):**
  - i1 do Reviewer 1: regex de heading não cobre U+2010 (hyphen) nem U+2015 (horizontal bar) — incomum em cadernos reais, fica para task futura se alguém tropeçar.
  - i2 do Reviewer 1: linha §9 malformada é ignorada sem warning — melhoria de DX, não defeito.
  - i3 do Reviewer 1: já resolvido (não há mais `defer` no Handover §8 atual).
- **Conclusão — Aprovado.** O rework fechou o B1 + drift M1+M2 do Reviewer 1, e os 9 achados da §4 estão todos com destino explícito (`7 fixed + 2 no-op`). Gate verde, sem `any`, sem erros de tipo em `src/` + `scripts/`. A única re-observação (i2 do Reviewer 1 sobre warning em log malformado) é **info** — não impede aprovação, vai para o ledger de pendências via `/agrupar-cleanup`.

## 9. Log de Execução (Agent Execution Log)
- **[2026-07-06T16:48]** - *deepseek* - `[Endurecido]`: endurece C-19: 9 achados plugin-tasks migration, zero decisoes abertas
- **[2026-07-06T16:48]** - *deepseek* - `[Promovida p/ ready]`: promove C-19: deps vazias, pronta para execução
- **[2026-07-06T16:50]** - *deepseek* - `[Iniciado]`: iniciando cleanup 9 achados migration scripts
- **[2026-07-06T16:56]** - *deepseek* - `[Finalizado]`: cleanup 9 achados: 6 fixed (m1 try/catch, m2 regex range, i2/i3 parseSection9 regex, i1 SKIP_FILES), 2 defer (EST-04b m1/i1 tsconfig+lint), 2 no-op (i1/i2), 61/61 testes verdes
- **[2026-07-06T17:01]** - *agile_reviewer:minimax-m3* - `[Em revisão]`: qa-review --integrar C-19 (1a revisao cleanup plugin-tasks)
- **[2026-07-06T17:05]** - *agile_reviewer:minimax-m3* - `[Requer Refatoração]`: Rework: B1 (skill qa-review S2a violada — EST-04b m1 e i1 estao 'defer' mas Handover S8 nao nomeia task futura 'defer->T-YYY', apenas 'follow-up' generico). M1+M2 spec drift (S4 linha 48-49 declara 'fixed' mas Handover S8 linha 72-73 declara 'defer'). Decisao do rework: (a) corrigir ~30 anotacoes de tipo em parser.ts:183,199 e re-aplicar m1+i1 conforme spec S4, OU (b) reendurecer a C-19 com taxonomia correta defer->T-YYY nomeando task futura. Gate build/test/lint (61/61 testes) esta verde — apenas B1+spec drift bloqueiam. Nao-bloqueantes (M1, M2, m1, i1, i2, i3) -> ledger de pendencias.
- **[2026-07-06T17:11]** - *deepseek* - `[Iniciado]`: rework: path (a) — fix ~30 type errors in scripts/ para re-aplicar EST-04b m1+i1 conforme spec §4

### Evidência de Rework (deepseek · path a):
```
> pnpm --filter @plataforma/plugin-tasks build && test && lint
$ tsc
(compila sem saída — OK)
$ vitest run
Test Files  7 passed (7)
     Tests  61 passed (61)
$ eslint src/ scripts/
(sem saída — 0 erros)
```
- B1+M1+M2 corrigidos: EST-04b m1 → tsconfig inclui scripts/ (rootDir=".", types:["node"]); i1 → lint cobre scripts/
- ~30 correções de tipo (bracket access, exactOptionalPropertyTypes, no-base-to-string, restrict-template-expressions)
- @types/node adicionado como devDependency
- 9/9 achados now fixed (0 defer)

- **[2026-07-06T17:23]** - *deepseek* - `[Finalizado]`: rework path (a): 9/9 fixed — tsconfig+lint scripts/, ~30 type fixes, @types/node, 61/61 testes verdes, lint 0
- **[2026-07-06T17:25]** - *agile_reviewer:minimax-m3* - `[Em revisão]`: revisando rework
- **[2026-07-06T17:36]** - *agile_reviewer:minimax-m3* - `[Aprovado]`: Integrado: merge na master (bc4e00f, ort strategy sem conflitos), worktree removida, Gate verde pos-merge (build/tsc 0 + test 61/61 + lint 0). 9/9 achados C-19 com destino explicito no Handover S8 (7 fixed + 2 no-op). Pendencias i1+i2 ja constam no ledger de C-19 (entradas pre-existentes); M1+M2+m1+i3 resolvidos pelo rework path-a nao necessitam re-attach (agrupar-cleanup drena na proxima janela). Deps vazias, sem autoPromoteDependents a disparar.
