---
id: T-1046
title: "Transferência de nós reais no RBSR: responder SignedNode + applyNodes sob escopo UCAN"
status: ready
complexity: 4
target_agent: protocol_agent # perfis: devops_agent, logic_agent, crypto_agent, frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential # parallel | sequential
dependencies: ["T-1033", "T-1032"] # IDs de tarefas que bloqueiam esta
blocks: [] # IDs de tarefas que esta bloqueia
capacity_target: sonnet # haiku | sonnet | opus-spike — preenchido no endurecimento (pass 2)
# hardened_at: <data/commit>  ← carimbado pelo endurecer-task no pass 2 (baseline p/ reendurecer)
decisions:
  - "D1: como o UCAN do requisitante viaja até o responder (campo no wire de MSG_REQUEST_NODES vs por-sessão no handshake) + quem constrói o loop responder (T-1032, T-1046 ou task nova)"
  - "D2: T-1046 consome applyNodes de @plataforma/core (esperar T-1033 fechar o BLOCKER de ciclo) ou de @plataforma/protocol (posição atual)? + adicionar storage/GraphStorePort ao RBSRSession (contrato compartilhado com T-1033/T-1034)"
  - "D3: trocar payload de MSG_NODES_RESPONSE de JSON-fingerprint para codec.encode(SignedNode[]) — quebra de wire consciente; compat com fluxo árvore-vazia"
  - "D4: fingerprint no requisitante pós-applyNodes — wire carrega {node, fingerprint} ou exporta-se nodeFingerprint(node) p/ recomputar localmente"
---

# T-1046 · Transferência de nós reais no RBSR: responder SignedNode + applyNodes sob escopo UCAN

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (`pnpm build`, `pnpm test`, `pnpm lint` na raiz afetam todos os pacotes)
- **Test Runner:** `vitest` (pacotes core/protocol) e `playwright` (E2E/Frontend)
- **Capacidade-alvo:** haiku | sonnet | opus-spike *(ver regra "Dimensionamento de Tarefas" no CLAUDE.md: spec sem decisões em aberto, contratos explícitos, sem API externa não-fixada, verificação por comando)*

## 1. Objetivo
Completar a **transferência do payload real dos nós** no RBSR — hoje o protocolo só troca fingerprints (esqueleto de reconciliação) e nunca transfere `SignedNode`, deixando `applyNodes` desligado do caminho de exchange.

> **ORIGEM (decisão do arquiteto, 2026-07-02, T-1032).** Achado [m2]: `respondNodes` (`packages/protocol/src/rbsr/exchange.ts:204`) envia `{id, fingerprint}` mas `applyNodes` espera `readonly SignedNode[]`; `reconcile` insere fingerprints na árvore local, não dados. Ou seja, o RBSR compara o que difere mas **não transfere o conteúdo divergente**. T-1032 (enforcement UCAN da exposição de fingerprints) marcou explicitamente esta conclusão como **fora do seu escopo** — é funcionalidade, não privacidade. Esta task a completa.

**Meta:** após o `reconcile` identificar os ids divergentes, o responder envia os **`SignedNode` completos** (não fingerprints) desses ids, o requisitante os aplica via `applyNodes`, e **ambos os lados aplicam o mesmo padrão de autz servidor-side** que T-1032 estabeleceu (validar UCAN + `canAccess` pontual sobre o escopo antes de servir qualquer `SignedNode`). Um peer sem UCAN sobre o subgrafo não recebe nem os fingerprints (T-1032) nem os nós (esta task).

## 2. Contexto RAG (Spec-Driven Development)
> **Fatos confirmados no código real (superapp @ master, verificados 2026-07-07 pelo endurecedor).**
> Line refs são do estado atual e servem de âncora derivada.
- [ ] `tasks/T-1032.md` (`status: ready`) — estabelece o **padrão de autz servidor-side em prosa** (§3, bloco DECISÃO): `validateUcan(ucan)` estrutural + `canAccess(ucan, <scopeId>, <ability>)` pontual. ⚠️ **Porém §4/§5 de T-1032 são stubs de template (promovida em lote)** — T-1032 **NÃO fixa** (a) como o UCAN do requisitante viaja no wire de `MSG_REQUEST_NODES`, nem (b) onde vive o loop responder. Ver Seção 6, Decisão D1.
- [ ] `tasks/T-1033.md` (`status: review`, BLOCKER de ciclo turbo **não resolvido**) — deveria mover `applyNodes` para `@plataforma/core`. ⚠️ **`packages/core/src/rbsr/` NÃO existe em master** (confirmado) — o `[READ]` de `applyNodes.ts` em core ainda não resolve. Ver Decisão D2.
- [ ] `packages/protocol/src/rbsr/exchange.ts` — hoje: `applyNodes` (linhas 142-164, ainda em protocol), `respondNodes(transport, peerId, tree, ids)` (linhas 189-203, serve **só fingerprint** via `encodeNodesResponse`, **sem storage nem UCAN**), `reconcile(session)` (linhas 211-316, aplica `localTree.insert({id,fingerprint})` — **não** `applyNodes`), `encodeNodesResponse`/`decodeNodesResponse` (linhas 65-87, JSON `{id, fp:number[]}`)
- [ ] `packages/protocol/src/ports.ts` — `SignedNode` (linha 23), `StoragePort` (linha 88, **SQL puro — sem getNode**), `GraphStorePort.getNode(id: EntityId): Promise<SignedNode | null>` (linha 191, **é aqui que o responder busca nós reais**), `EntityId = string` (linha 165)
- [ ] `packages/protocol/src/rbsr/BTree.ts` — `Fingerprint = Uint8Array` 32 bytes = **SHA-256 de `(id, signature)`** (linha 3); `insert(node: BTreeNode)` exige `{id, fingerprint}` (linha 94). ⚠️ **Não há função exportada que derive o fingerprint de um `SignedNode`.** Ver Decisão D4.
- [ ] `packages/protocol/src/codec.ts` (T-201) — `encode(value: unknown): Uint8Array` (linha 28) / `decode(bytes): unknown` (linha 36) via msgpackr, com `bigint` nativo (serve p/ `SignedNode`: tem `Uint8Array` + `hlc: bigint`)
- [ ] `packages/core/src/auth/ucan.ts` — `validateUcan` (linha 182), `SignedUcan` (linha 30), `UcanCapability` (linha 9), `issueUcanSync` (linha 147)
- [ ] `packages/protocol/src/auth/ucanScope.ts` — `canAccess(ucan, entityId, ability, now?): CanAccessResult` (linha 48), `scopeRBSRTree(tree, ucan): BTree` (linha 73), `extractEntityIds` (linha 31)
- [ ] `packages/transport/src/syncCoordinator.ts` — `SyncCoordinator` tem `this.storage: StoragePort` (linha 61) e monta `RBSRSession` em `startSession` (linhas 132-138). ⚠️ **Zero referências a UCAN em todo `packages/transport/src` (confirmado por grep).**
- [ ] `docs/conceitos/sync-dirigido-por-ucan.md` + `caderno-5-transport/01 §2.7` — garantia normativa (enforcement bilateral)

## 3. Escopo de Arquivos (Inputs e Outputs)
> ⚠️ **Escopo CONTINGENTE às decisões D1–D4 da Seção 6.** Os itens abaixo capturam o estado real e
> o que provavelmente muda; os contratos exatos (assinaturas de `respondWithNodes`, campo de storage
> no `RBSRSession`, wire do UCAN) **dependem das decisões pendentes** — worker NÃO deve fixá-los sozinho.
- **[READ]** `applyNodes` — hoje em `packages/protocol/src/rbsr/exchange.ts:142-164`; **após T-1033** em `packages/core/src/rbsr/applyNodes.ts` (path contingente à resolução de T-1033 — Decisão D2). Assinatura: `applyNodes(nodes: readonly SignedNode[], storage: StoragePort): Promise<{ imported: number; rejected: number }>`.
- **[READ]** `packages/protocol/src/ports.ts` — `GraphStorePort.getNode`, `StoragePort`, `SignedNode`, `EntityId`
- **[READ]** `packages/protocol/src/auth/ucanScope.ts` — `canAccess`; `packages/core/src/auth/ucan.ts` — `validateUcan`
- **[UPDATE]** `packages/protocol/src/rbsr/exchange.ts` — responder passa a servir `SignedNode[]` (via `GraphStorePort.getNode` dos ids requisitados) sob check UCAN; `reconcile` aplica `applyNodes(nodes, storage)` no payload recebido; wire de `MSG_NODES_RESPONSE` estende-se p/ `SignedNode` serializado via `codec.encode`/`decode` (T-201) — **encoding exato pende D3/D4**
- **[UPDATE]** `packages/transport/src/syncCoordinator.ts` — propagar UCAN + `GraphStorePort` à fase de transferência — **pende D1 (de onde vem o UCAN) e D2 (storage no session)**
- **[CREATE]** testes de transferência (Seção 4)

> **Coordenação:** depende de T-1033 (applyNodes no core, **em review/blocked**) e T-1032 (padrão de autz, **ready mas §4/§5 stub**). Ordem de merge pretendida: T-1033 → T-1032 → T-1046. NÃO reintroduzir o import `protocol → core` que T-1033 tenta remover.

## 4. Estratégia de Testes Estrita (Test-Driven Development)
> Enumeração provisória — **os casos 3–4 dependem de D1/D3** (contrato do UCAN no wire). Reendurecer após `decide`.
- [x] **Framework:** Vitest (Node puro) + `@plataforma/testkit` (SimNetwork/VirtualClock, T-005). Sem browser.
- [x] **Ambiente do Teste:** `pnpm --filter @plataforma/protocol test` (e core se `applyNodes` já migrou).
- [x] **Fora de Escopo:** E2E, browser, rede real.

**Casos (numerados):**
1. **Transferência aplica nós divergentes.** Responder com `GraphStorePort` populado; requisitante pede N ids divergentes → recebe `SignedNode[]`, `applyNodes` retorna `{imported: N, rejected: 0}`, e o `localTree` do requisitante passa a conter os N fingerprints.
2. **Nó com assinatura inválida é rejeitado.** 1 dos N nós vem com `signature` adulterada → `applyNodes` retorna `{imported: N-1, rejected: 1}`; nó inválido não entra no storage.
3. **[pende D1] Peer sem UCAN sobre o escopo não recebe nós.** Requisição sem UCAN (ou UCAN sem capability sobre os ids) → responder recusa antes de servir (`canAccess` → `{ok:false}`), requisitante recebe resposta vazia. *Depende de como o UCAN chega ao responder (D1).*
4. **[pende D1] UCAN expirado é recusado.** `canAccess(ucan, id, ability, now)` com `exp <= now` → `{ok:false, reason:'expired'}`, zero nós servidos.
5. **Lista vazia / nenhum id divergente.** `reconcile` com árvores iguais → nenhum request de nós, `applyNodes` não chamado.

> Não afeta UI (protocol/transport puro) — `ui: false`.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> -
> -

### Pegadinhas conhecidas *(preencher pelo Task Architect — armadilhas que derrubam um modelo leve)*
*(Liste aqui os erros prováveis e como evitá-los. Ex.: "mudar uma assinatura síncrona para `async`*
*exige `await` em TODOS os callers (controller, rota REST, MCP tools)"; "mapear `A.foo → bar`*
*ao passar para o método X"; "não duplicar a lógica de Y — chamar o método existente Z".)*
- *[Nenhuma identificada]*

1. **[TDD]** Escreva o teste em `...`
2. Implemente `...`
3. Refatore.

## 6. Feedback de Especificação (Spec Feedback Loop)
> **STATUS: `draft:pending_decision`** — 4 decisões de arquiteto abertas. NÃO são inventáveis a partir
> de fonte (o padrão que esta task deveria "reusar" ainda não existe concretamente no código nem numa
> spec fechada). Endurecedor (claude-opus, 2026-07-07) confrontou T-1032/T-1033 contra o código real.

**[DECISÃO D1]** Transporte do UCAN: Por-sessão (Opção A), estabelecido no `startSession`. O `RBSRSession` carrega o UCAN. O handler do `MSG_REQUEST_NODES` será implementado nesta mesma task.
**[DECISÃO D2]** Local do `applyNodes`: Consumir de `@plataforma/protocol` (Opção B) para não bloquear com a T-1033. Será criada uma task de follow-up para trocar o import quando a T-1033 fechar.
**[DECISÃO D3]** Encoding da resposta: Trocar para `codec.encode(nodes)` (Opção A). Quebra de wire consciente.
**[DECISÃO D4]** Recompor fingerprint: Exportar `nodeFingerprint(node)` e recomputar localmente (Opção A).

**Derivado e já fixável (não bloqueia — para o worker depois que D1–D4 fecharem):**
- Padrão de autz servidor-side: `validateUcan(ucan)` (`core/src/auth/ucan.ts:182`) + `canAccess(ucan, id, ability)` (`ucanScope.ts:48`) — derivado de T-1032 §3 (bloco DECISÃO).
- Responder busca nós reais via `GraphStorePort.getNode(id)` (`ports.ts:191`).
- Serialização via `codec.encode/decode` (T-201, `codec.ts:28/36`).

## 7. Definition of Done (DoD) & Reviewer Checklist
O agente `agile_reviewer` usará esta checklist para aprovar ou rejeitar o PR:
- [ ] O código segue estritamente os arquivos de Output especificados (sem criar arquivos não solicitados)?
- [ ] O `pnpm test` roda sem erros no ambiente especificado (Node/JSDOM)?
- [ ] Linter (`pnpm lint`) não acusa problemas?
- [ ] A implementação respeita a Regra do Que Não Fazer?
- [ ] **[gate de wiring — se a task entrega primitiva de autorização/privacidade]** existe caller de produção em `src/**` que a consome no caminho real, OU há task de integração linkada? (primitiva só testada = feature NÃO entregue)
- [ ] **[gate de acoplamento — se a task adiciona import cruzando pacote]** o import respeita a direção `protocol ← crypto ← core ← transport` (`visao-arquitetural.md §1`) e NÃO fecha ciclo?

### Verificação automática *(comandos exatos — worker E reviewer rodam e COLAM a saída)*
```bash
pnpm --filter @plataforma/protocol build     # tsc — exchange.ts com wire novo compila
pnpm --filter @plataforma/protocol test      # vitest run — casos 1-5 da §4 + sem regressão
pnpm --filter @plataforma/protocol lint      # eslint src/ — 0 erros (lint é parte do gate, Regra 3)
pnpm --filter @plataforma/transport build    # tsc — syncCoordinator com UCAN propagado
pnpm --filter @plataforma/transport test     # vitest run — sem regressão
pnpm --filter @plataforma/transport lint     # eslint src/ — 0 erros
# Se D2 resolver com applyNodes já em core, adicionar:
# pnpm --filter @plataforma/core build && pnpm --filter @plataforma/core test && pnpm --filter @plataforma/core lint
```
> **GATE DE EVIDÊNCIA:** nem o `finish` (worker) nem o veredito (reviewer) são válidos sem a
> saída literal desses comandos colada na seção 8. Marcar `[x]` sem evidência é violação.

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- 

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória — colar saída de build/tsc + test):**
```
(cole aqui a saída real de pnpm build e pnpm test)
```
- **Comentários de Revisão:**

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.

- **[2026-07-03 13:26:06]** - *system* - `[Migrado]`: spec_status:draft → status:draft:placeholder
- **[2026-07-03T20:02]** - *system* - `[Triado]`: Triagem em lote do backlog
- **[2026-07-08T05:16]** - *claude-opus* - `[Decisão pendente]`: Endurecimento pass-2: 4 decisoes de arquiteto abertas (D1 wire do UCAN no request + dono do loop responder; D2 posicao de applyNodes contingente a T-1033 em review-blocked + storage no RBSRSession; D3 encoding SignedNode no MSG_NODES_RESPONSE; D4 derivacao de fingerprint pos-apply). Fatos confirmados no codigo real do superapp; secoes 2/3/4/6/7 endurecidas com line-refs.
- **[2026-07-08T13:50]** - *Antigravity* - `[Decidido]`: Decisões fechadas conforme recomendação externa. Follow-up de layer criado.
- **[2026-07-09T18:11]** - *Antigravity* - `[Promovida p/ ready]`: draft:hardened com deps done — safety-net flip
