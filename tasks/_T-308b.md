---
id: T-308b
title: "Snapshot: decode + import (idempotente)"
status: draft
superseded_by: T-308
complexity: 2
target_agent: core_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-308a", "T-108a"]
blocks: []
parent: "T-308"
---

> **[SUPERSEDED]** Trabalho entregue por **T-308** (done) e já integrado na master do superapp.
> Decomposição não executada — mantida apenas para histórico. NÃO EXECUTAR.

# T-308b · Snapshot: decode + import (idempotente)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** haiku

## 1. Objetivo
Implementar `decodeSnapshot` + `importSnapshot` no pacote `@plataforma/core`: desserializa snapshot MessagePack e importa nós/arestas no SQLite via `INSERT OR IGNORE` (idempotente)
*(extraído de T-308 §1)*.

### Contratos exatos
```ts
// --- packages/core/src/snapshot.ts (extensão) ---
export function decodeSnapshot(data: Uint8Array): Snapshot;

export async function importSnapshot(
  storage: StoragePort,
  data: Uint8Array,
): Promise<{ importedNodes: number; importedEdges: number }>;
```

## 2. Contexto RAG
- [T-308a · export/encode](./T-308a.md)
- [T-108a · insertNode](./T-108a.md)

## 3. Escopo de Arquivos
- **[READ]** `packages/core/src/snapshot.ts` (T-308a)
- **[CREATE]** `packages/core/tests/snapshot.import.test.ts`
- **[UPDATE]** `packages/core/src/snapshot.ts`

## 4. Estratégia de Testes
- [x] **Framework:** Vitest + SQLite `:memory:`.

Casos de teste:
1. `decodeSnapshot(encodeSnapshot(s))` → roundtrip.
2. `importSnapshot` em DB limpo → dados recuperáveis via SELECT.
3. `importSnapshot` 2× → sem duplicatas (INSERT OR IGNORE).
4. Snapshot com 1000 nós → import < 500ms.

## 5. Instruções de Execução
1. Escreva testes.
2. Implemente `decodeSnapshot` (msgpackr.decode).
3. Implemente `importSnapshot` (transação com validação Layer 1).
4. Rode build + test.

## 6. Feedback de Especificação
- *(Nenhuma pendência)*

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/core build
pnpm --filter @plataforma/core test
```

### Checklist
- [ ] `importSnapshot` idempotente?
- [ ] Roundtrip export → import?
- [ ] 4 casos de teste passando?
- [ ] `pnpm --filter @plataforma/core build` e `test` verdes?

## 8. Log de Handover e Revisão Agile
### Handover do Executor:
-

### Parecer do Agente Revisor:
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução:**
```
(cole aqui a saída real de pnpm build e pnpm test)
```

## 9. Log de Execução
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
