---
id: C-07
title: "Cleanup: system-peer (4 não-bloqueantes)"
status: review
complexity: 2
target_agent: logic_agent # perfis: devops_agent, logic_agent, crypto_agent, frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential # parallel | sequential
dependencies: [] # IDs de tarefas que bloqueiam esta
blocks: [] # IDs de tarefas que esta bloqueia
spec_status: hardened
capacity_target: haiku
hardened_at: "2026-07-01T13:30:00-03:00"
hardened_by: deepseek
decisions: []
---

# C-07 · Cleanup: system-peer (4 não-bloqueantes)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (`pnpm build`, `pnpm test`, `pnpm lint` na raiz afetam todos os pacotes)
- **Test Runner:** `vitest` (pacotes core/protocol) e `playwright` (E2E/Frontend)
- **Capacidade-alvo:** haiku | sonnet | opus-spike *(ver regra "Dimensionamento de Tarefas" no CLAUDE.md: spec sem decisões em aberto, contratos explícitos, sem API externa não-fixada, verificação por comando)*

## 1. Objetivo
Resolver 4 achados não-bloqueantes acumulados em `system-peer` (genesis, WAL, URL validation, coupling).

## 2. Contexto RAG (Spec-Driven Development)
Achados extraídos do ledger `tasks/_pendencias.md`, originados da task:
- [T-209](../tasks/T-209.md) — genesis (WAL em :memory:, URL validation, getProvisionalFlag, coupling FPP)

## 3. Escopo de Arquivos (Inputs e Outputs)
*(Defina EXATAMENTE quais arquivos o agente deve ler, criar ou modificar. Não edite arquivos fora deste escopo)*
- **[UPDATE]** `packages/system-peer/src/genesis.ts`

## 4. Estratégia de Testes
- [x] **Framework:** Vitest (Node puro). Cleanup task — sem novos testes.
- [x] **Ambiente:** `pnpm --filter @plataforma/system-peer test` — todos os testes existentes devem continuar verdes.
- [x] **Fora de Escopo:** Novos casos de teste. O Gate é: zero regressão.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> -
> -

### Achados a corrigir (4 itens)
- [ ] [m1][T-209] `journal_mode = WAL` aplicado em `Database(':memory:')` é no-op silencioso (SQLite não suporta WAL em memória) — código morto no path padrão de testes (genesis.ts:71)
- [ ] [m2][T-209] `multiaddrHost` interpolado cru na URL de onboarding (sem validação de hostname RFC 1123 nem URL-encode) — host com `#`/`?` produz URL ambígua. Validar host com regex ou url-encode (genesis.ts:102, 143)
- [ ] [i1][T-209] `getProvisionalFlag()` é test-helper exposto na classe pública sem `@internal` — marcar com sufixo ou decorator (genesis.ts:147)
- [ ] [i2][T-209] Zero coupling com T-208 FPP — `genesis.ts` não importa `packages/transport/src/fpp.ts` (interface `GenesisWriter` do FPP não é consumida). Spec lista T-208 como dep. Aceitável para stub v0; registrar para integração futura via ledger (genesis.ts)

**Execução:** Para cada achado, aplique a correção descrita, verifique `pnpm --filter @plataforma/system-peer build && pnpm --filter @plataforma/system-peer test`, e commite por achado.

## 6. Feedback de Especificação (Spec Feedback Loop)
> **ATENÇÃO:** Se a spec (RAG) for ambígua, contraditória ou o design pattern imposto for impossível, **PARE**. Mude o status para `blocked` e escreva o motivo abaixo. Não alucine uma abstração não documentada.
- *[Nenhum problema identificado]*

## 7. Definition of Done (DoD) & Reviewer Checklist
O agente `agile_reviewer` usará esta checklist para aprovar ou rejeitar o PR:
- [ ] O código segue estritamente os arquivos de Output especificados (sem criar arquivos não solicitados)?
- [ ] O `pnpm test` roda sem erros no ambiente especificado (Node/JSDOM)?
- [ ] Linter (`pnpm lint`) não acusa problemas?
- [ ] A implementação respeita a Regra do Que Não Fazer?

### Verificação automática *(comandos exatos — worker E reviewer rodam e COLAM a saída)*
```bash
pnpm --filter @plataforma/system-peer build
pnpm --filter @plataforma/system-peer test
```
> **GATE DE EVIDÊNCIA:** nem o `finish` (worker) nem o veredito (reviewer) são válidos sem a
> saída literal desses comandos colada na seção 8. Marcar `[x]` sem evidência é violação.

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- 4 achados corrigidos em `apps/system-peer/src/genesis.ts`:
  - [m1][T-209] WAL pragma guardado com `if (dbPath !== ':memory:')`
  - [m2][T-209] `isValidMultiaddrHost()` adicionado — valida RFC 1123/IPv4 antes de construir URL
  - [i1][T-209] `getProvisionalFlag()` marcado como `@internal`
  - [i2][T-209] Zero coupling com FPP documentado no header do módulo

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória — colar saída de build/tsc + test):**
```
$ pnpm --filter system-peer build
$ tsc

$ pnpm --filter system-peer test
 RUN  v3.2.6

 ✓ tests/mock.test.ts (1 test) 2ms
 ✓ tests/signaling.test.ts (11 tests) 9ms
 ✓ tests/genesis.test.ts (11 tests) 60ms
 ✓ tests/signalingRelay.test.ts (2 tests) 474ms
 ✓ tests/admin.test.ts (12 tests)

 Test Files  5 passed (5)
      Tests  37 passed (37)
```
- **Comentários de Revisão:**

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-07-01T16:56]** - *deepseek* - `[Promovida p/ ready]`: spec_status hardened — flip draft→ready
- **[2026-07-01T19:52]** - *deepseek* - `[Iniciado]`: iniciando cleanup system-peer (4 não-bloqueantes)
- **[2026-07-01T19:56]** - *deepseek* - `[Finalizado]`: 4 achados corrigidos — build+test: 5 files, 37 tests green
