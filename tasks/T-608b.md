---
id: T-608b
title: "Bancada: aba Dados — inspetor de intents"
status: draft:triaged
complexity: 2
target_agent: frontend_agent # perfis: devops_agent, logic_agent, crypto_agent, frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential # parallel | sequential
dependencies: ["T-008", "T-605"] # IDs de tarefas que bloqueiam esta
blocks: [] # IDs de tarefas que esta bloqueia
parent: T-608
ui: true
capacity_target: # haiku | sonnet | opus-spike — preenchido no endurecimento (pass 2)
---

# T-608b · Bancada: aba Dados — inspetor de intents

> **Origem:** filha de [T-608](./T-608.md), decomposta em 2026-07-14 pela decisão (b) do handoff de
> 2026-07-08: o contrato importava `IntentNode` de `@plataforma/core` — tipo que **só existirá com
> T-605** (fluxo intent não-comutativo). Esta task carrega a dependência real que o T-608 original
> não declarava. **Reendurecer quando T-605 estiver `done`** (o shape de `IntentNode` vem de lá —
> não inventar).

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+ · **PM:** `pnpm` · **Monorepo:** Turborepo
- **Test Runner:** `vitest` (RTL/JSDOM) + `playwright` (smoke)

## 1. Objetivo
Adicionar o **inspetor de intents** à aba "Dados" da Bancada (entregue por T-608a): lista de
intents com estado (pending/finalized/rejected), número de aprovações, entidade alvo *(derivado de
`docs/plano-de-implementacao.md` §9 T-608, metade adiada)*.

### Contrato (a reendurecer contra T-605 done — NÃO fixável antes)
```tsx
import { IntentNode } from '@plataforma/core'; // shape REAL definido por T-605 — citar arquivo/linha no reendurecimento
// DadosTabProps ganha: intents: IntentNode[]
```

## 2. Contexto RAG (Spec-Driven Development)
- [T-605](./T-605.md) — fluxo intent não-comutativo (fonte do tipo)
- [T-608a](./T-608a.md) — navegador de linhagens (estrutura da aba)
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (§9 T-608)

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[READ]** local real de `IntentNode` em `packages/core/src/` (fixar no reendurecimento)
- **[UPDATE]** `apps/bancada/src/components/tabs/DadosTab.tsx`
- **[UPDATE]** `apps/bancada/tests/DadosTab.test.tsx`

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- Caso 7 do T-608 original: inspetor exibe pending, finalized e aprovações.
- Reendurecer os casos exatos contra a API real de T-605.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** iniciar antes de T-605 `done` + reendurecimento desta spec.
> - **NÃO** stubar `IntentNode` localmente.

1. *(pós-reendurecimento)* TDD sobre a API real de intents.

## 6. Feedback de Especificação (Spec Feedback Loop)
- *[Nenhum problema identificado]*

## 7. Definition of Done (DoD) & Reviewer Checklist
- [ ] Reendurecida contra T-605 done antes do start?
- [ ] Gate `pnpm --filter bancada build && test && lint` verde?

### Verificação automática
```bash
pnpm --filter bancada build
pnpm --filter bancada test
pnpm --filter bancada lint
```

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- 

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória):**
```
(cole aqui a saída real de pnpm build, pnpm test e pnpm lint)
```
- **Comentários de Revisão:**

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-07-14T12:33]** - *claude-fable* - `[Triado]`: Filha adiada de T-608 — carrega a dep real T-605; reendurecer quando T-605 done
