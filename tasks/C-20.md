---
id: C-20
title: "Cleanup: plugin-knowledge (4 nao-bloqueantes)"
status: done
complexity: 2
target_agent: deepseek # perfis: devops_agent, logic_agent, crypto_agent, frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential # parallel | sequential
dependencies: [] # IDs de tarefas que bloqueiam esta
blocks: [] # IDs de tarefas que esta bloqueia
capacity_target: haiku
# decisions: ["..."]          ← só quando status: draft:pending_decision (espelha a Seção 6)
---

# C-20 · Cleanup: plugin-knowledge (4 nao-bloqueantes)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (`pnpm build`, `pnpm test`, `pnpm lint` na raiz afetam todos os pacotes)
- **Test Runner:** `vitest` (pacote `@plataforma/plugin-knowledge`)
- **Capacidade-alvo:** haiku

## 1. Objetivo
Resolver 4 achados não-bloqueantes acumulados de refatoração, limpeza e robustez em `packages/plugin-knowledge/`.

## 2. Contexto RAG (Spec-Driven Development)
- [fts.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/src/fts.ts) — indexação full-text
- [writer.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/src/writer.ts) — writer de conhecimento
- [index.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/src/index.ts) — barrel do pacote
- [fts.test.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/tests/fts.test.ts) — testes de indexação
- [writer.test.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/tests/writer.test.ts) — testes de escrita

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[UPDATE]** [fts.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/src/fts.ts)
  - Remover `positions` de `TermEntry` (dead code).
    - Antiga assinatura:
      ```typescript
      interface TermEntry {
        count: number;
        positions: number[];
      }
      ```
    - Nova assinatura:
      ```typescript
      interface TermEntry {
        count: number;
      }
      ```
  - Normalizar regex de `splitTerms` para suporte a Unicode e aplicar normalização NFD para desacentuar termos antes de indexar e buscar.
    - Assinaturas de funções modificadas:
      ```typescript
      function splitTerms(text: string): string[]
      async search(term: string): Promise<FtsResult[]>
      ```
- **[UPDATE]** [writer.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/src/writer.ts)
  - Remover `repoRoot` da interface `WriterOptions` (dead code).
    - Antiga assinatura:
      ```typescript
      export interface WriterOptions {
        manifest: PluginManifest;
        fs: FsPort;
        commit: CommitPort;
        repoRoot: string;
        signal?: AbortSignal;
      }
      ```
    - Nova assinatura:
      ```typescript
      export interface WriterOptions {
        manifest: PluginManifest;
        fs: FsPort;
        commit: CommitPort;
        signal?: AbortSignal;
      }
      ```
- **[UPDATE]** [index.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/src/index.ts)
  - Reordenar exports alfabeticamente.
    - Exportações resultantes:
      ```typescript
      export { makeFts, type FtsIndex, type FtsOptions, type FtsResult } from "./fts.js";
      export { makeGraph, type KnowledgeGraph, type MakeGraphOptions, type PageNode } from "./graph.js";
      export { makeStaging, type StagingManager, type StagingOptions } from "./staging.js";
      export { makeWriter, type KnowledgeWriter, type WriterOptions } from "./writer.js";
      export type { Proposal } from "./types.js";
      ```
- **[UPDATE]** [writer.test.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/tests/writer.test.ts)
  - Ajustar as instanciações de `makeWriter` removendo o campo `repoRoot` (linhas 30, 56, 79, 100, 120).
- **[UPDATE]** [fts.test.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/tests/fts.test.ts)
  - Adicionar caso de teste para split de termos Unicode (acentuação, cedilha e traço longo/hífen Unicode).

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- **Framework:** `vitest`
- **Métricas/Cobertura:** Todos os testes de `packages/plugin-knowledge` devem passar (100% verde).
- **Ambiente do Teste:** Node puro (sem browser), usando mock/diretório temporário (como nos testes existentes).
- **Fora de Escopo:** Testes reais de git/commits que não usem porta mockada.
- **Casos de Teste Verificáveis:**
  1. No arquivo [fts.test.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/tests/fts.test.ts), verificar que `buildIndex` e `search` funcionam sem problemas e que `positions` foi removido (a asserção não usa posições).
  2. No arquivo [fts.test.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/tests/fts.test.ts), adicionar teste específico para palavras Unicode: buscas por termos acentuados (ex: "atenção") ou com caracteres Unicode especiais (ex: "guia-de-estudo" com travessão/hífen Unicode) devem encontrar corretamente o termo tanto em formato acentuado quanto normalizado/desacentuado.
  3. No arquivo [writer.test.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/tests/writer.test.ts), verificar que a chamada a `makeWriter` sem `repoRoot` compila e executa com sucesso todos os 5 testes existentes.
  4. Garantir que os testes de staging (`staging.test.ts`) e grafos (`graph.test.ts`) continuam passando sem regressões.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - NÃO refatore além dos achados listados (escopo fechado).
> - NÃO adicione dependências novas.

1. **[TDD]** No arquivo [fts.test.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/tests/fts.test.ts), adicione um novo caso de teste:
   ```typescript
   it("8. splitTerms com Unicode e acentos (ex: 'atenção', 'guia-rápido') → normaliza e busca corretamente", async () => {
     // Escrever arquivos no corpus temporário com palavras acentuadas/cedilhas e traços especiais
     // Executar busca com e sem acentos e verificar se retorna o mesmo slug
   });
   ```
2. **Remover posições (`positions`):** No arquivo [fts.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/src/fts.ts), remova a propriedade `positions` de `TermEntry` e o código que a popula em `buildIndex`.
3. **Normalizar Unicode:** No arquivo [fts.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/src/fts.ts), atualize `splitTerms` e `search` para normalizar o texto com `.normalize("NFD").replace(/[\u0300-\u036f]/g, "")` e usar split compatível com Unicode `/[^\p{L}\p{N}]+/u`.
4. **Remover repoRoot:** No arquivo [writer.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/src/writer.ts), remova `repoRoot` da interface `WriterOptions` e o comentário e destructuring associados em `makeWriter`.
5. **Atualizar testes do writer:** Em [writer.test.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/tests/writer.test.ts), remova todas as propriedades `repoRoot` das chamadas para `makeWriter`.
6. **Reorganizar Exports:** Em [index.ts](file:///C:/Dev2026/superapp/packages/plugin-knowledge/src/index.ts), reorganize os exports alfabeticamente conforme especificado na Seção 3.
7. **Verificar Gates:** Execute build, test e lint locais do pacote `packages/plugin-knowledge`.

## 6. Feedback de Especificação (Spec Feedback Loop)
- *[Nenhum problema identificado / Zero decisões em aberto]*

## 7. Definition of Done (DoD) & Reviewer Checklist
O agente `agile_reviewer` usará esta checklist para aprovar ou rejeitar o PR:
- [ ] O código segue estritamente os arquivos de Output especificados (sem criar arquivos não solicitados)?
- [ ] O `pnpm test` roda sem erros no ambiente especificado (Node)?
- [ ] Linter (`pnpm lint`) não acusa problemas?
- [ ] A implementação respeita a Regra do Que Não Fazer?
- [ ] Todas as 4 pendências descritas na §5 foram resolvidas com disposição individual no handover (Seção 8)?

### Verificação automática *(comandos exatos — worker E reviewer rodam e COLAM a saída)*
```bash
pnpm --filter @plataforma/plugin-knowledge build
pnpm --filter @plataforma/plugin-knowledge test
pnpm --filter @plataforma/plugin-knowledge lint
```
> **GATE DE EVIDÊNCIA:** nem o `finish` (worker) nem o veredito (reviewer) são válidos sem a
> saída literal desses comandos colada na seção 8. Marcar `[x]` sem evidência é violação.

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
**Disposição por achado (obrigatória):** liste CADA achado da §5 com seu destino:
- `- 1. [m2][EST-13b] → fixed (remover TermEntry.positions)`
- `- 2. [i6][EST-13b] → fixed (splitTerms regex normalizado Unicode + search também normaliza com NFD)`
- `- 3. [m1][EST-13c] → fixed (remover repoRoot do WriterOptions)`
- `- 4. [m3][EST-13c] → fixed (index.ts re-exports alfabeticos)`

Handover sem disposição per-item é inválido e será rejeitado pelo reviewer.

### Parecer do Agente Revisor (Reviewer):
**Reviewer 1 (minimax-m3, 2026-07-18):**
- [x] **Aprovado**
- [ ] **Requer Refatoração**

> **Nota sobre o artefato de gate:** o `.gate/047f11de...json` commitado na branch
> tem `treeSha=047f11de...` que **NÃO** bate com `git rev-parse HEAD^{tree}` atual
> (`fcf43a0a...`) — foi gerado contra o primeiro commit (`8ec6aa9`) e ficou stale
> após o fix de normalização no `search`. Re-rodei o gate completo abaixo.

- **Escopo auditado:** `git log master..HEAD` → 2 commits (`8ec6aa9`, `e90af59`),
  5 arquivos, +58/-30 linhas. Diff > 20 → fast-track não aplicável; revisão nível 0 completa.
- **Per-achado (§5):**
  - 1. `[m2][EST-13b]` remover `TermEntry.positions` — `src/fts.ts:23` encolheu para
    `{ count: number }`; ambas as populações em `buildIndex` removidas. **OK**.
  - 2. `[i6][EST-13b]` Unicode — helper `normalizeText` (NFD + strip combining) introduzido
    em `src/fts.ts:25-27`; `splitTerms` agora usa regex `/[^\p{L}\p{N}]+/u` (Unicode-aware)
    e `search` também normaliza o termo buscado; título no `search` também passa pelo
    `normalizeText` (consistência). **OK**.
  - 3. `[m1][EST-13c]` remover `repoRoot` de `WriterOptions` — `src/writer.ts` sem o
    campo; comentário morto "reserved for future use" também removido; destructuring
    limpo. **OK**.
  - 4. `[m3][EST-13c]` re-exports alfabéticos em `src/index.ts` — ordem: fts, graph,
    staging, writer, types (types por último como type-only). **OK** confere com a
    Seção 3.
- **Testes:** `tests/writer.test.ts` ajustado nas 5 instanciações (linhas 30, 56, 79,
  100, 120 da spec). `tests/fts.test.ts` ganhou o caso "split de termos com acentos,
  cedilha e traço longo" (corpo com `vocação`, `coração`, `traço–longo`/en-dash U+2013;
  busca com e sem acento retorna o mesmo slug). **OK** — cedilha coberta via
  `vocação`/`coração` no body; en-dash tratado como separador pelo regex Unicode.

- **Evidência de Execução (re-rodada no worktree contra `HEAD` atual, tree
  `fcf43a0a`):**
```
$ pnpm gate @plataforma/plugin-knowledge
✅ build | exit=0 | 6801ms
✅ test  | exit=0 | 4528ms
   ✓ tests/staging.test.ts (10 tests)
   ✓ tests/writer.test.ts (5 tests)
   ✓ tests/graph.test.ts  (7 tests)
   ✓ tests/fts.test.ts    (8 tests)
    Test Files  4 passed (4)
         Tests  30 passed (30)
✅ lint  | exit=0 | 5251ms
```

- **Comentários de Revisão:**
  - **MAJOR (não-bloqueante, mas deveria ir pro ledger):** o artefato de gate
    stale na branch (`.gate/047f11de...json`) deveria ter sido regerado pelo
    `finish` após o segundo commit. Se o pipeline automatizou gate-em-CI, OK;
    se foi o worker que esqueceu de re-rodar, o workflow não pegou — vale
    flag pro `manage-task.mjs` (ou aviso no handover).
  - Tudo o mais limpo, sem regressões, sem imports órfãos, lint sem warning.

- **Veredito:** APROVADO. Liberar para `/integrar-task C-20`.

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-07-17T19:37]** - *gemini* - `[Triado]`: triado
- **[2026-07-17T19:38]** - *gemini* - `[Endurecido]`: endureceu spec
- **[2026-07-18T10:17]** - *gemini* - `[Promovida p/ ready]`: draft:hardened com deps done — safety-net flip
- **[2026-07-18T10:39]** - *deepseek* - `[Iniciado]`: iniciando cleanup plugin-knowledge (4 achados não-bloqueantes)
- **[2026-07-18T10:46]** - *deepseek* - `[Finalizado]`: cleanup plugin-knowledge completo: positions removido, Unicode normalization, repoRoot removido, exports reordenados. Gate: build+test+lint verde.
- **[2026-07-18T10:58]** - *agile_reviewer:minimax-m3* - `[Em revisão]`: revisando
- **[2026-07-18T11:04]** - *agile_reviewer:minimax-m3* - `[Aprovado]`: Integrado: merge na master (commit f579e6d), slot liberado, Gate verde (build+test+lint exit=0, 30/30 tests pass). 1 não-bloqueante (MAJOR: gate artifact stale pós-segundo-commit) → ledger de pendências.
