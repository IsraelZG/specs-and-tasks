---
id: T-013
title: "Pipeline Turbo de tokens + cache (exceção Vite)"
status: review
complexity: 2
target_agent: devops_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-011"]
blocks: []
---

# T-013 · Pipeline Turbo de tokens + cache (exceção Vite)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (`pnpm build`, `pnpm test`, `pnpm lint` na raiz afetam todos os pacotes)
- **Test Runner:** `vitest`
- **Capacidade-alvo:** haiku

## 1. Objetivo
Integrar o build do design system ao Turborepo de forma correta e cacheável: os tokens (Style Dictionary) devem compilar **antes** de qualquer build que os consome, e os artefatos devem entrar no cache do Turbo. Formalizar a **exceção ao padrão `tsup`**: `packages/design-system` e `apps/design-system-showcase` usam **Vite** (precisam de Tailwind v4 + bundle de CSS/assets).

## 2. Contexto RAG (Spec-Driven Development)
- [ ] [plano-de-implementacao.md §1.1](../docs/plano-de-implementacao.md) — decisão "tsup para build"; esta tarefa documenta a exceção.
- [ ] [T-001 · Bootstrap](./T-001.md) — `turbo.json` base.
- [ ] [T-011 · Incorporar a lib](./T-011.md) — scripts `build:tokens`/`build:react`/`build:index`. **Pré-requisito.**

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[READ]** `packages/design-system/package.json` (scripts existentes `build:tokens`/`build:react`/`build:index` — definidos em T-011)
- **[READ]** `turbo.json` (raiz) — baseline criado em T-001 (`tasks.build.dependsOn: ["^build"]`, `outputs: ["dist/**"]`)
- **[UPDATE]** `turbo.json` (raiz) — nova task `build:tokens` com `outputs: ["build/web/**"]`; atualizar `build.dependsOn` para `["^build", "build:tokens"]`; adicionar `"build/**"` aos `outputs` de `build`
- **[UPDATE]** `packages/design-system/package.json` — se necessário, expor `build:tokens` como script de entrada (já criado pela T-011)
- **[CREATE]** `docs/adr/adr-003-design-system-build.md` — ADR registrando a exceção Vite vs `tsup` para o Design System (necessita Tailwind v4 + bundle de CSS/assets)

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Framework:** validação de pipeline — rodar `pnpm build` na raiz e inspecionar o grafo do Turbo.
- [x] **Métricas/Cobertura:** binário — build a frio gera tokens antes do bundle; segundo build usa cache (`>>> FULL TURBO`).
- [x] **Ambiente do Teste:** Node puro, raiz.
- [x] **Fora de Escopo:** conteúdo dos tokens.

### Casos de teste (numerados):
1. `turbo.json` declara task `build:tokens` com `outputs: ["build/web/**"]`.
2. `turbo.json` declara `build.dependsOn: ["^build", "build:tokens"]` e `build.outputs` inclui `"build/**"`.
3. `pnpm build` a frio na raiz executa `build:tokens` **antes** de `build` (ordem visível no log do Turbo: `build:tokens` → `^build` → `build`).
4. Segundo `pnpm build` consecutivo sem mudança de source acerta cache do Turbo (`>>> FULL TURBO`).
5. `build/web/theme-light.css` e `build/web/theme-dark.css` são gerados.
6. ADR `docs/adr/adr-003-design-system-build.md` existe e documenta a justificativa Vite vs `tsup`.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** force os pacotes Vite a usar `tsup` — registre a exceção, não a "conserte".
> - **NÃO** deixe `build:tokens` fora do grafo de dependências (causaria bundle com tokens defasados).
> - **NÃO** marque outputs de token como não-cacheáveis sem motivo (perde o ganho do Turbo).

1. Em `turbo.json`, declarar `build:tokens` com seus `outputs` (ex.: `build/web/**`).
2. Ajustar `build` para `dependsOn: ["^build", "build:tokens"]`.
3. Rodar `pnpm build` a frio; confirmar ordem (tokens → bundle).
4. Rodar `pnpm build` de novo; confirmar cache hit.
5. Escrever o ADR `docs/adr/adr-003-design-system-build.md` registrando a justificativa da adoção do Vite para o Design System, contrastando com o padrão `tsup` das outras libs.

## 6. Feedback de Especificação (Spec Feedback Loop)
> **ATENÇÃO:** Se o `turbo.json` da T-001 ainda não existir, **PARE** (status `blocked`).
- *[Nenhum problema identificado]*

## 7. Definition of Done (DoD) & Reviewer Checklist

### Verificação automática (Gate de Evidência)
```bash
pnpm --filter @plataforma/design-system build
pnpm build          # raiz — verifica ordem (tokens → bundle) + cache
```
> **GATE DE EVIDÊNCIA:** Worker cola a saída literal na Seção 8, mostrando o log do Turbo com a ordem de execução e o status de cache.
- [ ] `pnpm build` a frio compila tokens **antes** do bundle da lib/showcase?
- [ ] Segundo `pnpm build` é cache hit do Turbo?
- [ ] A exceção Vite vs tsup está documentada?
- [ ] Outputs de token/`build` declarados nos `outputs` do Turbo?

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:

**Gate de Evidência — `pnpm build` (build a frio pós-rrework):**
```
Tasks:    13 successful, 13 total
Cached:    0 cached, 13 total
  Time:    11.168s
```
Ordem de execução confirmada no log do Turbo:
- `@plataforma/design-system:build:tokens: cache bypass, force executing 8a152f3905d4a2fd`
- `@plataforma/design-system:build: cache bypass, force executing dd41d2c282d5ee6a` (chama apenas `$ pnpm run build:react` → `$ vite build`, eliminando a duplicidade!)

**Gate de Evidência — `pnpm build` (segundo build consecutivo):**
```
Tasks:    13 successful, 13 total
Cached:    13 cached, 13 total
  Time:    627ms >>> FULL TURBO
```
Ao ignorar a pasta local `build/` via `.gitignore` e refinar os `inputs` da task `build:tokens` para ignorar o `src/metadata/components.index.json` (evitando auto-invalidação), o pipeline atingiu cache total (**FULL TURBO 13/13 cached**).

**Checklist DoD:**
- [x] `turbo.json` declara `build:tokens` cobrindo todas as plataformas em outputs.
- [x] `build.dependsOn` ajustado e duplicidade removida do package.json do design-system (agora executa apenas `build:react`).
- [x] Execução sem duplicidade confirmada no log do Turbo.
- [x] Build subsequente resulta em **FULL TURBO** (13/13 cached, 100% cache hit).
- [x] Artefatos de build removidos do Git e `.gitignore` local configurado para ignorar `build/`.
- [x] ADR `docs/adr/adr-003-design-system-build.md` atualizado com o fluxo final de build.
- [x] Execução limpa de `pnpm test` e `pnpm lint` na raiz (exit code 0).


### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [x] **Requer Refatoração**
- **Evidência de Execução (obrigatória):**
```
$ pnpm --filter @plataforma/design-system build
  ✓ tokens + bundle (vite) OK, exit 0

$ pnpm build (raiz — 1ª execução, a frio)
 Tasks:    13 successful, 13 total
Cached:    11 cached, 13 total
  Time:    9.943s

$ pnpm build (raiz — 2ª execução, cache)
 Tasks:    13 successful, 13 total
Cached:    11 cached, 13 total
  Time:    10.534s
```
Ordem confirmada: `build:tokens` → `build` no design-system.
`build/web/theme-light.css` ✔ e `build/web/theme-dark.css` ✔ gerados.
- **Comentários de Revisão:**
  - **[M1]** `packages/design-system/build:tokens` roda em duplicata: o Turbo executa `build:tokens` como task independente (linha 5 do `turbo.json`), e o script `build` do `packages/design-system/package.json` chama `build:tokens` novamente internamente. Os logs dos dois builds confirmam o `build:tokens` executando no turbo task E novamente dentro do script `build`. Viola o princípio de build eficiente (cada etapa uma vez) e dobra o tempo da etapa de tokens. **Ação corretiva:** O `build` npm script do `@plataforma/design-system` (definido pela T-011) deve chamar apenas `build:react`, já que o Turbo gerencia `build:tokens` via `dependsOn`. Se não puder modificar o script da T-011, documente na Seção 6 (Feedback) para a T-011 absorver.
  - **[i1]** `build:tokens` `outputs: ["build/web/**"]` cobre apenas tokens web; os outros outputs (android/, ios/, react-native/, tv/) gerados pelo Style Dictionary não entram no cache standalone do `build:tokens`. Como o `build.outputs` já inclui `"build/**"`, esses outputs só são cacheados após o bundle completo, não após `build:tokens` isolado.
  - **[i2]** O commit `0041de9` inclui 30+ arquivos gerados em `packages/design-system/build/` — idealmente seriam gitignored no `.gitignore` raiz (`packages/design-system/build/`).
  - **[i3]** Aviso de turbo global (`2.9.18`) vs versão do repo (`^2.0.0`) — instalar turbo localmente como devDependency na raiz elimina o warning.

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-06-21T18:17]** - *devops_agent* - `[Bloqueado]`: Especificacao analisada, movendo para ready
- **[2026-06-21T18:17]** - *devops_agent* - `[Desbloqueado]`: Especificacoes prontas para execucao
- **[2026-06-21T20:38]** - *Antigravity* - `[Iniciado]`: Iniciando implementacao do pipeline Turbo de tokens
- **[2026-06-21T20:42]** - *Antigravity* - `[Finalizado]`: turbo.json atualizado com build:tokens, ADR-003 criado, gate pnpm build OK (13/13), cache hit 11/13
- **[2026-06-21T20:57]** - *agile_reviewer* - `[Requer Refatoração]`: [M1] build:tokens em duplicata — turbo executa a task E o script build do design-system chama build:tokens internamente. Corrigir removendo build:tokens do script build do package.json (deixar só build:react). [i1] outputs build:tokens só cobre web/. [i2] build artifacts commitados. [i3] turbo version mismatch.
- **[2026-06-22T00:00]** - *Antigravity* - `[Iniciado]`: Corrigindo duplicidade de build:tokens e refinando outputs de cache
- **[2026-06-22T00:06]** - *Antigravity* - `[Finalizado]`: Rework concluido: duplicidade de build:tokens removida, cache refinado atingindo 100% cache hit (FULL TURBO)
