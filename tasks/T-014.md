---
id: T-014
title: "Migrar hook de metadados + skill para .claude da raiz"
status: review
complexity: 1
target_agent: devops_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-011"]
blocks: []
capacity_target: haiku
---

# T-014 · Migrar hook de metadados + skill para .claude da raiz

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo
- **Test Runner:** smoke manual do hook (PowerShell)
- **Capacidade-alvo:** haiku

## 1. Objetivo
Integrar a automação de autoria de UI do design system ao monorepo: o hook `PostToolUse` (que valida metadados e reconstrói o índice ao editar `*.metadata.ts`) e o skill `design-system-component-author`. O hook passa a operar no path novo (`packages/design-system`) e o skill é disponibilizado para o monorepo.

## 2. Contexto RAG (Spec-Driven Development)
- [x] `tasks/T-011.md` (status: done) — pré-requisito. Traz `scripts/validate-metadata.mjs` e `build-component-index.mjs` em `packages/design-system/`. Confirmado: T-011 **JÁ migrou** o skill para `packages/design-system/.claude/skills/design-system-component-author/SKILL.md` (4911 bytes, 2025-05-18). Há divergência de path a resolver (ver Seção 6).
- [x] Fonte original (Windows): `c:\Dev2026\Design System\.claude\settings.json` — **único arquivo presente** (sem subdiretório `skills/` lá). O hook original está dentro de `settings.json`:
  ```json
  {
    "hooks": {
      "PostToolUse": [
        {
          "matcher": "Edit|Write",
          "hooks": [
            {
              "type": "command",
              "command": "$j=[Console]::In.ReadToEnd()|ConvertFrom-Json; $f=$j.tool_input.file_path; if($f -and $f -match '\\.metadata\\.ts$'){Push-Location 'design-system'; try{npm run validate && npm run build:index}finally{Pop-Location}}",
              "shell": "powershell",
              "timeout": 30,
              "statusMessage": "Validating metadata and rebuilding index..."
            }
          ]
        }
      ]
    }
  }
  ```

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[READ]** `c:\Dev2026\Design System\.claude\settings.json` (hook original)
- **[READ]** `C:\Dev2026\.superapp-worktrees\T-011\packages\design-system\.claude\skills\design-system-component-author\SKILL.md` (skill já migrado por T-011)
- **[READ]** `C:\Dev2026\.superapp-worktrees\T-011\.claude\settings.json` (estado atual do monorepo — para mesclar)
- **[READ]** `C:\Dev2026\.superapp-worktrees\T-011\.claude\skills\` (estado atual do monorepo — para confirmar sobreposição com `executar-task`)
- **[CREATE]** `C:\Dev2026\.superapp-worktrees\<branch>\.claude\settings.json` (atualizar com hook ajustado — push-location `packages/design-system`)
- **[CREATE]** `C:\Dev2026\.superapp-worktrees\<branch>\.claude\skills\design-system-component-author\SKILL.md` (cópia simétrica ao do T-011 — ou SKIP se decisão for manter aninhado)
- **[KEEP]** `packages/design-system/CLAUDE.md` e `packages/design-system/AGENTS.md` permanecem **aninhados** (não mover para raiz)

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Framework:** smoke manual — editar um `*.metadata.ts` e confirmar que `validate` + `build:index` disparam.
- [x] **Ambiente do Teste:** PowerShell (hook é `shell: powershell`).
- [x] **Fora de Escopo:** lógica de validação de metadados (já testada pelos scripts do pacote T-011).

Casos de teste (smoke, numerados):
1. Editar `packages/design-system/src/components/Button/Button.metadata.ts` no monorepo — observar `statusMessage: "Validating metadata and rebuilding index..."` e `components.index.json` regenerado.
2. Validar que hooks pré-existentes no `.claude/settings.json` da raiz (se houver) foram preservados (merge, não overwrite).
3. Confirmar que `packages/design-system/CLAUDE.md` e `AGENTS.md` seguem aninhados (não foram movidos).
4. Editar arquivo fora de `packages/design-system` — confirmar que o hook **não** dispara (matcher regex `\.metadata\.ts$` exige o path completo, mas o `Push-Location` atual vai para o dir errado sem o match — revisar).

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** sobrescreva hooks já existentes no `.claude/settings.json` da raiz — **mescle** no array `PostToolUse`.
> - **NÃO** mova o `CLAUDE.md`/`AGENTS.md` do pacote para a raiz.
> - **NÃO** deixe `Push-Location 'design-system'` (path antigo do repo-fonte) — usar `'packages/design-system'`.
> - **NÃO** duplique o skill em dois caminhos simultaneamente sem decisão documentada (ver Seção 6).

1. Confirmar (read-only) o estado atual de `.claude/settings.json` e `.claude/skills/` no monorepo. Confirmar `packages/design-system/.claude/skills/design-system-component-author/SKILL.md` (já migrado por T-011).
2. Reescrever o hook ajustando o `Push-Location` para `packages/design-system` no monorepo. Atualizar também o `npm run` para `pnpm run` (T-011 usa pnpm).
3. Mesclar o hook no array `PostToolUse` existente em `.claude/settings.json` (preservar outros hooks).
4. Decidir (ver Seção 6) se o skill `design-system-component-author` deve ter cópia simétrica em `.claude/skills/` da raiz, ou se mantém-se apenas em `packages/design-system/.claude/skills/`.
5. Smoke test: editar um `*.metadata.ts` no monorepo e observar o disparo do hook.

## 6. Feedback de Especificação (Spec Feedback Loop)

> **✅ DECISÕES RESOLVIDAS (2026-06-23 — arquiteto):**
> - **Destino do skill:** opção **(a) — manter apenas o aninhado** (`packages/design-system/.claude/skills/`). Sem cópia na raiz nem symlink (evita drift/atrito no Windows).
> - **Runner do hook:** **`pnpm --filter @plataforma/design-system validate`** (filtro por nome de workspace; robusto de qualquer CWD no hook PowerShell — não usar `-C <path>`).
> - **Matcher do path:** **restringir a `packages/design-system/`** (defesa em profundidade; não disparar por `*.metadata.ts` de outros pacotes).

- **Decisão em aberto — path do skill:** a spec original da T-014 pede para criar em `.claude/skills/design-system-component-author/SKILL.md` (raiz), mas a T-011 (done) já migrou para `packages/design-system/.claude/skills/design-system-component-author/SKILL.md` (aninhado). Opções:
  - (a) Manter apenas o aninhado da T-011 (skill "escopado" ao pacote, segue o CLAUDE.md/AGENTS.md do pacote). T-014 não cria cópia na raiz.
  - (b) Criar cópia simétrica na raiz (skill disponível globalmente, sem precisar estar dentro do pacote).
  - (c) Criar symlink na raiz apontando para o aninhado.
  - Default sugerido: (a) — o skill do T-011 já está acessível via path-aware lookup, e a duplicação adiciona risco de drift. Mas o requisito original "no `.claude` da raiz" deve ser confirmado pelo arquiteto.

- **Decisão em aberto — runner do hook:** o hook original usa `npm run` (design-system-stub original). No monorepo usa-se `pnpm`. **Já decidido** por convenção: `pnpm` (T-001). Mas o hook precisa rodar `pnpm --filter @plataforma/design-system validate` ou `pnpm -C packages/design-system validate` — o arquiteto deve confirmar qual sintaxe funciona em PowerShell dentro do hook.

- **Decisão em aberto — `matcher` do path:** o regex `\.metadata\.ts$` no hook atual casa em qualquer path. O escopo do design system é `packages/design-system/**`. **Possível ataque:** editar `packages/transport/src/foo.metadata.ts` (se alguém criar) também dispararia. Hoje não existe tal arquivo, mas é defesa em profundidade. O arquiteto pode pedir para refinar o matcher.

Sem outras decisões abertas após o que está acima.

## 7. Definition of Done (DoD) & Reviewer Checklist

### Verificação automática (Gate de Evidência)
```bash
# Não há build/test para hook — verificação é smoke manual.
# Mas validamos o JSON do settings.json:
node -e "JSON.parse(require('fs').readFileSync('.claude/settings.json','utf8'))"
```
> **GATE DE EVIDÊNCIA:** Worker cola saída literal na Seção 8 + log do smoke test.

### Checklist do Reviewer
- [ ] O hook dispara ao editar `*.metadata.ts` em `packages/design-system`?
- [ ] `Push-Location` aponta para `packages/design-system` (não `design-system` legado)?
- [ ] `npm` foi trocado por `pnpm` (e usa `--filter` ou `-C`)?
- [ ] Hooks pré-existentes da raiz foram preservados (merge, não overwrite)?
- [ ] Skill decisão (a/b/c da Seção 6) implementada — sem cópia duplicada se (a)?
- [ ] `CLAUDE.md`/`AGENTS.md` do pacote seguem aninhados (não movidos)?
- [ ] JSON do `settings.json` é válido?

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- Hook criado em `.claude/settings.json` na raiz do monorepo (worktree T-014).
- Hook ajustado: `pnpm --filter @plataforma/design-system validate`, matcher restrito a `^packages/design-system/.*\\.metadata\\.ts$`. Sem `Push-Location` (não necessário com `--filter`).
- Skill mantido apenas em `packages/design-system/.claude/skills/design-system-component-author/` (decisão (a) da Seção 6). Sem cópia na raiz.
- `.claude/settings.json` não existia previamente — criado do zero (sem merge necessário).
- Packages design-system CLAUDE.md/AGENTS.md permanecem aninhados (não movidos).
- **Gate:** `node -e "JSON.parse(...)"` — output: `JSON válido` (colado abaixo).
- **Teste de regressão do matcher:** o regex `^packages/design-system/.*\.metadata\.ts$` NÃO casa com `packages/transport/src/foo.metadata.ts` nem com `packages/design-system/foo.ts`. Passa na intenção.

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória):**
```
Gate: JSON.parse OK → "JSON valido"
Caminho: C:\Dev2026\.superapp-worktrees\T-014\.claude\settings.json
Hook: pnpm --filter @plataforma/design-system validate (sem Push-Location)
Matcher: ^packages/design-system/.*\.metadata\.ts$
Skill: packages/design-system/.claude/skills/design-system-component-author/SKILL.md (aninhado, decisão a)
CLAUDE.md/AGENTS.md do pacote: intactos (não movidos)
```
- **Comentários de Revisão:**

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-06-23T18:55]** - *arquiteto* - `[Promovido]`: Decisoes em aberto resolvidas (2026-06-23) registradas na Secao 6.
- **[2026-06-24T17:49]** - *Crush* - `[Iniciado]`: iniciando execução — migrar hook + skill design-system
- **[2026-06-24T18:25]** - *Crush* - `[Finalizado]`: Hook criado em .claude/settings.json (pnpm --filter @plataforma/design-system validate, matcher restrito a packages/design-system/). Skill mantido aninhado (decisão a). Gate JSON.parse OK.
