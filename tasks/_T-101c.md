---
id: T-101c
title: "Wrapper Blake2s256 + HKDF"
status: draft
superseded_by: T-101
complexity: 2
target_agent: crypto_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-101a"]
blocks: []
parent: "T-101"
---

> **[SUPERSEDED]** Trabalho entregue por **T-101** (done) e já integrado na master do superapp.
> Decomposição não executada — mantida apenas para histórico. NÃO EXECUTAR.

# T-101c · Wrapper Blake2s256 + HKDF

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** haiku

## 1. Objetivo
Implementar wrappers isomórficos para Blake2s-256 e HKDF-SHA256 no pacote `@plataforma/crypto`, usando `@noble/hashes`
*(extraído de T-101 §1)*.

### Contratos exatos
```ts
// --- packages/crypto/src/blake2.ts ---
export function blake2s256(data: Uint8Array): Promise<Uint8Array>; // 32 bytes

// --- packages/crypto/src/hkdf.ts ---
export function hkdfSha256(ikm: Uint8Array, salt: Uint8Array, info: Uint8Array, length: number): Promise<Uint8Array>;
```

## 2. Contexto RAG
- [T-101 · Wrappers cripto](./T-101.md)

## 3. Escopo de Arquivos
- **[CREATE]** `packages/crypto/src/blake2.ts`
- **[CREATE]** `packages/crypto/src/hkdf.ts`
- **[CREATE]** `packages/crypto/tests/blake2.test.ts`
- **[CREATE]** `packages/crypto/tests/hkdf.test.ts`
- **[UPDATE]** `packages/crypto/src/index.ts` — re-export

## 4. Estratégia de Testes
- [x] **Framework:** Vitest (Node puro).

Casos de teste:
1. `blake2s256` sobre vetor RFC 7693 §4 (dado vazio).
2. `blake2s256` determinístico.
3. `hkdfSha256` roundtrip com vetor RFC 5869 §A.1 (Test Case 1).
4. `hkdfSha256` com `length=0` → array vazio.
5. `hkdfSha256` com salt vazio vs não-vazio → resultados diferentes.

## 5. Instruções de Execução
1. **[TDD]** Crie testes com casos 1–5.
2. Implemente `blake2s256` via `@noble/hashes/blake2s`.
3. Implemente `hkdfSha256` via `@noble/hashes/hkdf`.
4. Re-exporte. Rode build + test.

## 6. Feedback de Especificação
- *(Nenhuma pendência)*

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/crypto build
pnpm --filter @plataforma/crypto test
```

### Checklist
- [ ] Blake2s-256 RFC 7693 vetor?
- [ ] HKDF RFC 5869 vetor?
- [ ] 5 casos de teste passando?
- [ ] `pnpm --filter @plataforma/crypto build` e `test` verdes?

## 8. Log de Handover e Revisão Agile
### Handover do Executor:
-

### Parecer do Agente Revisor:
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução:**
```
(cole aqui a saída real de pnpm build e pnpm test)
```

## 9. Log de Execução
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
