---
id: T-313
title: "Archive Cargo"
status: draft
complexity: 5
target_agent: core_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-301", "T-305a"]
blocks: []
spec_status: blocked-decision
capacity_target: sonnet
decisions: ["complexity 5 exige decomposição (CLAUDE.md) — fatiar em sub-tasks (ex.: packArchive, unpackArchive, assignCustodian, GC, migration blind_archives)"]
---

# T-313 · Archive Cargo — custódia cega

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar Archive Cargo no pacote `@plataforma/core`: empacotamento de lote por época (zstd + AES-256-GCM), `archive_id = blake2s256(ciphertext)`, manifesto `CONTENT` sob `SPECIFICATION:ARCHIVE_MANIFEST`, arestas `ARCHIVES` sob sync UCAN-scoped, entrega via canal `EPHEMERAL`, armazenamento em `device_state.db` (tabela `blind_archives`), atribuição por consistent hashing sobre `blind_scope_id`, GC por `expires_at`
*(derivado de `docs/plano-de-implementacao.md` §6 T-313 — "RFC-005 §A.11")*.

### Contratos exatos
```ts
// --- packages/core/src/archive/archiveCargo.ts ---
import { EntityId, ULID } from '@plataforma/protocol'; // T-102
import { StoragePort } from '@plataforma/protocol'; // T-004
import { Fingerprint } from '@plataforma/protocol'; // T-301a

export interface ArchiveCargoPayload {
  blindScopeId: Uint8Array;       // scope cego (SHA-256 do entity_id real)
  epochIndex: number;
  hlcRange: { start: number; end: number };
  size: number;                   // bytes do ciphertext
  expiresAt: number;              // timestamp Unix ms
}

export interface ArchiveManifest {
  archiveId: Fingerprint;         // blake2s256(ciphertext)
  blindScopeId: Uint8Array;
  epochIndex: number;
  hlcRange: { start: number; end: number };
  size: number;
  expiresAt: number;
}

export interface ArchiveCargo {
  ciphertext: Uint8Array;         // zstd + AES-256-GCM com chave de época do escopo
  manifest: ArchiveManifest;
}

export interface ArchiveConfig {
  epochKey: Uint8Array;           // chave AES-256 da época do escopo
  compressionLevel?: number;      // zstd (default 3)
}

/** Empacota lote de nós + arestas em Archive Cargo. */
export function packArchive(
  nodes: Uint8Array[],            // nós serializados
  edges: Uint8Array[],            // arestas serializadas
  epochIndex: number,
  entityId: EntityId,
  config: ArchiveConfig,
): Promise<ArchiveCargo>;

/** Desempacota e decifra Archive Cargo. */
export function unpackArchive(cargo: ArchiveCargo, config: ArchiveConfig): Promise<{ nodes: Uint8Array[]; edges: Uint8Array[] }>;

/** Atribui custodiante por consistent hashing sobre blind_scope_id. */
export function assignCustodian(blindScopeId: Uint8Array, custodians: EntityId[]): EntityId;

/** Verifica integridade self-certifying. */
export function verifyArchiveIntegrity(cargo: ArchiveCargo): boolean;

/** GC: remove archives expirados. */
export function garbageCollectArchives(storage: StoragePort): Promise<number>;
```
- `packArchive`: concatena nós+arestas → zstd → AES-256-GCM (key=epochKey, nonce aleatório) → `archiveId = blake2s256(ciphertext)`.
- `unpackArchive`: decifra → descomprime → retorna arrays originais.
- `verifyArchiveIntegrity`: confere `blake2s256(ciphertext) === archiveId` + tag GCM válida.
- Custodiante: `consistent hashing` sobre `blind_scope_id` para distribuir archives entre peers.
- GC: remove da tabela `blind_archives` onde `expires_at < now()`.
- Vetor adversarial 6 estendido: peer sem UCAN não enxerga arestas `ARCHIVES`.

## 2. Contexto RAG
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (§6 T-313, §2.5 vetor 6)
- [T-301a · B-Tree](./T-301a.md) — Fingerprint
- [T-305a · Sync UCAN](./T-305a.md) — enforcement de UCAN

## 3. Escopo de Arquivos
- **[READ]** `packages/crypto/src/aes.ts` (AES-256-GCM — T-101a)
- **[READ]** `packages/crypto/src/blake2.ts` (blake2s256 — T-101c)
- **[READ]** `packages/core/src/schema.ts` (MIGRATIONS — T-106a)
- **[CREATE]** `packages/core/src/archive/archiveCargo.ts`
- **[CREATE]** `packages/core/src/archive/blindArchives.ts` (tabela `blind_archives`)
- **[CREATE]** `packages/core/tests/archiveCargo.test.ts`
- **[UPDATE]** `packages/core/src/schema.ts` — migration: tabela `blind_archives`
- **[UPDATE]** `packages/core/src/index.ts` — re-export

## 4. Estratégia de Testes
- [x] **Framework:** Vitest + SQLite `:memory:`.
- [x] **Ambiente:** `pnpm --filter @plataforma/core test`.

Casos de teste:
1. `packArchive` + `unpackArchive` roundtrip: dados originais preservados.
2. `verifyArchiveIntegrity` com archive íntegro → true.
3. `verifyArchiveIntegrity` com ciphertext adulterado → false.
4. `verifyArchiveIntegrity` com archiveId errado → false (self-certifying).
5. `assignCustodian` determinístico: mesmo input → mesmo custodiante.
6. `assignCustodian` distribui uniformemente entre custodiantes.
7. Custodiante recupera ciphertext por `archiveId` sem jamais ver `pub_key`/autoria.
8. Peer sem UCAN não enxerga arestas `ARCHIVES` (vetor 6 estendido).
9. `garbageCollectArchives` remove archives expirados.
10. `packArchive` com nós e arestas vazias → archive válido mas vazio.

## 5. Instruções de Execução
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** exponha `pub_key` ou autoria ao custodiante. Archive é cego.
> - **NÃO** armazene archives no grafo principal. Use `device_state.db`.

1. **[TDD]** Escreva testes com SQLite + chaves AES de época.
2. Implemente `packArchive` com zstd + AES-256-GCM.
3. Implemente `unpackArchive` e `verifyArchiveIntegrity`.
4. Implemente `assignCustodian` com consistent hashing.
5. Adicione migration da tabela `blind_archives`.
6. Implemente `garbageCollectArchives`.
7. Rode build + test.

## 6. Feedback de Especificação
- [Nenhum problema identificado]

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/core build
pnpm --filter @plataforma/core test
```

### Checklist
- [ ] Empacotamento: zstd + AES-256-GCM?
- [ ] Self-certifying: `blake2s256(ciphertext)` + tag GCM?
- [ ] Custodiante não vê `pub_key`/autoria (cega)?
- [ ] GC por `expires_at`?
- [ ] 10 casos de teste passando (incluindo vetor 6 estendido)?
- [ ] `pnpm --filter @plataforma/core build` e `test` verdes?
