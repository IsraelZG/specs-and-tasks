---
id: T-702
title: "Canal do Private Swarm"
status: done
capacity_target: sonnet
decisions: []
complexity: 4
target_agent: transport_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-202", "T-105"]
blocks: []
---

# T-702 · Canal do Private Swarm

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar canal do Private Swarm no pacote `@plataforma/transport`: `Device_Sync_Key = HKDF(master, "device-sync-v1")`; `RendezvousId = blake2s256(Device_Sync_Key)`; documento casca dedicado; E2E com Device_Sync_Key; fora do RBSR principal; prioridade entre background e aba ativa
*(derivado de `docs/plano-de-implementacao.md` §10 T-702)*.

### Contratos exatos
```ts
// --- packages/transport/src/privateSwarm/privateSwarm.ts 
---
import { PeerId } from '@plataforma/protocol';

export interface PrivateSwarmConfig {
  masterKey: Uint8Array;          // chave mestra do dispositivo
  maxPeers: number;               // default 10 (dispositivos do mesmo usuário)
  priorityMode: 'background' | 'active';
}

export interface PrivateSwarmChannel {
  channelId: string;              // RendezvousId
  syncKey: Uint8Array;            // Device_Sync_Key (32 bytes)
  connectedDevices: PeerId[];
  createdAt: number;
}

export interface PrivateSwarmMessage {
  type: 'rascunho' | 'preferencia' | 'prefetch_cache';
  payload: Uint8Array;            // cifrado com Device_Sync_Key
  senderDeviceId: PeerId;
  timestamp: number;
  hlc: number;
}

export class PrivateSwarm {
  constructor(config: PrivateSwarmConfig);

  /** Inicializa canal com Device_Sync_Key. */
  initChannel(): Promise<PrivateSwarmChannel>;

  /** Conecta novo dispositivo ao private swarm. */
  connectDevice(devicePeerId: PeerId): Promise<void>;

  /** Envia mensagem cifrada E2E. */
  sendMessage(msg: PrivateSwarmMessage): Promise<void>;

  /** Recebe mensagens do private swarm. Retorna função de unsubscribe. */
  onMessage(handler: (msg: PrivateSwarmMessage) => void): () => void;

  /** Define prioridade (background vs aba ativa). */
  setPriority(mode: 'background' | 'active'): void;

  /** Verifica que RendezvousId não permite derivar a chave mestra. */
  static verifyDerivationUnidirectionality(masterKey: Uint8Array): boolean;
}
```
- `Device_Sync_Key = HKDF(master, "device-sync-v1")` — derivação unidirecional.
- `RendezvousId = blake2s256(Device_Sync_Key)` — observação do RendezvousId não permite derivar a mestra.
- Canal fora do RBSR principal: usa documento casca Automerge dedicado (T-403).
- E2E: todas as mensagens cifradas com `Device_Sync_Key`.
- Tráfego ilegível para o relay (asserção).
- Prioridade: background (baixa frequência) vs aba ativa (tempo real).

## 2. Contexto RAG
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (§10 T-702)
- [T-202 · Noise_XX](./T-202.md) — canal seguro
- [T-105 · PeerId](./T-105.md) — DevicePeerId

## 3. Escopo de Arquivos
- **[READ]** `packages/protocol/src/peerId.ts` (DevicePeerId — T-105)
- **[READ]** `packages/crypto/src/wrappers.ts` (HKDF — T-101)
- **[CREATE]** `packages/transport/src/privateSwarm/privateSwarm.ts`
- **[CREATE]** `packages/transport/tests/privateSwarm.test.ts`
- **[UPDATE]** `packages/transport/src/index.ts` — re-export

## 4. Estratégia de Testes
- [x] **Framework:** Vitest + SimNetwork (T-005).
- [x] **Ambiente:** `pnpm --filter @plataforma/transport test`.

Casos de teste:
1. `initChannel` deriva `Device_Sync_Key` e `RendezvousId`.
2. `RendezvousId` observado não permite derivar chave mestra (unidirecional).
3. `sendMessage` cifra payload com `Device_Sync_Key` (E2E).
4. `onMessage` handler recebe mensagem decifrada.
5. Tráfego no relay é ilegível (asserção: dados cifrados com AES-GCM).
6. `connectDevice` adiciona peer ao canal.
7. `setPriority('background')` reduz frequência de sync.
8. Fora do RBSR principal: mensagens não aparecem na tabela `nodes`.

## 5. Instruções de Execução
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** coloque mensagens do private swarm no RBSR principal.
> - **NÃO** permita que a chave mestra seja derivável do RendezvousId.

1. **[TDD]** Escreva testes com SimNetwork e HKDF.
2. Implemente `initChannel` com derivação HKDF.
3. Implemente `sendMessage` com cifra AES-GCM.
4. Implemente `verifyDerivationUnidirectionality`.
5. Re-exporte. Rode build + test.

## 6. Feedback de Especificação
- **Gap de spec (contornado de forma aditiva):** o contrato exato define `constructor(config: PrivateSwarmConfig)` sem nenhuma porta de rede/relógio, mas a Seção 4 exige testes com SimNetwork (T-005) — impossível sem injeção. Adotei o padrão já estabelecido no pacote (`SyncCoordinator`): segundo parâmetro **opcional** `deps?: PrivateSwarmDeps { transport?: NetworkAdapterPort; clock?: ClockPort }`. Nenhum campo do contrato foi alterado/removido; call sites que seguem a spec ao pé da letra continuam compilando.
- `verifyDerivationUnidirectionality` tem assinatura **síncrona** (`: boolean`), mas os wrappers de `@plataforma/crypto` são async — usei `@noble/hashes` direto (sync, mesmo backend dos wrappers) nesse método estático; `initChannel` usa os wrappers `hkdfSha256`/`blake2s256` como a Seção 3 indica.
- O "documento casca Automerge dedicado (T-403)" não existe no código (T-403 não integrada); o canal dedicado foi materializado como envelope de fio próprio (`v: 'private-swarm/v1'`, roteado por `channel = RendezvousId`), sem qualquer dependência de StoragePort — teste 8 cobre a exclusão do RBSR/tabela `nodes`.

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/transport build
pnpm --filter @plataforma/transport test
```

### Checklist
- [ ] `Device_Sync_Key = HKDF(master, "device-sync-v1")`?
- [ ] RendezvousId não reversível para chave mestra?
- [ ] E2E: mensagens cifradas com Device_Sync_Key?
- [ ] Fora do RBSR principal?
- [ ] 8 casos de teste passando?
- [ ] `pnpm --filter @plataforma/transport build` e `test` verdes?

## 8. Evidência de Execução (Worker)

**Worker:** claude-sonnet · **Branch:** `task/T-702` · **HEAD pushado:** `11741dc`
**Commits:** `363a87f` (PrivateSwarm + 8 testes), `7a6521f` (merge master pós-T-1033 — traz o `/* eslint-disable */` do WebRtcAdapter que desbloqueou o lint do pacote), `11741dc` (reconcilia pnpm-lock pós-merge).

### Gate de Evidência (saída literal — worktree `C:\Dev2026\.superapp-worktrees\T-702`)

`pnpm --filter @plataforma/transport build`:
```
$ tsc
```
(exit 0, sem erros)

`pnpm --filter @plataforma/transport test`:
```
 ✓ tests/privateSwarm.test.ts (8 tests) 91ms

 Test Files  15 passed (15)
      Tests  140 passed (140)
   Duration  2.45s (transform 797ms, setup 0ms, collect 5.02s, tests 2.13s, environment 3ms, prepare 2.53s)
```

`pnpm --filter @plataforma/transport lint`:
```
$ eslint src/
```
(exit 0, sem violações)

### Resumo
- `packages/transport/src/privateSwarm/privateSwarm.ts` [CREATE]: `Device_Sync_Key = HKDF-SHA256(master, salt=∅, "device-sync-v1", 32)`; `RendezvousId = blake2s256(syncKey)` (hex); corpo inteiro da mensagem cifrado AES-256-GCM sob a syncKey — relay vê só `{v, channel, nonce, ct, tag}`; prioridade `background` acumula e flusha a cada 30 s (`BACKGROUND_FLUSH_INTERVAL_MS`), `active` = tempo real; `verifyDerivationUnidirectionality` estático sync via @noble.
- `packages/transport/tests/privateSwarm.test.ts` [CREATE]: 8/8 casos da Seção 4 verdes (derivação, unidirecionalidade, E2E cifrado, decifração no handler, ilegibilidade no relay, connectDevice+maxPeers, prioridade com VirtualClock, fora do RBSR).
- `packages/transport/src/index.ts` [UPDATE]: re-export de `PrivateSwarm` + tipos.
- Desvios/observações registrados na Seção 6 (deps opcionais p/ SimNetwork; @noble direto no método estático sync; casca T-403 ausente no código).

## 9. Log de Execução (Agent Execution Log)
- **[2026-06-29T23:21]** - *Antigravity* - `[Promovida p/ ready]`: spec_status hardened — flip draft→ready
- **[2026-07-08T01:17]** - *claude-sonnet* - `[Iniciado]`: iniciando implementacao canal Private Swarm
- **[2026-07-08T10:36]** - *claude-sonnet* - `[Finalizado]`: PrivateSwarm implementado (HKDF device-sync-v1 + blake2s256 rendezvous + AES-GCM E2E, prioridade background/active). Gate verde: build (tsc exit 0) + test (15 files / 140 tests passed, 8/8 novos privateSwarm) + lint (eslint src/ exit 0). Branch task/T-702 @ 11741dc pushada. Evidencia literal na Secao 8; desvios aditivos registrados na Secao 6.
- **[2026-07-08T11:45]** - *agile_reviewer:minimax* - `[Em revisão]`: revisando

---

## 8b. Parecer do Agente Revisor (Reviewer)

### Parecer do Agente Revisor (Reviewer) (minimax, independente)
- [x] **Aprovado**
- [ ] **Requer Refatoração**

**Evidência de Execução (re-execução do Gate no worktree `C:\Dev2026\.superapp-worktrees\T-702`, branch `task/T-702` @ `11741dc`):**

`pnpm --filter @plataforma/transport build`:
```
$ tsc
```
(exit 0, sem erros)

`pnpm --filter @plataforma/transport test`:
```
$ vitest run

✓ tests/webrtc.test.ts (13 tests) 105ms
✓ tests/websocket.test.ts (17 tests) 191ms
✓ tests/noiseServer.test.ts (2 tests) 258ms
✓ tests/privateSwarm.test.ts (8 tests) 302ms
✓ tests/SwarmRegistry.audit.test.ts (7 tests) 15ms
✓ tests/noiseHandshake.test.ts (13 tests) 1714ms
✓ tests/SwarmRegistry.test.ts (14 tests) 8ms
✓ tests/fpp.test.ts (17 tests) 12ms
✓ tests/graphRouting.test.ts (11 tests) 8ms
✓ tests/syncCoordinator.test.ts (8 tests) 891ms
✓ tests/relayTrustModel.test.ts (11 tests) 4ms
✓ tests/mock.test.ts (1 test) 2ms
✓ tests/transportMatrix.test.ts (8 tests) 4ms
✓ tests/heartbeat.test.ts (6 tests) 5ms
✓ poc/noise-xx.poc.test.ts (4 tests) 30ms

Test Files  15 passed (15)
     Tests  140 passed (140)
```
(15 files · 140 tests passed · 0 failed)

`pnpm --filter @plataforma/transport lint`:
```
$ eslint src/
```
(exit 0, sem violações)

**Sondas adversariais (cobertura ativa, depois removidas — 6/6 passaram):**
- **S1 — authTag adulterado:** tampering do `tag` no envelope resulta em drop silencioso (handler não é chamado); E2E/AEAD se mantém íntegro.
- **S2 — channel mismatch:** envelope com `channel` diferente do nosso é descartado sem decifração.
- **S3 — masterKey fora do tamanho (16 bytes):** HKDF/blake2s256 derivam sem crash; `verifyDerivationUnidirectionality` retorna `true`.
- **S4 — `setPriority("active")` em modo background:** outbox pendente é flushado imediatamente após o switch.
- **S5 — multi-peer broadcast:** mensagem chega a **todos** os `connectedDevices`.
- **S6 — `BACKGROUND_FLUSH_INTERVAL_MS`:** constante declarada em 30_000 ms (consistente com a spec §1).

**Conformidade com a Spec (seções 1–7):**
- Seção 1 (Contrato exato): todos os tipos e assinaturas da `PrivateSwarm` batem (constructor com `PrivateSwarmConfig`; `initChannel`, `connectDevice`, `sendMessage`, `onMessage`, `setPriority`, `verifyDerivationUnidirectionality`).
- Seção 1 (Crypto): `Device_Sync_Key = HKDF-SHA256(master, salt=∅, info="device-sync-v1", 32)` e `RendezvousId = blake2s256(Device_Sync_Key)` (hex 64 chars) — verificados.
- Seção 1 (E2E + fora do RBSR): corpo cifrado com AES-256-GCM sob a syncKey; envelope visível no relay contém apenas `{v, channel, nonce, ct, tag}` — teste 5 confirma e S1 confirma que violação do tag é silenciosamente dropada (não vaza plaintext).
- Seção 4 (8 casos de teste): 8/8 verdes + sondas cobrem 6 bordas adicionais.
- Seção 7 (DoD): 6/6 itens do checklist confirmados.

**Gates arquiteturais transversais (Agile Reviewer §5.1):**
- **Wiring (primitiva de privacidade):** `PrivateSwarm` é exportado por `packages/transport/src/index.ts:30` mas não tem caller de produção no monorepo. Ver `Grep` em `packages/` e `apps/` — único import é o próprio `tests/privateSwarm.test.ts`. **Não é bloqueante**: T-702 entrega a primitiva (escopo declarado §3 cobre apenas `[CREATE]` + `[UPDATE]` re-export; nenhuma `[CREATE]` de caller de produção foi pedida); integração com app/product é trabalho de uma task futura. **(i1) INFO — registrar follow-up de integração no backlog quando aplicável.**
- **Acoplamento/ciclo:** `privateSwarm.ts` importa de `@plataforma/protocol` e `@plataforma/crypto` — direção `protocol ← crypto ← transport` respeitada, **sem ciclo**.

**DoD checklist (§7):**
- [x] `Device_Sync_Key = HKDF(master, "device-sync-v1")` — teste 1 + `privateSwarm.ts:191`.
- [x] RendezvousId não reversível para chave mestra — teste 2 + `verifyDerivationUnidirectionality` (estático).
- [x] E2E: mensagens cifradas com Device_Sync_Key — testes 3 e 5.
- [x] Fora do RBSR principal — teste 8 confirma zero chamadas a `StoragePort.exec`; canal roteado por `channel = RendezvousId` em envelope dedicado `private-swarm/v1`.
- [x] 8 casos de teste passando — `vitest run` 8/8 em `tests/privateSwarm.test.ts`.
- [x] `pnpm --filter @plataforma/transport build` e `test` verdes — ver Evidência de Execução acima; lint também verde (Gate INVIOLÁVEL pós-T-807/EST-02b).

**Achados:**

BLOCKER (0)
MAJOR (0)
MINOR (0)
INFO (1)
- **[i1]** `PrivateSwarm` entregue como primitiva sem caller de produção. Conforme spec §3 não há caller declarado; fica como follow-up natural de uma task de integração (fora do escopo de T-702).

**VEREDICTO: APROVADO**

Resumo: spec atendida ao pé da letra (com 3 desvios aditivos justificados em §6 — `PrivateSwarmDeps` para injetar `transport`/`clock` em teste, `@noble/hashes` direto no método estático sync, e envelope `private-swarm/v1` no lugar do "documento casca Automerge" da T-403 que não existe no código). Gate verde (build/test/lint), 8/8 testes da spec, 6/6 sondas adversariais verdes, sem ciclo de dependência, sem acoplamento indevido.
- **[2026-07-08T11:56]** - *agile_reviewer:minimax* - `[Aprovado]`: Integrado: merge na master (commit 79cdee2), worktree removida, Gate verde no superapp (pnpm --filter @plataforma/transport build/test/lint: 15 files / 140 tests passed, 0 errors). 1 nao-bloqueante (i1: primitiva sem caller de producao) -> ledger de pendencias.
