---
id: C-13
title: "Cleanup: bancada (T-111/T-512/T-807)"
status: ready
complexity: 2
target_agent: frontend_agent # perfis: devops_agent, logic_agent, crypto_agent, frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential # parallel | sequential
dependencies: [] # IDs de tarefas que bloqueiam esta
blocks: [] # IDs de tarefas que esta bloqueia
capacity_target: haiku
# decisions: ["..."]          ← só quando status: draft:pending_decision (espelha a Seção 6)
---

# C-13 · Cleanup: bancada (T-111/T-512/T-807)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (`pnpm build`, `pnpm test`, `pnpm lint` na raiz afetam todos os pacotes)
- **Test Runner:** `vitest` (pacotes core/protocol) e `playwright` (E2E/Frontend)

## 1. Objetivo
Resolver ~5 achados não-bloqueantes na Bancada (apps/bancada): singletons de módulo, divergência de paths spec↔impl, fragilidade em teste, console.log residuais e wiring de placeholders.

## 2. Contexto RAG
- [x] `tasks/T-512.md` — AuthTab (non-blocking findings m1-m4, i1)
- [x] `tasks/T-807.md` — MidiaTab (non-blocking findings i1-i2)
- [x] `tasks/T-111.md` — IdentidadeTab (zero non-blocking findings)

## 3. Escopo de Arquivos
- **[UPDATE]** `apps/bancada/src/App.tsx` — módulo refatorar singletons p/ factory function
- **[UPDATE]** `apps/bancada/src/components/tabs/AuthTab.tsx` — nada (path já correto; spec que está errada)
- **[UPDATE]** `apps/bancada/tests/AuthTab.test.tsx` — console.log residuals, fragilidade getByRole
- **[UPDATE]** `apps/bancada/tests/App.test.tsx` — console.log residuals
- **[UPDATE]** `apps/bancada/tests/e2e/auth.smoke.spec.ts` — console.log residuals

## 4. Lista de Achados
> **Regra: Disposição obrigatória por achado.** Cada achado DEVE ser fechado com `fixed`/`no-op`/`defer→T-YYY` no §8.

1. `[m1][T-512][bancada]` Singletons de módulo no `App.tsx` — `keyVault = new KeyVault(systemClock)` e `swarm = new SwarmRegistry()` no escopo do módulo; refatorar para factory function (App.tsx)
2. `[m2][T-512][bancada]` Path divergente: spec §3 diz `tabs/AuthTab.tsx`, impl em `components/tabs/AuthTab.tsx` — alinhar spec OU mover arquivo (AuthTab.tsx)
3. `[m3][T-512][bancada]` Fragilidade no teste 3: `getByRole('button', { name: /expandir/i })` casa "Expandir" e "Recolher"; inverter estado no segundo clique mascara edge case (AuthTab.test.tsx)
4. `[i1][T-512][bancada]` `console.log` residuals nos arquivos de teste — limpar para PR sem debug output (AuthTab.test.tsx, App.test.tsx, auth.smoke.spec.ts)
5. `[i2][T-807][bancada]` Wiring real dos placeholders `midiaUpload`/`midiaPlay`/`midiaKillWebSeed`/`midiaP2PMonitor` sem task de integração — criar followup ou documentar defer (App.tsx:32-52)

## 5. Feedback de Especificação (Spec Feedback Loop)
- *[Nenhum problema identificado]*

## 6. Definition of Done (DoD)
- [ ] Singletons `keyVault`/`swarm` refatorados para factory function?
- [ ] Path spec §3 alinhado com impl OU arquivo movido?
- [ ] Teste 3 robusto contra regex ambíguo?
- [ ] `console.log` removido dos 3 arquivos de teste?
- [ ] Wiring placeholders com task de integração OU defer documentado?
- [ ] 5 achados dispostos (fixed / no-op / defer)?

### Verificação automática
```bash
pnpm --filter @plataforma/bancada build
pnpm --filter @plataforma/bancada test
pnpm --filter @plataforma/bancada lint
pnpm --filter @plataforma/bancada test:e2e
```
> **GATE DE EVIDÊNCIA:** nem o `finish` (worker) nem o veredito (reviewer) são válidos sem a saída literal desses comandos colada na seção 8.

## 7. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- 

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória — colar saída de build/tsc + test + lint):**
```
(cole aqui a saída real de pnpm build, pnpm test e pnpm lint)
```
- **Comentários de Revisão:**

## 8. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-07-06T13:58]** - *deepseek* - `[Triado]`: triado — cleanup bancada, capacity=haiku

## 9. Log de Execução (Agent Execution Log)
- **[2026-07-06T17:20]** - *big-pickle* - `[Endurecido]`: endurece C-13: 5 achados bancada, zero decisoes abertas, capacity=haiku
- **[2026-07-06T17:20]** - *big-pickle* - `[Promovida p/ ready]`: draft:hardened com deps done — safety-net flip
