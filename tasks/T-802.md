---
id: T-802
title: "Remontagem verificada por Merkle/InfoHash (fronteira de confiança do BlobStorage)"
status: done
complexity: 3
target_agent: core_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-801"]
blocks: []
capacity_target: sonnet
---

# T-802 · Remontagem verificada por Merkle/InfoHash (fronteira de confiança do BlobStorage)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo — pacote alvo: `packages/media/`
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Adicionar a **camada de verificação de integridade** que falta na T-801. `Chunker.reassemble` (T-801)
apenas tolera entrega fora de ordem e valida sanidade de índices (`0..n-1`) — ele **não** detecta
reordenação maliciosa nem adulteração de ciphertext, porque AES-256-GCM autentica o conteúdo de cada
chunk isoladamente mas **não liga um chunk à sua posição**. Esta task implementa a **fronteira de
confiança**: verificar os chunks recebidos contra o **InfoHash** (raiz da Merkle Tree sobre os
`ciphertextHash`, em ordem de `index`) **antes** de confiar nos bytes — e só então remontar.

> **Contexto da decisão (T-801 §6, 2026-06-23):** decisão "C agora + B como T-802". `reassemble` ficou
> sendo o helper local tolerante; a garantia contra adulteração de ordem/conteúdo é desta task.

*(Derivado de `docs/caderno-3-sdk/05-media-transport-plane.md`: "o **InfoHash** é calculado **sobre o
ciphertext** — nunca sobre o plaintext" (linha 22) e "validação por chunk: a tag autentica cada peça
antes de entregar ao player" (linha 30). Verificado no disco 2026-06-23.)*

### Contratos exatos (assinaturas TS fixadas)

```ts
// --- packages/media/src/blob/verifyReassemble.ts 
---
import type { AesKey, Sha256Digest } from '@plataforma/crypto'; // T-101
import type { EncryptedChunk } from './Chunker.js';             // T-801

/** Erro de integridade: índices inconsistentes, ciphertext adulterado, ou InfoHash divergente. */
export class BlobIntegrityError extends Error {
  /** Motivo legível por máquina, para o caller decidir (rejeitar peer, retentar, etc.). */
  readonly reason:
    | 'index_not_contiguous'     // o conjunto de `index` ≠ 0..n-1 (faltando/duplicado)
    | 'ciphertext_hash_mismatch' // sha256(ciphertext) ≠ ciphertextHash declarado em algum chunk
    | 'infohash_mismatch'        // raiz Merkle recomputada ≠ expectedInfoHash (inclui reordenação)
    | 'empty';                   // lista de chunks vazia
  constructor(reason: BlobIntegrityError['reason'], message: string);
}

/**
 * Verifica a integridade do conjunto de chunks contra o InfoHash esperado, SEM decifrar.
 * Útil para validar antes de aceitar/baixar (não precisa da chave).
 * Passos (todos obrigatórios, nesta ordem):
 *  1. `chunks` não vazio (senão `BlobIntegrityError('empty')`).
 *  2. Sanidade: ordenado por `index`, o conjunto deve ser exatamente `0..n-1`
 *     (contíguo, único) — senão `BlobIntegrityError('index_not_contiguous')`.
 *  3. Para cada chunk: `sha256(chunk.ciphertext)` deve === `chunk.ciphertextHash`
 *     — senão `BlobIntegrityError('ciphertext_hash_mismatch')` (detecta ciphertext adulterado
 *     e `ciphertextHash` mentiroso, ANTES de qualquer decifração).
 *  4. `MerkleTree.build(ordenado.map(c => c.ciphertextHash))` (T-801) → a raiz deve === `expectedInfoHash`
 *     — senão `BlobIntegrityError('infohash_mismatch')` (detecta reordenação maliciosa e chunk trocado).
 * Retorna `true` se íntegro. NUNCA retorna `false` — divergência sempre lança (com `reason`).
 */
export function verifyChunks(
  chunks: EncryptedChunk[],
  expectedInfoHash: Sha256Digest,
): Promise<true>;

/**
 * Verifica (via `verifyChunks`) e SÓ ENTÃO remonta o plaintext (via `Chunker.reassemble` de T-801).
 * É a única forma segura de reconstruir um blob vindo de fonte não confiável (P2P/torrent).
 * Lança `BlobIntegrityError` (sem decifrar) se a verificação falhar; caso contrário devolve os bytes.
 */
export function verifyAndReassemble(
  chunks: EncryptedChunk[],
  key: AesKey,
  expectedInfoHash: Sha256Digest,
): Promise<Uint8Array>;
```

## 2. Contexto RAG (Spec-Driven Development)
- [`docs/caderno-3-sdk/05-media-transport-plane.md`](../docs/caderno-3-sdk/05-media-transport-plane.md) — "InfoHash calculado sobre o ciphertext, nunca sobre o plaintext" (linha 22); "validação por chunk: a tag autentica cada peça antes de entregar" (linha 30). Verificado no disco (2026-06-23).
- [`tasks/T-801.md`](T-801.md) (dependência) — produz `EncryptedChunk`, `Chunker.reassemble` (tolerante + sanidade `0..n-1`) e `MerkleTree.build`/`root` (InfoHash sobre `ciphertextHash`, async). §6 de T-801 documenta a decisão "C + T-802". Esta task **consome** esses artefatos; não os reabre.
- `packages/crypto/src/wrappers.ts` — `sha256(data): Promise<Sha256Digest>` (T-101). `Sha256Digest` é `Uint8Array`.

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[READ]** `packages/media/src/blob/Chunker.ts` — `EncryptedChunk`, `Chunker` (`reassemble`)
- **[READ]** `packages/media/src/blob/MerkleTree.ts` — `MerkleTree.build`, `get root`
- **[READ]** `packages/crypto/src/wrappers.ts` — `sha256`, `Sha256Digest`, `AesKey`
- **[CREATE]** `packages/media/src/blob/verifyReassemble.ts` — `BlobIntegrityError`, `verifyChunks`, `verifyAndReassemble`
- **[CREATE]** `packages/media/tests/blob/verifyReassemble.test.ts` — casos 1–7
- **[UPDATE]** `packages/media/src/index.ts` — re-export `verifyChunks`, `verifyAndReassemble`, `BlobIntegrityError`
- Não toque em `Chunker.ts`/`MerkleTree.ts` (são de T-801) nem em nada fora de `packages/media/**`.

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Framework:** Vitest (Node puro).
- [x] **Ambiente do Teste:** `pnpm --filter @plataforma/media test`.
- [x] **Fora de Escopo:** OPFS/IndexedDB, WebTorrent, rede, Key Vault, entrega de chave (UCAN). A chave chega pronta.

### Casos de teste (numerados — 1:1 em `verifyReassemble.test.ts`)
1. **Roundtrip íntegro:** `process(data, key)` (T-801) → `infoHash = (await MerkleTree.build(chunks.map(c=>c.ciphertextHash))).root` → `verifyAndReassemble(chunks, key, infoHash)` devolve bytes === `data`.
2. **InfoHash errado:** conjunto válido, mas `expectedInfoHash` com 1 byte trocado → `BlobIntegrityError` com `reason:'infohash_mismatch'`.
3. **Reordenação maliciosa (o caso que `reassemble` sozinho NÃO pega):** trocar os campos `index` de dois chunks (mantendo o conjunto `0..n-1`) → `verifyAndReassemble` lança `infohash_mismatch` (a raiz Merkle depende da ordem dos `ciphertextHash`).
4. **Ciphertext adulterado (1 byte):** adulterar 1 byte do `ciphertext` de um chunk (sem corrigir `ciphertextHash`) → `ciphertext_hash_mismatch`, **antes** de qualquer decifração.
5. **Índice faltando/duplicado:** remover um chunk do meio (conjunto ≠ `0..n-1`) → `index_not_contiguous`.
6. **`ciphertextHash` mentiroso:** chunk com `ciphertextHash` que não bate `sha256(ciphertext)` → `ciphertext_hash_mismatch`.
7. **`verifyChunks` sem decifrar:** retorna `true` para conjunto íntegro (não precisa de chave); lança `BlobIntegrityError` para as adulterações dos casos 2–6; lista vazia → `reason:'empty'`.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** decifre antes de verificar — a ordem é sanidade → ciphertextHash → InfoHash → (só então) `reassemble`. Decifrar primeiro anula a fronteira de confiança.
> - **NÃO** reimplemente a Merkle Tree nem o slice/encrypt — reutilize `MerkleTree.build` e `Chunker.reassemble` da T-801.
> - **NÃO** compare `Sha256Digest` com `===` (são `Uint8Array`); compare byte a byte — `a === b` em `Uint8Array` testa identidade de referência, não conteúdo.
> - **NÃO** retorne `false` em divergência — sempre lance `BlobIntegrityError` com o `reason` específico.
> - **NÃO** acesse Key Vault, UCAN, rede ou SQLite — a chave chega pronta por parâmetro.

### Pegadinhas conhecidas
- `MerkleTree.build`/`verifyProof` são **async** (`Promise`) na T-801 (decisão 2026-06-23) — use `await`. `getProof`/`root` são síncronos.
- A raiz Merkle **depende da ordem** dos `ciphertextHash`. Ordene os chunks por `index` **antes** de mapear para `ciphertextHash` (igual ao que `reassemble` faz), senão a comparação de InfoHash falha em entrega fora de ordem legítima.
- Comparação de digests: escreva um helper local `equalBytes(a, b)` (compara `length` + cada byte). Não use `Buffer` (mantenha Node-puro/`Uint8Array`, compatível com browser).
- `verifyChunks` **não** recebe a chave — é verificação de hash, não decifração. Só `verifyAndReassemble` recebe `key`.
- Lint `@typescript-eslint/no-non-null-assertion` está ativo — sem `!`; use guardas explícitas (como T-801 faz em `MerkleTree.ts`).

1. **[TDD]** Crie `tests/blob/verifyReassemble.test.ts` com os casos 1–7 (gere fixtures com `Chunker.process` real, como o caso 13 de T-801).
2. Implemente `BlobIntegrityError` (com `reason`), `verifyChunks` e `verifyAndReassemble` em `src/blob/verifyReassemble.ts`.
3. Re-exporte em `src/index.ts`.
4. Rode build + test + lint (Seção 7) e cole a saída literal na Seção 8.

## 6. Feedback de Especificação (Spec Feedback Loop)
- **Fora de escopo (não pendente) — verificação parcial/streaming:** validar chunk-a-chunk durante download progressivo (HTTP Range, caderno-3-sdk/05 §5.2) usando `MerkleTree.getProof`/`verifyProof` por peça é uma evolução (T-803+). Esta task verifica o conjunto completo antes de remontar. Deliberadamente adiado.
- **Fora de escopo (não pendente) — entrega de chave:** a `key` chega pronta; envelopamento sob chave de época e validação UCAN são do Key Vault (T-110/T-501), não desta task.
- *[Nenhuma decisão em aberto]*

## 7. Definition of Done (DoD) & Reviewer Checklist

### Verificação automática (Gate de Evidência)
```bash
pnpm --filter @plataforma/media build
pnpm --filter @plataforma/media test
pnpm --filter @plataforma/media lint
```
> **GATE DE EVIDÊNCIA:** nem o `finish` (worker) nem o veredito (reviewer) são válidos sem a saída literal desses comandos colada na Seção 8.

### Checklist do Reviewer
- [ ] `verifyAndReassemble` decifra **somente** após verificar (ordem: sanidade → ciphertextHash → InfoHash → reassemble)?
- [ ] **Reordenação maliciosa** (troca de `index` mantendo o conjunto) é detectada como `infohash_mismatch` (caso 3)? — ponto central da task.
- [ ] Ciphertext adulterado → `ciphertext_hash_mismatch` antes de decifrar (caso 4)?
- [ ] `verifyChunks` funciona **sem** a chave e lança com `reason` específico (caso 7)?
- [ ] Reutiliza `MerkleTree.build` e `Chunker.reassemble` de T-801 (não reimplementa)?
- [ ] Comparação de digests byte a byte (não `===` de referência)?
- [ ] Nenhum arquivo fora de `packages/media/**`; `Chunker.ts`/`MerkleTree.ts` intactos?
- [ ] 7 casos passando; `build`/`test`/`lint` verdes?

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- **[2026-06-29T11:38]** - *Crush* - Gate executado: lint ✅ (0 erros), build ✅ (tsc sem erros), test ✅ (4 arquivos, 24 testes, 0 falhas; 9/9 verifyReassemble passando).


### Parecer do Agente Revisor (Reviewer):
- [x] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória — colar saída de build/test/lint):**
```
$ pnpm --filter @plataforma/media build
$ tsc
(Exit 0 — sem erros)

$ pnpm --filter @plataforma/media lint
$ eslint src/
(Exit 0 — 0 erros, 0 warnings)

$ pnpm --filter @plataforma/media test
$ vitest run
 ✓ tests/mock.test.ts                       ( 1 test)  2ms
 ✓ tests/blob/MerkleTree.test.ts            ( 6 tests) 18ms
 ✓ tests/blob/verifyReassemble.test.ts      ( 9 tests) 23ms
 ✓ tests/blob/Chunker.test.ts               ( 8 tests) 240ms

 Test Files  4 passed (4)
      Tests  24 passed (24)
   Duration  875ms
(Exit 0 — 9/9 verifyReassemble passando; cobre casos 1-7 da spec)

$ Sondas adversariais (removidas após execução):
 ✓ P1: duplicate index (0,0,2,...)         → index_not_contiguous
 ✓ P2: reordenação maliciosa via swap      → infohash_mismatch
 ✓ P3: InfoHash zero-filled 32 bytes        → infohash_mismatch
 ✓ P4: ciphertext adulterado no ÚLTIMO      → ciphertext_hash_mismatch
 ✓ P5: roundtrip verifyAndReassemble       → === testData
 ✓ P6: ordem dos checks (ciphertextHash     → ciphertext_hash_mismatch
     antes de InfoHash)                       (prova que spec §5 está ok)
```
- **Comentários de Revisão:**

**Veredicto: APROVADO** — Gate verde, 9/9 casos da spec passando, ordem dos checks correta, scope limpo, nenhuma derivação da spec.

**Verificações do DoD (Seção 7):**
- [x] `verifyAndReassemble` decifra somente após verificar (sanidade → ciphertextHash → InfoHash → reassemble)? — **Sim** (`verifyReassemble.ts:80` chama `verifyChunks` antes de `Chunker.reassemble`).
- [x] Reordenação maliciosa detectada como `infohash_mismatch`? — **Sim** (caso 3 + sonda P2).
- [x] Ciphertext adulterado → `ciphertext_hash_mismatch` antes de decifrar? — **Sim** (caso 4 + sonda P4).
- [x] `verifyChunks` funciona sem chave e lança com `reason` específico? — **Sim** (casos 7a/7b/7c).
- [x] Reutiliza `MerkleTree.build` e `Chunker.reassemble` de T-801? — **Sim** (imports, não reimplementa).
- [x] Comparação de digests byte a byte? — **Sim** (`equalBytes` helper, `verifyReassemble.ts:20-26`).
- [x] Nenhum arquivo fora de `packages/media/**`; `Chunker.ts`/`MerkleTree.ts` intactos? — **Sim** (git diff confirma).
- [x] 7 casos passando; build/test/lint verdes? — **Sim** (9 testes, 24 totais no pacote).

**[m1] MINOR — `!` non-null assertion no arquivo de teste.**
- **Local:** `packages/media/tests/blob/verifyReassemble.test.ts:53-55, 98, 147-151, 22` (`mutateByte`).
- **Evidência:** spec §5 diz "Lint `@typescript-eslint/no-non-null-assertion` está ativo — sem `!`; use guardas explícitas (como T-801 faz em `MerkleTree.ts`)". O `src/` (impl) está limpo (usa guards `=== undefined`); o `tests/` usa `reordered[0]!` e `tampered[0]!.ciphertext`. O lint não flagou (provavelmente a regra não cobre `tests/`), mas a spec proíbe `!` no geral. Refatoração de hygiene — não bloqueia.
- **Ação corretiva:** substituir `x[0]!` por guards `if (x.length === 0) throw new Error(...)` ou usar `x.at(0) ?? throw` no helper `mutateByte` e nos pontos de swap. Não-bloqueante → ledger.

**[i1] INFO — `mutateByte` helper com assinatura `idx, delta = 1`.**
- **Local:** `verifyReassemble.test.ts:20-24`.
- Helper de teste legível e isolado. Adequado. Sem ação.

**[i2] INFO — Implementação enxuta (84 linhas src, 167 linhas test).**
- **Local:** `packages/media/src/blob/verifyReassemble.ts`.
- Segue o spec ao pé da letra, sem abstração especulativa. Verificação é feita por composição (sort + guard + sha256 + MerkleTree.build) sem reinventar nada. Acima do padrão para `capacity_target: sonnet` — caberia em Haiku. Positivo.

**Resumo:** Implementação fiel à spec, Gate verde, todas as sondas adversariais passam. Único achado é hygiene de `!` no teste. Sem bloqueantes — encaminho para integração.

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-06-23T18:26]** - *arquiteto* - `[Promovido]`: Spec endurecida: contratos TS fixados (verifyChunks/verifyAndReassemble/BlobIntegrityError), 7 casos de teste, RAG verificado no disco, decisao B da T-801. Zero decisoes em aberto.
- **[2026-06-29T14:36]** - *Crush* - `[Iniciado]`: iniciando implementação da remontagem verificada
- **[2026-06-29T14:38]** - *Crush* - `[Finalizado]`: Gate verde: lint 0, build ok, test 24/24 (9 verifyReassemble). Commit 7ed9b88 pushado.
- **[2026-06-29T14:53]** - *agile_reviewer* - `[Aprovado]`: Integrado: merge na master (commit aed6ba3), worktree removida, Gate verde (lint 0, build 0, test 24/24 — 9 verifyReassemble). 1 não-bloqueante (m1: ! non-null em test) → ledger de pendências.
