---
id: T-1053
title: "Escape hatch: reatribuir machine de task travada por veto multi-máquina"
status: ready
complexity: 2
target_agent: devops_agent # perfis: devops_agent, logic_agent, crypto_agent, frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential # parallel | sequential
test_profile: full # backend | ui | full — reduza só quando o escopo provar que browser não agrega
dependencies: [] # IDs de tarefas que bloqueiam esta
blocks: [] # IDs de tarefas que esta bloqueia
capacity_target: sonnet
# decisions: ["..."]          ← só quando status: draft:pending_decision (espelha a Seção 6)
---

# T-1053 · Escape hatch: reatribuir machine de task travada por veto multi-máquina

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Repo:** este é código de **tooling do controle** (Docs), não do superapp — não há worktree/branch
  `task/T-1053` no superapp, nem `pnpm gate <pkg>`. Verificação é `node --test` direto (ver §7).
- **Test Runner:** `node --test` (nativo, mesmo padrão de `tools/scripts/concluir-task.test.mjs`)
- **Capacidade-alvo:** sonnet — contratos e arquivos já fixados abaixo, zero decisões em aberto.

## 1. Objetivo
O veto multi-máquina (`tools/scripts/manage-task.mjs:40-47`, commit `5139906`) grava `machine:
<hostname>` no frontmatter no `start` e bloqueia `start`/`finish`/`pause`/`block` de qualquer outra
máquina. Isso fecha a duplicação que ocorreu no T-505a (retomado em duas máquinas), mas não tem via
formal de destravar uma task cuja máquina original ficou permanentemente inacessível (HD morto,
máquina reformatada, etc.) — hoje só dá pra "resolver" editando `machine:` à mão no Markdown, o que
o CLAUDE.md proíbe para status/log e é igualmente indesejável para este campo (contorna o mecanismo
sem deixar rastro).

Implementar um **escape hatch explícito e auditável**: um script standalone, chamado manualmente
(nunca por skill de worker/reviewer/`concluir-task.mjs`), que reatribui `machine` para outro
hostname e grava a justificativa no Log de Execução (Seção 9) da própria task — preservando o
histórico de por que o veto foi contornado.

## 2. Contexto RAG (Spec-Driven Development)
- [`tools/scripts/manage-task.mjs:40-47`](../tools/scripts/manage-task.mjs) — veto multi-máquina: lê
  `machine` do frontmatter via `readTaskField`, compara com `os.hostname()`, bloqueia
  `start`/`finish`/`pause`/`block` se divergente.
- [`tools/scripts/manage-task.mjs:117-118,124-144`](../tools/scripts/manage-task.mjs) — como o
  `machine` é gravado hoje: só em `start`, via `svc.setMeta(taskId, { worktree_path, machine:
  currentMachine })`.
- [`apps/nexus-backend/src/services/task.service.ts:353-361`](../apps/nexus-backend/src/services/task.service.ts)
  — `setMeta(id, fields)`: regex-replace de campos do frontmatter, sem log, sem rebuild de índice.
- [`apps/nexus-backend/src/services/task.service.ts:305-328`](../apps/nexus-backend/src/services/task.service.ts)
  — `reconcile(id, agent)`: padrão de referência para método que edita frontmatter **e** grava log
  (`appendLog` + timestamp truncado a 16 chars ISO). Confirmado por leitura direta: `reconcile` NÃO
  toca `machine` — é por isso que o veto sobrevive a uma reconciliação de status (comportamento
  correto, não mexer nisso aqui).
- [`tools/scripts/concluir-task.test.mjs:28-38`](../tools/scripts/concluir-task.test.mjs) — padrão de
  teste já usado para o veto (`writeTemp` gera uma task `.md` temporária com frontmatter fixo,
  `execSync` chama o script, `assert.match` na saída). Reusar o mesmo padrão aqui.

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[READ]** `apps/nexus-backend/src/services/task.service.ts` — métodos `setMeta`, `reconcile`,
  `assign` (padrões de referência; não alterar a assinatura de nenhum deles).
- **[READ]** `tools/scripts/manage-task.mjs` linhas 1-50 (veto + `readTaskField`) — só leitura, este
  arquivo **não é tocado** por esta task (o escape hatch é um script separado, de propósito).
- **[READ]** `tools/scripts/concluir-task.test.mjs` — padrão de teste (`writeTemp`, `execSync`).
- **[UPDATE]** `apps/nexus-backend/src/services/task.service.ts` — adicionar o método
  `reassignMachine` (assinatura exata abaixo).
- **[CREATE]** `tools/scripts/reassign-machine.mjs` — CLI standalone.
- **[CREATE]** `tools/scripts/reassign-machine.test.mjs` — 3 casos (§4).

> **Não tocar:** `tools/scripts/manage-task.mjs`, `tools/scripts/concluir-task.mjs`, e nenhuma skill
> em `.agents/skills/` ou `.claude/skills/`. Este comando é **deliberadamente não-automatizado** —
> não wire-o em nenhum fluxo de worker/reviewer/orquestrador. Ver §5 "REGRAS DO QUE NÃO FAZER".

### Contrato exato (assinatura TS fixada)
```ts
// --- apps/nexus-backend/src/services/task.service.ts (método novo na classe TaskService) ---

/**
 * Escape hatch do veto multi-máquina (manage-task.mjs:40-47): transfere a posse de `machine`
 * para outro hostname e grava a justificativa no Log de Execução. Uso manual — nenhuma skill
 * de worker/reviewer chama isto. `reason` é obrigatório (auditoria de por que o veto foi
 * contornado); vazio lança erro.
 */
reassignMachine(id: string, newMachine: string, agent: string, reason: string): TaskRecord;
```

```js
// --- tools/scripts/reassign-machine.mjs ---
// Uso: node reassign-machine.mjs <TaskID> <NovaMaquina> <Agente> "<motivo>"
// Ex.:  node reassign-machine.mjs T-505a DESKTOP-XYZ israel "Vivobook16 com HD morto, confirmado indisponível"
```

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- **Framework:** `node:test` + `node:assert` — mesmo padrão de `concluir-task.test.mjs` (sem
  dependência de build do `nexus-backend` além do já existente `pnpm --filter nexus-backend build`).
- **Ambiente do Teste:** Node puro, sem browser.
- **Fora de Escopo:** integração fim-a-fim com o veto de `manage-task.mjs` (o veto já tem teste
  próprio em `concluir-task.test.mjs:35-38` provando que ele lê o campo `machine` corretamente;
  esta task só precisa provar que `reassignMachine` escreve esse campo + loga, não re-testar o veto).

Casos de teste (`reassign-machine.test.mjs`):
1. Task com `machine: MAQUINA-A` no frontmatter → `reassignMachine(id, 'MAQUINA-B', 'israel',
   'HD morto')` → frontmatter final tem `machine: MAQUINA-B`; Log de Execução ganha uma linha
   `[Reatribuída]` contendo `MAQUINA-A`, `MAQUINA-B` e o motivo `HD morto`.
2. Task **sem** campo `machine` prévio → `reassignMachine` ainda funciona (adiciona o campo do
   zero); log registra a origem como "(nenhuma)" ou equivalente explícito.
3. CLI (`reassign-machine.mjs`) chamado sem motivo (`process.argv` só com TaskID + NovaMaquina +
   Agente, string de motivo vazia) → recusa com exit code 1 e mensagem de uso — não escreve nada
   no arquivo da task.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** adicione `reassign-machine` como ação de `manage-task.mjs` nem de `concluir-task.mjs` —
>   é um script separado, de invocação manual. Automatizá-lo (chamar a partir de uma skill) reabre
>   exatamente o buraco que o veto fechou.
> - **NÃO** enfraqueça o veto existente em `manage-task.mjs:40-47` — este script só treina uma porta
>   de saída auditável, não relaxa a checagem para as demais ações.
> - **NÃO** exija confirmação interativa (`readline`/prompt) — CLI roda non-interactive; a
>   "fricção" que impede uso descuidado é o **motivo obrigatório**, não um prompt de confirmação.

### Pegadinhas conhecidas
- `setMeta` (referência de leitura) usa regex `^${key}:\s*.*$` para substituir campo existente e
  `content.replace(/^---\r?\n/, ...)` para inserir campo novo no topo do frontmatter quando ausente
  — `reassignMachine` precisa do mesmo fallback (task pode nunca ter tido `machine`, caso de teste 2).
- `appendLog` (ver uso em `reconcile:322`) espera um timestamp truncado
  (`new Date().toISOString().slice(0, 16)`) — replicar o mesmo formato para o log ficar consistente
  com as demais linhas da Seção 9.
- O `TaskService` é importado via `dist/services/task.service.js` compilado
  (`apps/nexus-backend/dist/...`), não via TS direto — rode `pnpm --filter nexus-backend build`
  antes de testar o CLI (mesmo padrão de `manage-task.mjs`/`concluir-task.mjs`).

1. **[TDD]** Escreva `tools/scripts/reassign-machine.test.mjs` com os 3 casos da Seção 4.
2. Implemente `TaskService.reassignMachine` em `task.service.ts`, espelhando `setMeta` (edição de
   frontmatter) + `reconcile` (append de log, formato de timestamp).
3. Implemente `tools/scripts/reassign-machine.mjs`: parse de argv, validação de motivo não-vazio,
   import do `TaskService` compilado, chamada do método, mensagem de confirmação no stdout.
4. `pnpm --filter nexus-backend build && node --test tools/scripts/reassign-machine.test.mjs`;
   cole a saída na Seção 8.

## 6. Feedback de Especificação (Spec Feedback Loop)
- *[Nenhum problema identificado — contratos derivados diretamente de `setMeta`/`reconcile`
  existentes, sem decisão de arquiteto pendente]*

## 7. Definition of Done (DoD) & Reviewer Checklist
- [ ] `reassignMachine` atualiza `machine` no frontmatter (com e sem campo prévio) e grava
      `[Reatribuída]` no Log de Execução com máquina antiga → nova + motivo?
- [ ] CLI recusa motivo vazio (exit 1, sem tocar o arquivo)?
- [ ] `reassign-machine.mjs` **não** é chamado por nenhuma skill/script existente (grep confirma
      zero referências fora do próprio arquivo + teste)?
- [ ] `manage-task.mjs:40-47` (veto) permanece byte-a-byte inalterado?
- [ ] 3 casos de teste passando sob `node --test`?

### Verificação automática *(comandos exatos — worker E reviewer rodam e COLAM a saída)*
```bash
pnpm --filter nexus-backend build
node --test tools/scripts/reassign-machine.test.mjs
```
> Sem `pnpm gate` — esta task não toca o superapp (código), só tooling do controle (Docs). O
> `pnpm --filter nexus-backend build` é obrigatório porque o CLI importa o `TaskService` compilado
> em `dist/`, não o `.ts` direto (mesmo padrão de `manage-task.mjs`).

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- 

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória — colar saída de build/tsc + test + lint):**
```
(cole aqui a saída real de pnpm build, pnpm test e pnpm lint)
```
- **Comentários de Revisão:**

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-07-25T13:20]** - *claude-sonnet* - `[Triado]`: Endurecida na criação: contratos TS exatos (reassignMachine), escopo fixo, 3 casos de teste, DoD por comando. Escape hatch para o veto multi-máquina (manage-task.mjs:40-47 / commit 5139906).
- **[2026-07-25T13:20]** - *claude-sonnet* - `[Endurecido]`: Contratos TS exatos (reassignMachine), escopo de arquivos fixo (3 arquivos), 3 casos de teste TDD, DoD por comando (node --test, sem pnpm gate — task só toca tooling do controle). Zero decisões em aberto.
- **[2026-07-25T13:20]** - *claude-sonnet* - `[Promovida p/ ready]`: Sem dependências; spec fixada no endurecimento — pronta para execução.
