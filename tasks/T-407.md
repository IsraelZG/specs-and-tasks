---
id: T-407
title: "Link multiaddr out-of-band"
status: draft
complexity: 2
target_agent: protocol_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-105"]
blocks: []
---

# T-407 · Link multiaddr out-of-band

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** haiku

## 1. Objetivo
Implementar geração e parse de link/QR com multiaddr no fragment no pacote `@plataforma/protocol`: primeiro contato via relay stateless; peer novo entra em rede P2P-pura por link, sem cloud
*(derivado de `docs/plano-de-implementacao.md` §7 T-407)*.

### Contratos exatos
```ts
// --- packages/protocol/src/outOfBand.ts ---
import { Multiaddr } from './multiaddr.js'; // T-105b
import { Ed25519PublicKey } from '@plataforma/crypto'; // T-101a

export interface OutOfBandLink {
  multiaddr: Multiaddr;          // multiaddr do peer remoto
  ephemeralPubKey: Ed25519PublicKey; // chave pública efêmera p/ primeiro contato
  nonce: Uint8Array;             // 16 bytes — nonce do convite
  relayHint?: string;            // multiaddr de relay stateless (opcional)
}

/** Gera link out-of-band a partir de multiaddr + chave efêmera. */
export function generateOobLink(
  multiaddr: Multiaddr,
  ephemeralPubKey: Ed25519PublicKey,
  relayHint?: string,
): string;

/** Parse de link out-of-band → estrutura. */
export function parseOobLink(link: string): OutOfBandLink;

/** Gera representação QR-ready (compacta, uppercase base64url-safe). */
export function toQrPayload(link: OutOfBandLink): string;

/** Parse de QR payload → estrutura. */
export function fromQrPayload(qr: string): OutOfBandLink;
```
- `generateOobLink`: formato `plataforma://connect#multiaddr=<encoded>&ephemeral=<b64>&nonce=<b64>&relay=<encoded>`.
- `parseOobLink`: parse do formato URL com fragment.
- `toQrPayload` / `fromQrPayload`: versão compacta para QR code (limite ~2KB).
- Primeiro contato: peer remoto usa relay stateless para estabelecer conexão inicial.

## 2. Contexto RAG
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (§7 T-407)
- [T-105b · Multiaddr](./T-105b.md) — encode/parse de multiaddr

## 3. Escopo de Arquivos
- **[READ]** `packages/protocol/src/multiaddr.ts` (T-105b)
- **[CREATE]** `packages/protocol/src/outOfBand.ts`
- **[CREATE]** `packages/protocol/tests/outOfBand.test.ts`
- **[UPDATE]** `packages/protocol/src/index.ts` — re-export

## 4. Estratégia de Testes
- [x] **Framework:** Vitest (Node puro).
- [x] **Ambiente:** `pnpm --filter @plataforma/protocol test`.

Casos de teste:
1. `generateOobLink` → `parseOobLink` roundtrip: todos os campos preservados.
2. `parseOobLink` com link malformado → erro.
3. `parseOobLink` sem multiaddr → erro.
4. `toQrPayload` → `fromQrPayload` roundtrip.
5. Link com relay hint → `parseOobLink` inclui relay.
6. Link sem relay hint → `relayHint` undefined.
7. QR payload < 2048 bytes para link típico.
8. Dois links com nonces diferentes → links distintos (unicidade).

## 5. Instruções de Execução
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** use query params no link — use fragment (`#`).
> - **NÃO** gere links com chave privada — use `ephemeralPubKey` pública.

1. **[TDD]** Escreva testes com multiaddrs e chaves Ed25519.
2. Implemente `generateOobLink` com formato URI.
3. Implemente `parseOobLink` com validação.
4. Implemente `toQrPayload` / `fromQrPayload`.
5. Re-exporte. Rode build + test.

## 6. Feedback de Especificação
- [Nenhum problema identificado]

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/protocol build
pnpm --filter @plataforma/protocol test
```

### Checklist
- [ ] Link usa fragment (`#multiaddr=...`), não query?
- [ ] Roundtrip geração ↔ parse preserva todos os campos?
- [ ] QR payload compacto (< 2KB)?
- [ ] 8 casos de teste passando?
- [ ] `pnpm --filter @plataforma/protocol build` e `test` verdes?
