---
id: T-605
title: "Fluxo intent não-comutativo"
status: draft
complexity: 5
target_agent: logic_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-604", "T-108"]
blocks: []
---

# T-605 · Fluxo de intent não-comutativo

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar fluxo de intent não-comutativo no pacote `@plataforma/core`: `CONTENT:INTENT` + arestas `SPENDS→head` (âncora de serialização), `CREDITS→entity_id`, `APPROVES` (K=1 caso comum), aplicador determinístico criando `MUTATES` + `TRANSFERS`; merge aditivo de créditos concorrentes (nunca LWW); `pending` local-não-replicado vs `finalized` durável
*(derivado de `docs/plano-de-implementacao.md` §9 T-605)*.

### Contratos exatos
```ts
// --- packages/core/src/intent/intentFlow.ts ---
import { EntityId, ULID } from '@plataforma/protocol'; // T-102
import { StoragePort } from '@plataforma/protocol'; // T-004

export interface IntentPayload {
  type: 'transfer' | 'credit' | 'debit';
  amount: number;
  targetEntityId: EntityId;       // destinatário (CREDITS) ou origem (SPENDS)
  referenceHeadId: ULID;          // head da linhagem como âncora de serialização
}

export interface IntentNode {
  intentId: ULID;
  intent: IntentPayload;
  state: 'pending' | 'approved' | 'finalized' | 'rejected';
  createdBy: EntityId;
  spendsAnchors: ULID[];          // arestas SPENDS→head
  creditsAnchors: EntityId[];     // arestas CREDITS→entity_id
  approvals: Map<EntityId, Uint8Array>; // APPROVES: entityId → assinatura
}

export interface IntentResult {
  success: boolean;
  mutatesEdgeId?: string;         // aresta MUTATES criada
  transfersEdgeId?: string;       // aresta TRANSFERS criada
  pendingIntents: EntityId[];     // intents ainda pendentes
  reason?: string;
}

export interface IntentValidator {
  /** Cria intent CONTENT:INTENT no grafo. */
  createIntent(intent: IntentPayload, authorEntityId: EntityId): Promise<IntentNode>;

  /** Aplica intent: valida invariantes (Zen Engine T-604) + serialização. */
  applyIntent(intentId: ULID, storage: StoragePort): Promise<IntentResult>;

  /** Aprova intent (K=1 comum; K>1 para quorum). */
  approveIntent(intentId: ULID, approverEntityId: EntityId, signature: Uint8Array): Promise<void>;

  /** Finaliza intent: persiste estado `finalized` durável. */
  finalizeIntent(intentId: ULID): Promise<void>;

  /** Merge aditivo: soma créditos concorrentes (nunca LWW). */
  mergeConcurrentIntents(intents: IntentNode[]): IntentPayload;

  /** Detecta conflito estrutural SPENDS (double-spend). */
  detectSpendsConflict(intents: IntentNode[]): boolean;
}
```
- `SPENDS→head`: âncora de serialização, previne double-spend (vetor adversarial 8).
- `APPROVES`: K=1 no caso comum (posse determinística da linhagem por `hash(entity_id)`).
- `applyIntent`: valida via Zen Engine (T-604) + cria `MUTATES` + `TRANSFERS`.
- `mergeConcurrentIntents`: créditos concorrentes somados aditivamente, nunca LWW.
- `pending` vs `finalized`: pending é local-não-replicado; finalized é durável no grafo.

## 2. Contexto RAG
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (§9 T-605, §2.5 vetor 8)
- [T-604 · Zen Engine](./T-604.md) — invariante T1, validação
- [T-108a · insertNode](./T-108a.md) — persistência de arestas

## 3. Escopo de Arquivos
- **[READ]** `packages/core/src/rules/ZenEngine.ts` (ZenEngine — T-604)
- **[READ]** `packages/core/src/lineage.ts` (insertNode — T-108a)
- **[CREATE]** `packages/core/src/intent/intentFlow.ts`
- **[CREATE]** `packages/core/tests/intentFlow.test.ts`
- **[UPDATE]** `packages/core/src/index.ts` — re-export

## 4. Estratégia de Testes
- [x] **Framework:** Vitest + SQLite `:memory:`.
- [x] **Ambiente:** `pnpm --filter @plataforma/core test`.

Casos de teste:
1. `createIntent` cria `CONTENT:INTENT` com arestas `SPENDS` e `CREDITS`.
2. `applyIntent` com `SPENDS` válido → `MUTATES` + `TRANSFERS` criados.
3. Vetor adversarial 8: duas intents com `SPENDS` para o mesmo head → segunda não finaliza.
4. Detecção de double-spend sob partição → freeze escopado da linhagem.
5. `mergeConcurrentIntents`: 2 créditos concorrentes → soma (não LWW).
6. `approveIntent` com K=1 → intent aprovada.
7. `finalizeIntent` move de `pending` para `finalized` durável.
8. Conflito estrutural detectável sem decifrar valores (asserção).

## 5. Instruções de Execução
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** permita double-spend. SPENDS no mesmo head = conflito.
> - **NÃO** use LWW para merge de créditos. Use soma aditiva.

1. **[TDD]** Escreva testes com SQLite e ZenEngine stub.
2. Implemente `createIntent` com criação de nós/arestas.
3. Implemente `applyIntent` com validação de invariantes.
4. Implemente `detectSpendsConflict` para prevenção de double-spend.
5. Implemente `mergeConcurrentIntents` com soma aditiva.
6. Rode build + test.

## 6. Feedback de Especificação
- [Nenhum problema identificado]

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/core build
pnpm --filter @plataforma/core test
```

### Checklist
- [ ] SPENDS ancora serialização (previne double-spend)?
- [ ] Merge aditivo de créditos (nunca LWW)?
- [ ] Vetor adversarial 8 completo?
- [ ] pending vs finalized distinção correta?
- [ ] 8 casos de teste passando?
- [ ] `pnpm --filter @plataforma/core build` e `test` verdes?
