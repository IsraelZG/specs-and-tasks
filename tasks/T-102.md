---
id: T-102
title: "ULID + EntityId"
status: rework
complexity: 2
parent_task: null
subtasks: []
dependencies: ["T-003"]
blocks: []
target_agent: logic_agent
reviewer_agent: "agile_reviewer"
execution_mode: sequential
---

# T-102 · ULID + EntityId

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar `ULID` (Universally Unique Lexicographically Sortable Identifier) e o tipo `EntityId` no pacote `@plataforma/core`, consumindo `ClockPort` e `RandomPort` para determinismo nos testes.
*(Derivado de `docs/conceitos/entity-id.md` — entity_id é o ULID do nó-raiz da linhagem; `docs/plano-de-implementacao.md` §4 T-102; `docs/conceitos/ulid.md` — estrutura, Crockford Base32, convenção do 11º caractere VFK)*

### Contratos exatos (assinaturas TS fixadas)

```ts
// --- packages/core/src/ulid.ts ---
import type { ClockPort, RandomPort } from '@plataforma/protocol'; // T-004

/** ULID como string Base32 Crockford (26 caracteres). Especificação: github.com/ulid/spec.
 *  Estrutura: [48 bits timestamp ms][80 bits random] → 26 chars Crockford Base32.
 *  Derivado de docs/conceitos/ulid.md §Contrato. */
export type ULID = string;

/** EntityId = ULID do nó-raiz da linhagem. Sempre igual ao id do primeiro nó da entidade.
 *  Derivado de docs/conceitos/entity-id.md §Definição e §Contrato. */
export type EntityId = ULID;

/** Conjunto de portas injetáveis para ULIDFactory.
 *  Derivado de plano §0.1 item 4 "Portas antes de adapters". */
export interface ULIDFactoryOptions {
  clock: ClockPort;
  random: RandomPort;
}

/** Fábrica de ULIDs determinística (via portas injetadas).
 *  NÃO usa Date.now() nem Math.random() — apenas ClockPort e RandomPort. */
export class ULIDFactory {
  constructor(opts: ULIDFactoryOptions);

  /** Gera novo ULID. Timestamp de clock.now() (48 bits) + random.bytes(10) (80 bits).
   *  Resultado: string de 26 chars Crockford Base32 ordenável lexicograficamente por timestamp.
   *  O 11º caractere (index 10) reflete os bits iniciais do random (VFK — docs/conceitos/ulid.md §Contrato). */
  generate(): ULID;

  /** Decodifica ULID string → { timestamp: number (ms desde epoch), random: Uint8Array (10 bytes) }. */
  static decode(ulid: ULID): { timestamp: number; random: Uint8Array };

  /** Valida se string é ULID sintaticamente válido (26 chars, alfabeto Crockford). */
  static isValid(candidate: string): candidate is ULID;
}

/** Codificador/decodificador Base32 Crockford.
 *  Alfabeto: 0123456789ABCDEFGHJKMNPQRSTVWXYZ (32 chars, sem I L O U).
 *  Derivado de docs/conceitos/ulid.md §Definição. */
export const CrockfordBase32: {
  encode(bytes: Uint8Array): string;
  decode(str: string): Uint8Array;
};
```

## 2. Contexto RAG (Spec-Driven Development)
- [entity-id.md](../docs/conceitos/entity-id.md) — semântica de entity_id, herança na linhagem, contrato `entity_id = id do nó-raiz`
- [ulid.md](../docs/conceitos/ulid.md) — estrutura ULID, Crockford Base32, convenção do 11º caractere VFK
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) §4 T-102 — critério de aceite "ordenação ≡ ordem temporal sob clock virtual"
- [T-004.md](./T-004.md) §1 — assinaturas exatas de `ClockPort` e `RandomPort` (status: done)
- [T-003.md](./T-003.md) §1 — `VirtualClock` e `SeededRandom` disponíveis em `@plataforma/testkit` (status: done)

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[READ]** `packages/protocol/src/ports.ts` (ClockPort, RandomPort — T-004, status: done)
- **[READ]** `docs/conceitos/entity-id.md`
- **[READ]** `docs/conceitos/ulid.md`
- **[CREATE]** `packages/core/src/ulid.ts` — ULIDFactory + CrockfordBase32 + tipos
- **[CREATE]** `packages/core/tests/ulid.test.ts` — 11 casos de teste (ver Seção 4)
- **[UPDATE]** `packages/core/src/index.ts` — re-export `ULID`, `EntityId`, `ULIDFactory`, `CrockfordBase32`
- **[UPDATE]** `packages/core/package.json` — dep `@plataforma/protocol: "workspace:*"` + dep dev `@plataforma/testkit: "workspace:*"`

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Framework:** Vitest (Node puro).
- [x] **Ambiente do Teste:** `pnpm --filter @plataforma/core test`.
- [x] **Fora de Escopo:** Integração com SQLite.

Casos de teste (numerados) — todos derivados das specs RAG:

```ts
// packages/core/tests/ulid.test.ts
import { describe, it, expect } from 'vitest';
import { ULIDFactory, CrockfordBase32 } from '../src/ulid';
import { VirtualClock } from '@plataforma/testkit';
import { SeededRandom } from '@plataforma/testkit';

describe('ULIDFactory', () => {
  // Caso 1: timestamp 0 → 10 primeiros chars são '0000000000'
  // Derivado: ulid.md §Contrato "48 bits timestamp ms"; timestamp=0 → 48 bits zeros
  //           → primeiros 10 chars Crockford = '0000000000'
  it('1. timestamp=0 → ULID começa com "0000000000"', () => {
    const factory = new ULIDFactory({ clock: new VirtualClock(0), random: new SeededRandom('a') });
    expect(factory.generate().slice(0, 10)).toBe('0000000000');
  });

  // Caso 2: dois ULIDs com mesmo clock → lexicograficamente crescentes (random diferente)
  // Derivado: ulid.md §Contrato "Ordenação"; plano §4 T-102 "ordenação ≡ ordem temporal"
  it('2. dois ULIDs em sequência com mesmo clock → lexicograficamente crescentes', () => {
    const factory = new ULIDFactory({ clock: new VirtualClock(1000), random: new SeededRandom('b') });
    const a = factory.generate();
    const b = factory.generate();
    expect(a < b).toBe(true);
  });

  // Caso 3: roundtrip decode
  // Derivado: ulid.md §Contrato "48 bits timestamp ms"
  it('3. decode(generate()) → timestamp e random corretos (roundtrip)', () => {
    const clock = new VirtualClock(1234567890);
    const factory = new ULIDFactory({ clock, random: new SeededRandom('c') });
    const ulid = factory.generate();
    const { timestamp, random } = ULIDFactory.decode(ulid);
    expect(timestamp).toBe(1234567890);
    expect(random).toBeInstanceOf(Uint8Array);
    expect(random.length).toBe(10);
  });

  // Caso 4: isValid com ULID válido
  // Derivado: ulid.md §Definição "26 caracteres, case-insensitive"
  it('4. isValid("01ARZ3NDEKTSV4RRFFQ69G5FAV") → true', () => {
    expect(ULIDFactory.isValid('01ARZ3NDEKTSV4RRFFQ69G5FAV')).toBe(true);
  });

  // Caso 5: isValid com string curta
  it('5. isValid("invalid") → false (tamanho errado)', () => {
    expect(ULIDFactory.isValid('invalid')).toBe(false);
  });

  // Caso 6: isValid com caractere fora do alfabeto Crockford ('U' não pertence)
  // Derivado: ulid.md §Definição "Crockford Base32 (sem I L O U)"
  it('6. isValid("01ARZ3NDEKTSV4RRFFQ69G5FAU") → false (char "U" não é Crockford)', () => {
    expect(ULIDFactory.isValid('01ARZ3NDEKTSV4RRFFQ69G5FAU')).toBe(false);
  });

  // Caso 7: CrockfordBase32 encode/decode roundtrip
  // Derivado: ulid.md §Definição "Crockford Base32"
  it('7. CrockfordBase32 encode + decode roundtrip com 16 bytes', () => {
    const bytes = new Uint8Array(16).fill(0xAB);
    const encoded = CrockfordBase32.encode(bytes);
    const decoded = CrockfordBase32.decode(encoded);
    expect(decoded).toEqual(bytes);
  });

  // Caso 8: seeds diferentes → ULIDs diferentes
  // Derivado: plano §2.1 "SeededRandom: cenários 100% reproduzíveis por seed"
  it('8. seeds diferentes → ULIDs diferentes', () => {
    const f1 = new ULIDFactory({ clock: new VirtualClock(0), random: new SeededRandom('seed-A') });
    const f2 = new ULIDFactory({ clock: new VirtualClock(0), random: new SeededRandom('seed-B') });
    expect(f1.generate()).not.toBe(f2.generate());
  });

  // Caso 9: mesma seed e clock fixo → ULIDs idênticos (determinismo)
  // Derivado: plano §2.1 "SeededRandom: cenários 100% reproduzíveis por seed"
  it('9. mesma seed e clock fixo → ULIDs idênticos (determinismo)', () => {
    const make = () => new ULIDFactory({ clock: new VirtualClock(0), random: new SeededRandom('det-seed') });
    expect(make().generate()).toBe(make().generate());
  });

  // Caso 10: EntityId é assignable de ULID (type-level)
  // Derivado: entity-id.md §Contrato "entity_id = id do nó-raiz"; T-102 §1
  it('10. EntityId é assignable de ULID (type-level compile check)', () => {
    // Se este arquivo compilar sem erro de tipo, o caso passa
    const ulid: import('../src/ulid').ULID = '01ARZ3NDEKTSV4RRFFQ69G5FAV';
    const entityId: import('../src/ulid').EntityId = ulid;
    expect(typeof entityId).toBe('string');
  });

  // Caso 11: 11º caractere (index 10) reflete random, não timestamp (VFK)
  // Derivado: ulid.md §Contrato "11º caractere... discriminador de tabela VFK"
  //           plano §4 T-102 "convenção do 11º caractere (N/não-N) da VFK"
  // Nota: este caso documenta o contrato VFK; a factory NÃO força o 11º char — é responsabilidade
  //        do gerador de nó/aresta (T-106) garantir o discriminador correto.
  it('11. ULID gerado tem exatamente 26 chars; o 11º (index 10) é char Crockford válido', () => {
    const factory = new ULIDFactory({ clock: new VirtualClock(1000), random: new SeededRandom('vfk') });
    const ulid = factory.generate();
    expect(ulid.length).toBe(26);
    const crockfordAlphabet = '0123456789ABCDEFGHJKMNPQRSTVWXYZ';
    expect(crockfordAlphabet.includes(ulid[10])).toBe(true);
  });
});
```

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** importe `uuid` ou `ulid` do npm. Implementação própria consumindo portas.
> - **NÃO** use `Date.now()` ou `Math.random()` — apenas `ClockPort` e `RandomPort`.
> - **NÃO** redefina `ClockPort`/`RandomPort` — importe de `@plataforma/protocol` (T-004, done).

1. **[TDD]** Crie `packages/core/tests/ulid.test.ts` com os 11 casos da Seção 4. Falham (implementação não existe).
2. Adicione em `packages/core/package.json`:
   - `"dependencies": { "@plataforma/protocol": "workspace:*" }`
   - `"devDependencies": { "@plataforma/testkit": "workspace:*" }`
3. Implemente `CrockfordBase32` em `packages/core/src/ulid.ts`:
   - Alfabeto: `'0123456789ABCDEFGHJKMNPQRSTVWXYZ'` (32 chars).
   - `encode(bytes: Uint8Array): string` — converte bytes para Base32 Crockford.
   - `decode(str: string): Uint8Array` — reconverte.
4. Implemente `ULIDFactory`:
   - `generate()`: `timestamp = this.clock.now()` (48 bits) + `randomBytes = this.random.bytes(10)` (80 bits) → `CrockfordBase32.encode(concat(timestamp48bits, randomBytes))`.
   - `static decode(ulid)`: extrai primeiros 10 chars → timestamp 48 bits; últimos 16 chars → random 10 bytes.
   - `static isValid(candidate)`: length === 26 && todos chars ∈ alfabeto Crockford.
5. Exporte `ULID`, `EntityId`, `ULIDFactory`, `CrockfordBase32` em `packages/core/src/index.ts`.
6. Rode build + test (Seção 7) e cole saída.

## 6. Feedback de Especificação (Spec Feedback Loop)
- *[Nenhum problema identificado — contratos derivados de ulid.md, entity-id.md, plano §4 T-102 e T-004 (done)]*

> **Nota sobre VFK (caso 11):** `ulid.md §Contrato` diz que o 11º char é `N` para nodes e outro valor para edges. A `ULIDFactory` gera o 11º char a partir do random — o chamador (T-106, schema de nodes/edges) é responsável por mascarar ou re-amostrar o random até obter o discriminador correto. Esta task não impõe essa restrição na fábrica; documentado no caso 11.

## 7. Definition of Done (DoD) & Reviewer Checklist

### Verificação automática (Gate de Evidência)
```bash
pnpm --filter @plataforma/core build
pnpm --filter @plataforma/core test
```
> **GATE DE EVIDÊNCIA:** Worker cola saída literal na Seção 8. Esperado: 11 testes passando.

### Checklist do Reviewer
- [ ] `ULIDFactory` consome `ClockPort`/`RandomPort` (não `Date.now`/`Math.random`)?
- [ ] `CrockfordBase32` encode/decode roundtrip (caso 7)?
- [ ] 11 casos de teste passando (incluindo determinismo, VFK)?
- [ ] `pnpm --filter @plataforma/core build` e `test` verdes?
- [ ] Sem imports de `uuid`, `ulid` ou similares do npm?

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- Rework efetuado para corrigir os 10 problemas de lint apontados no arquivo `packages/core/src/ulid.ts`.
- Removidas todas as *non-null assertions* (`!`) e implementadas verificações seguras de fallback com `?? 0` ou checagens explícitas de existência (ex: `char !== undefined`).
- Corrigida a restrição de tipos de concatenadores string na linha 20 para evitar o erro de restrict-plus-operands.
- Validação final bem-sucedida: `pnpm lint`, `pnpm build` e `pnpm test` executados com sucesso (saídas 100% limpas/verdes).

#### Nova Evidência do Rework:
```bash
$ pnpm --filter @plataforma/core lint
$ eslint src/
(silêncio — 0 problemas/warnings)

$ pnpm --filter @plataforma/core build
$ tsc
(silêncio — sem erros)

$ pnpm --filter @plataforma/core test
$ vitest run
 ✓ tests/mock.test.ts (1 test) 2ms
 ✓ tests/schema.test.ts (7 tests) 13ms
 ✓ tests/ulid.test.ts (11 tests) 5ms

 Test Files  3 passed (3)
      Tests  19 passed (19)
```

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [x] **Requer Refatoração**
- **Evidência de Execução (obrigatória):**
```bash
$ pnpm --filter @plataforma/core build
$ tsc
(silêncio — sem erros)

$ pnpm --filter @plataforma/core test
$ vitest run
 ✓ tests/mock.test.ts (1 test) 2ms
 ✓ tests/schema.test.ts (7 tests) 13ms
 ✓ tests/ulid.test.ts (11 tests) 6ms

 Test Files  3 passed (3)
      Tests  19 passed (19)
   Start at  22:03:22
   Duration  490ms

$ pnpm --filter @plataforma/core lint
$ eslint src/
(silêncio — 0 problemas)
```
- **Comentários de Revisão:**

**Rework anterior:** 10 erros de lint resolvidos (non-null assertions → `?? 0` / `!== undefined`, restrict-plus-operands corrigido). Build + lint + test: tudo verde agora. ✅

**Sondas adversariais (5 testes):** 4 passaram, 1 expôs bug real:

**[M1] Encoding de timestamp perde precisão com valores grandes** · `packages/core/src/ulid.ts:96-102`
- **Evidência:** Sonda `probe-D` com `timestamp = 0xFFFFFFFFFFFF` (máximo 48 bits = 281474976710655). `generate()` → `decode()` retorna `255` em vez de `281474976710655`.
- **Causa:** Divisão por ponto flutuante (`/`) na decomposição do timestamp em 6 bytes (linhas 97–102). Para valores > ~2^53/divisor, a precisão float64 perde bits baixos.
- **Referência na Spec:** Seção 1 — `generate()` deve produzir ULID cujo `decode()` retorna o timestamp original (caso 3 exige roundtrip). Seção 4 caso 3 — "decode(generate()) → timestamp e random corretos".
- **Ação corretiva:** Substituir a decomposição por ponto flutuante por operações bitwise com `BigInt`:
  ```ts
  const tsBig = BigInt(timestamp);
  tsBytes[0] = Number((tsBig >> 40n) & 0xffn);
  tsBytes[1] = Number((tsBig >> 32n) & 0xffn);
  tsBytes[2] = Number((tsBig >> 24n) & 0xffn);
  tsBytes[3] = Number((tsBig >> 16n) & 0xffn);
  tsBytes[4] = Number((tsBig >> 8n) & 0xffn);
  tsBytes[5] = Number(tsBig & 0xffn);
  ```
  E adicionar teste com timestamp máximo (= `0xFFFFFFFFFFFF`) para prevenir regressão.

**Verificação de escopo:** 4 arquivos alterados (`ulid.ts`, `ulid.test.ts`, `index.ts`, `package.json`) — todos no escopo declarado (Seção 3). ✅

**Verificação DoD:**
- [x] `ULIDFactory` consome `ClockPort`/`RandomPort` (sem `Date.now`/`Math.random`) ✅
- [x] `CrockfordBase32` encode/decode roundtrip (caso 7) ✅
- [x] 11 casos de teste passando ✅
- [x] `pnpm build` + `test` + `lint` verdes ✅
- [x] Sem imports de `uuid`/`ulid` npm ✅

**Veredito:** `rework` — 1 MAJOR (M1: encoding perde precisão com timestamps grandes). Os 10 erros de lint do rework anterior foram corrigidos. Após correção do M1, a task estará pronta para aprovação.

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-06-21T20:38]** - *spec_hardener* - `[Endurecido]`: Contratos TS fixados. 11 casos de teste concretos com código derivado de ulid.md, entity-id.md, T-004 (done) e T-003 (done). VFK documentado no caso 11. Nenhuma decisão em aberto.
- **[2026-06-21T20:48]** - *devops_agent* - `[Bloqueado]`: Especificacao resolvida, preparando para ready
- **[2026-06-21T20:48]** - *devops_agent* - `[Desbloqueado]`: Especificacoes prontas para execucao
- **[2026-06-22T00:36]** - *DeepSeek* - `[Iniciado]`: iniciando implementação de ULIDFactory + CrockfordBase32 no @plataforma/core
- **[2026-06-22T00:41]** - *DeepSeek* - `[Finalizado]`: ULIDFactory + CrockfordBase32 implementados. 11/11 testes passando. Build silencioso, 19/19 total.
- **[2026-06-22T00:46]** - *agile_reviewer* - `[Requer Refatoração]`: Falha no lint do pacote @plataforma/core: 10 erros em packages/core/src/ulid.ts
- **[2026-06-22T00:51]** - *Antigravity* - `[Iniciado]`: Iniciando rework de T-102 para corrigir linting de ulid.ts
- **[2026-06-22T00:53]** - *Antigravity* - `[Finalizado]`: Rework de linting concluido. eslint src/ zerado, build e testes verdes (19/19)
- **[2026-06-22T01:05]** - *agile_reviewer* - `[Requer Refatoração]`: [M1] Encoding de timestamp perde precisão com valores grandes — divisão float deve ser substituída por BigInt bitwise. Sondas: 4/5 passaram; probe-D expôs o bug. Lint anterior: resolvido.
