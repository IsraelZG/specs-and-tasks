---
id: T-807
title: "Bancada: aba Mídia"
status: draft
complexity: 2
target_agent: frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-008", "T-801"]
blocks: []
ui: true
---

# T-807 · Bancada: aba Mídia

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (React Testing Library) / `playwright` (E2E)
- **Capacidade-alvo:** haiku

## 1. Objetivo
Implementar aba "Mídia" no app Bancada (T-008): upload de arquivo de teste, escolha de modo de cifra (convergent/unique), visualização de seeders/fontes, player, botão "derrubar WebSeed"
*(derivado de `docs/plano-de-implementacao.md` §11 T-807)*.

### Contratos
```tsx
// --- apps/bancada/src/tabs/MidiaTab.tsx ---
import { InfoHash, ChunkingMode } from '@plataforma/protocol'; // T-801a

export interface FileUploadState {
  infoHash: InfoHash | null;
  fileName: string;
  size: number;
  cipherMode: ChunkingMode;
  progress: number;              // 0–100
  seeders: number;
  leechers: number;
  webSeedAvailable: boolean;
}

export interface MidiaTabProps {
  onUpload: (file: File, mode: ChunkingMode) => Promise<InfoHash>;
  onPlay: (infoHash: InfoHash) => void;
  onKillWebSeed: (infoHash: InfoHash) => void;
}

export function MidiaTab(props: MidiaTabProps): JSX.Element;
```
- Upload: input file + seletor de modo de cifra (`convergent` | `unique`).
- Progresso de upload com barra.
- Visualização: tabela de seeders (peerId, progress, speed) e fontes (WebTorrent, WebSeed).
- Player: `<video>` ou `<audio>` para o arquivo cifrado.
- Botão "Derrubar WebSeed" → testa fallback P2P.

## 2. Contexto RAG
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (§11 T-807)
- [T-008 · App Bancada](./T-008.md) — shell de abas
- [T-801a · Chunking](./T-801a.md) — InfoHash, ChunkingMode

## 3. Escopo de Arquivos
- **[READ]** `packages/protocol/src/media/chunking.ts` (ChunkingMode — T-801a)
- **[READ]** `apps/bancada/src/` (estrutura de abas — T-008)
- **[CREATE]** `apps/bancada/src/tabs/MidiaTab.tsx`
- **[CREATE]** `apps/bancada/tests/MidiaTab.test.tsx`
- **[CREATE]** `apps/bancada/e2e/midia.smoke.test.ts` (Playwright smoke)
- **[UPDATE]** `apps/bancada/src/App.tsx` — adicionar aba "Mídia"

## 4. Estratégia de Testes
- [x] **Framework:** Vitest + React Testing Library (JSDOM).
- [x] **E2E:** Playwright smoke test.

Casos de teste (Vitest):
1. `MidiaTab` renderiza input de upload e seletor de modo.
2. Seleção de modo `convergent` vs `unique`.
3. Upload simulado → barra de progresso visível.
4. Tabela de seeders/fontes renderizada.
5. Botão "Derrubar WebSeed" → chama `onKillWebSeed`.
6. Player renderizado quando `infoHash` disponível.

Playwright smoke:
7. Fluxo upload → play de arquivo de teste.

## 5. Instruções de Execução
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** faça upload real nos testes unitários.
> - **NÃO** exponha o conteúdo decifrado na UI de debug.

1. **[TDD]** Escreva testes com upload e player stubs.
2. Implemente `MidiaTab` com upload, seletor de modo e player.
3. Implemente tabela de seeders/fontes.
4. Integre ao shell de abas da Bancada.
5. Escreva Playwright smoke test.
6. Rode build + test.

## 6. Feedback de Especificação
- [Nenhum problema identificado]

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter bancada build
pnpm --filter bancada test
pnpm --filter bancada test:e2e
```

### Checklist
- [ ] Upload com seletor de modo (convergent/unique)?
- [ ] Visualização de seeders/fontes?
- [ ] Player funcional?
- [ ] Botão "Derrubar WebSeed"?
- [ ] 6 casos unit + Playwright smoke?
- [ ] `pnpm --filter bancada build` e `test` verdes?
