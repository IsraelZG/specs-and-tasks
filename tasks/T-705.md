---
id: T-705
title: "Cerimônia QR + SAS"
status: draft
spec_status: blocked-decision
complexity: 5
target_agent: crypto_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-202", "T-105", "T-501", "T-110", "T-004"]
blocks: []
capacity_target: sonnet
hardened_at: "2026-06-29T19:50:00-03:00"
---

# T-705 · Cerimônia QR + SAS — pareamento de dispositivo

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro) + `playwright` (E2E)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar cerimônia de pareamento QR + SAS no pacote `@plataforma/core`: par efêmero de pareamento (TTL 5 min) + QR (multiaddr transiente, chave efêmera, nonce); canal Noise_XX de cerimônia; confirmação SAS derivada do transcript; emissão de `ASSET:PERMISSION` + `DELEGATED_TO` → chave definitiva do novo dispositivo + bump da Época de Identidade
*(derivado de `docs/plano-de-implementacao.md` §10 T-705 — "RFC-005 §A.5")*.

### Contratos exatos
```ts
// --- packages/core/src/pairing/pairingCeremony.ts ---
import { PeerId } from '@plataforma/protocol'; // T-105a
import { Ed25519PrivateKey, Ed25519PublicKey } from '@plataforma/crypto'; // T-101a

export interface PairingSession {
  sessionId: string;             // ULID da sessão
  ephemeralKeyPair: { priv: Ed25519PrivateKey; pub: Ed25519PublicKey };
  nonce: Uint8Array;             // 16 bytes
  multiaddr: string;             // multiaddr transiente
  createdAt: number;
  expiresAt: number;             // TTL 5 min
}

export interface QrPayload {
  multiaddr: string;
  ephemeralPubKey: Ed25519PublicKey;
  nonce: Uint8Array;
  checksum: Uint8Array;          // blake2s256 dos campos acima
}

export interface SasConfirmation {
  displayCode: string;           // 6 dígitos para confirmação humana
  transcript: Uint8Array;        // transcript do Noise_XX (derivação do SAS)
  verified: boolean;             // se usuário confirmou
}

export interface PairingResult {
  success: boolean;
  newDevicePeerId?: PeerId;
  permissionEntityId?: string;   // ASSET:PERMISSION emitido
  delegatedToEntityId?: string;  // DELEGATED_TO
  identityEpochBumped: boolean;
  reason?: string;
}

export interface PairingCeremony {
  /** Inicia sessão de pareamento (Lado A: device existente). */
  initiatePairing(deviceKey: Ed25519PrivateKey): Promise<PairingSession>;

  /** Gera QR code a partir da sessão. */
  generateQR(session: PairingSession): Promise<QrPayload>;

  /** Lado B: lê QR e inicia Noise_XX de cerimônia. */
  scanQR(qr: QrPayload, newDeviceKey: Ed25519PrivateKey): Promise<PairingSession>;

  /** Deriva SAS do transcript Noise_XX para confirmação humana. */
  deriveSAS(session: PairingSession): Promise<SasConfirmation>;

  /** Confirma SAS (usuário comparou os 6 dígitos). */
  confirmSAS(session: PairingSession, confirmed: boolean): Promise<PairingResult>;

  /** Finaliza: emite ASSET:PERMISSION + DELEGATED_TO + bump de época. */
  finalizePairing(session: PairingSession): Promise<PairingResult>;
}
```
- Par efêmero: TTL 5 minutos; chaves efêmeras destruídas após pareamento.
- QR: multiaddr transiente, chave efêmera pública, nonce, checksum.
- SAS: 6 dígitos derivados do transcript Noise_XX — confirmação humana contra MITM.
- Chave mestra **nunca** atravessa o canal.
- Novo dispositivo obtém chaves de época via `requestEpochKey` (T-110).
- Revogação do dispositivo (lápide + bump) nega `requestEpochKey` subsequente.

## 2. Contexto RAG
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (§10 T-705)
- [T-202a · Noise_XX](./T-202a.md) — canal de handshake
- [T-105a · PeerId](./T-105a.md) — PeerId
- [T-501b · UCAN issue/verify](./T-501b.md) — emissão de permissão
- [T-110 · Key Vault](./T-110.md) — requestEpochKey

## 3. Escopo de Arquivos
- **[READ]** `packages/transport/src/noiseHandshake.ts` (Noise_XX — T-202a)
- **[READ]** `packages/core/src/auth/ucan.ts` (issueUcan — T-501b)
- **[READ]** `packages/core/src/keyVault.ts` (requestEpochKey — T-110)
- **[CREATE]** `packages/core/src/pairing/pairingCeremony.ts`
- **[CREATE]** `packages/core/tests/pairingCeremony.test.ts`
- **[CREATE]** `apps/bancada/e2e/pairing.test.ts` (Playwright)
- **[UPDATE]** `packages/core/src/index.ts` — re-export

## 4. Estratégia de Testes
- [x] **Framework:** Vitest (Node puro) + Playwright (E2E).

Casos de teste (Vitest):
1. `initiatePairing` cria sessão com par efêmero e TTL.
2. `generateQR` → `scanQR` roundtrip: mesma sessão.
3. `deriveSAS` gera 6 dígitos determinísticos do transcript.
4. `confirmSAS` com SAS divergente → aborta (vetor MITM).
5. `finalizePairing`: `ASSET:PERMISSION` + `DELEGATED_TO` + bump de época.
6. Chave mestra não atravessa o canal (asserção).
7. `scanQR` com QR expirado (TTL 5 min excedido) → erro.
8. Revogação do dispositivo → `requestEpochKey` negado.

Playwright:
9. Dois contexts: pareamento completo via QR simulado.
10. SAS divergente → cerimônia aborta (vetor MITM).

## 5. Instruções de Execução
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** envie a chave mestra pelo canal. Use chave efêmera.
> - **NÃO** pule a confirmação SAS — é a defesa contra MITM.

1. **[TDD]** Escreva testes com Noise_XX e pares de chaves.
2. Implemente `initiatePairing` com par efêmero (TTL 5 min).
3. Implemente `generateQR` e `scanQR`.
4. Implemente `deriveSAS` e `confirmSAS`.
5. Implemente `finalizePairing` com emissão de permissões.
6. Escreva teste Playwright com 2 contexts.
7. Rode build + test.

## 6. Feedback de Especificação

### Decisões de Arquiteto (2026-07-01, resolvidas via `/arquiteto-decisoes`)

**Decisão #1 — handshake hash: DECIDIDO opção (A).** `NoiseHandshakeResult` (em
`@plataforma/transport`) ganha o campo `handshakeHash: Uint8Array`. Mudança mínima e localizada,
não duplica a orquestração de handshake que já existe em `initiateNoiseXX`/`respondNoiseXX`.

**Decisão #2 — injeção de rede/storage: DECIDIDO opção (A).** `PairingCeremony` recebe
`StoragePort` e `NetworkAdapterPort` explicitamente (construtor/classe da cerimônia) — mesmo
padrão de I/O explícito aplicado em T-504/T-707 neste lote de decisões.

**Decisão #3 — algoritmo SAS: DECIDIDO opção (A).**
`SAS = (HKDF-SHA256(h, salt="", info="SAS") % 1000000).toString().padStart(6, '0')`. Uniformemente
distribuído, resistente a bias, consistente com o uso de HKDF em T-511.

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/core build
pnpm --filter @plataforma/core test
pnpm --filter bancada test:e2e
```

### Checklist
- [ ] Par efêmero com TTL 5 min?
- [ ] QR + SAS contra MITM?
- [ ] Chave mestra nunca atravessa o canal?
- [ ] `ASSET:PERMISSION` + `DELEGATED_TO` + bump de época?
- [ ] 8 unit + 2 E2E passando?
- [ ] `pnpm --filter @plataforma/core build` e `test` verdes?

## 8. Log de Handover e Revisão Agile
### Handover do Executor:
- 

### Parecer do Agente Revisor:
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução:**
```
(cole aqui a saída real de pnpm build e pnpm test)
```

## 9. Log de Execução
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
