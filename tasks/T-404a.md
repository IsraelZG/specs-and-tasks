---
id: T-404a
title: "CPE: tryPromote (core loop + NAT decision)"
status: draft
complexity: 3
target_agent: logic_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-402", "T-006b"]
blocks: ["T-404b"]
parent: "T-404"
---

# T-404a · CPE: tryPromote (core loop + NAT decision)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro, SimNetwork)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar `tryPromote` no `ConnectionPromotionEngine`: tenta migrar conexão relay → WebRTC direta com base no perfil NAT do peer. Usa SimNetwork (T-006b) para testes determinísticos
*(extraído de T-404 §1)*.

### Contratos exatos
```ts
// --- packages/transport/src/promotion/Engine.ts ---
import { PeerId } from '@plataforma/protocol'; // T-004
import { NatType } from '@plataforma/testkit'; // T-006b

export enum PromotionState { RELAY_ONLY, PROMOTING, DIRECT, FAILED }

export class ConnectionPromotionEngine {
  constructor(maxAttempts?: number);

  /** Tenta promover conexão relay → direta. Retorna novo estado. */
  tryPromote(peerId: PeerId, natType: NatType): Promise<PromotionState>;
}
```
- NAT `Symmetric` → `FAILED` na primeira tentativa.
- NAT `Cone` → `DIRECT` após handshake WebRTC bem-sucedido.
- Make-before-break: relay só fecha após WebRTC confirmado.
- `maxAttempts` (default 3): após N falhas → `FAILED` permanente.

## 2. Contexto RAG
- [T-402 · WebRTC](./T-402.md)
- [T-006b · NAT](./T-006b.md)

## 3. Escopo de Arquivos
- **[READ]** `packages/testkit/src/SimNetwork.ts` (T-006b)
- **[CREATE]** `packages/transport/src/promotion/Engine.ts`
- **[CREATE]** `packages/transport/tests/promotion.try.test.ts`

## 4. Estratégia de Testes
- [x] **Framework:** Vitest + SimNetwork.

Casos de teste:
1. NAT `Symmetric` → `tryPromote` retorna `FAILED`.
2. NAT `Cone` → `tryPromote` retorna `DIRECT`.
3. 3 tentativas falhas → `FAILED` permanente.
4. Make-before-break: estado `RELAY_ONLY` até `DIRECT` confirmado.
5. `tryPromote` com peer já `DIRECT` → no-op.

## 5. Instruções de Execução
1. Escreva testes com SimNetwork + NAT profiles.
2. Implemente `tryPromote` (NAT decision + WebRTC attempt).
3. Rode build + test.

## 6. Feedback de Especificação
- *(Nenhuma pendência)*

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/transport build
pnpm --filter @plataforma/transport test
```

### Checklist
- [ ] NAT Symmetric → nunca promove?
- [ ] Make-before-break?
- [ ] 5 casos de teste passando?
- [ ] `pnpm --filter @plataforma/transport build` e `test` verdes?

## 8. Log de Handover e Revisão Agile
### Handover do Executor:
-

### Parecer do Agente Revisor:
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução:**
```
(cole aqui a saída real)
```

## 9. Log de Execução
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
