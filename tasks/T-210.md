---
id: T-210
title: "Convite ASSET:INVITE"
status: done
spec_status: hardened
capacity_target: sonnet
decisions: []
hardened_at: "2026-06-29T19:30:00-03:00"
complexity: 4
target_agent: logic_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-105", "T-501"]
blocks: []
---

# T-210 · Convite `ASSET:INVITE` — emissão e cerimônia

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar emissão e cerimônia de consumo do `ASSET:INVITE` no pacote `@plataforma/core`: asset consumível com expiry/single-use, payload com multiaddr hints, invite_code, inviter_peer_id e assinatura, com cerimônia completa de validação
*(derivado de `docs/plano-de-implementacao.md` §5 T-210)*.

### Contratos exatos
```ts
// --- packages/core/src/invite.ts ---
import { PeerId, StoragePort } from '@plataforma/protocol';
import { Ed25519PrivateKey, Ed25519PublicKey } from '@plataforma/crypto';

export interface InvitePayload {
  multiaddrHints: string[];      // multiaddr hints do inviter
  inviteCode: string;            // código único do convite
  inviterPeerId: PeerId;
  expiry: number;                // timestamp Unix ms
}

export interface SignedInvite {
  payload: InvitePayload;
  signature: Uint8Array;         // Ed25519 do inviter sobre payload canônico
}

export interface InviteCeremonyResult {
  success: boolean;
  profileEntityId?: string;      // PROFILE:AUTHENTICATION criado
  vouchesEdgeId?: string;        // aresta VOUCHES_FOR
  inviteTombstoneId?: string;    // lápide do convite consumido
}

/** Emite convite assinado. */
export function issueInvite(
  inviterKey: Ed25519PrivateKey,
  inviterPeerId: PeerId,
  multiaddrHints: string[],
  expiryMs?: number, // default 3600000 (1h)
): Promise<SignedInvite>;

/** Valida convite (assinatura + expiry + não-gasto). */
export function validateInvite(
  storage: StoragePort,
  invite: SignedInvite,
  inviterPubKey: Ed25519PublicKey,
): Promise<boolean>;

/** Cerimônia completa de consumo do convite. */
export function consumeInvite(
  storage: StoragePort,
  invite: SignedInvite,
  inviteePeerId: PeerId,
  inviteePubKey: Ed25519PublicKey,
  inviterPubKey: Ed25519PublicKey,
): Promise<InviteCeremonyResult>;
```
- `validateInvite`: verifica assinatura Ed25519, expiry não ultrapassado, e que o invite_code não consta na tabela `tombstones` como consumido.
- `consumeInvite`: executa validação → Noise (conexão implícita) → cria `PROFILE:AUTHENTICATION` + aresta `VOUCHES_FOR` → lápide do convite (single-use).
- Vetor adversarial 9: convite reutilizado/expirado → cerimônia de consumo falha.

## 2. Contexto RAG
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (§5 T-210, §2.5 vetor 9)
- [T-105 · PeerId](./T-105.md) — PeerId
- [T-501 · UCAN issue/verify](./T-501.md) — assinatura Ed25519

## 3. Escopo de Arquivos
- **[READ]** `packages/crypto/src/ed25519.ts` (T-101a)
- **[READ]** `packages/protocol/src/peerId.ts` (T-105a)
- **[CREATE]** `packages/core/src/invite.ts`
- **[CREATE]** `packages/core/tests/invite.test.ts`
- **[UPDATE]** `packages/core/src/index.ts` — re-export

## 4. Estratégia de Testes
- [x] **Framework:** Vitest + SQLite `:memory:`.
- [x] **Ambiente:** `pnpm --filter @plataforma/core test`.

Casos de teste:
1. `issueInvite` + `validateInvite` roundtrip: convite válido → true.
2. `validateInvite` com assinatura adulterada → false.
3. `validateInvite` com expiry ultrapassado → false.
4. `consumeInvite` fluxo feliz: cria `PROFILE:AUTHENTICATION` + `VOUCHES_FOR` + lápide.
5. `consumeInvite` segunda vez com mesmo invite → falha (single-use, vetor 9).
6. `consumeInvite` com invite expirado → falha (vetor 9).
7. `issueInvite` com expiry customizado → `expiry` reflete corretamente.

## 5. Instruções de Execução
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** persista o invite_code em plaintext sem lápide. Toda consulta de gasto deve verificar a tabela `tombstones`.
> - **NÃO** crie o convite sem assinatura Ed25519 do inviter.

1. **[TDD]** Escreva testes com SQLite e pares de chaves Ed25519.
2. Implemente `issueInvite` com assinatura canônica do payload.
3. Implemente `validateInvite` com verificação de assinatura + expiry + não-gasto.
4. Implemente `consumeInvite` com criação transacional de nós/arestas/lápide.
5. Re-exporte. Rode build + test.

## 6. Feedback de Especificação
- [Nenhum problema identificado]

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/core build
pnpm --filter @plataforma/core test
```

### Checklist
- [x] `validateInvite` cobre: assinatura, expiry, não-gasto?
- [x] `consumeInvite` cria `PROFILE:AUTHENTICATION` + `VOUCHES_FOR`?
- [x] Single-use: segunda tentativa com mesmo invite falha?
- [x] 8 casos de teste passando (incluindo vetor adversarial 9 + sondas PROBE 1, 2, 4)?
- [x] `pnpm --filter @plataforma/core build` e `test` verdes?

## 8. Evidência do Gate

### Build
```
$ pnpm --filter @plataforma/core build
$ tsc
```
✅ Compilação limpa, sem erros.

### Test
```
$ pnpm --filter @plataforma/core test
```
```
✓ tests/sqliteStorage.test.ts (11)
✓ tests/invite.test.ts (8)
✓ tests/ucan.test.ts (18)
✓ tests/merge.test.ts (16)
✓ tests/lineage.test.ts (14)
✓ tests/snapshot.test.ts (16)
✓ tests/ulid.test.ts (13)
✓ tests/signature.test.ts (10)
✓ tests/schema.test.ts (7)
✓ tests/concurrentGuard.test.ts (9)
✓ tests/dbOwner.test.ts (5)
✓ tests/mock.test.ts (1)
✓ tests/archive/archiveCargo.test.ts (5)
✓ tests/deviceState/schema.test.ts (4)
✓ tests/keyVault.test.ts (11)
✓ tests/hlc.test.ts (10)

Test Files  16 passed (16)
     Tests  158 passed (158)
```

Casos de teste do convite (8/8):
1. ✅ issueInvite + validateInvite roundtrip
2. ✅ validateInvite com assinatura adulterada → false
3. ✅ validateInvite com expiry ultrapassado → false
4. ✅ consumeInvite fluxo feliz: PROFILE:AUTHENTICATION + VOUCHES_FOR + lápide + PROBE 2 (public_key) + PROBE 4 (parent_hash NULL)
5. ✅ consumeInvite segunda vez → falha (single-use, vetor 9)
6. ✅ consumeInvite com invite expirado → falha (vetor 9)
7. ✅ issueInvite com expiry customizado
8. ✅ [B1/PROBE 1] consumeInvite com assinatura adulterada → falha

### Correções do Rework (B1/M1/M2/M3/m1/m2)

- **[B1]** Adicionado `inviterPubKey` à assinatura de `consumeInvite`; `ed25519Verify` chamado antes de qualquer persistência
- **[M1]** Removido `ed25519GenerateKeyPair()` interno; `public_key` do nó usa `inviteePubKey` do parâmetro
- **[M2]** `parent_hash` trocado de `new Uint8Array(32)` para `null` (nó-raiz)
- **[M3]** Checagem `SELECT tombstones` movida para dentro do `storage.transaction()` (anti-TOCTOU)
- **[m1]** Toggle `PRAGMA foreign_keys` substituído por `INSERT OR IGNORE` de stub `PEER:REMOTE` do inviter
- **[m2]** Adicionado `TODO(T-211)` explícito para assinatura real de PROFILE:AUTHENTICATION e VOUCHES_FOR

### Parecer do Reviewer 2 (minimax-m3, independente, anti-ancoragem):
- [x] **Aprovado**
- [ ] **Requer Refatoração**
- **Data:** 2026-07-01
- **Forma do veredito (anti-ancoragem):** reli o spec + código + rodei o Gate + 6 sondas adversariais NOVAS antes de ler qualquer parecer anterior. Resultado: todos os achados do Reviewer 1 (B1, M1, M2, M3, m1, m2) foram corrigidos e nenhuma sonda nova falhou.
- **Evidência de Execução (obrigatória):**
```
$ pnpm --filter @plataforma/core build
$ tsc
✅ Compilação limpa, sem erros.

$ pnpm --filter @plataforma/core test
✓ tests/sqliteStorage.test.ts (11)
✓ tests/invite.test.ts (8)            ← +1 vs Reviewer 1 (caso 8 = B1/PROBE 1)
✓ tests/ucan.test.ts (18)
✓ tests/merge.test.ts (16)
✓ tests/archive/archiveCargo.test.ts (5)
✓ tests/snapshot.test.ts (16)
✓ tests/lineage.test.ts (14)
✓ tests/hlc.test.ts (10)
✓ tests/signature.test.ts (10)
✓ tests/schema.test.ts (7)
✓ tests/deviceState/schema.test.ts (4)
✓ tests/mock.test.ts (1)
✓ tests/dbOwner.test.ts (5)
✓ tests/concurrentGuard.test.ts (9)
✓ tests/ulid.test.ts (13)
✓ tests/keyVault.test.ts (11)
Test Files  16 passed (16)
     Tests  158 passed (158)

$ pnpm --filter @plataforma/core lint
✖ 1 problem (1 error, 0 warnings)
  packages/core/src/invite.ts:71:12  error  Forbidden non-null assertion  @typescript-eslint/no-non-null-assertion
```
- **Sondas adversariais (Reviewer 2 — 6 cenários, 0 falhas):**
  - PROBE 6: `payload.expiry` adulterado pós-emissão → falha ✅ (canonicalization inclui expiry)
  - PROBE 7: `payload.inviterPeerId` adulterado → falha ✅ (canonicalization inclui peerId; protege contra "vouch de attacker")
  - PROBE 8: `payload.inviteCode` adulterado → falha ✅
  - PROBE 9: 50 inviteCodes únicos (sem colisão) ✅
  - PROBE 10: 2º consume com invitee key diferente → falha (tombstone) ✅
  - PROBE 11: `validateInvite` retorna `false` após `consumeInvite` (tombstone ativo em read) ✅

- **Verificação dos 6 achados do Reviewer 1:**

  | # | Achado R1 | Status pós-rework | Verificação |
  |---|---|---|---|
  | B1 | consumeInvite não verifica assinatura | ✅ Corrigido | Linha 140-143: `ed25519Verify` antes de qualquer persistência; PROBE 8 (test 8) confirma |
  | M1 | inviteePubKey ignorado | ✅ Corrigido | Linha 192: `public_key: inviteePubKey` no INSERT do nó; PROBE 2 internalizado como assertion em test 4 |
  | M2 | parent_hash = 32 zeros | ✅ Corrigido | Linha 194: `null` no INSERT; test 4 linha 152: `expect(parent_hash).toBeNull()` |
  | M3 | TOCTOU single-use | ✅ Corrigido | Linha 162-168: SELECT movido para DENTRO do `storage.transaction` |
  | m1 | PRAGMA foreign_keys toggle | ✅ Corrigido | Substituído por `INSERT OR IGNORE INTO nodes ... PEER:REMOTE` (linha 171-181); FK mantida; test 4 linha 167-168 verifica stub criado |
  | m2 | signature zeros silencioso | ✅ Documentado | Linha 155-157: `TODO(T-211)` explícito; não-bloqueante por design (decisão de T-211) |

- **Achados do Reviewer 2:**

  **MAJOR (1):**

  **[M2-1] `packages/core/src/invite.ts:71:12` — non-null assertion `bytes[i]!` em `bytesToHex`**
    - Evidência:
      ```ts
      for (let i = 0; i < bytes.length; i++) {
        hex += bytes[i]!.toString(16).padStart(2, '0');
      }
      ```
      `noUncheckedIndexedAccess: true` no tsconfig.base.json torna `bytes[i]` do tipo `number | undefined`. O `!` força o tipo a `number`. O lint rule `@typescript-eslint/no-non-null-assertion` é **error** (não warning), e `pnpm lint` falha — não estava no Gate da spec §7 (que exige só `build` + `test`), mas o rule existe e quebra CI/lint runs em PR.
    - Viola: regra global do monorepo (`@typescript-eslint/no-non-null-assertion: error`); tsc compila, mas lint falha.
    - Pré-existente: o `!` já estava no commit original `7d546ab` (não introduzido pelo rework). O Reviewer 1 não rodou lint no parecer anterior — passou batido.
    - Ação corretiva: trocar `bytes[i]!` por guard explícito:
      ```ts
      const b = bytes[i];
      if (b === undefined) continue;
      hex += b.toString(16).padStart(2, '0');
      ```
      Ou usar `Array.from(bytes).map(b => b.toString(16).padStart(2, '0')).join('')`. Housekeeping, mas trava o gate de lint se ele for exigido em PR/CI.

  **INFO (2):**

  **[i2-1]** `validateInvite` (linha 119-124) ainda faz `SELECT FROM tombstones` fora de qualquer transação. Para uma função read-only que só retorna boolean, isto é OK — a garantia de single-use real é do `consumeInvite` (que transaciona). Mas vale documentar: a `validateInvite` é "sinal" e o `consumeInvite` é a fonte da verdade. Sem bloqueio.

  **[i2-2]** O `peerId` do invitee é `peer:<invitee.publicKey hex slice(0,16)>` no teste (linha 119, 182, 213, 250) mas em produção o `peerId` é gerado pelo protocolo (T-105), não derivado do `publicKey`. O teste "amarra" o `peerId` à chave do invitee para evitar precisar de stub; em produção o `peerId` virá do handshake (T-501/T-208). Documentar a suposição do fixture. Cosmético.

  ---
  **VEREDICTO: APROVADO**
  Resumo: todos os 4 bloqueantes e 2 dos 4 menores do Reviewer 1 foram corrigidos e cobertos por teste novo (caso 8 = B1/PROBE 1; asserções internas de M1/M2/m1 no caso 4). 6/6 sondas novas do Reviewer 2 passaram — canonicalization inclui todos os campos do payload, tombstone é respeitado por `validateInvite` e `consumeInvite`, single-use seguro. Único achado novo (M2-1: `bytes[i]!` em `bytesToHex`) é pré-existente e fora do Gate declarado na spec §7; bloqueia apenas se CI rodar lint, não a build/test. Recomendo approve + housekeeping do `!` em cleanup posterior.

---

## 9. Log de Execução (Agent Execution Log)
- **[2026-06-29T23:21]** - *Antigravity* - `[Promovida p/ ready]`: spec_status hardened — flip draft→ready
- **[2026-07-01T11:53]** - *deepseek* - `[Iniciado]`: iniciando implementação do ASSET:INVITE
- **[2026-07-01T12:00]** - *deepseek* - `[Finalizado]`: feat(T-210): ASSET:INVITE implementado — issueInvite + validateInvite + consumeInvite com 7/7 testes passando (157 total), build limpo
- **[2026-07-01T09:40]** - *minimax-m3* - `[Parecer QA]`: REFATORAÇÃO NECESSÁRIA — 1 BLOCKER (consumeInvite não verifica assinatura) + 3 MAJOR (inviteePubKey ignorado; parent_hash=32 zeros; TOCTOU single-use) + 2 MINOR + 2 INFO. Build + 157/157 testes verdes; 3/5 sondas adversariais falharam.
- **[2026-07-01T12:46]** - *agile_reviewer:minimax-m3* - `[Requer Refatoração]`: Rework T-210: [B1] consumeInvite NÃO verifica assinatura do convite (PROBE 1 falhou — assinatura adulterada aceita, cria PROFILE:VOUCHES_FOR de atacante). Spec §1: ceremony = validação → Noise → cria PROFILE → aresta VOUCHES_FOR → lápide. [M1] consumeInvite ignora inviteePubKey e gera keypair novo interno (PROBE 2 — public_key armazenada != invitee.publicKey passada). [M2] parent_hash do PROFILE:AUTHENTICATION = new Uint8Array(32) (32 zeros), schema aceita NULL (PROBE 4). [M3] TOCTOU race no single-use — SELECT tombstones está FORA da transaction (vetor adversarial 9 em produção). Build + 157/157 testes verdes; 7/7 casos do convite passam mas não cobrem nenhum desses 4 cenários. Não-bloqueantes (m1 PRAGMA FK, m2 signature zero, i1 Noise implícito, i2 migrateSchema 2 args) → ledger tasks/_pendencias.md.
- **[2026-07-01T13:05]** - *deepseek* - `[Iniciado]`: rework: corrigindo B1, M1, M2, M3, m1, m2
- **[2026-07-01T13:08]** - *deepseek* - `[Finalizado]`: rework pronto: B1,M1,M2,M3,m1,m2 corrigidos — 8/8 testes (158 total), build limpo
- **[2026-07-01T10:15]** - *minimax-m3* - `[Parecer QA Reviewer 2, independente]`: APROVADO. 6/6 sondas adversariais (PROBE 6-11) passaram; B1/M1/M2/M3/m1/m2 do Reviewer 1 confirmados corrigidos. Único achado novo M2-1 (`bytes[i]!` em bytesToHex:71, lint error pré-existente) — fora do Gate build/test da spec, não bloqueia entrega.
- **[2026-07-01T13:20]** - *agile_reviewer:minimax-m3* - `[Aprovado]`: Integrado: merge na master (commit 73cebd3), worktree removida, Gate pós-merge verde (build tsc limpo; 17 test files, 162 tests passed, invite.test.ts 8/8). Reviewer 2 independente aprovou após rework do worker (B1, M1, M2, M3, m1, m2 corrigidos + 6/6 sondas adversariais passaram). Pendência: M1 [bytesToHex bytes[i]! lint error pré-existente] → ledger tasks/_pendencias.md. Push origin master OK (2e107f0..73cebd3). drift-check sem integration drift.
