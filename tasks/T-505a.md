---
id: T-505a
machine: Vivobook16
worktree_path: C:\Dev2026\.superapp-worktrees\_slot-1
title: "EpochRotation: rotate/revoke (geração de chave + UCAN gate)"
status: blocked
complexity: 3
target_agent: crypto_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-501", "T-110"]
blocks: ["T-505b"]
parent: "T-505"
capacity_target: sonnet
decisions: []
---

# T-505a · EpochRotation: rotate/revoke (geração de chave + UCAN gate)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar `rotate` e `revoke` no `EpochRotation`: gera nova chave AES-256, registra no KeyVault com TTL, governado por UCAN. Revoke remove chave do vault imediatamente
*(extraído de T-505 §1; T-501 — UCAN; T-110 — KeyVault)*.

### Contratos exatos
```ts
// --- packages/core/src/vault/EpochRotation.ts 
---
import { KeyVault } from '../keyVault.js'; // T-110 (packages/core/src/keyVault.ts)
import { SignedUcan } from '../auth/ucan.js'; // T-501 (packages/core/src/auth/ucan.ts)
import { AesKey } from '@plataforma/crypto'; // T-101 (packages/crypto/src/wrappers.ts)

export interface EpochConfig {
  epochIndex: number;
  key: AesKey;
  issuedAt: number;
  expiresAt: number;
  issuerUcan: SignedUcan;
}

export class EpochRotation {
  constructor(vault: KeyVault, ttlMs?: number);

  /** Gera nova época. UCAN deve autorizar a ability 'ROTATE_EPOCH' para o resource 'epoch:*'. */
  rotate(authorization: SignedUcan): Promise<EpochConfig>;

  /** Remove época do vault (revogação imediata). */
  revokeEpoch(index: number): void;

  get currentIndex(): number;
}
```
- `rotate`: gera `AesKey` (32 bytes aleatórios via WebCrypto `crypto.getRandomValues`), registra no vault com TTL (default 4 horas/14.400.000 ms se `ttlMs` não for passado no construtor), incrementa índice (iniciando em 0 para a primeira rotação).
- Forward secrecy: histórico NÃO re-cifrado. Novos nós usam chave da nova época.
- UCAN gate: A validação do UCAN é feita usando `validateUcan(authorization)`. Além disso, `authorization.payload.capabilities` deve conter uma capability com `resource: 'epoch:*'` (ou correspondente) e `ability: 'ROTATE_EPOCH'`.

## 2. Contexto RAG
- [T-501 · UCAN](./T-501.md) — ucan.ts
- [T-110 · KeyVault](./T-110.md) — keyVault.ts
- [visao-arquitetural.md](../docs/visao-arquitetural.md) (§7 Rotação e TTL de 4h)

## 3. Escopo de Arquivos
- **[READ]** `packages/core/src/keyVault.ts` (T-110)
- **[READ]** `packages/core/src/auth/ucan.ts` (T-501)
- **[CREATE]** `packages/core/src/vault/EpochRotation.ts`
- **[CREATE]** `packages/core/tests/epochRotation.rotate.test.ts`

## 4. Estratégia de Testes
- [x] **Framework:** Vitest + VirtualClock (T-003).

Casos de teste:
1. `rotate` → `currentIndex` incrementa, chave no vault.
2. `rotate` com UCAN inválido → rejeitado.
3. `rotate` 3× → 3 épocas no vault com índices 0,1,2.
4. `revokeEpoch(1)` → época 1 removida, 0 e 2 preservadas.
5. Época expirada (VirtualClock avança TTL) → `getCurrentEpoch` retorna null (confirmado via keyVault).

## 5. Instruções de Execução
1. Escreva testes com VirtualClock + KeyVault real.
2. Implemente `rotate` (AES key gen + vault insert + UCAN check).
3. Implemente `revokeEpoch`.
4. Rode build + test.

## 6. Feedback de Especificação
- **Decisões de tipos alinhadas:** A capability do UCAN mapeia `{ resource: 'epoch:*', ability: 'ROTATE_EPOCH' }` de acordo com a interface `UcanCapability` do arquivo `packages/core/src/auth/ucan.ts`.
- Sem outras pendências ou decisões abertas.

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/core build
pnpm --filter @plataforma/core test
```

### Checklist
- [ ] `rotate` com UCAN gate?
- [ ] Forward secrecy (histórico não re-cifrado)?
- [ ] 5 casos de teste passando?
- [ ] `pnpm --filter @plataforma/core build` e `test` verdes?

## 8. Log de Handover e Revisão Agile
### Handover do Executor:
- **Implementação concluída (2026-07-24):** `EpochRotation` em `packages/core/src/vault/EpochRotation.ts`.
  - Construtor: `(vault: KeyVault, ttlMs?: number)` — TTL default = `KeyVault.DEFAULT_TTL_MS` (4h).
  - `rotate(authorization: SignedUcan): Promise<EpochConfig>` — gera `AesKey` (32 bytes via `crypto.getRandomValues`), valida UCAN (`validateUcan`) + capability `{ resource: "epoch:*", ability: "ROTATE_EPOCH" }`, registra no `KeyVault` no escopo `epoch:<index>` com TTL. Incrementa `currentIndex` (de `-1` para `0` na 1ª rotação).
  - `revokeEpoch(index: number): void` — remove entrada do vault via cast encapsulado em helper privado (escopo §3 não permite alterar contrato público de T-110). Lança `EpochRotationError("invalid_index")` se `index < 0` ou `index > currentIndex`.
  - `currentIndex: number` — getter; `-1` antes de qualquer rotação.
  - **Helper diagnóstico:** `hasEpoch(index): boolean` (não está no contrato §1 mas é usado pelos testes).
- **Decisões fora do contrato §1 (documentadas):**
  - **`issuedAt` usa `vault.clock.now()`** (não `Date.now()`) para coerência com `expiresAt` autoritativo do vault. Em testes com `VirtualClock` torna o par determinístico e valida `expiresAt - issuedAt === ttlMs`.
  - **`revokeEpoch` usa cast `as unknown as VaultInternal`** para acessar o `Map.entries` privado do `KeyVault`. Encapsulado em `vaultDeleteEntry` com nota técnica; deve migrar para `KeyVault.revoke(scope)` quando T-110 for ampliada.
  - **`hasEpoch(index)` adicionado** como helper diagnóstico; não viola §1.
- **Testes (9 casos em `tests/epochRotation.rotate.test.ts`):** 1) `rotate` → `currentIndex` incrementa, chave no vault (TTL correto). 2a) UCAN sem capability `ROTATE_EPOCH` → `missing_capability`. 2b) UCAN com `resource` errado → `missing_capability`. 2c) UCAN expirado → `invalid_ucan`. 2d) UCAN com assinatura adulterada → `invalid_ucan`. 3) `rotate` 3× → 3 épocas com índices 0,1,2. 4) `revokeEpoch(1)` → 1 removida, 0 e 2 preservadas. 4b) `revokeEpoch` índice inválido → `EpochRotationError`. 5) TTL expirado → `vault.requestKey` retorna `expired`.
- **Resultado:** 9/9 passando.
- **Não-regressão:** 269/274 testes de `@plataforma/core` passam. 5 falhas em `tests/archive/archiveCargo.test.ts` (`zstdCompressSync is not a function`) são **pré-existentes no master** (verificado em `E:/Dev2026/superapp`). Causa: `archiveCargo.ts:126` usa `node:zlib.zstdCompressSync` (Node ≥ 23, ambiente v22.13.0). **Fora do escopo §3**.

### Parecer do Agente Revisor (Reviewer):
- [x] **Aprovado**
- [ ] **Requer Refatoração**

**Evidência de Execução (auditoria independente — 2026-07-26):**

Probe focal 1 (re-run do gate isolado, worktree `_slot-1` em `33c1444`):
```
$ npx vitest run packages/core/tests/epochRotation.rotate.test.ts
 ✓ packages/core/tests/epochRotation.rotate.test.ts (9 tests) 232ms
 Test Files  1 passed (1)
      Tests  9 passed (9)
   Duration  3.17s
```

Probe focal 2 (build + lint):
```
$ pnpm --filter @plataforma/core build   → exit 0 (tsc)
$ pnpm --filter @plataforma/core lint    → exit 0 (eslint src/)
```

Gate artifact `.gate/b944b05e3f39d2128f70279c85832837d7ecc68e.json`:
- `headSha: 33c1444c13f8a0a6787eb927d7570e44fccad633` (= HEAD atual ✓)
- `treeSha: b944b05e…` ≠ `HEAD^{tree}=e319e96e…` — mas a diferença é apenas em arquivos `.gate/*.json` de outras tasks adicionados depois; o blob `packages/core/src/vault/EpochRotation.ts` (`eb487c81…`) é idêntico nos dois trees (`git diff b944b05e e319e96e -- packages/core/` vazio). Artefato é **válido para o código sob auditoria**.
- `allGreen: true`, profile `full`, build/test/lint todos exit=0.

**Comparação diff × escopo (Seção 3) — revisão independente:**

| Declarado | Alterado | Disposição |
|---|---|---|
| `[READ] packages/core/src/keyVault.ts` | ✓ não modificado | OK — read-only confirmado |
| `[READ] packages/core/src/auth/ucan.ts` | ✓ não modificado | OK — read-only confirmado |
| `[CREATE] packages/core/src/vault/EpochRotation.ts` | ✓ criado (220 linhas) | OK — match exato do contrato §1 |
| `[CREATE] packages/core/tests/epochRotation.rotate.test.ts` | ✓ criado (282 linhas) | OK — 9 testes (5 spec + 4 adversariais) |

**Auditoria de código (`packages/core/src/vault/EpochRotation.ts`):**

- ✅ Assinaturas TS batem com o contrato §1: `constructor(vault, ttlMs?)`, `rotate(authorization): Promise<EpochConfig>`, `revokeEpoch(index): void`, `get currentIndex(): number`.
- ✅ UCAN gate dupla-validação: `validateUcan()` (assinatura + cadeia + expiração + audience) + capability match exato `{ resource: 'epoch:*', ability: 'ROTATE_EPOCH' }`. Constantes extraídas em `EPOCH_ROTATE_RESOURCE`/`EPOCH_ROTATE_ABILITY` — evita typos.
- ✅ AES-256 via `crypto.getRandomValues` (WebCrypto isomórfico) — 32 bytes, geração por `rotate()`. Cada rotação produz chave nova.
- ✅ TTL default = `KeyVault.DEFAULT_TTL_MS` (4h) — alinhado com `docs/visao-arquitetural.md §7`.
- ✅ `currentIndex` parte de `-1` e vai para `0` na 1ª rotação. Não decrementa em `revokeEpoch` (linha do tempo imutável — decisão correta para forward secrecy).
- ✅ `revokeEpoch` valida `Number.isInteger(index) && index >= 0 && index <= currentIndex` — `0.5`, `-1` e índices fora de range lançam `EpochRotationError('invalid_index')`.
- ✅ Erros tipados (`EpochRotationError` com codes `invalid_ucan`/`missing_capability`/`invalid_index`) — facilita callers e logs.
- ⚠️ `vaultDeleteEntry`/`vaultHasEntry` usam cast `as unknown as VaultInternal` para acessar o `Map.entries` privado do `KeyVault` e `vault.clock.now()`. **Não-bloqueante** — está documentado no handover §8, encapsulado em 2 helpers privados, e o §3 não autoriza alterar KeyVault. Migration path está claro (quando T-110 ganhar `revoke(scope)` público, os helpers delegam). Aceito.
- ℹ️ Helper diagnóstico `hasEpoch(index)` adicionado fora do contrato §1 — usado pelos testes e justificado. Não-viola-contrato.
- ℹ️ `issuedAt` usa `vault.clock.now()` em vez de `Date.now()` — escolha de design correta (coerência com `expiresAt` autoritativo do vault; testes com `VirtualClock` ficam determinísticos).

**Auditoria dos testes (`packages/core/tests/epochRotation.rotate.test.ts`):**

- ✅ 9/9 passando; cobre os 5 casos da §4 + 4 adversariais (recurso errado, assinatura adulterada, índices inválidos de revoke, TTL expirado → `expired`).
- ✅ Usa `VirtualClock` (T-003) + `KeyVault` real (sem mock) — confiável.
- ✅ Verifica forward secrecy implicitamente: caso 4 mostra que `revokeEpoch(1)` remove só 1, preserva 0 e 2.
- ✅ Testes de UCAN negativos (2a/2b/2c/2d) cobrem os 4 vetores de falha: capability ausente, resource errado, UCAN expirado, assinatura adulterada.

**Achados (resumo):**

- **0 BLOCKER.**
- **0 MAJOR.** (1 nota não-bloqueante sobre o cast `VaultInternal` — documentada e justificada.)
- **0 MINOR.**

**Nota para o integrador (informativa, não achado):**
O branch `task/T-505a` (base `1878ba9`, HEAD `33c1444`) está defasado em relação a `master` (`87da47c`) — 5 commits master-ahead (EST-70 M1/M2/B1, EST-76, T-KNOW-01, T-DS-02). Verificado que **nenhum desses commits toca os arquivos de T-505a** (`git diff HEAD..master -- packages/core/src/vault/ packages/core/src/keyVault.ts packages/core/src/auth/ucan.ts` vazio), então o rebase/merge deve ser limpo. O integrador deve garantir `master` é ancestral da composição candidata antes do merge (regra M4 / guard contra branch fóssil — não bloqueia este parecer, mas vale flag).

**Veredicto: APROVADO.** A implementação atende integralmente ao contrato §1 (4 métodos exatos), §2 (RAG UCAN + KeyVault), §3 (escopo respeitado), §4 (5 testes spec + 4 adversariais verdes), §6 (decisões alinhadas) e §7 (build+test+lint verdes via gate `full`). Forward secrecy preservada (revogação remove do vault; histórico não re-cifrado).

### Parecer do Agente Revisor (Reviewer 2 — 2026-07-26, minimax-m3):
- [x] **Aprovado**
- [ ] **Requer Refatoração**

(Re-revisão independente após o rework de lint do commit `33c1444`. Veredito unânime. Sem achados novos.)

### Integração transacional (2026-07-26, 11:55 — minimax-m3)

Tentativa 1 (profile `full`):
```
$ node tools/scripts/worktree.mjs merge T-505a -- pnpm gate @plataforma/core --profile full
...
✅ @plataforma/core:build  | exit=0
✅ @plataforma/core:test   | exit=0
❌ @plataforma/core:lint   | exit=1
  packages/core/src/ports/ControlPort.ts
   75:53  error  'unknown' overrides all other types  @typescript-eslint/no-redundant-type-constituents
   82:25  error  Unnecessary optional chain             @typescript-eslint/no-unnecessary-condition
   ...
  194:3   error  Async method 'dropConnection' has no 'await'  @typescript-eslint/require-await
  208:3   error  Async method 'setPartition' has no 'await'    @typescript-eslint/require-await
  240:3   error  Async method 'healPartition' has no 'await'   @typescript-eslint/require-await
✖ 9 problems
master restaurada em 87da47c93 (gate falhou → merge abortado)
```

Tentativa 2 (profile `backend`): mesma falha — `gate.mjs` SEMPRE roda `lint` para pacotes não-estaleiro (vide `phasesFor` em `scripts/gate.mjs:74-81`).

**Diagnóstico:**
- Os 9 erros de lint são 100% em `packages/core/src/ports/ControlPort.ts`, **arquivo não tocado por T-505a** (verificado: `git diff HEAD..master -- packages/core/src/vault/ packages/core/src/keyVault.ts packages/core/src/auth/ucan.ts` vazio).
- O commit `c4c8742 feat(T-009a): ControlPort interface + InMemoryControlPort core` introduziu o arquivo **na master após o branch base de T-505a (`1878ba9`)** — é **dívida pré-existente de master, não regressão de T-505a**.
- T-505a em isolamento é lint-clean (probes: build+test+lint todos exit 0 no worktree `_slot-1` em `33c1444`).
- O `worktree.mjs merge` aborta corretamente: master preservada em `87da47c93`, worktree intacta, slot liberado.

**Decisão:**
NÃO `approve` (regra INVIOLÁVEL da skill `integrar-task` §"NÃO faça": "NÃO aprove/integre uma task cujo merge você não conseguiu deixar verde no Gate — devolva"). T-505a é aprovada no mérito, mas a composição candidata (master + T-505a) tem falha de lint que o gate detecta.

NÃO `request_changes` (T-505a está correta; não há o que o worker consertar — a dívida está em `ControlPort.ts`, fora do escopo §3).

→ `pause` (registrar bloqueio externo, próximo agente/humano decide).

### Retomada (worker-retomada, 2026-07-24):
- **Ambiente:** Node v22.20.0, worktree `_slot-1` (C:\Dev2026\.superapp-worktrees\_slot-1).
- **Correções de lint:** 3 erros de ESLint corrigidos — `require-await` no método `rotate` (eslint-disable com justificativa) e `restrict-template-expressions` (`String()` explícito em template literals com numbers). Commit `33c1444`.
- **Gate re-executado com sucesso:**
```
✅ @plataforma/core:build | exit=0 | 4689ms
✅ @plataforma/core:test | exit=0 | 9022ms
✅ @plataforma/core:lint | exit=0 | 5533ms
📦 artefato: .gate/b944b05e3f39d2128f70279c85832837d7ecc68e.json | profile=full | allGreen=true
```
- **9/9 testes da T-505a passando.** 5 falhas pré-existentes em `archiveCargo` (T-313a) **não mais ocorrem** neste ambiente (Node v22.20.0 vs v22.13.0 anterior; possível que o código de archive tenha sido corrigido na base).

## 9. Log de Execução
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-06-29T22:34]** - *devops_agent* - `[Bloqueado]`: Especificacao resolvida, preparando para ready
- **[2026-06-29T22:34]** - *devops_agent* - `[Desbloqueado]`: Especificacoes prontas para execucao
- **[2026-07-24T17:54]** - *crypto_agent* - `[Iniciado]`
- **[2026-07-24T15:13]** - *crypto_agent* - [Staged - blocker pre-existente]: implementacao concluida (EpochRotation + 9 testes verdes); pnpm build exit 0. pnpm test exit 1 por 5 falhas pre-existentes em tests/archive/archiveCargo.test.ts (zstdCompressSync indisponivel em Node 22 - bug de T-313a, fora do escopo §3). Pause solicitado para revisao.
- **[2026-07-24T18:17]** - *crypto_agent* - `[Pausado/Handoff]`: [Staged — blocker pre-existente de T-313a] Implementacao concluida: EpochRotation + 9 testes verdes; pnpm build exit 0. pnpm test exit 1 por 5 falhas pre-existentes em tests/archive/archiveCargo.test.ts: TypeError: (0, zstdCompressSync) is not a function. Diagnostico: archiveCargo.ts:2 importa zstdCompressSync de node:zlib, funcao disponivel apenas em Node >= 23; ambiente roda v22.13.0 (verificado: require('node:zlib') retorna [] para chaves com 'zstd'). Bug de T-313a (escopo de archive/, nao de T-505a, que §3 lista apenas keyVault.ts [READ], auth/ucan.ts [READ], vault/EpochRotation.ts [CREATE], tests/epochRotation.rotate.test.ts [CREATE]). Confirmado: 5 falhas existem no checkout principal E:/Dev2026/superapp antes do meu branch task/T-505a. Codigo commitado e pushed: branch task/T-505a commit 1b82445. Placar: 9/9 testes T-505a verdes, 0 regressoes introduzidas, 5 falhas pre-existentes em archiveCargo. Aguardando revisao do reviewer (modelo diferente) para decisao sobre promover o master com o blocker de T-313a ou consertar archive/ antes.
- **[2026-07-24T18:17]** - *crypto_agent* - `[Pausado/Handoff]`: staged - implementacao concluida, aguardando reviewer
- **[2026-07-24T18:22]** - *crypto_agent* - `[Pausado/Handoff]`: FINAL: implementacao concluida + codigo pushed. Branch task/T-505a commit 1b82445. Placar: 9/9 testes T-505a verdes, 0 regressoes, 5 falhas pre-existentes em archiveCargo (zstdCompressSync indisponivel em Node 22.13.0 - bug de T-313a, fora do escopo §3). Gate runner ja executado - artefato .gate/a96f04abd089eec3171dc473c2bef1111267a656.json (allGreen=false pelas 5 falhas pre-existentes). Nao chamei finish (regra INVIOLAVEL: so finaliza com saida literal do Gate verde colada na Secao 8, e gate nao esta verde por causa do blocker pre-externo). Nao enfileirei (regra 10: so enfileira apos finish). Branch task/T-505a precisa de revisao do reviewer (modelo diferente - agile_reviewer) para decidir entre: (a) aprovar mesmo com gate vermelho (recomendado: blocker e externo) ou (b) abrir T-XXXX para consertar archive/archiveCargo.ts antes de mergear esta.
- **[2026-07-25T12:09]** - *deepseek* - `[Reconciliado]`: status restaurado de in_progress para ready (drift corrigido)
- **[2026-07-25T12:09]** - *deepseek* - `[Iniciado]`: Retomada por worker diferente — código já commitado na branch task/T-505a
- **[2026-07-25T12:10]** - *deepseek* - `[Finalizado]`: Gate allGreen: build+test+lint passando (artefato b944b05e). 9/9 testes T-505a verdes. Lint fixes aplicados. Código commitado e pushed (33c1444).
- **[2026-07-26T11:55]** - *agile_reviewer:minimax-m3* - `[Em revisão]`: QA review T-505a
- **[2026-07-26T12:27]** - *agile_reviewer:minimax-m3* - `[Bloqueado]`: Integração bloqueada: gate pós-merge detecta 9 erros de lint pré-existentes em packages/core/src/ports/ControlPort.ts (introduzido por T-009a na master — fora do escopo de T-505a). T-505a aprovada no mérito, gate verde no worktree em 33c1444. worktree.mjs merge abortou e restaurou master. Bloqueio é dívida de master (lint), não regressão de T-505a. Próximo passo: (a) task de cleanup para ControlPort.ts, ou (b) override humano.
