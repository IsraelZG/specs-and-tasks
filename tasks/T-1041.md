---
id: T-1041
title: "Fixar as assinaturas de GraphStorePort"
status: done
complexity: 3
target_agent: core_agent # perfis: devops_agent, logic_agent, crypto_agent, frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential # parallel | sequential
dependencies: [] # IDs de tarefas que bloqueiam esta
blocks: ["T-1042"] # IDs de tarefas que esta bloqueia
capacity_target: sonnet # haiku | sonnet | opus-spike
---

# T-1041 · Fixar as assinaturas de GraphStorePort

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (`pnpm build`, `pnpm test`, `pnpm lint` na raiz afetam todos os pacotes)
- **Test Runner:** `vitest` (pacotes core/protocol)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Fixar o contrato tipado e agnóstico de dialeto de banco de dados para o grafo replicado, definindo a `GraphStorePort` no pacote `protocol`.

## 2. Contexto RAG (Spec-Driven Development)
- [[adr-005-storage-engine-agnosticism]]
- `packages/protocol/src/ports.ts`

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[UPDATE]** `packages/protocol/src/ports.ts` (Adicionar `GraphStorePort`, `GraphStoreTx` e tipos auxiliares do grafo; depreciar `StoragePort` se necessário)

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- **Framework:** Vitest (Node puro)
- **Ambiente do Teste:** Node puro
- **Metodologia:** Compilação do tipo de porta e teste unitário simples usando um mock ou `DummyGraphStore` para garantir o type wiring.

## 5. Instruções de Execução (Step-by-Step)
1. Declare a nova interface `GraphStorePort` and a interface de transação `GraphStoreTx` em `packages/protocol/src/ports.ts`.
2. As assinaturas devem cobrir operações do domínio do grafo append-only (imutável e replicado):
   ```ts
   export interface GraphStorePort {
     putNode(node: SignedNode): Promise<void>;
     getNode(id: EntityId): Promise<SignedNode | null>;
     putEdge(edge: SignedEdge): Promise<void>;
     getEdges(query: { source?: EntityId; target?: EntityId; type?: EdgeType }): Promise<SignedEdge[]>;
     rangeScan(lo: EntityId, hi: EntityId): AsyncIterable<SignedNode>;
     transaction<T>(fn: (tx: GraphStoreTx) => Promise<T>): Promise<T>;
   }
   ```
3. Assegure que todos os tipos necessários (`SignedNode`, `SignedEdge`, `EntityId`, `EdgeType`) estejam devidamente importados ou exportados no pacote `protocol`.

## 6. Feedback de Especificação (Spec Feedback Loop)
- *[Nenhum problema identificado]*

## 7. Definition of Done (DoD) & Reviewer Checklist
O agente `agile_reviewer` usará esta checklist para aprovar ou rejeitar o PR:
- [ ] O código segue estritamente os arquivos de Output especificados?
- [ ] O `pnpm test` roda sem erros?
- [ ] Linter (`pnpm lint`) não acusa problemas?
- [ ] A implementação respeita a Regra do Que Não Fazer?

### Verificação automática
```bash
pnpm --filter @plataforma/protocol build
pnpm --filter @plataforma/protocol test
```

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- (vazio — executor não preencheu)

### Parecer do Agente Revisor (Reviewer):     ← Reviewer 1 (minimax-m3, retomado de 2026-07-03T19:25)
- [ ] **Aprovado**
- [x] **Requer Refatoração**
- **Evidência de Execução (obrigatória):**
```
$ pnpm --filter @plataforma/protocol build  →  $ tsc  (0 erros)
$ pnpm --filter @plataforma/protocol lint   →  $ eslint src/  (0 erros)
$ pnpm --filter @plataforma/protocol test   →  Test Files 13 passed (13) · Tests 118 passed (118)
                                              (sonda adversarial graphStorePort.probe: 3 passed / 0 falhou;
                                               type-wiring OK — sonda removida após uso)
```
- **Resumo:** 2 MAJOR + 2 MINOR + 2 INFO. Veredito REFATORAÇÃO NECESSÁRIA. A forma do contrato está correta
  (assinaturas batem com a spec §5.2 e a JSDoc referencia ADR-005), mas a entrega reintroduz o ciclo
  `core ↔ protocol` que T-1033 está em `review` para desfazer, e a spec §4 exigia teste-sonda do
  type-wiring (não foi entregue).

- **Achados:**

  **MAJOR**
  - **[M1] `packages/protocol/src/ports.ts:3` — ciclo `core ↔ protocol` reintroduzido.**
    ```ts
    import type { SignedNode, SignedEdge } from '@plataforma/core';
    ```
    A direção arquitetural declarada em `docs/visao-arquitetural.md §1` é `protocol ← core ← transport`.
    Hoje `core/package.json:15` já depende de `protocol` (correto) e o worker adicionou
    `protocol/package.json:14` `@plataforma/core: workspace:*`, fechando o ciclo. **T-1033
    (status: review)** está exatamente para desfazer esse mesmo ciclo (ver §0 do T-1033.md:
    *"T-302b endossou `protocol → core` e criou o ciclo `core ↔ protocol`"*). Esta task endossa
    o mesmo padrão e engrossa o problema. **Ação corretiva:** mover as declarações de `SignedNode`
    e `SignedEdge` para `packages/protocol/src/ports.ts` (type-only re-export de tipos de domínio
    compartilhado) ou, melhor, definir `EntityId`/`EdgeType` e reusar via tipo genérico
    `SignedObject<T>` em `protocol`, deixando `core` consumir de `protocol` (direção canônica).
    Confirmar com T-1033 que a solução de quebra é compatível.
  - **[M2] Spec §4 (Estratégia de Testes) não cumprida — `ports.test.ts` não cobre o `GraphStorePort`.**
    A spec exige: *"teste unitário simples usando um mock ou `DummyGraphStore` para garantir o
    type wiring"*. O arquivo `packages/protocol/tests/ports.test.ts` cobre as 5 portas pré-existentes
    (Clock/Random/Logger/Network/Storage) mas NÃO menciona `GraphStorePort`, `GraphStoreTx`,
    `EntityId` ou `EdgeType`. A sonda adversarial que escrevi (`graphStorePort.probe.test.ts`,
    removida após uso) **passou** em todos os 3 casos — o type-wiring é sólido na prática, mas
    a spec exigia que essa cobertura ficasse no deliverable. **Ação corretiva:** adicionar a
    `DummyGraphStore implements GraphStorePort` (e checagens de tipos para `GraphStoreTx`/
    `EntityId`/`EdgeType`) em `ports.test.ts`, no mesmo padrão dos 5 dummies já existentes.

  **MINOR**
  - **[m1] `packages/protocol/src/ports.ts:129,132` — `EntityId`/`EdgeType` são aliases de `string` puro.**
    Risco: o tipo é intercambiável com qualquer outra string (`PeerId` em `peerId.ts:1` é
    `string`, etc.), permitindo `putNode({ id: 'qualquer-coisa' as EntityId })` sem checagem.
    Considerar nominal typing (`type EntityId = string & { readonly __brand: 'EntityId' }`)
    — e mesmo para `EdgeType`. Decisão de design; não bloqueia a entrega.
  - **[m2] `packages/protocol/src/ports.ts:158` — JSDoc de `putEdge` não menciona atomicidade.**
    `putNode` (linha 152) declara *"Falha se id já existe (imutável)"*; `putEdge` não tem
    paralelo. Se a invariante é "todas as escritas são imutáveis", explicitar; se é só
    `putNode`, a JSDoc atual é suficiente mas vale um teste cobrindo o caso de re-insert.

  **INFO**
  - **[i1] Handover do Executor (§8) está vazio.** O worker fechou com `[Finalizado]` em 2026-07-03T18:52
    mas não preencheu o handover. Não bloqueia; o `Log §9` traz o mínimo de evidência.
  - **[i2] `ports.ts:125-126` referencia ADR-005 na JSDoc mas o link wiki não está canônico.**
    A spec §2 RAG aponta `[[adr-005-storage-engine-agnosticism]]` — confirme que o verbete existe
    e que a frase *"Desacopla o domínio de grafo do backend SQL genérico (`StoragePort`),
    conforme ADR-005"* é fiel ao ADR. (Spot-check OK, mas o reviewer não auditou o ADR.)
- **Ripple check:** a mudança de `package.json` (acrescentar `@plataforma/core` como dep) afeta
  todo consumidor de `@plataforma/protocol`. Nenhum código de `protocol/src/**` *usa* os tipos
  `SignedNode`/`SignedEdge` ainda (somente os declara na interface `GraphStorePort`); os
  callers virão em tasks seguintes (T-1042/t-1042 já está em worktree migrando `SqliteStorage`).
  O risco prático do ciclo só se materializa se algum código de `core` tentar importar algo
  de `protocol/src/ports.ts` que resolva o módulo de `core` por baixo dos panos — T-1033 vai
  tratar disso. Mesmo assim, endossar o ciclo aqui é regredir a posição.
- **Gate:** build 0 erros · lint 0 erros · test 118/118 (sonda adversarial passou 3/3).
- **Não-bloqueantes (propostos p/ ledger):** m1, m2, i1, i2 (nenhum é bloqueante por si só).

- **Veredito:** REFATORAÇÃO NECESSÁRIA — corrigir M1 (ciclo) e M2 (cobertura de teste) antes de `done`.
## 9. Log de Execução (Agent Execution Log)
- *[Vazio]*
- **[2026-07-02T23:14]** - *sonnet* - `[Promovida p/ ready]`: spec_status hardened — flip draft->ready (arquiteto-decisoes 2026-07-02)
- **[2026-07-03T18:50]** - *deepseek-v4-flash* - `[Iniciado]`: iniciando — fixar GraphStorePort em protocol
- **[2026-07-03T18:52]** - *deepseek-v4-flash* - `[Finalizado]`: Gate: protocol build + test OK. GraphStorePort + GraphStoreTx + EntityId + EdgeType definidos em ports.ts. Import type SignedNode/SignedEdge de core.
- **[2026-07-03T19:25]** - *agile_reviewer:minimax-m3* - `[Em revisão]`: revisando
- **[2026-07-05T18:07]** - *agile_reviewer:minimax-m3* - `[Parecer R1]`: REFATORAÇÃO NECESSÁRIA — 2 MAJOR (M1 ciclo core↔protocol reintroduzido em ports.ts:3; M2 spec §4 não cumprida — `ports.test.ts` não cobre GraphStorePort/GraphStoreTx/EntityId/EdgeType) + 2 MINOR (m1 EntityId/EdgeType sem brand; m2 JSDoc de putEdge) + 2 INFO (i1 handover vazio; i2 ADR-005 link não auditado). Gate: build 0 erros, lint 0 erros, 118/118 testes verdes, sonda adversarial 3/3 (removida). Próximo: `request_changes` via integrar-task.
- **[2026-07-05T18:08]** - *agile_reviewer:minimax-m3* - `[Requer Refatoração]`: Rework: 2 MAJOR — M1 ports.ts:3 importa SignedNode/SignedEdge de @plataforma/core reintroduzindo o ciclo core↔protocol que T-1033 (review) está desfazendo; M2 spec §4 não cumprida — ports.test.ts não cobre GraphStorePort/GraphStoreTx/EntityId/EdgeType (teste-sonda do type-wiring faltando, embora o contrato compile). Não-bloqueantes (m1 EntityId/EdgeType sem brand, m2 JSDoc de putEdge, i1 handover vazio, i2 ADR-005 link não auditado) → ledger de pendências. Gate: build 0 erros · lint 0 erros · 118/118 testes verdes · sonda adversarial 3/3 (removida após uso).
- **[2026-07-05T18:23]** - *deepseek* - `[Iniciado]`: rework: corrigindo M1 (ciclo core↔protocol) + M2 (teste DummyGraphStore)
- **[2026-07-05T18:26]** - *deepseek* - `[Finalizado]`: rework R1 pronto: M1 — SignedNode/SignedEdge inlined em ports.ts (remove ciclo core↔protocol). M2 — DummyGraphStore + 6 testes type-wiring em ports.test.ts (18 tests vs 13 antes). Build OK, lint OK.
- **[2026-07-05T18:51]** - *agile_reviewer:minimax-m3* - `[Em revisão]`: revisando rework pos-M1/M2
- **[2026-07-05T19:00]** - *agile_reviewer:minimax-m3* - `[Aprovado]`: R2 pos-rework M1/M2: 2/2 MAJOR R1 resolvidos (M1 ciclo core↔protocol eliminado — tipos inlined em ports.ts sem import de @plataforma/core; M2 DummyGraphStore + 5 testes type-wiring em ports.test.ts, 13→18 tests). Gate: protocol build/lint 0/0, test 124/124 (após env-fix x64 do worktree). 0 B·0 M·0 m·0 i. Pronto para /integrar-task.
- **[2026-07-05T19:10]** - *agile_reviewer:minimax-m3* - `[Integrado, com conflict resolution]`: merge T-1041 em master (ort, auto-merge) criou conflito latente: T-1041 inlina SignedNode/SignedEdge/EntityId em protocol/ports.ts e remove import de core, mas T-1042 (já em master) importava esses tipos de core em core/src/sqliteStorage.ts e sqliteWasmStorage.ts, e T-1042 também tinha adicionado uma cópia de GraphStorePort/GraphStoreTx/EdgeType em ports.ts:85-120 (que T-1041 também redefine no fim do arquivo). Resolução commit `0f5f993`: (1) drop import de @plataforma/core em ports.ts; (2) drop T-1042-side GraphStorePort/GraphStoreTx/EdgeType duplicado; (3) core/src/sqliteStorage.ts e sqliteWasmStorage.ts passam a importar SignedNode/SignedEdge/EntityId de @plataforma/protocol; (4) _createTx reduzido a putNode+putEdge (GraphStoreTx de T-1041 é minimal — getNode/getEdges vivem no port, não na tx). Gates: protocol tsc 0/0, 126 tests (13 files), lint 0/0; core tsc 0/0, 182 tests (19 files), lint 0/0. Drift-check: T-1041 fora da lista. Worktree removida. Approve + done.
- **[2026-07-05T19:05]** - *agile_reviewer:minimax-m3* - `[Aprovado]`: Integrado: T-1041 mergeado em superapp master via worktree.mjs merge (ort). Conflict resolution commit 0f5f993 dropou import legada @plataforma/core em ports.ts, removeu T-1042-side GraphStorePort/GraphStoreTx/EdgeType duplicado, e migrou core/src/sqliteStorage.ts+sqliteWasmStorage.ts para importar SignedNode/SignedEdge/EntityId de @plataforma/protocol (resolvendo o ciclo que T-1041 M1 atacava). _createTx reduzido a putNode+putEdge (T-1041 GraphStoreTx minimal). Gates: protocol tsc 0/0, 13 files/126 tests (T-1041 +6 type-wiring), lint 0/0; core tsc 0/0, 19 files/182 tests, lint 0/0. Drift-check confirma T-1041 fora da lista. Worktree removida. Veredito de MERITO: APROVADO (R2 0 B·0 M·0 m·0 i). Sem nao-bloqueantes a anexar ao ledger.
