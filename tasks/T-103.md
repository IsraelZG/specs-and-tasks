---
id: T-103
title: "HLC (Hybrid Logical Clock) completo"
status: done
complexity: 3
parent_task: null
subtasks: []
dependencies: ["T-003"]
blocks: []
target_agent: logic_agent
reviewer_agent: "agile_reviewer"
execution_mode: sequential
---

# T-103 · HLC (Hybrid Logical Clock) completo

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar `HybridLogicalClock` no pacote `@plataforma/core`, consumindo `ClockPort` de T-004
*(derivado de `docs/conceitos/hlc.md` — contrato completo: par (pt, c), empacotamento `(pt << 16) | c`, invariantes de monotonicidade e pai)*.

### Contratos exatos (assinaturas TS fixadas)

```ts
// --- packages/core/src/hlc.ts ---
import { ClockPort } from '@plataforma/protocol'; // T-004

/** HLC empacotado como inteiro 48+16 bits: (pt << 16) | c.
 *  Derivado de docs/conceitos/hlc.md. */
export type HLCTimestamp = bigint;

/** Componentes desempacotados do HLC. */
export interface HLCParts {
  pt: number; // wall-clock em ms (48 bits efetivos)
  c:  number; // contador lógico (16 bits, 0–65535)
}

/** Limite de drift: se pt_remoto > wall_clock_local + MAX_DRIFT_MS, rejeita.
 *  Derivado de docs/conceitos/hlc.md §Contrato. */
export const MAX_DRIFT_MS = 300_000; // 5 minutos

/** Relógio Lógico Híbrido. Estado mutável: mantém (pt, c) corrente.
 *  Invariantes documentados em docs/conceitos/hlc.md. */
export class HybridLogicalClock {
  constructor(clock: ClockPort);

  /** Retorna HLC corrente (sem avançar). */
  now(): HLCTimestamp;

  /** Evento local: avança pt para max(clock.now(), pt) e incrementa c (ou zera se pt avançou). */
  tick(): HLCTimestamp;

  /** Evento de recebimento: incorpora HLC remoto. Aplica regras de monotonicidade e drift. */
  receive(remote: HLCTimestamp): HLCTimestamp;

  /** Desempacota HLC em (pt, c). */
  static unpack(hlc: HLCTimestamp): HLCParts;

  /** Empacota (pt, c) em HLCTimestamp. */
  static pack(pt: number, c: number): HLCTimestamp;

  /** Compara dois HLCs. Retorna <0, 0, >0. */
  static compare(a: HLCTimestamp, b: HLCTimestamp): number;
}
```

### Regras de drift (derivado de hlc.md §Contrato)
- Se `pt_remoto > wall_clock_local + MAX_DRIFT_MS` → HLC remoto é rejeitado (nó em quarentena). O relógio local **não** incorpora o valor.
- `tick()`: `pt = max(clock.now(), pt)`; se pt mudou → `c = 0`; senão → `c++`. Se `c` atingir 65535, avance `pt` em 1ms e resete `c = 0`.
- `receive(remote)`: `pt = max(clock.now(), pt, remote_pt)`; `c = (pt === pt_anterior && pt === remote_pt) ? max(c, remote_c) + 1 : (pt === remote_pt ? remote_c + 1 : 0)`. Se `c` atingir 65535, avance `pt` em 1ms e resete `c = 0`.

## 2. Contexto RAG (Spec-Driven Development)
- [hlc.md](../docs/conceitos/hlc.md) — contrato normativo: propriedades, invariantes, drift
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (§4 T-103)

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[READ]** `packages/protocol/src/ports.ts` (ClockPort — T-004)
- **[READ]** `docs/conceitos/hlc.md`
- **[CREATE]** `packages/core/src/hlc.ts` — HybridLogicalClock + tipos
- **[CREATE]** `packages/core/tests/hlc.test.ts`
- **[UPDATE]** `packages/core/src/index.ts` — re-export
- **[UPDATE]** `packages/core/package.json` — dep `@plataforma/protocol: "workspace:*"` (já adicionado em T-102)

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Framework:** Vitest (Node puro).
- [x] **Ambiente do Teste:** `pnpm --filter @plataforma/core test`. Usar `VirtualClock` de T-003 para determinismo.
- [x] **Fora de Escopo:** Validação de transporte (T-201), assinatura (T-107).

Casos de teste (numerados):
1. `tick()` com clock fixo: primeiro tick → pt=clock, c=0; segundo tick → pt igual, c=1.
2. `tick()` após `clock.advance(100)`: pt avança, c zera.
3. `receive(hlc_remoto)` com `hlc_remoto < hlc_local`: HLC local não regride (monotonicidade).
4. `receive(hlc_remoto)` com `hlc_remoto > hlc_local`: pt avança, c=remote_c+1 (evento de recebimento).
5. `receive(hlc_remoto)` com `hlc_remoto.pt` além de `MAX_DRIFT_MS`: rejeitado (erro).
6. `HLC.compare(a, b)` ordenação correta para (pt_a < pt_b), (pt_a == pt_b, c_a < c_b), (iguais).
7. `HLC.pack` + `HLC.unpack` roundtrip.
8. Sequência de 1000 `tick()` com clock avançando → pt monotônico, c cicla 0→65535.
9. Dois peers com mesmo clock: `peerA.tick()` e `peerB.receive(peerA.now())` → `peerA.now() < peerB.now()` (causal).
10. `tick()` após 65536 chamadas sem avanço de pt → c satura e lança erro (overflow de 16 bits).

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** use `Date.now()` diretamente — apenas `ClockPort`.
> - **NÃO** permita regressão do HLC (monotonicidade é invariante).

1. **[TDD]** Crie `packages/core/tests/hlc.test.ts` com casos 1–10 usando `VirtualClock` de T-003.
2. Implemente `HybridLogicalClock` com os métodos `tick()`, `receive()`, `now()`, `pack()`, `unpack()`, `compare()`.
3. Implemente regra de drift: se `remote_pt > clock.now() + MAX_DRIFT_MS` → erro.
4. Re-exporte em `packages/core/src/index.ts`.
5. Rode build + test (Seção 7) e cole saída.

## 6. Feedback de Especificação (Spec Feedback Loop)
*(Nenhuma pendência)*

## 7. Definition of Done (DoD) & Reviewer Checklist

### Verificação automática (Gate de Evidência)
```bash
pnpm --filter @plataforma/core build
pnpm --filter @plataforma/core test
```
> **GATE DE EVIDÊNCIA:** Worker cola saída literal na Seção 8.

### Checklist do Reviewer
- [ ] `HybridLogicalClock` implementa `tick()`, `receive()`, `now()` com assinaturas exatas?
- [ ] Invariante de pai: `HLC(filho) > HLC(pai)` — testado?
- [ ] Drift: pt_remoto > wall_clock + 5min → rejeitado?
- [ ] 10 casos de teste passando?
- [ ] `pnpm --filter @plataforma/core build` e `test` verdes?

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- **Resolução de [B1]**: Refatorado o método `unpack` para desempacotar o timestamp HLC usando diretamente bitwise shift com BigInt (`hlc >> 16n` e `hlc & 0xFFFFn`) antes da conversão para `Number`, prevenindo qualquer perda de precisão ou truncamento acima de `Number.MAX_SAFE_INTEGER`.
- **Resolução de [M1]**: Ajustada a interpolação no construtor de erro da linha 60, aplicando `.toString()` explicitamente a todas as variáveis numéricas do template literal para resolver as violações de `@typescript-eslint/restrict-template-expressions`.
- Validação bem-sucedida com `pnpm lint`, `pnpm build` e `pnpm test` retornando saídas perfeitamente limpas/verdes.

#### Nova Evidência do Rework:
```bash
$ pnpm --filter @plataforma/core lint
$ eslint src/
(silêncio — 0 problemas/warnings)

$ pnpm --filter @plataforma/core build
$ tsc
(silêncio — sem erros)

$ pnpm --filter @plataforma/core test
$ vitest run
 ✓ tests/mock.test.ts (1 test) 3ms
 ✓ tests/hlc.probe.test.ts (1 test) 2ms
 ✓ tests/schema.test.ts (7 tests) 16ms
 ✓ tests/hlc.test.ts (10 tests) 36ms

 Test Files  4 passed (4)
      Tests  19 passed (19)
```
### Parecer do Agente Revisor (Reviewer):
- [x] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória — pós-rework):**
```
$ pnpm --filter @plataforma/core lint
$ eslint src/
(silêncio — 0 erros)

$ pnpm --filter @plataforma/core build
$ tsc
(silêncio — sem erros)

$ pnpm --filter @plataforma/core test
$ vitest run
  ✓ tests/mock.test.ts (1 test) 2ms
  ✓ tests/hlc.probe.test.ts (1 test) 2ms
  ✓ tests/schema.test.ts (7 tests) 13ms
  ✓ tests/hlc.test.ts (10 tests) 34ms

 Test Files  4 passed (4)
      Tests  19 passed (19)
   Duration  625ms
```
- **Comentários de Revisão:**
  - B1 da revisão anterior corrigido: `unpack` usa `hlc >> 16n` e `hlc & 0xFFFFn` em BigInt antes de `Number()`, preservando precisão para timestamps acima de `MAX_SAFE_INTEGER`. Sondas confirmam roundtrip com pt=1.74T ms.
  - M1 da revisão anterior corrigido: template literal usa `.toString()` em todas as variáveis numéricas.
  - Sondas adversariais (3 casos: drift boundary, large timestamp, multiple receives) todas passaram.

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-06-22T00:38]** - *DeepSeek* - `[Iniciado]`: iniciando implementação HLC
- **[2026-06-22T00:42]** - *DeepSeek* - `[Finalizado]`: HLC implementado: tick/receive/now/pack/unpack/compare + drift check (MAX_DRIFT_MS=5min). Fix: unpack usa Math.floor (>> é signed 32-bit). Build ok. Tests: 18/18 (10 HLC + 7 schema + 1 mock).
- **[2026-06-22T00:51]** - *agile_reviewer* - `[Requer Refatoração]`: B1: perda de precisão em unpack para timestamps atuais. M1: erros de lint de typescript-eslint no hlc.ts.
- **[2026-06-22T00:51]** - *Antigravity* - `[Iniciado]`: Iniciando rework de T-103 para corrigir precisão e lint
- **[2026-06-22T00:56]** - *Antigravity* - `[Finalizado]`: Rework concluido: precisao de unpack em bigint resolvida (B1) e lint corrigido (M1). 19/19 testes e eslint ok.
- **[2026-06-22T01:05]** - *agile_reviewer* - `[Aprovado]`: B1 e M1 corrigidos. unpack usa BigInt shifts, lint 0 erros. 19/19 testes passando. Sondas confirmam drift boundary, large timestamp precision, multiple receives.
