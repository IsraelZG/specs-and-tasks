---
id: C-22
title: "Cleanup: plugin-agent-harness (3 nao-bloqueantes)"
status: draft:hardened
complexity: 2
target_agent: deepseek # perfis: devops_agent, logic_agent, crypto_agent, frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential # parallel | sequential
dependencies: [] # IDs de tarefas que bloqueiam esta
blocks: [] # IDs de tarefas que esta bloqueia
capacity_target: haiku
# decisions: ["..."]          ← só quando status: draft:pending_decision (espelha a Seção 6)
---

# C-22 · Cleanup: plugin-agent-harness (3 nao-bloqueantes)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (`pnpm build`, `pnpm test`, `pnpm lint` na raiz afetam todos os pacotes)
- **Test Runner:** `vitest` (pacotes core/protocol) e `playwright` (E2E/Frontend)
- **Capacidade-alvo:** haiku | sonnet | opus-spike *(ver regra "Dimensionamento de Tarefas" no CLAUDE.md: spec sem decisões em aberto, contratos explícitos, sem API externa não-fixada, verificação por comando)*

## 1. Objetivo
Resolver 3 achados não-bloqueantes acumulados em `packages/plugin-agent-harness/`.

## 2. Contexto RAG (Spec-Driven Development)
- [ ] [packages/plugin-agent-harness/src/runner.ts](file:///c:/Dev2026/superapp/packages/plugin-agent-harness/src/runner.ts) — runner de agente
- [ ] [packages/plugin-agent-harness/tests/runner.test.ts](file:///c:/Dev2026/superapp/packages/plugin-agent-harness/tests/runner.test.ts) — testes do runner
- [ ] [packages/plugin-fs-tools/src/index.ts](file:///c:/Dev2026/superapp/packages/plugin-fs-tools/src/index.ts) — define PluginTools e Tool (fonte dos tipos)

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[UPDATE]** [packages/plugin-agent-harness/src/runner.ts](file:///c:/Dev2026/superapp/packages/plugin-agent-harness/src/runner.ts)
  - Importar o tipo [PluginTools](file:///c:/Dev2026/superapp/packages/plugin-fs-tools/src/index.ts#L21-L29) de `@plataforma/plugin-fs-tools`.
  - Atualizar a interface [CancelWatcherOpts](file:///c:/Dev2026/superapp/packages/plugin-agent-harness/src/runner.ts#L12-L19) para tipar a propriedade `tools` como [PluginTools](file:///c:/Dev2026/superapp/packages/plugin-fs-tools/src/index.ts#L21-L29).
  - Atualizar o método [run](file:///c:/Dev2026/superapp/packages/plugin-agent-harness/src/runner.ts#L83) para remover o cast `as unknown as CancelWatcherOpts['tools']` na linha 124 ao instanciar o cancel watcher.
  - Remover a verificação `?.` ao chamar `tools.readFile.execute` na linha 27, utilizando asserção não-nula (`tools.readFile.execute!`) ou condicional (`if (tools.readFile.execute)`) para satisfazer o compilador.
  - Exportar a função [startCancelWatcher](file:///c:/Dev2026/superapp/packages/plugin-agent-harness/src/runner.ts#L21) para viabilizar spying de testes, preferencialmente envelopada em um objeto exportado `_internals` para compatibilidade com o sistema de ESM do Vitest:
    ```typescript
    export const _internals = {
      startCancelWatcher,
    };
    ```
    E atualizar a chamada interna em [run](file:///c:/Dev2026/superapp/packages/plugin-agent-harness/src/runner.ts#L83) para `_internals.startCancelWatcher(...)`.
- **[UPDATE]** [packages/plugin-agent-harness/tests/runner.test.ts](file:///c:/Dev2026/superapp/packages/plugin-agent-harness/tests/runner.test.ts)
  - Reforçar o teste `3` (linha 118) para asserir que `startCancelWatcher` não foi chamado usando `vi.spyOn(_internals, 'startCancelWatcher')`.
  - Reforçar o teste `5` (linha 171) usando `vi.useFakeTimers()` e `vi.advanceTimersByTimeAsync(1000)` para disparar a verificação e asserir com `expect(readFileSpy).toHaveBeenCalled().` Garantir a limpeza dos timers com `vi.useRealTimers()`.

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Framework:** Vitest (configuração contida no pacote `@plataforma/plugin-agent-harness`).
- [x] **Métricas/Cobertura:** Garantia de 100% de sucesso nos testes unitários e de integração existentes. Cobertura explícita de invocação do cancel watcher e sua inibição.
- [x] **Ambiente do Teste:** Node.js puro (in-memory) via Vitest runner.
- [x] **Fora de Escopo:** Browser, testes E2E, e chamadas reais para LLM externo.
- [x] **Casos de Teste (Cenários Verificáveis):**
  1. **Compilação TypeScript Sem Erros:** Garantir que o comando `pnpm --filter @plataforma/plugin-agent-harness build` execute com sucesso e sem erros de tipos ou referências.
  2. **Reforço do Caso de Teste 3 ( runner.test.ts ):** Spyar `_internals.startCancelWatcher` e comprovar que o spy NÃO registrou nenhuma chamada quando `cancelWatcher: false` é passado para [run](file:///c:/Dev2026/superapp/packages/plugin-agent-harness/src/runner.ts#L83).
  3. **Reforço do Caso de Teste 5 ( runner.test.ts ):** Ativar fake timers (`vi.useFakeTimers()`), rodar o runner e avançar o tempo artificialmente (`vi.advanceTimersByTimeAsync(1000)`) para que a chamada agendada aconteça e possamos asserir de fato que `readFileSpy` foi chamado.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - NÃO refatore além dos achados listados (escopo fechado)
> - NÃO adicione dependências novas

> **Regra: Disposição obrigatória por achado.** Cada achado abaixo DEVE ser fechado com um
> destino da taxonomia: `fixed` (ref arquivo/commit), `no-op` (justificativa em 1 linha),
> ou `defer→T-YYY` (task futura nomeada).

1. **[m1][EST-06][plugin-agent-harness]** Test 5 (runner.test.ts L150-176) é no-op. Cria `readFileSpy` mas não assere que foi chamado. Track: usar `vi.useFakeTimers()` + asserir `readFileSpy` chamado.
   - *Ação:* Ajustar o teste "5" em `runner.test.ts` para habilitar `vi.useFakeTimers()`, avançar o tempo e asserir que `readFileSpy` foi chamado.
2. **[m2][EST-06][plugin-agent-harness]** Test 3 (runner.test.ts L97-115) é fraco. Não assere que `startCancelWatcher` NÃO foi chamado quando `cancelWatcher: false`. Track: exportar `startCancelWatcher` para spy.
   - *Ação:* Exportar `startCancelWatcher` de `runner.ts` (preferencialmente envelopado em um objeto `_internals` exportado) e usar `vi.spyOn` no teste "3" para asserir que não foi chamado.
3. **[m3][EST-06][plugin-agent-harness]** `as unknown as` cast em runner.ts:85. Track: tipar `CancelWatcherOpts.tools` como `PluginTools` e remover cast + `?.`.
   - *Ação:* Importar `PluginTools` no `runner.ts`, tipar `CancelWatcherOpts.tools` com ele, remover o cast `as unknown as` na chamada a `startCancelWatcher` e remover a interrogação `?.` de `tools.readFile.execute`.

## 6. Feedback de Especificação (Spec Feedback Loop)
> **ATENÇÃO:** Se a spec (RAG) for ambígua, contraditória ou o design pattern imposto for impossível, **PARE**. Mude o status para `blocked` e escreva o motivo abaixo. Não alucine uma abstração não documentada.
- *[Nenhum problema identificado]*

## 7. Definition of Done (DoD) & Reviewer Checklist
O agente `agile_reviewer` usará esta checklist para aprovar ou rejeitar o PR:
- [ ] O código segue estritamente os arquivos de Output especificados (sem criar arquivos não solicitados)?
- [ ] O `pnpm test` roda sem erros no ambiente especificado (Node/JSDOM)?
- [ ] Linter (`pnpm lint`) não acusa problemas?
- [ ] A implementação respeita a Regra do Que Não Fazer?
- [ ] **[gate de wiring — se a task entrega primitiva de autorização/privacidade]** existe caller de produção em `src/**` que a consome no caminho real, OU há task de integração linkada? (primitiva só testada = feature NÃO entregue)
- [ ] **[gate de acoplamento — se a task adiciona import cruzando pacote]** o import respeita a direção `protocol ← crypto ← core ← transport` (`visao-arquitetural.md §1`) e NÃO fecha ciclo?

### Verificação automática *(comandos exatos — worker E reviewer rodam e COLAM a saída)*
```bash
pnpm --filter @plataforma/plugin-agent-harness build      # tsc — precisa terminar sem erro
pnpm --filter @plataforma/plugin-agent-harness test       # precisa ficar verde, sem regressão
pnpm --filter @plataforma/plugin-agent-harness lint       # ZERO erros novos (rode o baseline ANTES de tocar; regressão de lint bloqueia no review)
```
> **GATE DE EVIDÊNCIA:** nem o `finish` (worker) nem o veredito (reviewer) são válidos sem a
> saída literal desses comandos colada na seção 8. Marcar `[x]` sem evidência é violação.

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
**Disposição por achado (obrigatória):** liste CADA achado da §5 com seu destino:
- `- [id] → fixed (ref) | no-op (justificativa) | defer→T-YYY`
Handover sem disposição per-item é inválido e será rejeitado pelo reviewer.

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória — colar saída de build/tsc + test + lint):**
```
(cole aqui a saída real de pnpm build, pnpm test e pnpm lint)
```
- **Comentários de Revisão:**

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-07-17T19:38]** - *gemini* - `[Triado]`: triado
- **[2026-07-17T19:38]** - *gemini* - `[Endurecido]`: endureceu spec
