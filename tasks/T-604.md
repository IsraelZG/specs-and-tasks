---
id: T-604
title: "Zen Engine Embarcado + Invariante T1"
status: draft
spec_status: blocked-decision
complexity: 3
target_agent: logic_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-601"]
blocks: []
capacity_target: sonnet
decisions:
  - "GoRules Zen dependency package name and version installation"
  - "Isomorphic WASM loading and CSP restriction handling in browser/Worker"
  - "ZenRule JSON expression schema definition"
hardened_at: "2026-06-29T20:05:00-03:00"
---

# T-604 · Zen Engine Embarcado + Invariante T1

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar `ZenEngine` embarcado no pacote `@plataforma/core`: motor de regras (GoRules Zen WASM) para validação de invariantes transacionais — ex: saldo(t+1) = saldo(t) + delta, nunca negativo sem colateral
*(derivado de `docs/plano-de-implementacao.md` §4 T-604; T-601 — resolução de fork usa Zen para validar merge)*.

### Contratos
```ts
// --- packages/core/src/rules/ZenEngine.ts ---

export interface ZenRule {
  id: string;
  expression: string; // expressão Zen (JSON logic)
  onFailure: 'reject' | 'warn';
}

export interface ZenContext {
  facts: Record<string, unknown>;   // estado anterior
  changes: Record<string, unknown>;  // delta proposto
  result: Record<string, unknown>;   // estado resultante
}

export class ZenEngine {
  constructor();

  /** Carrega regras. */
  loadRules(rules: ZenRule[]): void;

  /** Avalia contexto contra regras. Retorna true se todas passam. */
  evaluate(ctx: ZenContext): Promise<{ passed: boolean; failures: ZenRule[] }>;

  /** Invariante T1: saldo nunca fica negativo (anterior + delta >= 0). */
  static readonly INVARIANT_T1: ZenRule;
}
```
- `evaluate`: roda todas as regras carregadas contra `ctx`; se `onFailure='reject'`, retorna `passed=false`.
- Invariante T1: `result.balance >= 0` — rejeita transação que tornaria saldo negativo.
- Isomórfico: engine roda em Worker (não na thread principal), mas o contrato da classe é síncrono.

## 2. Contexto RAG
- [T-601 · Fork/Merge](./T-601.md)
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (§4 T-604)

## 3. Escopo de Arquivos
- **[CREATE]** `packages/core/src/rules/ZenEngine.ts`
- **[CREATE]** `packages/core/tests/zen.test.ts`
- **[UPDATE]** `packages/core/src/index.ts`

## 4. Estratégia de Testes
- [x] **Framework:** Vitest (Node puro).

Casos de teste:
1. `evaluate` com saldo suficiente → `passed: true`.
2. `evaluate` com saldo negativo → `passed: false`, `failures` contém T1.
3. Regra customizada: `changes.amount <= facts.limit` → rejeita se ultrapassa.
4. Múltiplas regras: uma falha, outras passam → `passed: false`, só a falha listada.

## 5. Instruções de Execução
1. Escreva testes com `ZenContext` mockados.
2. Implemente `ZenEngine` com evaluator de expressões.
3. Defina `INVARIANT_T1`.
4. Re-exporte. Rode build + test.

## 6. Feedback de Especificação

> [!WARNING]
> **Decisões abertas para o Arquiteto:**
>
> 1. **Nome e Versão da Dependência do GoRules**:
>    - Qual pacote npm do GoRules Zen deve ser instalado no monorepo (ex. `@gorules/zen` ou `zen-rules`) e qual a versão exata autorizada?
>
> 2. **Carregamento de WASM em Ambientes Isomórficos (Worker / Browser)**:
>    - O motor GoRules roda compiled WASM. O carregamento assíncrono de arquivos `.wasm` no browser e dentro de Workers pode violar políticas de CSP (Content Security Policy) dos clientes. Qual a estratégia de empacotamento recomendada (inlining do WASM em base64 no bundle, ou fetch dinâmico com URL configurável)?
>
> 3. **Schema das Expressões `ZenRule`**:
>    - A propriedade `expression` é descrita como "JSON logic" (expressão Zen). GoRules Zen possui seu próprio formato e interpretador de decisões estruturadas (Zen Expression). É necessário documentar e testar o parser do formato exato para evitar falhas de runtime na avaliação de regras dinâmicas.

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/core build
pnpm --filter @plataforma/core test
```

### Checklist
- [ ] `evaluate` valida múltiplas regras?
- [ ] Invariante T1: saldo negativo → rejeitado?
- [ ] 4 casos de teste passando?
- [ ] `pnpm --filter @plataforma/core build` e `test` verdes?

## 8. Log de Handover e Revisão Agile
### Handover do Executor:
- 

### Parecer do Agente Revisor:
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução:**
```
(cole aqui a saída real de pnpm build e pnpm test)
```

## 9. Log de Execução
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
