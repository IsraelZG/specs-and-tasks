---
id: C-10
title: "Cleanup: core (blindArchives/T-1036/T-1042/T-1045)"
status: draft:triaged
complexity: 2
target_agent: logic_agent # perfis: devops_agent, logic_agent, crypto_agent, frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential # parallel | sequential
dependencies: [] # IDs de tarefas que bloqueiam esta
blocks: [] # IDs de tarefas que esta bloqueia
capacity_target: haiku
# decisions: ["..."]          ← só quando status: draft:pending_decision (espelha a Seção 6)
---

# C-10 · Cleanup: core (blindArchives/T-1036/T-1042/T-1045)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (`pnpm build`, `pnpm test`, `pnpm lint` na raiz afetam todos os pacotes)
- **Test Runner:** `vitest` (pacotes core/protocol) e `playwright` (E2E/Frontend)
- **Capacidade-alvo:** haiku | sonnet | opus-spike *(ver regra "Dimensionamento de Tarefas" no CLAUDE.md: spec sem decisões em aberto, contratos explícitos, sem API externa não-fixada, verificação por comando)*

## 1. Objetivo
Resolver ~15 achados não-bloqueantes acumulados em `@plataforma/core`: blindArchives (índices, limpeza, tipos, testes), keyVault (runtime guards), SqliteStorage (transactions, lint), ProjectionManager (testes, callers), timestamps ms.

## 2. Contexto RAG
- [x] `tasks/T-313c.md` — blindArchives spec
- [x] `tasks/T-1036.md` — keyVault spec
- [x] `tasks/T-1042.md` — SqliteStorage spec
- [x] `tasks/T-1045.md` — ProjectionManager spec
- [x] `tasks/T-106.md` — timestamps ms migration
- [x] `tasks/_pendencias.md` — itens originais

## 3. Escopo de Arquivos
- **[UPDATE]** `packages/core/src/archive/blindArchives.ts` — índices, tipos, `includeExpired`, consistency
- **[UPDATE]** `packages/core/src/vault/keyVault.ts` — runtime guards + try/catch
- **[UPDATE]** `packages/core/src/storage/sqliteStorage.ts` — transaction, eslint directives
- **[UPDATE]** `packages/core/src/schema.ts` — timestamps ms
- **[UPDATE]** `packages/core/src/projection.ts` — caller integration
- **[CREATE]** `packages/core/tests/boundary-blindArchives.test.ts` — boundary tests
- **[UPDATE]** `.gitignore` — `*.probe.test.ts`

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [ ] **Framework:** (Vitest para Node puro / Playwright para E2E / React Testing Library em JSDOM)
- [ ] **Métricas/Cobertura:** (Ex: Testar todos os ramos de erro, testar a assinatura inválida)
- [ ] **Ambiente do Teste:** (Node puro, sem browser / Headless browser)
- [ ] **Fora de Escopo:** (O que NÃO precisa ser testado)
> **Se a task afeta UI** (frontend_agent ou escopo em `apps/*-frontend/**`): unit/JSDOM NÃO
> basta. Especifique aqui um **smoke Playwright** (browser real) OU exija a verificação manual
> do revisor (subir o app e exercitar o fluxo) — o `agile-reviewer` trata isso como BLOCKER de
> processo. Marque `ui: true` no frontmatter para deixar explícito.

## 5. Lista de Achados
> **Regra: Disposição obrigatória por achado.** Cada achado abaixo DEVE ser fechado com um destino da taxonomia: `fixed` (ref arquivo/commit), `no-op` (justificativa), ou `defer→T-YYY`.

1. `[m1][T-313c][core]` blindArchives índice `expires_at` + `blind_scope_id` — blindArchives.ts:16-25
2. `[i1][T-313c][core]` `now?` param não declarado na spec — blindArchives.ts:42,64,82
3. `[i2][T-313c][core]` `rowToBlindArchive` cast `ArrayBuffer` frágil — blindArchives.ts:29-32
4. `[i3][T-313c][core]` `includeExpired=false` default não filtra — blindArchives.ts:60-90
5. `[i4][T-313c][core]` `includeExpired` faltando em `listBlindArchivesByScope` — blindArchives.ts:78-90
6. `[i6][T-313c][core]` `expiresAt` inconsistente entre manifest e coluna — blindArchives.ts:38-58
7. `[i7][T-313c][core]` ORDER BY não-determinístico (1s resolução) — blindArchives.ts:85
8. `[i8][T-313c][core]` Gap cobertura boundary `== now` — blindArchives.test.ts:96-190
9. `[i9][T-313c][core]` `*.probe.test.ts` ao .gitignore
10. `[m1][T-1036][core]` `requestEpochKey` runtime guard payload sanity — keyVault.ts:74-83
11. `[m2][T-1036][core]` `requestEpochKey` sem try/catch — keyVault.ts:85-99
12. `[m1][T-1042][core]` `GraphStoreTx` contrato expandido — ports.ts:78-83
13. `[m2][T-1042][core]` `SqliteStorage.transaction` `Function.length` frágil — sqliteStorage.ts:113
14. `[i1][T-1042][core]` eslint-disable directives obsoletas — sqliteStorage.ts:196-202
15. `[m1][T-1045][core]` triggers vs código — schema.ts:42-47
16. `[m2][T-1045][core]` ProjectionManager sem caller — projection.ts:20
17. `[i2][T-1045][core]` Sem teste SqliteStorage real — projection.test.ts
18. `[M1][T-106][core/schema]` timestamps ms em `nodes.created_at`/`edges.created_at` — schema.ts:13,25
19. `[m2][T-022][monorepo]` turbo build cycle `protocol↔core↔testkit`

**Para cada achado, registrar disposição no §8 Handover.**

## 6. Feedback de Especificação (Spec Feedback Loop)
> **ATENÇÃO:** Se a spec (RAG) for ambígua, contraditória ou o design pattern imposto for impossível, **PARE**. Mude o status para `blocked` e escreva o motivo abaixo. Não alucine uma abstração não documentada.
- *[Nenhum problema identificado]*

## 7. Definition of Done (DoD)
- [ ] Índices `idx_blind_archives_expires_at` e `idx_blind_archives_scope_created` criados?
- [ ] `requestEpochKey` com guard de payload + try/catch → `denied`?
- [ ] `SqliteStorage.transaction` refatorado para métodos explícitos?
- [ ] timestamps ms consistentes em todas as tabelas?
- [ ] 19 achados dispostos (fixed / no-op / defer)?

### Verificação automática
```bash
pnpm --filter @plataforma/core build
pnpm --filter @plataforma/core test
pnpm --filter @plataforma/core lint
```

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- 

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória — colar saída de build/tsc + test + lint):**
```
(cole aqui a saída real de pnpm build, pnpm test e pnpm lint)
```
- **Comentários de Revisão:**

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-07-06T13:58]** - *deepseek* - `[Triado]`: triado — cleanup core, capacity=haiku
