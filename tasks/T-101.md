---
id: T-101
title: "Wrappers cripto"
status: done
complexity: 3
parent_task: null
subtasks: []
dependencies: ["T-001"]
blocks: []
target_agent: crypto_agent
reviewer_agent: "agile_reviewer"
execution_mode: sequential
capacity_target: sonnet
---

# T-101 · Wrappers cripto

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (pacotes core)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Criar wrappers isomórficos (Node/Browser) para as primitivas criptográficas do pacote `@plataforma/crypto`
*(derivado de `docs/plano-de-implementacao.md` §1.1 — crypto: "Ed25519, AES-256-GCM, HKDF, BIP39, PBKDF2, SSS, blake2s256 — wrappers sobre WebCrypto/noble"; `docs/visao-arquitetural.md` §1 camada 2 "Cripto: assinaturas Ed25519, cifra AES-256-GCM")*.

### Contratos exatos (assinaturas TS fixadas)

```ts
// --- packages/crypto/src/wrappers.ts 
---

// === Ed25519 (assinatura/verificação) ===

/** Chave privada Ed25519 — 32 bytes. Derivado de `docs/visao-arquitetural.md` §1 camada 2. */
export type Ed25519PrivateKey = Uint8Array;
/** Chave pública Ed25519 — 32 bytes. */
export type Ed25519PublicKey = Uint8Array;
/** Assinatura Ed25519 — 64 bytes. */
export type Ed25519Signature = Uint8Array;

/** Gera par de chaves Ed25519. Backend: @noble/curves ou WebCrypto conforme ambiente. */
export function ed25519GenerateKeyPair(): Promise<{ publicKey: Ed25519PublicKey; privateKey: Ed25519PrivateKey }>;

/** Assina `message` com chave privada. */
export function ed25519Sign(privateKey: Ed25519PrivateKey, message: Uint8Array): Promise<Ed25519Signature>;

/** Verifica assinatura. */
export function ed25519Verify(publicKey: Ed25519PublicKey, message: Uint8Array, signature: Ed25519Signature): Promise<boolean>;

// === AES-256-GCM (cifra simétrica autenticada) ===

/** Chave AES-256 — 32 bytes. */
export type AesKey = Uint8Array;
/** Nonce/IV AES-GCM — 12 bytes (recomendado NIST). */
export type AesNonce = Uint8Array;
/** Resultado de cifra AES-GCM: { ciphertext, authTag }. */
export interface AesGcmResult { ciphertext: Uint8Array; authTag: Uint8Array; }

/** Cifra `plaintext` com AES-256-GCM. Gera nonce internamente (12 bytes) e o retorna. */
export function aesGcmEncrypt(key: AesKey, plaintext: Uint8Array): Promise<{ nonce: AesNonce; ciphertext: Uint8Array; authTag: Uint8Array }>;

/** Decifra `ciphertext` com AES-256-GCM. Lança se authTag não confere. */
export function aesGcmDecrypt(key: AesKey, nonce: AesNonce, ciphertext: Uint8Array, authTag: Uint8Array): Promise<Uint8Array>;

// === SHA-256 ===

/** Digest SHA-256 — 32 bytes. */
export type Sha256Digest = Uint8Array;

export function sha256(data: Uint8Array): Promise<Sha256Digest>;

// === Blake2s-256 ===

export function blake2s256(data: Uint8Array): Promise<Uint8Array>;

// === HKDF (SHA-256) ===

export function hkdfSha256(ikm: Uint8Array, salt: Uint8Array, info: Uint8Array, length: number): Promise<Uint8Array>;
```

## 2. Contexto RAG (Spec-Driven Development)
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (§1.1 árvore crypto, §0.1 item 5 isomorfismo)
- [visao-arquitetural.md](../docs/visao-arquitetural.md) (§1 camada 2 Cripto)

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[READ]** `docs/plano-de-implementacao.md` §1.1
- **[CREATE]** `packages/crypto/src/wrappers.ts` — funções Ed25519 + AES-GCM + SHA-256 + blake2s256 + HKDF acima
- **[CREATE]** `packages/crypto/tests/wrappers.test.ts` — vetores conhecidos (RFC 8032 para Ed25519, NIST CAVP para AES-GCM)
- **[UPDATE]** `packages/crypto/src/index.ts` — re-exportar wrappers
- **[UPDATE]** `packages/crypto/package.json` — adicionar deps `@noble/curves`, `@noble/hashes`

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Framework:** Vitest (Node puro).
- [x] **Ambiente do Teste:** Node puro, `pnpm --filter @plataforma/crypto test`.
- [x] **Fora de Escopo:** Testes em browser real (testados no CI via playwright em tarefas posteriores).

Casos de teste (numerados):
1. `ed25519GenerateKeyPair` gera chaves de 32 bytes; `publicKey !== privateKey`.
2. `ed25519Sign` + `ed25519Verify` roundtrip com vetor RFC 8032 §7.1 (TEST 2).
3. `ed25519Verify` retorna `false` com assinatura corrompida (1 bit trocado).
4. `ed25519Verify` retorna `false` com chave pública errada.
5. `aesGcmEncrypt` + `aesGcmDecrypt` roundtrip com plaintext variável (vazio, 1 byte, 1 KB, 1 MB).
6. `aesGcmDecrypt` lança com authTag corrompido.
7. `aesGcmDecrypt` lança com ciphertext corrompido.
8. `sha256` sobre `Uint8Array` vazio retorna digest RFC 6234 (e3b0c442...).
9. `blake2s256` sobre vetor RFC 7693.
10. `hkdfSha256` roundtrip com vetor RFC 5869 §A.1 (Test Case 1).

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** escreva primitivas criptográficas do zero. Use `@noble/curves`, `@noble/hashes` e `WebCrypto`.
> - **NÃO** dependa de libs exclusivas do Node.js (`crypto` nativo) sem fallback para browser. A camada deve ser 100% isomórfica.
> - **NÃO** implemente PBKDF2, BIP39 ou SSS nesta task — pertencem ao domínio da T-104 (Derivação de Chaves).

1. **[TDD]** Escreva `packages/crypto/tests/wrappers.test.ts` com os 10 casos da Seção 4 (usando `describe`/`it` Vitest). Testes devem falhar inicialmente (funções inexistentes).
2. Adicione `@noble/curves` e `@noble/hashes` em `packages/crypto/package.json` como dependencies.
3. Implemente `packages/crypto/src/wrappers.ts` com as assinaturas exatas da Seção 1.
   - Use `@noble/curves/ed25519` para Ed25519.
   - Use `@noble/hashes/sha256`, `@noble/hashes/blake2s`, `@noble/hashes/hkdf` para hashes.
   - Use WebCrypto (`crypto.subtle`) para AES-256-GCM (isomórfico: browser e Node ≥20).
4. Re-exporte tudo em `packages/crypto/src/index.ts`.
5. Rode build + test (Seção 7) e cole saída.

## 6. Feedback de Especificação (Spec Feedback Loop)
*(Nenhuma pendência)*

## 7. Definition of Done (DoD) & Reviewer Checklist

### Verificação automática (Gate de Evidência)
```bash
pnpm --filter @plataforma/crypto build
pnpm --filter @plataforma/crypto test
```
> **GATE DE EVIDÊNCIA:** Worker cola a saída literal na Seção 8.

### Checklist do Reviewer
- [ ] 5 primitivas exportadas com assinaturas exatas da Seção 1?
- [ ] 10 casos de teste passando com vetores RFC/NIST conhecidos?
- [ ] Código isomórfico (sem `import 'crypto'` do Node; WebCrypto + noble apenas)?
- [ ] `pnpm --filter @plataforma/crypto build` e `test` verdes?

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- **REWORK #2:** Corrigido [B2] — 6 funções sync restauradas para `Promise<T>` via `Promise.resolve()`, mantendo 0 erros de lint.
- **REWORK #1:** Corrigido [B1] — validação `key.length !== 32` em `aesGcmEncrypt`/`aesGcmDecrypt`. Corrigido [M1] — 14 erros de lint.
- Implementadas 5 primitivas em `packages/crypto/src/wrappers.ts` isomórficas.
- 10 casos de teste com vetores RFC 8032, RFC 5869, RFC 6234, RFC 7693.

### Parecer do Agente Revisor (Reviewer):
- [x] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória — após correção do B2):**
```bash
# pnpm --filter @plataforma/crypto lint
$ eslint src/
# (silêncio — 0 erros)

# pnpm --filter @plataforma/crypto build
$ tsc
# (silêncio — sem erros)

# pnpm --filter @plataforma/crypto test
$ vitest run
  ✓ tests/mock.test.ts (1 test) 2ms
  ✓ tests/wrappers.test.ts (10 tests) 2190ms
    ✓ AES-256-GCM > 5. encrypt + decrypt roundtrip com plaintexts variáveis  2114ms

 Test Files  2 passed (2)
      Tests  11 passed (11)
   Duration  2.72s
```
- **Comentários de Revisão:**
  - **[INFO i1]** A correção do blocker **`[B2]`** foi verificada com sucesso. Todas as 6 primitivas criptográficas retornam agora instâncias válidas de `Promise` e não valores síncronos diretamente. As sondas adversariais confirmaram o comportamento assíncrono real.
  - **[INFO i2]** A validação do tamanho da chave no AES-GCM (blocker **`[B1]`**) continua ativa e as chaves com tamanho incorreto são rejeitadas corretamente via rejeição de Promise.
  - **[INFO i3]** A análise estática de lint continua 100% limpa (blocker **`[M1]`** resolvido).

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-06-21T17:01]** - *DeepSeek* - `[Iniciado]`: iniciando implementação das 5 primitivas criptográficas (Ed25519, AES-256-GCM, SHA-256, blake2s256, HKDF)
- **[2026-06-21T17:10]** - *DeepSeek* - `[Finalizado]`: 5 primitivas criptográficas implementadas. Build+test verdes: 2 test files, 11/11 passed. Ed25519 + AES-256-GCM + SHA-256 + blake2s256 + HKDF com vetores RFC.
- **[2026-06-21T17:19]** - *agile_reviewer* - `[Requer Refatoração]`: aesGcmEncrypt aceita chaves com tamanho incorreto permitindo cifras AES-128/192 em vez de AES-256-GCM. Falha também no lint com 14 erros.
- **[2026-06-21T17:21]** - *DeepSeek* - `[Iniciado]`: retomando para corrigir [B1] validação de chave AES + [M1] 14 erros de lint
- **[2026-06-21T17:24]** - *DeepSeek* - `[Finalizado]`: REWORK: B1 corrigido (validação key.length===32 no AES-GCM). M1 corrigido (0 erros lint: imports sha2/blake2, randomSecretKey, sem async desnecessário, sem type assertions). Lint+build+test verdes: 11/11 passed.
- **[2026-06-21T17:37]** - *agile_reviewer* - `[Requer Refatoração]`: [B2] Desvio de contrato de API (retornos sincronos em vez de Promise)
- **[2026-06-21T17:44]** - *DeepSeek* - `[Iniciado]`: retomando para corrigir [B2] — restabelecer Promise nos retornos via Promise.resolve()
- **[2026-06-21T17:45]** - *DeepSeek* - `[Finalizado]`: REWORK #2: B2 corrigido — 6 funções restauradas para Promise<T> via Promise.resolve(). Lint 0 erros, build+test verdes: 11/11 passed.
- **[2026-06-21T17:51]** - *agile_reviewer* - `[Aprovado]`: Wrappers cripto aprovados. Retorno assincrono real restabelecido e validado com sucesso. Build, lint e testes OK.
