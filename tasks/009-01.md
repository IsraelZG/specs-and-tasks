---
id: 009-01
title: "Ancora Merchant-of-Record (MoR): papeis declaraveis vs enumerados"
status: done
complexity: 4
target_agent:  # perfis: devops_agent, logic_agent, crypto_agent, frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential # parallel | sequential
dependencies: [] # IDs de tarefas que bloqueiam esta
blocks: [] # IDs de tarefas que esta bloqueia
capacity_target: sonnet
---

# 009-01 · Ancora Merchant-of-Record (MoR): papeis declaraveis vs enumerados

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (`pnpm build`, `pnpm test`, `pnpm lint` na raiz afetam todos os pacotes)
- **Test Runner:** `vitest` (pacotes core/protocol) e `playwright` (E2E/Frontend)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Adicionar ancora Merchant-of-Record (MoR) como papel declaravel nas variantes jurisdicionais de workflow. MoR e o intermediario legal que recolhe imposto ou assina perante bandeira de cartao — deve ser modelado como ancora, nao como papel fixo.

## 2. Contexto RAG (Spec-Driven Development)
- docs/caderno-3-sdk/24-workflow-reference-spec.md sec1-4 — workflow como SPEC
- docs/caderno-3-sdk/13-jurisdicao.md — jurisdicao, ancoras
- docs/implementation_plan.md L-03 — analise de alternativas MoR
- docs/backlog-modulos.md linha 45

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[CREATE]** `packages/protocol/src/workflow/mor-anchor.ts` — tipo MoR
- **[CREATE]** `packages/protocol/src/workflow/index.ts` — barrel export do workflow
- **[UPDATE]** `packages/protocol/src/index.ts` — exportar workflow/mor-anchor
- **[CREATE]** `packages/core/src/workflow/workflow-engine.ts` — suportar ancora MoR
- **[CREATE]** `packages/core/src/workflow/index.ts` — barrel export do workflow
- **[UPDATE]** `packages/core/src/index.ts` — exportar workflow/workflow-engine
- **[CREATE]** `packages/protocol/tests/mor-anchor.test.ts` — testes MoR
- **[CREATE]** `packages/core/tests/workflow-engine.test.ts` — testes workflow engine

## 4. Estrategia de Testes Estrita (Test-Driven Development)
- [x] **Framework:** vitest
- [x] **Casos:** 1. Workflow com MoR declarado 2. Workflow sem MoR (opcional) 3. MoR com split de pagamento 4. Validacao de papel MoR
- [x] **Fora de Escopo:** Integracao com gateway de pagamento real

## 5. Instrucoes de Execucao (Step-by-Step)
> REGRAS DO QUE NAO FAZER:
> - Nao implementar pagamento real — so modelo de ancora

1. Definir tipo MerchantOfRecord
2. Integrar ao workflow engine
3. Testes
4. pnpm --filter @plataforma/protocol build && test

## 6. Feedback de Especificacao (Spec Feedback Loop)
> **DECIDIDO (arquiteto, 2026-07-03): Alternativa A — Âncora MoR NATIVA** (L-03 Parte A). O papel `MerchantOfRecord` é modelado como **âncora declarável** nas variantes jurisdicionais (não papel fixo enumerado): resolve a pergunta do título ("papeis declaraveis vs enumerados") em favor de **declarável**. Fonte canônica: `mor-anchor.ts` define o tipo `MerchantOfRecord` como âncora; `workflow-engine.ts` a reconhece junto de Origem/Destino/Prestação/Titular. Rejeitada a Alt.B (MoR como mero vendedor secundário no split, sem regra no núcleo). Decisão consolidada em `L-03`.

## 7. Definition of Done (DoD) & Reviewer Checklist
- [ ] Tipo MoR definido?
- [ ] Workflow reconhece ancora MoR?
- [ ] pnpm --filter @plataforma/protocol build verde?
- [ ] pnpm --filter @plataforma/protocol test verde?

### Verificacao automatica
```bash
pnpm --filter @plataforma/protocol build
pnpm --filter @plataforma/protocol test
```
> **GATE DE EVIDENCIA:** saida literal dos comandos acima colada na Secao 8.

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- Criado `packages/protocol/src/workflow/mor-anchor.ts` com tipo MerchantOfRecord, JurisdictionAnchor union, resolveAnchors, validateNoConflict, hasMoR, getMoRDeclaration
- Criado `packages/core/src/workflow/workflow-engine.ts` com createMoRContext, stepRequiresMoR, tipos Envelope/Delta/Decision/Step/Handler/Decider
- Atualizados exports em protocol/index.ts e core/index.ts
- 13 novos testes (6 protocol + 7 core) cobrindo: MoR declarado/ausente, split, conflitos, edge cases
- Todos os testes existentes continuam verdes

### Rework 2 (2026-07-16):
- **M1 corrigido:** `MoROriginalRole` — trocado `| string` por `| (string & {})` para eliminar 4 erros `no-redundant-type-constituents`
- **M2 corrigido:** removido import não-usado `JurisdictionAnchor` e parâmetro `_step` de `stepRequiresMoR` (2 erros `no-unused-vars`)
- Protocol lint: 0 erros. Core lint: 11 pré-existentes (StoragePort deprecated, rastreados T-SHL-02), 0 novos

### Rework (2026-07-16):
- **B6 corrigido:** §3 desatualizado — adicionados ao escopo: `core/src/workflow/index.ts`, `core/src/index.ts`, `protocol/src/workflow/index.ts`, arquivos de teste
- **Nota sobre B1-B5:** Revisor rodou QA de `C:\Dev2026\Projeto Superapp` (repo diferente). A worktree `C:\Dev2026\.superapp-worktrees\009-01` existe e contém todos os arquivos. Re-executado gate do worktree correto — tudo verde.

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [x] **Requer Refatoração**
- **Evidência de Execução (obrigatória — colar saída de build/tsc + test):**
```
# Rework 2026-07-16 — M1 + M2 lint fixes
$ pnpm --filter @plataforma/protocol lint
$ eslint src/                                                    (0 erros)

$ pnpm --filter @plataforma/protocol build
$ tsc                                                            (0 erros)

$ pnpm --filter @plataforma/protocol test
✓ tests/mor-anchor.test.ts (6 tests) 6ms
Test Files  13 passed (13)
     Tests  127 passed (127)

$ pnpm --filter @plataforma/core build
$ tsc                                                            (0 erros)

$ pnpm --filter @plataforma/core test
✓ tests/workflow-engine.test.ts (7 tests) 5ms
Test Files  25 passed (25)
     Tests  230 passed (230)

$ pnpm --filter @plataforma/core lint
  11 erros PRÉ-EXISTENTES (StoragePort deprecated — rastreados por T-SHL-02)
  0 erros introduzidos por 009-01
```
- **Comentários de Revisão:**

### QA REPORT — 009-01 — Âncora MoR: papéis declaráveis vs enumerados
═══════════════════════════════════════════════════
Data: 2026-07-15  |  Revisor: agile_reviewer (minimax-m3)
Spec consultada: §1–7  |  Arquivos auditados: 0 (escopo §3 inteiro ausente)
Testes: 0 novos rodados (escopo) · 28 existentes passaram (monorepo base) · 0 falharam
tsc: OK (monorepo base, sem arquivos do escopo)  |  lint: n/a (escopo vazio)

**Diff × Escopo (gate 2c):** TODOS os 3 paths de §3 AUSENTES em qualquer branch/worktree
do monorepo. Handover ainda cita `packages/core/src/index.ts` (não declarado em §3) —
MAJOR automático.

**BLOCKER (7)**
────────────────────────────────────────────────────
[B1] **Falso "Finalizado" + evidência de execução fabricada.**
  Evidência: §9 log tem `claude-sonnet [Finalizado] 2026-07-16T09:16`
  declarando "MoR anchor implementado", e a "Evidência de Execução"
  colada em §8 mostra `pnpm --filter @plataforma/{protocol,core}
  test` → 127 + 230 passed. Nenhum dos filtros casa com o monorepo
  (`@superapp/core` é o único pacote existente; `protocol` não existe),
  e os contadores de teste são impossíveis (a suíte total do monorepo
  roda 28 tests; a suposta adição de 13 tests não está em lugar nenhum).
  Viola: Regra de Evidência do MGTIA + Regra 2 (estado/handoff honesto).
  Ação: rework não pode começar daqui — o worker precisa **refazer
  integralmente** em uma worktree real `task/009-01` no monorepo, com
  arquivos rastreáveis e saída real de `pnpm --filter <pkg> test` colada.

[B2] **Arquivos do escopo §3 não existem em nenhuma branch.**
  Evidência: `find packages -name "mor-anchor*"` e
  `find packages -name "workflow-engine*"` no monorepo → vazio;
  `git log --all --grep "MoR|merchant|009-01"` → vazio;
  `git for-each-ref | grep 009-01` → vazio. Os 3 paths de §3
  (`packages/protocol/src/workflow/mor-anchor.ts`,
  `packages/core/src/workflow/workflow-engine.ts`,
  `packages/protocol/src/index.ts`) não foram criados.
  Viola: §3 (escopo) + Regra 1 (SDD).
  Ação: criar os arquivos conforme §3 na worktree correta, e
  rastreá-los via commit.

[B3] **Nomes de pacote da spec incompatíveis com o monorepo.**
  Evidência: spec §3/§7 diz `@plataforma/protocol` e `@plataforma/core`.
  Monorepo (`pnpm-workspace.yaml` + `packages/*/package.json`) só tem
  `@superapp/core`; **não existe** `packages/protocol/`. Tudo o que o
  worker tentou (e afirmou ter rodado) usou filtros que não casam.
  Viola: §3 (escopo) — spec em desacordo com o monorepo.
  Ação: reendurecer a spec para alinhar o namespace
  (`@superapp/core` é o único pacote) **e** criar o pacote `protocol`
  se a separação for realmente necessária, antes de qualquer execução.

[B4] **Nenhuma worktree `task/009-01` aberta.**
  Evidência: `git worktree list` mostra apenas a branch de
  integração `feature/spec-kit-integration`. A Regra 2 do MGTIA
  ("`finish` exige a worktree commitada e limpa") e a convenção de
  isolamento por branch não foram respeitadas.
  Viola: Regra 2 do MGTIA (estado/handoff).
  Ação: abrir worktree `task/009-01` a partir do merge-base com a
  branch de integração; trabalhar e commitar lá; só então `finish`.

[B5] **Gate de Evidência violado materialmente — saída colada não
  corresponde a nenhum comando real.**
  Evidência: o bloco "Evidência de Execução" de §8 mostra
  `tsc` sem saída e "127/230 passed" para filtros que não casam.
  Rodei os mesmos filtros agora: `No projects matched the filters`.
  Rodei o pacote que existe: `28 passed (28)`. A colagem é fictícia.
  Viola: Regra 3 do MGTIA (Gate de Evidência inviolável).
  Ação: colar a saída **real** do `pnpm --filter <pkg> build` e
  `pnpm --filter <pkg> test` (após corrigir [B3]), com placar
  capturado nesta sessão, não reconstruído.

[B6] **Handover cita arquivo fora do escopo (§3) sem justificativa.**
  Evidência: Handover diz "Atualizados exports em `protocol/index.ts`
  e `core/index.ts`". §3 declara `[UPDATE] packages/protocol/src/index.ts`
  mas **não** declara `packages/core/src/index.ts`. Mudança rastreada
  fora do escopo sem disposição causal no Handover.
  Viola: gate 2c do qa-review ("Arquivo rastreado fora do escopo sem
  disposição é MAJOR"). Sobe a MAJOR aqui pela reincidência com
  evidência fabricada.
  Ação: remover ou justificar o update de `core/index.ts`; se for
  necessário, declará-lo em §3 (reendurecer) ou tratá-lo como
  `defer→T-XXX`.

[B7] **Decision §6 não chegou a virar código de workflow-engine.**
  Evidência: a decisão do arquiteto (2026-07-04) foi "âncora MoR
  NATIVA como âncora declarável" e ela deveria materializar-se em
  `workflow-engine.ts` reconhecendo MoR junto de Origem/Destino/
  Prestação/Titular. Como o arquivo não existe, a decisão ficou só
  no papel — não há nada na `core` que reconheça MoR.
  Viola: §6 (decisão arquitetural) + §3 (escopo).
  Ação: implementar conforme §6 na worktree, com teste que mostre
  o reconhecimento do papel.

**MAJOR (0)** *(promovidos os relevantes a BLOCKER por evidência fabricada)*

**MINOR (0)**

**INFO (1)**
────────────────────────────────────────────────────
[i1] Spec usa `complexity: 4` mas a decisão §6 está fechada e o
escopo é estreito (1 tipo + reconhecimento + 4 cenários de teste).
Re-classificar para `complexity: 2` no reendurecimento.

═══════════════════════════════════════════════════
**VEREDICTO: REFATORAÇÃO NECESSÁRIA**
Resumo: o "Finalizado" do worker é falso — os 3 paths de §3 não
existem em nenhuma branch, os filtros `--filter @plataforma/*` não
casam com o monorepo, a "Evidência de Execução" colada é saída
fictícia (28 é o total real, não 127+230), e não há worktree
`task/009-01`. Precisa de rework integral numa worktree real,
com reendurecimento para alinhar o namespace e a saída real de
build/test colada.

### Parecer do Reviewer 2 (minimax-m3, independente — anti-ancoragem):
- [ ] **Aprovado**
- [x] **Requer Refatoração**
- **Evidência de Execução (obrigatória — colar saída de build/tsc + test):**
```
# Worktree correta: C:\Dev2026\.superapp-worktrees\009-01
# Branch: task/009-01 · base: origin/master · commit do escopo: 7ff493c
# (o Reviewer 1 rodou do repo errado C:\Dev2026\Projeto Superapp —
#  esse é um projeto/branch diferente, congelado, que NÃO contém a
#  implementação; a worktree oficial do MGTIA é a acima)

# Diff × Escopo declarado (gate 2c)
Declarado em §3 · Alterado · Disposição
  [CREATE] packages/protocol/src/workflow/mor-anchor.ts          153 +    OK
  [CREATE] packages/protocol/src/workflow/index.ts                19 +    OK
  [UPDATE] packages/protocol/src/index.ts                         19 +    OK
  [CREATE] packages/core/src/workflow/workflow-engine.ts         122 +    OK
  [CREATE] packages/core/src/workflow/index.ts                    14 +    OK
  [UPDATE] packages/core/src/index.ts                             15 +    OK
  [CREATE] packages/protocol/tests/mor-anchor.test.ts            181 +    OK
  [CREATE] packages/core/tests/workflow-engine.test.ts            131 +    OK
8/8 paths casados. Sem drift.

# Gate (build + test) na worktree correta
$ pnpm --filter @plataforma/protocol build
$ tsc                                                        (sem erros)
$ pnpm --filter @plataforma/protocol test
 RUN v3.2.6 C:/Dev2026/.superapp-worktrees/009-01/packages/protocol
  ✓ tests/ports.test.ts              (18 tests)  16ms
  ✓ tests/codec.test.ts              (12 tests)  20ms
  ✓ tests/wireFormat.test.ts         (11 tests)  83ms
  ✓ tests/rbsr.test.ts               (18 tests)  10ms
  ✓ tests/rbsr.ucanScope.test.ts     ( 6 tests)  13ms
  ✓ tests/rbsr.transfer.test.ts      (13 tests)  14ms
  ✓ tests/rangeFooter.test.ts        ( 6 tests)   7ms
  ✓ tests/rbsr.exchange.test.ts      ( 8 tests)  14ms
  ✓ tests/peerId.test.ts             (12 tests)   6ms
  ✓ tests/outOfBand.test.ts          ( 8 tests)   7ms
  ✓ tests/rbsr.waves.test.ts         ( 8 tests)   6ms
  ✓ tests/mor-anchor.test.ts         ( 6 tests)   6ms    ← NOVO (009-01)
  ✓ tests/mock.test.ts               ( 1 test )   2ms
  Test Files  13 passed (13)
       Tests  127 passed (127)

$ pnpm --filter @plataforma/core build
$ tsc                                                        (sem erros)
$ pnpm --filter @plataforma/core test
  ✓ ... 25 files (incluindo tests/workflow-engine.test.ts (7 tests) 7ms) ← NOVO (009-01)
  Test Files  25 passed (25)
       Tests  230 passed (230)

# Gate (lint) — FALHA em código NOVO
$ pnpm --filter @plataforma/protocol lint
  packages/protocol/src/workflow/mor-anchor.ts
    34:5  error  "seller" overridden by string in this union type   @typescript-eslint/no-redundant-type-constituents
    35:5  error  "reseller" overridden by string in this union type
    36:5  error  "platform" overridden by string in this union type
    37:5  error  "payment_gateway" overridden by string in this union type
  ✖ 4 problems (4 errors, 0 warnings)

$ pnpm --filter @plataforma/core lint
  packages/core/src/workflow/workflow-engine.ts
     2:3  error  'JurisdictionAnchor' is defined but never used    @typescript-eslint/no-unused-vars
   116:3  error  '_step' is defined but never used                 @typescript-eslint/no-unused-vars
  (mais 11 erros PRÉ-EXISTENTES em blindArchives/invite/schema/
   deviceState-schema/sqliteStorage — StoragePort deprecado, todos
   anteriores a 009-01; mesmos erros já rastreados pelo ledger
   de T-SHL-02 i1 — não contam para 009-01)
  ✖ 13 problems (13 errors, 0 warnings) — 2 introduzidos por 009-01
```
- **Comentários de Revisão:**

### QA REPORT 2 — 009-01 — Âncora MoR: papéis declaráveis vs enumerados
═══════════════════════════════════════════════════
Data: 2026-07-15  |  Revisor 2: agile_reviewer (minimax-m3, independente)
Anti-ancoragem: formado o veredito lendo spec + worktree + diff + Gate
+ sondas **antes** de comparar com o parecer anterior.
Spec consultada: §1–7 (revisada após reendurecimento)  |  Arquivos: 8 no escopo
Testes: 13 novos (6 protocol mor-anchor + 7 core workflow-engine)
rodados · 357 totais (127 + 230) passaram · 0 falharam
tsc: OK nos dois pacotes  |  lint: 2 erros em código NOVO de 009-01

**Reconciliação com o parecer anterior (R1, minimax-m3):**
- **B1–B5 do R1: FALSOS POSITIVOS.** O R1 auditou o repo errado
  (`C:\Dev2026\Projeto Superapp`, congelado, monorepo pequeno só
  com `@superapp/core` e 28 tests). O repo ativo do MGTIA é o Turbo
  monorepo em `C:\Dev2026\.superapp-worktrees\<task>`, com
  `@plataforma/{protocol,core}` reais. Todos os "ausentes" do R1
  existem na worktree correta `009-01`. Erro de navegação meu; mea
  culpa. **Reconhecido.**
- **B6 do R1: VÁLIDO e corrigido.** §3 desatualizado, expansion
  feita (adicionados `core/src/workflow/index.ts`, `core/src/index.ts`,
  `protocol/src/workflow/index.ts`, arquivos de teste).
- **B7 do R1: VÁLIDO e corrigido.** Decisão §6 (âncora MoR
  declarável) materializou-se em `workflow-engine.ts` via
  `createMoRContext` que resolve MoR junto de
  origin/destination/labor/data_subject (a "Prestação" do §6
  corresponde a "labor" no tipo; "Titular" a "data_subject"; ver
  `mor-anchor.ts:14-19`).
- **i1 do R1: NÃO corrigido.** `complexity: 4` segue na frontmatter.
  Re-classificação é housekeeping, não-bloqueante, fica no ledger
  para próxima campanha de endurecimento.

**Achados novos desta revisão (R2):**

**MAJOR (2)**
────────────────────────────────────────────────────
[M1] **Lint: `MoROriginalRole` tem constituintes redundantes**
  (4 erros `@typescript-eslint/no-redundant-type-constituents`).
  Evidência: `packages/protocol/src/workflow/mor-anchor.ts:33-38`
  declara `| 'seller' | 'reseller' | 'platform' | 'payment_gateway' | string` —
  o `string` final torna os 4 literais redundantes (qualquer string
  casa). ESLint recusa.
  Viola: Gate de Evidência (lint entrou no gate em 2026-07-06 — ver
  T-807, EST-02b, EST-02c).
  Ação: usar o pattern idiomático para "enum aberta":
    `export type MoROriginalRole = 'seller' | 'reseller' | 'platform' | 'payment_gateway' | (string & {});`
  O `string & {}` força o TS a manter a união fechada+intellisense
  dos literais sem que o ESLint reclame de redundância. Alternativa:
  mover os 4 literais para um JSDoc `@see` e deixar só `string`.

[M2] **Lint: vars/imports não usados em `workflow-engine.ts`.**
  (2 erros `@typescript-eslint/no-unused-vars`).
  Evidência:
  - `packages/core/src/workflow/workflow-engine.ts:2` — `JurisdictionAnchor`
    importado mas nunca referenciado (só `AnchorDeclaration`,
    `AnchorResolution`, `MerchantOfRecord`, `MultiJurisdictionOperation`
    são usados do protocol).
  - `packages/core/src/workflow/workflow-engine.ts:116` — parâmetro
    `_step: Step` na assinatura de `stepRequiresMoR` é ignorado pelo
    corpo (a função retorna só `ctx.hasMoR`). O `_` prefix não basta
    pro `@typescript-eslint/no-unused-vars` quando o nome do tipo é
    não-trivial; ou usa `argsToIgnore: 'after-used'`/`varsIgnorePattern`
    configurado, ou o parâmetro precisa sumir.
  Viola: Gate de Evidência (lint no gate).
  Ação: remover `JurisdictionAnchor` do import; remover o parâmetro
  `_step` da assinatura de `stepRequiresMoR` (e atualizar o caller
  de teste em `tests/workflow-engine.test.ts:119,129`).

**MAJOR pré-existente (rastreado, NÃO introduzido por 009-01 — referência):**
  `StoragePort` deprecado em 11 chamadas (blindArchives/invite/
  schema/deviceState-schema/sqliteStorage) — já coberto pelo
  `i1[T-SHL-02]` no ledger. Track: rework próprio de T-SHL-01/02.
  009-01 não é responsável.

**MINOR (0)**

**INFO (0)** *(i1 da R1 permanece no ledger)*

═══════════════════════════════════════════════════
**VEREDICTO: REFATORAÇÃO NECESSÁRIA**

**Resumo honesto:** o Reviewer 1 errou o repo (B1–B5 foram FP meus);
o worker está certo sobre isso. O trabalho de fato é real, completo,
e o §3 está coerente com o diff. Restam 2 MAJORs de lint no código
NOVO da task — fixes triviais (~5 linhas no total), mas a regra
explícita do MGTIA desde 2026-07-06 ("lint no gate") exige
REFATORAÇÃO, não aprovação silenciosa. Precedente literal: T-807,
EST-02b, EST-02c.

### Parecer do Reviewer 3 (minimax-m3, independente — anti-ancoragem):
- [x] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória — colar saída de build/tsc + test):**
```
# Worktree: C:\Dev2026\.superapp-worktrees\009-01
# Branch: task/009-01 · base: origin/master · commits:
#   7ff493c  feat(009-01): MoR anchor as declarable jurisdictional role
#   1109853  fix(009-01): [M1] MoROriginalRole string & {} + [M2] remove unused JurisdictionAnchor import and _step param
# working tree: clean, ahead of origin/task/009-01 by 0 (push já feito)

# Diff × Escopo declarado (gate 2c)
Declarado em §3                                              Alterado  Disposição
  [CREATE] packages/protocol/src/workflow/mor-anchor.ts        153 +   OK
  [CREATE] packages/protocol/src/workflow/index.ts              19 +   OK
  [UPDATE] packages/protocol/src/index.ts                       19 +   OK
  [CREATE] packages/core/src/workflow/workflow-engine.ts       120 +   OK
  [CREATE] packages/core/src/workflow/index.ts                  14 +   OK
  [UPDATE] packages/core/src/index.ts                           15 +   OK
  [CREATE] packages/protocol/tests/mor-anchor.test.ts          181 +   OK
  [CREATE] packages/core/tests/workflow-engine.test.ts         131 +   OK
8/8 paths. Sem drift. Total: 652 insertions (R2 era 654; -2 do fix M2).

# Gate (build + test + lint) na worktree correta
$ pnpm --filter @plataforma/protocol build
$ tsc                                                        (sem erros)

$ pnpm --filter @plataforma/protocol test
  ✓ ... 13 files (incluindo tests/mor-anchor.test.ts (6 tests) 5ms)
  Test Files  13 passed (13)
       Tests  127 passed (127)
  Duration  3.25s

$ pnpm --filter @plataforma/core build
$ tsc                                                        (sem erros)

$ pnpm --filter @plataforma/core test
  ✓ ... 25 files (incluindo tests/workflow-engine.test.ts (7 tests) 7ms)
  Test Files  25 passed (25)
       Tests  230 passed (230)
  Duration  7.24s

# Lint — código NOVO de 009-01: 100% verde
$ pnpm --filter @plataforma/protocol lint
  (sem saída de erro — 0 erros, 0 warnings)

$ pnpm --filter @plataforma/core lint
  packages/core/src/workflow/workflow-engine.ts   ← arquivo NOVO: LIMPO
  packages/core/src/{archive,invite,schema,deviceState-schema,sqliteStorage}.ts
    11 erros PRÉ-EXISTENTES de `StoragePort` deprecado — TODOS em
    arquivos não-introduzidos por 009-01; já rastreados por
    T-SHL-02 i1 no ledger. 009-01 não é responsável.
  ✖ 11 problems (11 errors, 0 warnings) — 0 introduzidos por 009-01.
```

- **Comentários de Revisão:**

### QA REPORT 3 — 009-01 — Âncora MoR: papéis declaráveis vs enumerados
═══════════════════════════════════════════════════
Data: 2026-07-16  |  Revisor 3: agile_reviewer (minimax-m3, independente)
Anti-ancoragem: spec + worktree + diff + Gate lidos a frio **antes**
de comparar com pareceres R1/R2.
Spec consultada: §1–7  |  Arquivos auditados: 8 (todos no escopo)
Testes: 13 novos (6 protocol + 7 core) · 357 totais · 0 falharam
tsc: OK nos dois pacotes  |  lint: 0 erros em código NOVO de 009-01

**Reconciliação:**
- M1 (R2) — `MoROriginalRole` redundante → **CORRIGIDO**.
  Verificação: `mor-anchor.ts:33-38` agora usa o pattern idiomático
  `| (string & {})` (mantém intellisense dos 4 literais +
  extensibilidade sem redundância). Lint: 0 erros em protocol.
- M2 (R2) — `JurisdictionAnchor` import + `_step` param unused →
  **CORRIGIDOS**. Verificação:
  - `workflow-engine.ts:1-6` não importa mais `JurisdictionAnchor`
  - `workflow-engine.ts:113-120` — `stepRequiresMoR` agora tem
    assinatura `(ctx: MoRWorkflowContext): boolean` (sem `_step`)
  - 2 callers de teste atualizados (`tests/workflow-engine.test.ts:119,129`)

**Sondas (anti-passividade):** os 13 testes novos cobrem §4
explicitamente — MoR declarado (1), MoR ausente (2), split (3),
conflito (4), sem-âncoras (5), stepRequiresMoR true/false (6, 7).
**Sondagem adicional (não exigir rework):** considerei
edge-cases de `MoROriginalRole = string` (custom role) — o type
aceita via `string & {}` e o `getMoRDeclaration` retorna o `role`
cru, sem validação. **Não-bloqueante:** a validação por role
custom é responsabilidade do consumidor (Zen Engine). Track: INFO.

**Gate 2c (diff × escopo):** 8/8 paths casados. Nenhuma mudança
rastreada fora do §3. O fix commit `1109853` é cirúrgico (3
arquivos, 7+/9- linhas). Sem scope creep.

**Lint pré-existente (não-conta):** 11 erros `StoragePort`
deprecado em arquivos não-introduzidos por 009-01 — já cobertos
pelo ledger (`i1[T-SHL-02]`). Track de T-SHL-01/02, não de 009-01.

═══════════════════════════════════════════════════
**VEREDICTO: APROVADO**
Resumo: o rework R3 fechou M1 e M2 do parecer R2. Build, test e
lint (no código novo) 100% verdes. 8/8 paths do §3 no diff sem
drift. A decisão arquitetural §6 (MoR como âncora declarável)
está materializada em `workflow-engine.ts` (MoR resolvido junto
de origin/destination/labor/data_subject). 13 testes novos
cobrem os 4 casos exigidos em §4. Pronto para integrar.

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.

- **[2026-07-03 13:26:06]** - *system* - `[Migrado]`: spec_status:draft → status:draft:placeholder
- **[2026-07-03T20:02]** - *system* - `[Triado]`: Triagem em lote do backlog
- **[2026-07-03T20:03]** - *system* - `[Endurecido]`: Endurecimento em lote (dependencies done/empty)
- **[2026-07-03T20:07]** - *system* - `[Promovida p/ ready]`: Promovida pelo arquiteto (arquiteto-promover)
- **[2026-07-03T21:06]** - *opencode-zen-ent* - `[Demovido]`: spec incompleta—placeholders sem Seções 1-5 preenchidas. Demovida para re-endurecimento.
- **[2026-07-03T21:27]** - *opencode-zen-ent* - `[Triado]`: triada
- **[2026-07-03T21:27]** - *opencode-zen-ent* - `[Decisão pendente]`: decisao pendente: implementation_plan.md secao Moat
- **[2026-07-04T00:11]** - *claude-opus* - `[Decidido]`: decisao: A ancora MoR nativa declaravel (nao papel fixo)
- **[2026-07-04T00:12]** - *claude-opus* - `[Promovida p/ ready]`: safety-net pos-decisao
- **[2026-07-16T09:05]** - *claude-sonnet* - `[Iniciado]`: iniciando execução
- **[2026-07-16T09:16]** - *claude-sonnet* - `[Finalizado]`: MoR anchor implementado: mor-anchor.ts (tipo MerchantOfRecord + resolveAnchors/validateNoConflict), workflow-engine.ts (createMoRContext + stepRequiresMoR). 13 novos testes, todos verdes. Protocol: 127 pass, Core: 230 pass.
- **[2026-07-16T09:20]** - *agile_reviewer:minimax-m3* - `[Em revisão]`: revisando 009-01
- **[2026-07-16T09:26]** - *agile_reviewer:minimax-m3* - `[Requer Refatoração]`: Rework (009-01): B1 [Finalizado] falso + Evidência fabricada (127/230 passed não existem; monorepo real = 28 tests; filtros @plataforma/* não casam); B2 3 paths de §3 ausentes em qualquer branch (mor-anchor.ts / workflow-engine.ts / index.ts); B3 nomes de pacote da spec incompatíveis com o monorepo (@plataforma/{protocol,core} → só existe @superapp/core; packages/protocol/ não existe); B4 nenhuma worktree task/009-01 aberta; B5 Gate de Evidência violado materialmente; B6 Handover cita packages/core/src/index.ts fora de §3; B7 decisão §6 não chegou a virar código. i1 (complexity 4→2) → ledger. Não-bloqueantes → ledger.
- **[2026-07-16T09:31]** - *claude-sonnet* - `[Iniciado]`: rework: corrigindo B6 — declarar core/index.ts no escopo §3
- **[2026-07-16T09:33]** - *claude-sonnet* - `[Finalizado]`: rework pronto: B6 corrigido (§3 desatualizado — adicionados core/index.ts, barrel exports, testes ao escopo). B1-B5 eram falsos positivos (revisor rodou de repo errado). Gate re-executado: protocol 127 pass, core 230 pass.
- **[2026-07-16T09:36]** - *agile_reviewer:minimax-m3* - `[Em revisão]`: revisando 009-01 (rework 2)
- **[2026-07-16T09:41]** - *agile_reviewer:minimax-m3* - `[Requer Refatoração]`: Rework R2 (009-01): worktree correta encontrada (.superapp-worktrees/009-01); build+test 100% verdes; B1-B5 da R1 eram FP (rodei do repo errado, mea culpa) e B6/B7 corrigidos. Restam 2 MAJORs de lint no código NOVO: M1 mor-anchor.ts:34-37 (4 erros @typescript-eslint/no-redundant-type-constituents — 'seller'/'reseller'/'platform'/'payment_gateway' redundantes com 'string'; fix: string & {}); M2 workflow-engine.ts:2 + 116 (JurisdictionAnchor unused import + _step unused param). Fixes triviais (~5 linhas). Regra lint-no-gate de 2026-07-06 (precedente T-807, EST-02b, EST-02c) exige REFATORAÇÃO, não aprovação silenciosa.
- **[2026-07-16T09:44]** - *claude-sonnet* - `[Iniciado]`: rework: corrigindo M1 (lint MoROriginalRole) + M2 (unused vars workflow-engine)
- **[2026-07-16T09:48]** - *claude-sonnet* - `[Finalizado]`: rework pronto: M1 (MoROriginalRole string & {}) + M2 (unused import/param removidos). Protocol lint 0 erros, core lint 0 novos. Gate: protocol 127 pass, core 230 pass.
- **[2026-07-16T09:50]** - *agile_reviewer:minimax-m3* - `[Em revisão]`: revisando 009-01 (rework 3 — R3)
- **[2026-07-16T09:58]** - *agile_reviewer:minimax-m3* - `[Aprovado]`: Integrado: merge na master (485f0e3), worktree removida, Gate verde pos-merge (protocol: tsc ok + 127/127 tests; core: tsc ok + 230/230 tests; lint 0 erros no código novo de 009-01). Não-bloqueantes (i1, i2) → ledger de pendências. Push: 107dbe7..485f0e3 master -> master.
