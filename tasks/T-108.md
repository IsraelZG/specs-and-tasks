---
id: T-108
title: "Linhagem Layer 2"
status: review
complexity: 3
target_agent: logic_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-107", "T-106"]
blocks: ["T-601"]
---

# T-108 · Linhagem Layer 2

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar o motor de linhagem (Layer 2) no pacote `@plataforma/core`: navegação em DAG via arestas `MUTATES`, validação de cadeia de hash, detecção de ciclos, e seleção de `head` por HLC máximo
*(derivado de `docs/visao-arquitetural.md` §5 "Imutabilidade Dupla" — Layer 2: aresta MUTATES referencia hash da versão anterior; `docs/conceitos/entity-id.md` — entity_id herda do nó-raiz; `docs/conceitos/hlc.md` — head = nó de maior HLC na linhagem)*.

### Contratos exatos (assinaturas TS fixadas)

```ts
// --- packages/core/src/lineage.ts ---
import { SignedNode, SignedEdge } from './signature.js'; // T-107
import { ULID, EntityId } from './ulid.js'; // T-102
import { HLCTimestamp } from './hlc.js'; // T-103
import { StoragePort } from '@plataforma/protocol'; // T-004

/** Hash SHA-256 de um nó (32 bytes). Usado na aresta MUTATES para referenciar o pai. */
export type NodeHash = Uint8Array;

/** Calcula SHA-256 do canonicalize(nó) — identifica unicamente o conteúdo. */
export function hashNode(node: SignedNode): NodeHash;

// === Operações de linhagem ===

/** Insere nó no grafo com validação Layer 2:
 *  1. Se há pai (aresta MUTATES de entrada), valida que:
 *     - parentHash confere com hashNode(pai)
 *     - HLC(filho) > HLC(pai) (invariante de pai — hlc.md)
 *     - entity_id do filho = entity_id do pai (herança — entity-id.md)
 *  2. Se é nó-raiz (sem pai), entity_id = id.
 *  3. Detecta e rejeita ciclos.
 *  4. Persiste nó + aresta MUTATES no storage.
 *  5. Atualiza entity_heads (insere ou UPDATE se HLC maior).
 */
export async function insertNode(
  storage: StoragePort,
  node: SignedNode,
  parentId?: ULID,
): Promise<void>;

/** Navega a linhagem de uma entidade do head à raiz (ordem reversa de HLC). */
export async function getLineage(storage: StoragePort, entityId: EntityId): Promise<SignedNode[]>;

/** Retorna o head atual de uma entidade (nó com maior HLC na entity_heads). */
export async function getHead(storage: StoragePort, entityId: EntityId): Promise<SignedNode | null>;

/** Verifica se a cadeia de hash é íntegra da raiz ao head. */
export async function validateChain(storage: StoragePort, entityId: EntityId): Promise<boolean>;

/** Detecta se há fork (duas arestas MUTATES ativas com mesmo source_id). */
export async function detectFork(storage: StoragePort, sourceId: ULID): Promise<boolean>;
```

### Tabelas adicionais (migration v2 do schema T-106)
```sql
-- entity_heads: um registro por linhagem, apontando pro nó mais recente
CREATE TABLE IF NOT EXISTS entity_heads (
  entity_id  TEXT NOT NULL PRIMARY KEY,  -- EntityId (ULID do nó-raiz)
  head_id    TEXT NOT NULL REFERENCES nodes(id),
  head_hlc   INTEGER NOT NULL           -- HLCTimestamp empacotado (bigint como INTEGER)
);

-- Gatilho: ao inserir nó, atualiza entity_heads se HLC maior
CREATE TRIGGER IF NOT EXISTS trg_nodes_insert_entity_head
AFTER INSERT ON nodes
BEGIN
  INSERT INTO entity_heads (entity_id, head_id, head_hlc)
  VALUES (NEW.entity_id, NEW.id, NEW.hlc)
  ON CONFLICT(entity_id) DO UPDATE
  SET head_id = NEW.id, head_hlc = NEW.hlc
  WHERE NEW.hlc > head_hlc;
END;
```

## 2. Contexto RAG (Spec-Driven Development)
- [visao-arquitetural.md](../docs/visao-arquitetural.md) (§5 Imutabilidade Dupla — Layer 2)
- [entity-id.md](../docs/conceitos/entity-id.md) — herança de entity_id, head via HLC
- [hlc.md](../docs/conceitos/hlc.md) — invariante de pai, seleção de head

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[READ]** `packages/core/src/signature.ts` (SignedNode, canonicalize — T-107)
- **[READ]** `packages/core/src/ulid.ts` (ULID, EntityId — T-102)
- **[READ]** `packages/core/src/hlc.ts` (HLCTimestamp — T-103)
- **[READ]** `packages/core/src/schema.ts` (MIGRATIONS — T-106)
- **[CREATE]** `packages/core/src/lineage.ts` — hashNode, insertNode, getLineage, getHead, validateChain, detectFork
- **[CREATE]** `packages/core/tests/lineage.test.ts`
- **[UPDATE]** `packages/core/src/schema.ts` — adicionar migration v2 (entity_heads + trigger)
- **[UPDATE]** `packages/core/src/index.ts` — re-export

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Framework:** Vitest (Node puro).
- [x] **Ambiente do Teste:** `pnpm --filter @plataforma/core test`. SQLite `:memory:` via `better-sqlite3`.
- [x] **Fora de Escopo:** Sync de rede, RBSR, UCAN.

Casos de teste (numerados):
1. `insertNode(nó-raiz)`: persistido, `entity_id = id`, `entity_heads` tem 1 registro.
2. `insertNode(filho, parentId=raiz)`: filho tem `entity_id` do pai, `head_hlc` atualizado.
3. `insertNode` com `HLC(filho) ≤ HLC(pai)` → erro (invariante de pai).
4. `insertNode` com `parentHash` errado → erro (cadeia quebrada).
5. `getLineage(entityId)` retorna [head, ..., raiz] em ordem reversa.
6. `getHead(entityId)` retorna nó com maior HLC.
7. `validateChain(entityId)` → true para cadeia íntegra; false se adulterada.
8. `detectFork(sourceId)`: insere 2 filhos do mesmo pai → fork detectado.
9. Ciclo: `insertNode(A → B → tentar A)` → erro.
10. Linhagem com 100 nós: `validateChain` percorre toda a cadeia sem erro.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** permita ciclos na inserção de arestas MUTATES.
> - **NÃO** aceite HLC(filho) ≤ HLC(pai) — invariante de pai é inviolável.

1. **[TDD]** Crie `packages/core/tests/lineage.test.ts` com casos 1–10 usando SQLite `:memory:`.
2. Adicione migration v2 (`entity_heads` + trigger) em `packages/core/src/schema.ts`.
3. Implemente `hashNode` (SHA-256 do canonicalize).
4. Implemente `insertNode` com validações (hash, HLC, ciclo, entity_id).
5. Implemente `getLineage`, `getHead`, `validateChain`, `detectFork`.
6. Re-exporte em `packages/core/src/index.ts`.
7. Rode build + test (Seção 7) e cole saída.

## 6. Feedback de Especificação (Spec Feedback Loop)
*(Nenhuma pendência)*

## 7. Definition of Done (DoD) & Reviewer Checklist

### Verificação automática (Gate de Evidência)
```bash
pnpm --filter @plataforma/core build
pnpm --filter @plataforma/core test
```
> **GATE DE EVIDÊNCIA:** Worker cola saída literal na Seção 8.

### Checklist do Reviewer
- [ ] `insertNode` valida invariante de pai (HLC, hash, entity_id)?
- [ ] Detecção de ciclo e fork implementadas?
- [ ] `entity_heads` mantido por trigger com comparação de HLC?
- [ ] 10 casos de teste passando?
- [ ] `pnpm --filter @plataforma/core build` e `test` verdes?

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- **Build:** `tsc` — zero errors
- **Test:** 7 files, 61 tests pass (1 mock + 12 ulid + 10 hlc + 11 keyVault + 7 schema + 10 signature + 10 lineage)
- **Casos cobertos:** 10/10 (nó-raiz, filho, HLC invariante, parentHash, getLineage, getHead, validateChain, detectFork, ciclo, 100 nós)
- **Arquivos criados:**
  - `packages/core/src/lineage.ts` — hashNode, insertNode, getLineage, getHead, validateChain, detectFork
  - `packages/core/tests/lineage.test.ts` — 10 casos
- **Arquivos alterados:**
  - `packages/core/src/schema.ts` — migration v2 (entity_members + entity_heads)
  - `packages/core/src/index.ts` — re-export lineage functions
  - `packages/core/tests/schema.test.ts` — updated index expectations (+2 indexes)

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória):**
```
=== BUILD ===
$ tsc
(zero errors)

=== TEST ===
$ vitest run

 RUN  v3.2.6  C:/Dev2026/.superapp-worktrees/T-108/packages/core

 ✓ tests/mock.test.ts       (1 test)   2ms
 ✓ tests/ulid.test.ts       (12 tests) 5ms
 ✓ tests/schema.test.ts     (7 tests)  14ms
 ✓ tests/keyVault.test.ts   (11 tests) 4ms
 ✓ tests/hlc.test.ts        (10 tests) 37ms
 ✓ tests/signature.test.ts  (10 tests) 94ms
 ✓ tests/lineage.test.ts    (10 tests) 323ms

 Test Files  7 passed (7)
      Tests  61 passed (61)
   Duration  949ms
```
- **Comentários de Revisão:**

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-06-22T19:42]** - *DeepSeek* - `[Iniciado]`: iniciando implementação da Linhagem Layer 2
- **[2026-06-22T19:49]** - *DeepSeek* - `[Finalizado]`: Linhagem Layer 2 implementada: insertNode com validação HLC/hash/entity_id/ciclo, getLineage, getHead, validateChain, detectFork. Migration v2: entity_members + entity_heads. Build zero errors, 61/61 tests pass.
