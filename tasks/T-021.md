---
id: T-021
title: "Arcabouco de E2E Playwright compartilhado (@plataforma/testkit) + showcase como 1o consumidor"
status: in_progress
complexity: 3
target_agent: frontend_agent # perfis: devops_agent, logic_agent, crypto_agent, frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential # parallel | sequential
dependencies: ["T-011", "T-012", "T-015", "T-018", "T-019"] # IDs de tarefas que bloqueiam esta
blocks: [] # IDs de tarefas que esta bloqueia
---

# T-021 · Arcabouço de E2E Playwright compartilhado (`@plataforma/testkit`) + showcase como 1º consumidor

> **Esta task entrega uma FERRAMENTA, não testes de um único app.** O E2E de UI nasce como um preset compartilhado em `@plataforma/testkit`, consumido por **qualquer** app do superapp (`apps/web`, `apps/bancada`, `apps/system-peer` quando saírem de stub; ver T-009 casos 8–9, hoje adiados). O **showcase do design-system é apenas o primeiro consumidor** — escolhido por ser o único alvo renderável hoje. As specs E2E concretas (os 5 componentes) provam a ferramenta; elas **não** são o ponto, o preset reutilizável é.

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (`pnpm build`, `pnpm test`, `pnpm lint` na raiz afetam todos os pacotes)
- **Test Runner:** `playwright` (browser real — Chromium headless no CI)
- **Capacidade-alvo:** sonnet *(o preset é uma função fina sobre `defineConfig`; o julgamento está em qual interação real cada componente precisa — drag, foco, transição — não repetível por find-and-replace)*

## 1. Objetivo
Criar o **arcabouço de testes de UI E2E do superapp** como um preset compartilhado e fazer o showcase consumi-lo:

1. **A ferramenta:** `createPlaywrightConfig(...)` em `@plataforma/testkit` (pacote workspace já existente) — uma função fina sobre o `defineConfig` nativo do Playwright que fixa os defaults do projeto (Chromium, `webServer` com reuse local / start no CI, `retries` no CI, reporter, `forbidOnly` no CI). Qualquer app passa só o seu comando de boot e a baseURL. **Sem classe, sem plugin system** — uma função que retorna `PlaywrightTestConfig`.
2. **O 1º consumidor:** `apps/design-system-showcase/playwright.config.ts`, um arquivo de ~5 linhas que chama o preset apontando para o harness de boot já existente `pnpm --filter design-system-showcase dev` (T-012 caso 3 / T-015 smoke caso 5).
3. **A prova:** specs E2E dos 5 componentes do design-system onde o JSDOM da T-018 é insuficiente (drag real, focus-trap real, clique-fora com stacking context real).
4. **O CI:** um pipeline task `test:e2e` no Turbo, de modo que **todo app com script `test:e2e`** entra no CI automaticamente (showcase hoje, `bancada`/`web` depois — sem editar o CI de novo).

**Por que `@plataforma/testkit` e não um config local no design-system:** um `playwright.config.ts` enfiado em `packages/design-system` não é compartilhável — seria a "reinvenção em cada app" que esta task existe para evitar. O testkit já é o pacote de utilitários de teste do monorepo (`@plataforma/testkit`, usado por client-sdk/core/etc.); o preset de E2E mora lá pelo mesmo motivo.

**Por que não é uma task de E2E de fluxo de produto:** hoje `apps/web`, `apps/bancada` e `apps/system-peer` são stubs sem UI renderável — não há fluxo real para testar ainda. O único alvo renderável é o showcase. Esta task não inventa fluxos inexistentes; cobre a lacuna real (interações que `@testing-library/react` + JSDOM da T-018 não simula com fidelidade) **e** deixa a ferramenta pronta para os apps reais herdarem.

### Contrato exato (assinatura TS fixada do preset)

```ts
// --- packages/testkit/src/playwright.ts ---
import { defineConfig, type PlaywrightTestConfig } from "@playwright/test";

export interface UiE2EOptions {
  /** Comando que sobe o app sob teste. Ex.: "pnpm --filter design-system-showcase dev". */
  webServerCommand: string;
  /** URL onde o app responde quando pronto. Ex.: "http://localhost:5173". */
  baseURL: string;
  /** Diretório dos specs, relativo ao config do consumidor. Default: "./e2e". */
  testDir?: string;
}

/**
 * Preset de E2E compartilhado do superapp. Todo app consome isto em vez de
 * escrever um playwright.config do zero.
 * Defaults fixados (consumidor NÃO repete): projeto único Chromium; webServer
 * com { command: webServerCommand, url: baseURL, reuseExistingServer: !process.env.CI };
 * retries: process.env.CI ? 2 : 0; forbidOnly: !!process.env.CI; reporter "line".
 */
export function createPlaywrightConfig(opts: UiE2EOptions): PlaywrightTestConfig {
  return defineConfig({
    testDir: opts.testDir ?? "./e2e",
    forbidOnly: !!process.env.CI,
    retries: process.env.CI ? 2 : 0,
    reporter: "line",
    use: { baseURL: opts.baseURL },
    projects: [{ name: "chromium", use: { /* devices['Desktop Chrome'] */ } }],
    webServer: {
      command: opts.webServerCommand,
      url: opts.baseURL,
      reuseExistingServer: !process.env.CI,
    },
  });
}
```
> O corpo acima é a referência; o Worker pode ajustar detalhes do `projects[0].use` (ex.: importar `devices` de `@playwright/test`), mas **a assinatura e os defaults são fixos**.

## 2. Contexto RAG (Spec-Driven Development)
- [x] `packages/testkit/package.json` (`@plataforma/testkit`, `exports: { ".": "./src/index.ts" }`) — pacote workspace existente; **é onde o preset mora**. Adicionar `@playwright/test` como dependência dele.
- [x] `docs/rfcs/rfc- plugin_architecture_blueprint - draft.md`, Seção 3.1 — confirma que Lookout é escopo visual, não funcional (justifica por que esta task usa Playwright e não Lookout; o gate visual é a T-020)
- [x] `tasks/T-012.md` (`done`) e `tasks/T-015.md` (`done`, caso 5) — o app `apps/design-system-showcase` e o harness de boot `pnpm --filter design-system-showcase dev`. O showcase é o 1º consumidor; o config dele aponta para esse comando.
- [x] `tasks/T-018.md` (Seção 4, Tier A critério 2 — "arraste de slider" via evento sintético JSDOM) — confirma a limitação que esta task resolve para os componentes de arraste
- [x] `tasks/T-019.md` (matriz CI x64 + arm64) — o step de E2E roda nos dois runners; browser instalado via `npx playwright install --with-deps chromium`
- [x] `tasks/T-009.md` (`done`, casos 8–9 adiados — E2E da Bancada) — **2º consumidor futuro** do mesmo preset; não está no escopo desta task, mas é a razão de a ferramenta ser compartilhada e não local.
- [x] `packages/design-system/src/components/<Nome>/<Nome>.metadata.ts` dos componentes listados na Seção 3 (contrato de props real)

## 3. Escopo de Arquivos (Inputs e Outputs)
**A ferramenta compartilhada (`@plataforma/testkit`):**
- **[CREATE]** `packages/testkit/src/playwright.ts` — `createPlaywrightConfig(opts: UiE2EOptions)` conforme contrato da Seção 1
- **[UPDATE]** `packages/testkit/src/index.ts` — re-export de `createPlaywrightConfig` e `UiE2EOptions`
- **[UPDATE]** `packages/testkit/package.json` — adicionar `"@playwright/test"` em `dependencies`

**O 1º consumidor (showcase):**
- **[READ]** `packages/design-system/src/components/Slider/`, `Carousel/`, `Resizable/`, `Modal/`, `Sheet/`, `Popover/` (código + metadata)
- **[CREATE]** `apps/design-system-showcase/playwright.config.ts` — ~5 linhas: `import { createPlaywrightConfig } from "@plataforma/testkit"` + `export default createPlaywrightConfig({ webServerCommand: "pnpm --filter design-system-showcase dev", baseURL: "http://localhost:<porta do dev>", testDir: "./e2e" })`
- **[CREATE]** `apps/design-system-showcase/e2e/<Nome>.e2e.spec.ts` — um arquivo por componente da lista abaixo (specs ficam com o app sob teste, não no pacote da lib)
- **[UPDATE]** `apps/design-system-showcase/package.json` — script `"test:e2e": "playwright test"` + devDeps `"@plataforma/testkit": "workspace:*"` e `"@playwright/test"`

**O CI (compartilhado, picks up todos os consumidores):**
- **[UPDATE]** `turbo.json` — expor `test:e2e` como pipeline task (assim qualquer app com script `test:e2e` roda via `turbo run test:e2e`, sem novo edit de CI por app)
- **[UPDATE]** `.github/workflows/ci.yml` — nos jobs da matriz T-019: step `npx playwright install --with-deps chromium` + `pnpm turbo run test:e2e`

- Não toque em `src/components/**` — se um teste revelar bug real, documente na Seção 6 e `pause`, não corrija aqui.
- Não recrie testes que o T-018 (JSDOM) já cobre adequadamente — esta task é só para a sublista abaixo.
- Não crie um `playwright.config.ts` dentro de `packages/design-system` — o config mora no consumidor (showcase) e os defaults moram no preset (testkit).

### Lista de componentes desta task (apenas onde browser real importa)
1. **Slider** — arraste real de pointer (mousedown na thumb → mousemove com coordenadas → mouseup) deve mover o valor; JSDOM não tem layout real para calcular posição.
2. **Carousel** — swipe/drag real entre slides; depende de cálculo de largura real do container, que JSDOM não renderiza.
3. **Resizable** — arraste do handle de resize altera dimensão real do painel; depende de layout real.
4. **Modal** — foco real preso dentro do diálogo (focus-trap) ao pressionar Tab repetidamente atravessando o DOM real, incluindo elementos fora do componente que deveriam ficar inacessíveis — diferente do teste JSDOM da T-018, que verifica o listener de teclado, não o resultado real do foco do browser.
5. **Sheet** e **Popover** — comportamento de `z-index`/clique-fora real depende de stacking context calculado pelo motor de renderização, não simulável com fidelidade em JSDOM.

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Framework:** Playwright, `@playwright/test`, Chromium — via o preset `@plataforma/testkit`
- [x] **Métricas/Cobertura:** cada um dos 5 componentes tem ao menos 1 spec cobrindo a interação real, com asserção de resultado (valor alterado, foco preso, elemento visível/oculto) — não é "smoke", é asserção de comportamento. Mais: o preset compila e é importável por um 2º consumidor hipotético (verificação de que é genérico, não acoplado ao showcase).
- [x] **Ambiente do Teste:** Chromium real (headless no CI, headed local para debug). O showcase é subido pelo `webServer` do preset via **`pnpm --filter design-system-showcase dev`** (harness da T-012/T-015). **Sem decisão a tomar** — o comando está fixado.
- [x] **Fora de Escopo:** qualquer asserção visual/pixel (isso é T-020/gate visual); componentes não listados; fluxos de produto em `apps/*` (não existem ainda); E2E da Bancada (T-009 casos 8–9 — consumidor futuro do mesmo preset, fora desta task).

Casos de teste (numerados — um por componente + ferramenta + infra):

**Ferramenta (preset compartilhado):**
0. `createPlaywrightConfig({ webServerCommand: "x", baseURL: "http://localhost:1" })` retorna um `PlaywrightTestConfig` com `webServer.command === "x"`, `webServer.url === "http://localhost:1"`, `testDir === "./e2e"` (default) e `reuseExistingServer === !process.env.CI`. *(É o teste que prova que a ferramenta é genérica, não hardcoded para o showcase. Vitest no `@plataforma/testkit`.)*

**Slider (arraste real de pointer):**
1. `mousedown` na thumb, `mousemove` com coordenadas calculadas a partir de `boundingBox()`, `mouseup` → valor do slider alterado para o esperado (ex.: de 50% para 75%). *(Derivado de T-018 §4 Tier A2.)*

**Carousel (swipe/drag entre slides):**
2. `mousedown` no container, `mousemove` com deslocamento horizontal > 50px, `mouseup` → slide visível mudou (`data-active` ou índice diferente).

**Resizable (drag do handle):**
3. `mousedown` no handle, `mousemove` com coordenadas calculadas, `mouseup` → `boundingBox().width` diferente do inicial.

**Modal (focus-trap com Tab real):**
4. Modal aberto → ciclo completo de Tab até `document.activeElement` voltar ao primeiro focável do modal; nenhum elemento fora do modal recebe foco.

**Sheet e Popover (stacking context / clique-fora):**
5. Sheet aberto → clique fora → Sheet fecha (ausente do DOM ou `hidden`).
6. Popover aberto → clique fora → Popover fecha.

**Infra (CI):**
7. `pnpm turbo run test:e2e` no CI (runners `ubuntu-latest` x64 e `ubuntu-24.04-arm` arm64 da matriz T-019) → todos os specs passam. Browser via `npx playwright install --with-deps chromium`. O Turbo seleciona o showcase hoje e qualquer app futuro com `test:e2e` sem novo edit.
> **Task afeta UI** (`frontend_agent`) — o smoke Playwright exigido pela regra geral É o conteúdo desta task. `ui: true` aplicável.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - Não use Lookout aqui — esta task é funcional/interação, Lookout (T-020) é visual/pixel.
> - Não escreva specs E2E para os 34 componentes restantes do T-018 "por garantia" — só os 5 listados.
> - Não derrube o servidor dev do showcase entre specs — o `webServer` do preset cuida disso.
> - Não duplique os defaults do preset no config do consumidor — o config do showcase só passa `webServerCommand`/`baseURL`/`testDir`. Se você se pegar repetindo `retries`/`reporter` no config do app, está acoplando errado.
> - Não coloque o `playwright.config.ts` nem os specs dentro de `packages/design-system` — eles vão no consumidor (`apps/design-system-showcase`); a parte reutilizável vai no `@plataforma/testkit`.

### Pegadinhas conhecidas
- A baseURL do preset precisa casar com a porta real do `dev` do showcase (Vite). Confirme a porta no `vite.config.ts` do showcase (ou fixe-a) antes de escrever `baseURL`; o `webServer.url` é o que o Playwright usa para saber que o app subiu.
- Drag real no Playwright é `page.mouse.move` + `down`/`up` em sequência com coordenadas absolutas — não há helper único de "drag"; calcule a partir do `boundingBox()` real de cada componente, não de valores fixos.
- Focus-trap do Modal: `page.keyboard.press('Tab')` repetidamente + `page.evaluate(() => document.activeElement)` a cada passo — o ciclo completo (último focável → volta ao primeiro) é o que prova o trap.
- CI precisa instalar os browsers do Playwright (`npx playwright install --with-deps chromium`) nos dois runners da matriz T-019 — step novo, não vem de cache de pnpm/node_modules.
- O preset é importado por um config `.ts` na raiz de um app; garanta que `@plataforma/testkit` resolva por `workspace:*` no `package.json` do showcase (mesmo padrão do consumo da lib pela T-012).

### Passos
1. **[Ferramenta]** Crie `packages/testkit/src/playwright.ts` com `createPlaywrightConfig` (assinatura/defaults da Seção 1), re-exporte no `index.ts`, adicione `@playwright/test` ao `package.json` do testkit. Escreva o caso 0 (vitest) provando que é genérico.
2. **[Consumidor]** Crie `apps/design-system-showcase/playwright.config.ts` consumindo o preset com `webServerCommand: "pnpm --filter design-system-showcase dev"` e a baseURL da porta do Vite. Adicione `test:e2e` + devDeps no `package.json` do showcase.
3. **[Prova — TDD]** Para cada um dos 5 componentes: leia o código real (não só a metadata) para confirmar geometria/seletores, escreva `apps/design-system-showcase/e2e/<Nome>.e2e.spec.ts`. Rode `pnpm --filter design-system-showcase test:e2e` (headed para debug, headless para confirmar) após cada um.
4. **[CI]** Exponha `test:e2e` no `turbo.json` e adicione ao `.github/workflows/ci.yml` (matriz T-019): `npx playwright install --with-deps chromium` + `pnpm turbo run test:e2e`.
5. Cole a saída final (local + link do run de CI com os dois jobs da matriz) na Seção 8.

## 6. Feedback de Especificação (Spec Feedback Loop)
> **ATENÇÃO:** Se a spec (RAG) for ambígua, contraditória ou o design pattern imposto for impossível, **PARE**. Mude o status para `blocked` e escreva o motivo abaixo. Não alucine uma abstração não documentada.
- **[Adaptação, não-bloqueante] Caso 2 (Carousel):** a spec §4 prescreve `mousedown`/`mousemove`/`mouseup` (drag) para trocar de slide. O componente real (`packages/design-system/src/components/Carousel/Carousel.tsx`) é **scroll-snap nativo + botões Prev/Next** (`scrollBy(clientWidth)`) — **não há handler de pointer-drag no container**, então um drag de mouse não move nada (overflow nativo só rola via wheel/touch/scrollbar/botão). O spec E2E foi escrito sobre a **interação real que troca o slide**: clique no Next + asserção de que `scrollLeft` do container mudou. O ganho sobre o JSDOM (geometria de scroll: `scrollLeft`/`scrollWidth`/`clientWidth`, todos zerados sem layout) é idêntico ao que o caso 2 pretendia. Decisão de não bloquear a task inteira por uma diferença de gesto num componente; reviewer decide se quer um follow-up para drag-to-scroll no próprio componente.

## 7. Definition of Done (DoD) & Reviewer Checklist
- [ ] `@plataforma/testkit` exporta `createPlaywrightConfig` com a assinatura/defaults da Seção 1, e o caso 0 (vitest) prova que é genérico (não hardcoded para o showcase)
- [ ] `apps/design-system-showcase/playwright.config.ts` consome o preset e só passa `webServerCommand`/`baseURL`/`testDir` (sem repetir defaults)
- [ ] Os 5 componentes têm `apps/design-system-showcase/e2e/<Nome>.e2e.spec.ts` com asserção de comportamento real (não smoke)
- [ ] `turbo.json` expõe `test:e2e`; `pnpm turbo run test:e2e` roda os specs do showcase
- [ ] CI roda E2E nos dois jobs da matriz T-019, com `npx playwright install --with-deps chromium` explícito
- [ ] Nenhum arquivo em `src/components/**` modificado; nenhum `playwright.config.ts` dentro de `packages/design-system`
- [ ] Nenhuma duplicação dos testes JSDOM já existentes do T-018

### Verificação automática
```bash
pnpm --filter @plataforma/testkit build
pnpm --filter @plataforma/testkit test          # caso 0 — preset genérico
pnpm --filter design-system-showcase build
pnpm --filter design-system-showcase test:e2e
pnpm turbo run test:e2e                          # seleção via Turbo (showcase hoje, apps futuros depois)
```
> **GATE DE EVIDÊNCIA:** nem o `finish` (worker) nem o veredito (reviewer) são válidos sem a
> saída literal desses comandos colada na Seção 8 — local **e** o link do run de CI mostrando os
> dois jobs da matriz T-019 verdes para o step E2E. Marcar `[x]` sem evidência é violação.

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
-

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória — colar saída local de test:e2e + link do run de CI):**
```
(cole aqui a saída real de pnpm test:e2e local e o link do workflow run)
```
- **Comentários de Revisão:**

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-06-23T17:48]** - *Crush* - `[Promovido]`: E2E preset + showcase: contratos TS fixados (createPlaywrightConfig, UiE2EOptions), 5 componentes com specs, Gate definido, zero decisões em aberto na Seção 6.
- **[2026-06-28T18:57]** - *Claude-Opus* - `[Iniciado]`: iniciando: preset E2E @plataforma/testkit + showcase como 1o consumidor
