---
id: T-603
title: "Committer determinístico"
status: draft:triaged
capacity_target: sonnet
complexity: 4
target_agent: logic_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-108", "T-602"]
blocks: []
---

# T-603 · Committer determinístico (colapso v4)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar committer determinístico no pacote `@plataforma/core`: agente local commita com desempate determinístico; co-assinatura opcional via ephemeral quando a SPEC exigir; comutativo: N peers commitando simultaneamente não geram fork no caso comum
*(derivado de `docs/plano-de-implementacao.md` §9 T-603)*.

### Contratos exatos
```ts
// --- packages/core/src/sync/deterministicCommitter.ts ---
import { EntityId } from '@plataforma/protocol'; // T-102
import { Ed25519PrivateKey, Ed25519Signature } from '@plataforma/crypto'; // T-101a

export interface CommitCandidate {
  entityId: EntityId;            // document/linhagem
  changes: Uint8Array[];         // changes a commitar
  authorPubKey: Uint8Array;
  hlc: number;
}

export interface DeterministicTiebreak {
  /** Desempate determinístico: menor hash(entity_id ‖ HLC) vence. */
  winner: EntityId;
  reason: 'lower_hash' | 'higher_hlc' | 'single_author';
}

export interface CoSignRequest {
  commitId: string;
  requiredCoSigners: EntityId[]; // spec-driven
  signatures: Map<EntityId, Ed25519Signature>;
}

export interface CommitterResult {
  commitNodeId: EntityId;
  tiebreak: DeterministicTiebreak;
  coSignatures: CoSignRequest | null; // null se não exige co-assinatura
}

export class DeterministicCommitter {
  /** Ordena candidatos deterministicamente. */
  rankCandidates(candidates: CommitCandidate[]): CommitCandidate[];

  /** Executa commit com desempate determinístico. */
  commit(candidates: CommitCandidate[], options?: { requireCoSign?: boolean }): Promise<CommitterResult>;

  /** Solicita co-assinatura de peers via ephemeral channel. */
  requestCoSignature(request: CoSignRequest): Promise<void>;

  /** Verifica se co-assinatura está completa. */
  isCoSignComplete(request: CoSignRequest): boolean;
}
```
- `rankCandidates`: ordena por `hash(entity_id ‖ HLC)`; menor hash vence.
- Desempate determinístico: não depende de ordem de recebimento ou latência de rede.
- Co-assinatura: opcional, exigida pela SPEC para determinados tipos de entidade; via ephemeral (T-403).
- N peers commitando simultaneamente → não geram fork no caso comum (comutativo).

## 2. Contexto RAG
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (§9 T-603)
- [T-108a · insertNode](./T-108a.md) — persistência de nós
- [T-602 · Commit Automerge](./T-602.md) — ciclo de commit

## 3. Escopo de Arquivos
- **[READ]** `packages/core/src/lineage.ts` (insertNode — T-108a)
- **[READ]** `packages/core/src/sync/commitCycle.ts` (CommitCycle — T-602)
- **[CREATE]** `packages/core/src/sync/deterministicCommitter.ts`
- **[CREATE]** `packages/core/tests/deterministicCommitter.test.ts`
- **[UPDATE]** `packages/core/src/index.ts` — re-export

## 4. Estratégia de Testes
- [x] **Framework:** Vitest (Node puro).
- [x] **Ambiente:** `pnpm --filter @plataforma/core test`.

Casos de teste:
1. `rankCandidates` ordena deterministicamente (mesmo input → mesma ordem).
2. `commit` com candidato único → commit direto sem tiebreak.
3. `commit` com 3 candidatos → desempate por menor hash.
4. Dois peers commitando simultaneamente → não geram fork (comutativo).
5. `requestCoSignature` envia requisição via ephemeral channel.
6. `isCoSignComplete` com todas as assinaturas → true.
7. `isCoSignComplete` faltando assinatura → false.
8. Co-assinaturas persistidas no nó final após completas.

## 5. Instruções de Execução
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** use round-robin ou ordem de chegada para desempate — use hash determinístico.
> - **NÃO** commite sem verificar co-assinatura quando SPEC exigir.

1. **[TDD]** Escreva testes com múltiplos candidatos e co-assinaturas.
2. Implemente `rankCandidates` com hash determinístico.
3. Implemente `commit` com desempate e co-assinatura.
4. Implemente `requestCoSignature` e `isCoSignComplete`.
5. Re-exporte. Rode build + test.

## 6. Feedback de Especificação
- [Nenhum problema identificado]

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/core build
pnpm --filter @plataforma/core test
```

### Checklist
- [ ] Desempate determinístico (hash, não ordem de chegada)?
- [ ] Comutativo: N peers simultâneos não geram fork?
- [ ] Co-assinatura spec-driven?
- [ ] 8 casos de teste passando?
- [ ] `pnpm --filter @plataforma/core build` e `test` verdes?

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-07-03 13:26:06]** - *system* - `[Migrado]`: spec_status:triaged → status:draft:triaged

- **[2026-07-03 13:28:59]** - *system* - `[Migrado]`: spec_status:draft → status:draft:placeholder
- **[2026-07-03T20:02]** - *system* - `[Triado]`: Triagem em lote do backlog
