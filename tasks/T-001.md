---
id: T-001
title: "Bootstrap do monorepo"
status: done
complexity: 2
target_agent: devops_agent
reviewer_agent: agile_reviewer 
execution_mode: sequential
dependencies: []
blocks: ["T-002", "T-004"]
---

# T-001 · Bootstrap do monorepo (Exemplo Gold Standard)

> **ALVO: repo NOVO do produto (`superapp`), vazio — bootstrap GREENFIELD.** Esta task roda na
> worktree do `superapp` (criada por `pnpm wt new T-001`), NÃO no repo de controle (`Docs`). Crie a
> fundação do zero (`package.json` raiz, `pnpm-workspace.yaml`, `turbo.json`, `tsconfig.base.json`,
> linter) + os pacotes vazios. O `nexus`/MGTIA NÃO existe aqui — é outro repo (o controle).

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** \`pnpm\` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (você o configura nesta task — greenfield).
- **Test Runner:** \`vitest\` (pacotes core/protocol) e \`playwright\` (E2E/Frontend)
- **Capacidade-alvo:** haiku

## 1. Objetivo
Criar a fundação física do projeto (monorepo). Configurar \`pnpm workspaces\`, \`turborepo\`, \`tsconfig.base.json\`, linter, e instanciar os pacotes raiz (ainda vazios) exigidos pela arquitetura.

## 2. Contexto RAG (Spec-Driven Development)
- [visao-arquitetural.md](../docs/visao-arquitetural.md) (Seção: Arquitetura de Implementação)
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (Seção: 1.1 Monorepo)

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[CREATE]** \`package.json\` (raiz: \`private\`, workspaces, scripts \`build\`/\`test\`/\`lint\` via turbo)
- **[CREATE]** \`pnpm-workspace.yaml\` (mapeando \`packages/*\` e \`apps/*\`)
- **[CREATE]** \`turbo.json\` (pipelines \`build\`/\`lint\`/\`dev\`)
- **[CREATE]** \`tsconfig.base.json\` (regras estritas TS)
- **[CREATE]** Diretórios e pacotes: 
  - \`packages/protocol/package.json\` e \`src/index.ts\`
  - \`packages/crypto/package.json\` e \`src/index.ts\`
  - \`packages/core/package.json\` e \`src/index.ts\`
  - \`packages/transport/package.json\` e \`src/index.ts\`
  - \`packages/media/package.json\` e \`src/index.ts\`
  - \`packages/workers/package.json\` e \`src/index.ts\`
  - \`packages/client-sdk/package.json\` e \`src/index.ts\`
  - \`packages/testkit/package.json\` e \`src/index.ts\`
  - \`apps/system-peer/package.json\` e \`src/index.ts\`
  - \`apps/bancada/package.json\` e \`src/index.ts\`
  - \`apps/web/package.json\` e \`src/index.ts\`
- **[CREATE]** \`tests/mock.test.ts\` para CADA sub-pacote (para provar que o Vitest orquestrado roda).

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [ ] **Framework:** \`vitest\` orquestrado pelo \`turborepo\`.
- [ ] **Métricas/Cobertura:** Apenas testes estruturais mockados nesta fase. Não há métrica de cobertura de linhas exigida na T-001. O critério de sucesso é binário: o CI de build passa ou não passa.
- [ ] **Ambiente do Teste:** Node puro, executado a partir do diretório raiz.
- [ ] **Fora de Escopo:** Testes lógicos reais.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** instale bibliotecas de features (\`react\`, \`express\`, \`sqlite\`). Instale APENAS devDependencies necessárias para fundação (\`turbo\`, \`vitest\`, \`typescript\`, \`eslint\`, \`typescript-eslint\`, \`prettier\`).
> - **NÃO** crie lógicas dentro dos \`index.ts\`. Exporte apenas \`export const VERSION = "0.0.1";\`.
> - **NÃO** edite arquivos fora do Escopo de Arquivos listado na Seção 3.

> **📄 TEMPLATES OBRIGATÓRIOS (Use EXATAMENTE como descrito):**
> 
> **\`tsconfig.base.json\`**:
> \`\`\`json
> {
>   "compilerOptions": {
>     "strict": true,
>     "noUncheckedIndexedAccess": true,
>     "exactOptionalPropertyTypes": true,
>     "noPropertyAccessFromIndexSignature": true,
>     "moduleResolution": "bundler",
>     "target": "ESNext"
>   }
> }
> \`\`\`
> 
> **\`package.json\` base para todos os pacotes**:
> \`\`\`json
> {
>   "name": "@plataforma/<nome>",
>   "version": "0.0.1",
>   "private": true,
>   "type": "module",
>   "exports": { ".": "./src/index.ts" },
>   "scripts": {
>     "build": "tsc",
>     "test": "vitest run",
>     "lint": "eslint src/"
>   },
>   "devDependencies": {
>     "typescript": "^5.8.0",
>     "vitest": "^3.0.0",
>     "eslint": "^9.0.0",
>     "typescript-eslint": "^8.0.0"
>   }
> }
> \`\`\`
> 
> **\`eslint.config.js\` na raiz**:
> \`\`\`javascript
> import tseslint from "typescript-eslint";
> export default tseslint.config(
>   ...tseslint.configs.strictTypeChecked,
>   {
>     languageOptions: {
>       parserOptions: {
>         project: "./tsconfig.base.json"
>       }
>     }
>   }
> );
> \`\`\`

1. **[TDD]** Crie os arquivos \`tests/mock.test.ts\` em todos os 11 pacotes com \`expect(true).toBe(true)\`.
2. Configure \`pnpm-workspace.yaml\` e o \`package.json\` raiz com os scripts de build/test via turbo.
3. Crie os pacotes listados na Seção 3 com os respectivos \`package.json\` nomeados (\`@plataforma/protocol\`, etc).
4. Configure \`turbo.json\`.
5. Valide rodando \`pnpm install\` seguido de \`pnpm -r build\` e \`pnpm -r test\`.

## 6. Feedback de Especificação (Spec Feedback Loop)
> **Refinamentos aplicados pelo Worker para o Gate passar (greenfield com pnpm 11 + turbo 2 +
> typescript-eslint 8) — absorver nos templates desta spec:**
> - **Build scripts (pnpm 11):** o pnpm bloqueia postinstall de dependências por padrão. Adicionar
>   ao `pnpm-workspace.yaml`: `allowBuilds:` → `esbuild: true` (campo do pnpm 11; `onlyBuiltDependencies`
>   é do pnpm 10 e é ignorado). Sem isso, `pnpm install` sai com `ERR_PNPM_IGNORED_BUILDS` e o Gate trava.
> - **`tsconfig.base.json`:** o template não emitia — faltam `module: "ESNext"`, `declaration`,
>   `skipLibCheck` e `outDir`/`include` por pacote. Cada pacote ganhou um `tsconfig.json` que estende a base.
> - **`eslint.config.js`:** `project: "./tsconfig.base.json"` quebra em monorepo (path relativo por pacote).
>   Trocado por `projectService: true` + `tsconfigRootDir`, que acha o `tsconfig.json` de cada pacote.
> - **`turbo.json`:** turbo 2 usa a chave `tasks` (não `pipeline`).

## 7. Definition of Done (DoD) & Reviewer Checklist

### Verificação automática (Gate de Evidência)
O Worker deve colar a saída literal destes comandos na Seção 8 (Handover), rodados na raiz do
`superapp` (greenfield — só há os pacotes desta task):
```bash
pnpm install
pnpm -r build
pnpm -r test
pnpm -r lint
```
Todos devem retornar Exit Code 0.

### Checklist do Reviewer
O agente \`agile_reviewer\` usará esta checklist para aprovar ou rejeitar o PR:
- [ ] O \`pnpm-workspace.yaml\` mapeia \`packages/*\` e \`apps/*\`?
- [ ] Existem os 11 pacotes/apps na árvore, exportando apenas uma constante básica?
- [ ] \`pnpm -r build && test && lint\` passam (Exit Code 0)?
- [ ] Não há vazamento de dependências de runtime onde deveriam ser devDependencies?

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- **[Claude]** Bootstrap greenfield concluído no `superapp` (branch `task/T-001`): 8 packages
  (protocol, crypto, core, transport, media, workers, client-sdk, testkit) + 3 apps (system-peer,
  bancada, web), cada um com `package.json @plataforma/*`, `src/index.ts` (só `export const VERSION`),
  `tests/mock.test.ts` e `tsconfig.json`. Raiz: package.json + pnpm-workspace.yaml + turbo.json +
  tsconfig.base.json + eslint.config.js + .npmrc. Refinamentos de template na Seção 6.
- **Gate de Evidência — todos Exit Code 0:**
```
---
id: T-001
title: "Bootstrap do monorepo"
status: review
complexity: 2
target_agent: devops_agent
reviewer_agent: agile_reviewer 
execution_mode: sequential
dependencies: []
blocks: ["T-002", "T-004"]
---

# T-001 · Bootstrap do monorepo (Exemplo Gold Standard)

> **ALVO: repo NOVO do produto (`superapp`), vazio — bootstrap GREENFIELD.** Esta task roda na
> worktree do `superapp` (criada por `pnpm wt new T-001`), NÃO no repo de controle (`Docs`). Crie a
> fundação do zero (`package.json` raiz, `pnpm-workspace.yaml`, `turbo.json`, `tsconfig.base.json`,
> linter) + os pacotes vazios. O `nexus`/MGTIA NÃO existe aqui — é outro repo (o controle).

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (você o configura nesta task — greenfield).
- **Test Runner:** `vitest` (pacotes core/protocol) e `playwright` (E2E/Frontend)
- **Capacidade-alvo:** haiku

## 1. Objetivo
Criar a fundação física do projeto (monorepo). Configurar `pnpm workspaces`, `turborepo`, `tsconfig.base.json`, linter, e instanciar os pacotes raiz (ainda vazios) exigidos pela arquitetura.

## 2. Contexto RAG (Spec-Driven Development)
- [visao-arquitetural.md](../docs/visao-arquitetural.md) (Seção: Arquitetura de Implementação)
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (Seção: 1.1 Monorepo)

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[CREATE]** `package.json` (raiz: `private`, workspaces, scripts `build`/`test`/`lint` via turbo)
- **[CREATE]** `pnpm-workspace.yaml` (mapeando `packages/*` e `apps/*`)
- **[CREATE]** `turbo.json` (pipelines `build`/`lint`/`dev`)
- **[CREATE]** `tsconfig.base.json` (regras estritas TS)
- **[CREATE]** Diretórios e pacotes: 
  - `packages/protocol/package.json` e `src/index.ts`
  - `packages/crypto/package.json` e `src/index.ts`
  - `packages/core/package.json` e `src/index.ts`
  - `packages/transport/package.json` e `src/index.ts`
  - `packages/media/package.json` e `src/index.ts`
  - `packages/workers/package.json` e `src/index.ts`
  - `packages/client-sdk/package.json` e `src/index.ts`
  - `packages/testkit/package.json` e `src/index.ts`
  - `apps/system-peer/package.json` e `src/index.ts`
  - `apps/bancada/package.json` e `src/index.ts`
  - `apps/web/package.json` e `src/index.ts`
- **[CREATE]** `tests/mock.test.ts` para CADA sub-pacote (para provar que o Vitest orquestrado roda).

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [ ] **Framework:** `vitest` orquestrado pelo `turborepo`.
- [ ] **Métricas/Cobertura:** Apenas testes estruturais mockados nesta fase. Não há métrica de cobertura de linhas exigida na T-001. O critério de sucesso é binário: o CI de build passa ou não passa.
- [ ] **Ambiente do Teste:** Node puro, executado a partir do diretório raiz.
- [ ] **Fora de Escopo:** Testes lógicos reais.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** instale bibliotecas de features (`react`, `express`, `sqlite`). Instale APENAS devDependencies necessárias para fundação (`turbo`, `vitest`, `typescript`, `eslint`, `typescript-eslint`, `prettier`).
> - **NÃO** crie lógicas dentro dos `index.ts`. Exporte apenas `export const VERSION = "0.0.1";`.
> - **NÃO** edite arquivos fora do Escopo de Arquivos listado na Seção 3.

> **📄 TEMPLATES OBRIGATÓRIOS (Use EXATAMENTE como descrito):**
> 
> **`tsconfig.base.json`**:
> ```json
> {
>   "compilerOptions": {
>     "strict": true,
>     "noUncheckedIndexedAccess": true,
>     "exactOptionalPropertyTypes": true,
>     "noPropertyAccessFromIndexSignature": true,
>     "moduleResolution": "bundler",
>     "target": "ESNext"
>   }
> }
> ```
> 
> **`package.json` base para todos os pacotes**:
> ```json
> {
>   "name": "@plataforma/<nome>",
>   "version": "0.0.1",
>   "private": true,
>   "type": "module",
>   "exports": { ".": "./src/index.ts" },
>   "scripts": {
>     "build": "tsc",
>     "test": "vitest run",
>     "lint": "eslint src/"
>   },
>   "devDependencies": {
>     "typescript": "^5.8.0",
>     "vitest": "^3.0.0",
>     "eslint": "^9.0.0",
>     "typescript-eslint": "^8.0.0"
>   }
> }
> ```
> 
> **`eslint.config.js` na raiz**:
> ```javascript
> import tseslint from "typescript-eslint";
> export default tseslint.config(
>   ...tseslint.configs.strictTypeChecked,
>   {
>     languageOptions: {
>       parserOptions: {
>         project: "./tsconfig.base.json"
>       }
>     }
>   }
> );
> ```

1. **[TDD]** Crie os arquivos `tests/mock.test.ts` em todos os 11 pacotes com `expect(true).toBe(true)`.
2. Configure `pnpm-workspace.yaml` e o `package.json` raiz com os scripts de build/test via turbo.
3. Crie os pacotes listados na Seção 3 com os respectivos `package.json` nomeados (`@plataforma/protocol`, etc).
4. Configure `turbo.json`.
5. Valide rodando `pnpm install` seguido de `pnpm -r build` e `pnpm -r test`.

## 6. Feedback de Especificação (Spec Feedback Loop)
> **Refinamentos aplicados pelo Worker para o Gate passar (greenfield com pnpm 11 + turbo 2 +
> typescript-eslint 8) — absorver nos templates desta spec:**
> - **Build scripts (pnpm 11):** o pnpm bloqueia postinstall de dependências por padrão. Adicionar
>   ao `pnpm-workspace.yaml`: `allowBuilds:` → `esbuild: true` (campo do pnpm 11; `onlyBuiltDependencies`
>   é do pnpm 10 e é ignorado). Sem isso, `pnpm install` sai com `ERR_PNPM_IGNORED_BUILDS` e o Gate trava.
> - **`tsconfig.base.json`:** o template não emitia — faltam `module: "ESNext"`, `declaration`,
>   `skipLibCheck` e `outDir`/`include` por pacote. Cada pacote ganhou um `tsconfig.json` que estende a base.
> - **`eslint.config.js`:** `project: "./tsconfig.base.json"` quebra em monorepo (path relativo por pacote).
>   Trocado por `projectService: true` + `tsconfigRootDir`, que acha o `tsconfig.json` de cada pacote.
> - **`turbo.json`:** turbo 2 usa a chave `tasks` (não `pipeline`).

## 7. Definition of Done (DoD) & Reviewer Checklist

### Verificação automática (Gate de Evidência)
O Worker deve colar a saída literal destes comandos na Seção 8 (Handover), rodados na raiz do
`superapp` (greenfield — só há os pacotes desta task):
```bash
pnpm install
pnpm -r build
pnpm -r test
pnpm -r lint
```
Todos devem retornar Exit Code 0.

### Checklist do Reviewer
O agente `agile_reviewer` usará esta checklist para aprovar ou rejeitar o PR:
- [ ] O `pnpm-workspace.yaml` mapeia `packages/*` e `apps/*`?
- [ ] Existem os 11 pacotes/apps na árvore, exportando apenas uma constante básica?
- [ ] `pnpm -r build && test && lint` passam (Exit Code 0)?
- [ ] Não há vazamento de dependências de runtime onde deveriam ser devDependencies?

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- **[Claude]** Bootstrap greenfield concluído no `superapp` (branch `task/T-001`): 8 packages
  (protocol, crypto, core, transport, media, workers, client-sdk, testkit) + 3 apps (system-peer,
  bancada, web), cada um com `package.json @plataforma/*`, `src/index.ts` (só `export const VERSION`),
  `tests/mock.test.ts` e `tsconfig.json`. Raiz: package.json + pnpm-workspace.yaml + turbo.json +
  tsconfig.base.json + eslint.config.js + .npmrc. Refinamentos de template na Seção 6.
- **Gate de Evidência — todos Exit Code 0:**
```bash
$ pnpm install   → esbuild postinstall: Done · Done in 4.3s (pnpm v11.1.2) · exit: 0
$ pnpm -r build  → 11/11 pacotes: "tsc … Done" · exit: 0
$ pnpm -r test   → 11/11 pacotes: "Test Files 1 passed | Tests 1 passed" (vitest 3.2.6) · exit: 0
$ pnpm -r lint   → 11/11 pacotes: "eslint src/ … Done" (zero violações) · exit: 0
```

### Parecer do Agente Revisor (Reviewer):
- [x] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória):**
  ```bash
  # pnpm install
  Scope: all 12 workspace projects
  Already up to date
  Done in 620ms using pnpm v11.1.2

  # pnpm -r build
  Scope: 11 of 12 workspace projects
  apps/bancada build$ tsc
  apps/system-peer build$ tsc
  apps/web build$ tsc
  packages/client-sdk build$ tsc
  packages/client-sdk build: Done
  apps/bancada build: Done
  packages/core build$ tsc
  apps/system-peer build: Done
  packages/crypto build$ tsc
  packages/media build$ tsc
  apps/web build: Done
  packages/protocol build$ tsc
  packages/media build: Done
  packages/testkit build$ tsc
  packages/crypto build: Done
  packages/transport build$ tsc
  packages/core build: Done
  packages/workers build$ tsc
  packages/protocol build: Done
  packages/transport build: Done
  packages/testkit build: Done
  packages/workers build: Done

  # pnpm -r test
  Scope: 11 of 12 workspace projects
  apps/bancada test$ vitest run
  apps/system-peer test$ vitest run
  packages/client-sdk test$ vitest run
  apps/web test$ vitest run
  apps/system-peer test:  RUN  v3.2.6 C:/Dev2026/.superapp-worktrees/T-001/apps/system-peer
  apps/bancada test:  RUN  v3.2.6 C:/Dev2026/.superapp-worktrees/T-001/apps/bancada
  packages/client-sdk test:  RUN  v3.2.6 C:/Dev2026/.superapp-worktrees/T-001/packages/client-sdk
  apps/web test:  RUN  v3.2.6 C:/Dev2026/.superapp-worktrees/T-001/apps/web
  apps/web test:  ✓ tests/mock.test.ts (1 test) 2ms
  apps/bancada test:  ✓ tests/mock.test.ts (1 test) 2ms
  apps/system-peer test:  ✓ tests/mock.test.ts (1 test) 2ms
  packages/client-sdk test:  ✓ tests/mock.test.ts (1 test) 2ms
  apps/system-peer test:  Test Files  1 passed (1)
  apps/bancada test:  Test Files  1 passed (1)
  apps/bancada test:       Tests  1 passed (1)
  apps/bancada test:    Start at  08:50:32
  apps/web test:  Test Files  1 passed (1)
  apps/web test:       Tests  1 passed (1)
  apps/web test:    Start at  08:50:32
  apps/system-peer test:       Tests  1 passed (1)
  apps/system-peer test:    Start at  08:50:32
  apps/bancada test:    Duration  1.33s (transform 159ms, setup 0ms, collect 158ms, tests 2ms, environment 0ms, prepare 242ms)
  apps/web test:    Duration  1.33s (transform 146ms, setup 0ms, collect 145ms, tests 2ms, environment 0ms, prepare 275ms)
  apps/system-peer test:    Duration  1.33s (transform 298ms, setup 0ms, collect 283ms, tests 2ms, environment 0ms, prepare 142ms)
  packages/client-sdk test:  Test Files  1 passed (1)
  packages/client-sdk test:       Tests  1 passed (1)
  packages/client-sdk test:    Start at  08:50:32
  packages/client-sdk test:    Duration  1.39s (transform 53ms, setup 0ms, collect 50ms, tests 2ms, environment 0ms, prepare 391ms)
  apps/system-peer test: Done
  apps/bancada test: Done
  packages/core test$ vitest run
  packages/crypto test$ vitest run
  apps/web test: Done
  packages/media test$ vitest run
  packages/client-sdk test: Done
  packages/protocol test$ vitest run
  packages/crypto test:  RUN  v3.2.6 C:/Dev2026/.superapp-worktrees/T-001/packages/crypto
  packages/media test:  RUN  v3.2.6 C:/Dev2026/.superapp-worktrees/T-001/packages/media
  packages/protocol test:  RUN  v3.2.6 C:/Dev2026/.superapp-worktrees/T-001/packages/protocol
  packages/core test:  RUN  v3.2.6 C:/Dev2026/.superapp-worktrees/T-001/packages/core
  packages/media test:  ✓ tests/mock.test.ts (1 test) 2ms
  packages/media test:  Test Files  1 passed (1)
  packages/media test:       Tests  1 passed (1)
  packages/media test:    Start at  08:50:35
  packages/media test:    Duration  1.10s (transform 84ms, setup 0ms, collect 82ms, tests 2ms, environment 0ms, prepare 166ms)
  packages/core test:  ✓ tests/mock.test.ts (1 test) 2ms
  packages/core test:  Test Files  1 passed (1)
  packages/core test:       Tests  1 passed (1)
  packages/core test:    Start at  08:50:35
  packages/core test:    Duration  1.21s (transform 51ms, setup 0ms, collect 51ms, tests 2ms, environment 0ms, prepare 311ms)
  packages/protocol test:  ✓ tests/mock.test.ts (1 test) 2ms
  packages/crypto test:  ✓ tests/mock.test.ts (1 test) 2ms
  packages/crypto test:  Test Files  1 passed (1)
  packages/protocol test:  Test Files  1 passed (1)
  packages/protocol test:       Tests  1 passed (1)
  packages/protocol test:    Start at  08:50:35
  packages/crypto test:       Tests  1 passed (1)
  packages/crypto test:    Start at  08:50:35
  packages/crypto test:    Duration  1.36s (transform 72ms, setup 0ms, collect 68ms, tests 2ms, environment 0ms, prepare 222ms)
  packages/protocol test:    Duration  1.31s (transform 63ms, setup 0ms, collect 61ms, tests 2ms, environment 0ms, prepare 154ms)
  packages/media test: Done
  packages/testkit test$ vitest run
  packages/core test: Done
  packages/transport test$ vitest run
  packages/crypto test: Done
  packages/workers test$ vitest run
  packages/protocol test: Done
  packages/testkit test:  RUN  v3.2.6 C:/Dev2026/.superapp-worktrees/T-001/packages/testkit
  packages/transport test:  RUN  v3.2.6 C:/Dev2026/.superapp-worktrees/T-001/packages/transport
  packages/workers test:  RUN  v3.2.6 C:/Dev2026/.superapp-worktrees/T-001/packages/workers
  packages/testkit test:  ✓ tests/mock.test.ts (1 test) 2ms
  packages/testkit test:  Test Files  1 passed (1)
  packages/testkit test:       Tests  1 passed (1)
  packages/testkit test:    Start at  08:50:37
  packages/testkit test:    Duration  774ms (transform 60ms, setup 0ms, collect 57ms, tests 2ms, environment 0ms, prepare 282ms)
  packages/transport test:  ✓ tests/mock.test.ts (1 test) 2ms
  packages/transport test:  Test Files  1 passed (1)
  packages/transport test:       Tests  1 passed (1)
  packages/transport test:    Start at  08:50:37
  packages/transport test:    Duration  813ms (transform 42ms, setup 0ms, collect 39ms, tests 2ms, environment 0ms, prepare 169ms)
  packages/testkit test: Done
  packages/workers test:  ✓ tests/mock.test.ts (1 test) 2ms
  packages/workers test:  Test Files  1 passed (1)
  packages/workers test:       Tests  1 passed (1)
  packages/workers test:    Start at  08:50:37
  packages/workers test:    Duration  903ms (transform 58ms, setup 0ms, collect 56ms, tests 2ms, environment 0ms, prepare 126ms)
  packages/transport test: Done
  packages/workers test: Done

  # pnpm -r lint
  Scope: 11 of 12 workspace projects
  apps/system-peer lint$ eslint src/
  apps/bancada lint$ eslint src/
  apps/web lint$ eslint src/
  packages/client-sdk lint$ eslint src/
  packages/client-sdk lint: Done
  packages/core lint$ eslint src/
  apps/bancada lint: Done
  packages/crypto lint$ eslint src/
  apps/system-peer lint: Done
  packages/media lint$ eslint src/
  apps/web lint: Done
  packages/core lint: Done
  packages/media lint: Done
  packages/protocol lint$ eslint src/
  packages/testkit lint$ eslint src/
  packages/transport lint$ eslint src/
  packages/workers lint$ eslint src/
  packages/crypto lint: Done
  packages/protocol lint: Done
  packages/testkit lint: Done
  packages/transport lint: Done
  packages/workers lint: Done
  ```
- **Comentários de Revisão:** Bootstrap do monorepo superapp executado de acordo com a especificação Greenfield. Todos os 11 pacotes e apps foram criados no escopo especificado, o Vitest está configurado e integrado de forma a orquestrar os testes, o build com `tsc` e linting com `eslint` passam sem qualquer erro. Adicionalmente, foi executado um teste sonda de resolução e exportação do TS provando que o setup está íntegro para importação entre diretórios. Aprovado para merge.

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-06-20T18:09]** - *DeepSeek* - `[Iniciado]`: iniciando bootstrap do monorepo superapp
- **[2026-06-21T11:29]** - *Claude* - `[Finalizado]`: Bootstrap do monorepo: 11 pacotes/apps; Gate 4/4 verde (install/build/test/lint, todos exit 0; 11 testes mock passando). Refinamentos pnpm11/turbo2/eslint na Secao 6.
- **[2026-06-21T11:56]** - *agile_reviewer* - `[Aprovado]`: Bootstrap do monorepo superapp aprovado com 11 pacotes configurados e Gate 100% verde (build, test, lint).
