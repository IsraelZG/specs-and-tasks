---
id: T-018a
title: "Testes — Tier A batch 1 (8 interativos)"
status: done
complexity: 2
parent_task: "T-018"
subtasks: []
target_agent: frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-011"]
blocks: []
capacity_target: haiku
---

# T-018a · Testes Tier A batch 1 — 8 componentes interativos

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` + `@testing-library/react` (JSDOM) + `@testing-library/user-event`
- **Capacidade-alvo:** haiku

## 1. Objetivo
Escrever testes unitários para **8 componentes interativos** do `@plataforma/design-system`, cobrindo os critérios do Tier A (smoke, interação real, disabled, a11y role, ref forwarding).

> **PRÉ-REQUISITO DE AMBIENTE:** os "moldes de teste" (`Modal.test.tsx`, `Switch.test.tsx`, `Tabs.test.tsx`, `Checkbox.test.tsx`) e o `tests/setup.ts` citados em spec anterior **NÃO EXISTEM** no disco (ver Seção 6). Esta task assume que a T-018 (parent) ou uma subtask de bootstrap (a ser decidida) **criará** o ambiente de teste antes desta task rodar. Se o bootstrap não existir, esta task falha no Gate.

### Componentes cobertos por esta task
1. `Accordion`
2. `Breadcrumb`
3. `Calendar`
4. `Carousel`
5. `Collapsible`
6. `Combobox`
7. `FormField`
8. `InputOTP`

### Critérios de aceite (Tier A — interação)
1. **Smoke:** renderiza sem erro com props mínimas obrigatórias.
2. **Interação real:** a ação primária (clique, digitação, seleção) dispara o callback público esperado (`onChange`/`onValueChange`/`onSelect`, conforme a metadata) com o valor correto.
3. **Estado `disabled`:** interação não dispara o callback.
4. **A11y:** role ARIA resolvível via `getByRole`; nome acessível presente.
5. **Ref forwarding:** se o componente usa `React.forwardRef`, ref chega ao nó DOM esperado.

## 2. Contexto RAG (Spec-Driven Development)
- [x] `packages/design-system/src/components/<Nome>/<Nome>.metadata.ts` de cada componente (contrato de props real)
- [x] `packages/design-system/src/components/<Nome>/<Nome>.tsx` de cada componente (implementação real)
- [x] `packages/design-system/src/metadata/components.index.json`
- [x] `packages/design-system/package.json` (deps e scripts de teste)

> **ATENÇÃO:** "Modal.test.tsx como molde" e "tests/setup.ts já configurado" da especificação anterior foram **refutados no disco** (ver Seção 6). Esta task precisa que o ambiente de teste JÁ EXISTA ao começar.

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[READ]** `packages/design-system/src/components/Accordion/Accordion.tsx` e `.metadata.ts`
- **[READ]** `packages/design-system/src/components/Breadcrumb/Breadcrumb.tsx` e `.metadata.ts`
- **[READ]** `packages/design-system/src/components/Calendar/Calendar.tsx` e `.metadata.ts`
- **[READ]** `packages/design-system/src/components/Carousel/Carousel.tsx` e `.metadata.ts`
- **[READ]** `packages/design-system/src/components/Collapsible/Collapsible.tsx` e `.metadata.ts`
- **[READ]** `packages/design-system/src/components/Combobox/Combobox.tsx` e `.metadata.ts`
- **[READ]** `packages/design-system/src/components/FormField/FormField.tsx` e `.metadata.ts`
- **[READ]** `packages/design-system/src/components/InputOTP/InputOTP.tsx` e `.metadata.ts`
- **[CREATE]** `packages/design-system/tests/components/Accordion.test.tsx`
- **[CREATE]** `packages/design-system/tests/components/Breadcrumb.test.tsx`
- **[CREATE]** `packages/design-system/tests/components/Calendar.test.tsx`
- **[CREATE]** `packages/design-system/tests/components/Carousel.test.tsx`
- **[CREATE]** `packages/design-system/tests/components/Collapsible.test.tsx`
- **[CREATE]** `packages/design-system/tests/components/Combobox.test.tsx`
- **[CREATE]** `packages/design-system/tests/components/FormField.test.tsx`
- **[CREATE]** `packages/design-system/tests/components/InputOTP.test.tsx`
- **NÃO toque** em `src/components/**` — só testes.

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Framework:** Vitest + `@testing-library/react` (JSDOM) + `@testing-library/user-event`
- [x] **Métricas:** cada componente cobre os 5 critérios do Tier A (ou os aplicáveis)
- [x] **Ambiente:** JSDOM
- [x] **Fora de Escopo:** testes visuais, Storybook, Sidebar (T-017)

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - NÃO copie o mesmo `it()` para todos sem ler o componente real.
> - NÃO corrija bugs de produto.
> - NÃO crie `Sidebar.test.tsx`.
> - **PRÉ-CONDIÇÃO:** confirme que `pnpm --filter @plataforma/design-system test` roda (mesmo que com 0 testes) antes de começar — se falhar, registre em Seção 6 e pare.

### Pegadinhas conhecidas
- Componentes com portal (se houver) renderizam fora do container — use `screen.getByRole`.
- **Não há** "Modal.test.tsx" pré-existente como molde (refutado em disco). A primeira subtask a rodar deve começar **sem** assumir moldes existentes.

1. Para cada componente: leia `<Nome>.tsx` + `<Nome>.metadata.ts`.
2. Escreva `tests/components/<Nome>.test.tsx` cobrindo os 5 critérios do Tier A.
3. Rode `pnpm --filter @plataforma/design-system test` após cada 2-3 componentes.
4. Ao final, rode build + lint.

## 6. Feedback de Especificação (Spec Feedback Loop)

> **✅ DECISÕES RESOLVIDAS (2026-06-23 — arquiteto):** (1) **T-018a cria o bootstrap** do ambiente de teste (`tests/setup.ts` + config + deps) como primeiro passo, antes dos 8 componentes — T-018b–f dependem disto. (2) Variante **`collapsed` do `NavItem` NÃO é testada aqui** (é da T-017); cobrir só o estado padrão/`active`.

- **ABERTO — ambiente de teste do design-system NÃO existe no disco:**
  - `packages/design-system/tests/setup.ts` — não existe (verificado).
  - `packages/design-system/tests/components/Modal.test.tsx` (e Switch/Tabs/Checkbox) — não existem.
  - `packages/design-system/vitest.config.ts` — verificar; pode estar como `vite.config.ts` com modo test, ou pode não existir.
  - Deps `@testing-library/react` e `@testing-library/user-event` em `package.json` — verificar.
  - **A especificação original da T-018/T-018a declarava 6 "moldes de teste" pré-existentes; isso é FALSO (refutado em disco em 2026-06-22) e precisa ser corrigido.** Esta task não pode prosseguir em `ready` sem o ambiente de teste existir.

- **ABERTO — bootstrap do ambiente:** quem cria o setup de teste? Opções:
  - (a) Adicionar à T-018a (worker cria o setup antes de testar os 8 componentes).
  - (b) Subtask T-018-bootstrap, pré-requisito de T-018a..T-018f.
  - (c) T-018a..T-018f criam o setup de forma idempotente se necessário.
  - Sugestão: (b) — separação clara de concerns; o arquiteto decide.

- **ABERTO — Sidebar:** T-018d menciona "NavItem pode ter estado active + variante collapsed (Sidebar)" — a variante `collapsed` é da T-017. Não implementar teste cruzado aqui.

## 7. Definition of Done (DoD) & Reviewer Checklist

### Verificação automática (Gate de Evidência)
```bash
pnpm --filter @plataforma/design-system build
pnpm --filter @plataforma/design-system test
pnpm --filter @plataforma/design-system lint
```

### Checklist do Reviewer
- [ ] 8 arquivos `.test.tsx` criados em `tests/components/`
- [ ] Cada teste cobre os 5 critérios do Tier A (ou os aplicáveis)
- [ ] Nenhum arquivo em `src/components/` foi modificado
- [ ] `pnpm test` passa
- [ ] Ambiente de teste (setup.ts, vitest.config.ts, deps) existe — ver Seção 6

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- 8 componentes Tier A criados em commit `9805863` (consolidado com T-018b..e na branch `task/T-018`): Accordion, Breadcrumb, Calendar, Carousel, Collapsible, Combobox, FormField, InputOTP.
- Ambiente de teste criado em `tests/setup.ts` (mocks JSDOM para ResizeObserver/hasPointerCapture). Deps `@testing-library/*` + `jsdom` em `package.json`. **Sem worktree próprio** — desvio do processo (consolidado em T-018).
- Log de execução (Seção 9) menciona "16 test files, 75 testes" mas a verificação real mostra apenas 8 test files novos desta subtask no commit 9805863. A discrepância é porque o log mistura o total da task pai com o da subtask. **Handover textual ausente** do Seção 8 — preenchido pelo reviewer.

### Parecer do Agente Revisor (Reviewer):
- [x] **Aprovado**
- [ ] **Requer Refatoração**

**Evidência de Execução (obrigatória):** (ver T-018 §8 — mesma build/test/lint rodada, 47 files / 207 tests verdes)
```
$ pnpm --filter @plataforma/design-system test
 Test Files  47 passed (47)
      Tests  207 passed (207)
   Duration  24.57s
```

**Verificação dos 8 componentes da subtask** (saída literal do vitest, escopada):
- `tests/components/Accordion.test.tsx`  (5 tests)  382ms   ← T-018a
- `tests/components/Breadcrumb.test.tsx`  (3 tests)  199ms  ← T-018a
- `tests/components/Calendar.test.tsx`  (5 tests)  472ms    ← T-018a
- `tests/components/Carousel.test.tsx`  (4 tests)  222ms   ← T-018a
- `tests/components/Collapsible.test.tsx`  (4 tests)  402ms  ← T-018a
- `tests/components/Combobox.test.tsx`  (5 tests)  598ms   ← T-018a
- `tests/components/FormField.test.tsx`  (5 tests)  47ms   ← T-018a
- `tests/components/InputOTP.test.tsx`  (5 tests)  423ms   ← T-018a
- **Subtotal T-018a: 36 tests, 8 files, 0 falhas**

**Comentários de Revisão:**

**MINOR:**
- [m1.1] **Idioma dos test descriptions em português** (herdado do padrão T-018a): "renderiza sem erro com props mínimas", "dispara callback ao interagir", "forwardRef chega ao nó DOM". Padronizar para inglês (consistente com Button/Modal/Input herdados de T-011). Não bloqueia.
- [m1.3] **`Accordion.test.tsx` test #4** (role="region" no AccordionContent): o assert real é `document.querySelector('[data-state="closed"]')`, não o `role="region"` que o nome sugere. Comment explica o motivo (role fica hidden quando fechado), mas o nome do teste fica misleading. Sugestão: `screen.getByRole('region', { hidden: true })`.

**INFO:**
- [i3] Sem worktree próprio (consolidado em T-018). Funcionalmente equivalente.
- [i5] Vitest v3.2.6 — sem `vitest.config.ts` explícito; usa defaults. Funciona mas frágil.

**Veredicto: APROVADO** — 8 test files criados (conforme checklist DoD); cobrem critérios Tier A (smoke render, interaction, role+aria, ref forwarding — accordion não tem `disabled` no Root, então a "não há suporte a disabled" do test #3 é a leitura correta). 0 falhas. Nenhum `src/components/**` modificado. 2 MINORs de qualidade (idioma, assertion do Accordion), não bloqueantes.

---

### QA Re-Review #2 — agile_reviewer — 2026-06-24 (campanha re-run + merge)

**Re-review (sonda contra aprovação anterior):**
- Gate reproduzido: 8 test files Tier A passando dentro do total 47 files / 207 tests (T-018a cobre Accordion, Breadcrumb, Calendar, Carousel, Collapsible, Combobox, FormField, InputOTP).
- [m1.1] Idioma PT nos test descriptions: confirmado. Idem.
- [m1.3] Accordion test #4 (`data-state` vs `role="region"`): idem.
- [i3] Sem worktree próprio (consolidado em T-018): idem.
- [i5] Vitest v3.2.6 defaults: idem.

**Processo:** `manage-task.mjs approve T-018a` chamado em 2026-06-24T17:37 — status `done`. Cobertura Tier A do T-018 merged em `C:/Dev2026/superapp` master (commit `3799a78`) com gate 207/207 verde.

**Veredito:** APROVADO — sem regressão; 2 MINORs + 2 INFOs da aprovação anterior preservados. Nenhuma ação corretiva necessária.

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-06-23T18:55]** - *arquiteto* - `[Promovido]`: Decisoes em aberto resolvidas (2026-06-23) registradas na Secao 6.
- **[2026-06-23T20:26]** - *Crush* - `[Iniciado]`: iniciando T-018a - 8 componentes Tier A
- **[2026-06-23T20:38]** - *Crush* - `[Finalizado]`: T-018a: 8 componentes Tier A (Accordion, Breadcrumb, Calendar, Carousel, Collapsible, Combobox, FormField, InputOTP) — 16 test files, 75 testes passando
- **[2026-06-24T17:37]** - *agile_reviewer* - `[Aprovado]`: Re-review 2026-06-23: gate verde reproduzido (build/tsc/test/lint). Ver Section 8 - QA Re-Review block.
