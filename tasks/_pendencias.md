# Ledger de Pendências (não-bloqueantes)

> Achados **não-bloqueantes** (MAJOR/MINOR/INFO) que reviewers encontraram mas que **não**
> impedem o merge. Em vez de virarem tasks `-followup` (que poluem a fila), acumulam aqui.
> Periodicamente, `/agrupar-cleanup` agrupa por área, gera **uma** task de cleanup e remove as
> linhas consumidas. **Não edite à mão durante review** — o `integrar-task`/`qa-review --integrar`
> anexa automaticamente ao mover a task para `done`.

Formato: `- [ ] [severidade][T-XXX][pacote/área] achado — referência (arquivo:linha ou seção)`
Severidade: `M` (major não-bloqueante) · `m` (minor) · `i` (info).

---

<!-- BEGIN PENDENCIAS -->
- [ ] [m1][T-407][protocol] Spec inconsistente: §1 declara `generateOobLink(multiaddr, ephemeralPubKey, relayHint?)` com 3 params, mas `OutOfBandLink.nonce` é obrigatório e o formato `plataforma://connect#…&nonce=<b64>…` exige-no. Impl adicionou `nonce: Uint8Array` como 3º param obrigatório (outOfBand.ts:38-43) — interpretação correta. Reendurecer a spec para alinhar a assinatura com a impl antes que callers dependam dela (tasks/T-407.md:44-48 vs outOfBand.ts:38-43)
- [ ] [m1][T-407][protocol] Tamanho do `nonce` não validado em runtime — spec §1 comenta `// 16 bytes` mas `parseOobLink`/`fromQrPayload` aceitam qualquer `Uint8Array`. Não é bug (interface sem invariante) mas vale validar no boundary se `nonce` for reaproveitado por consumidor de baixo nível (outOfBand.ts:65, 69, 101)
- [ ] [m1][T-407][protocol] Mudança fora do escopo declarado em §3: `packages/protocol/src/index.ts:33-34` re-exporta `generateOobLink`/`parseOobLink`/`toQrPayload`/`fromQrPayload`/`OobLinkError`/`OutOfBandLink` — necessário para consumo externo mas não constava em [CREATE]/[EDIT] do escopo (src/index.ts:33-34)
- [ ] [i1][T-407][protocol] `outOfBand.test.ts:47,73` usam `import('../src/outOfBand.js').OutOfBandLink` inline em vez de `import type { OutOfBandLink }` no topo — cosmético (outOfBand.test.ts:47, 73)
- [ ] [m1][T-208][protocol] Mudança de escopo não declarada: ClockPort estendido com setTimeout/clearTimeout — spec §3 só declarava GenesisWriter (ports.ts:58-78)
- [ ] [m2][T-208][transport] Mudança de escopo não declarada: devDep @plataforma/testkit adicionada ao package.json — spec §3 declarava como [READ] (package.json:15)
- [ ] [m3][T-208][transport] Cobertura incompleta do caso 8: idempotência de writeGenesis só cobre GENESIS já assentado via start() idempotente, não re-entrada em GENESIS (fpp.test.ts:118-130)
- [ ] [i1][T-208][transport] onPeerFound descarta o peerId silenciosamente (void peerId) — útil para logging/diagnóstico (fpp.ts:128-130)
- [ ] [i2][T-208][transport] _emit usa console.error para exceções de handler — defensivo, não na spec (fpp.ts:240-248)
- [ ] [m1][T-212][protocol] Handover do executor contém "snapshot.ts não existe na worktree" — factualmente incorreto (arquivo existe e workaround já removido por T-308-rework-2); item de DoD §7 fica satisfeito pela rework-2 de T-308, não por T-212 — housekeeping, não bloqueia (tasks/T-212.md:119)
- [ ] [m1][T-802][media] `!` non-null assertion no arquivo de teste — spec §5 proíbe `!` no geral (src está limpo); refatorar `reordered[0]!`/`tampered[0]!`/etc para guards (verifyReassemble.test.ts:53-55, 98, 147-151, 22)
- [ ] [m1][T-1009][testkit] Desvio do contrato TS: parâmetro `pattern` em vez de `jsonEscapedPattern`; spec §1 diz "como aparece no JSON source" mas §4 caso 1 usa pattern decoded — impl escolheu a interpretação do §4 (alinhada com os testes). Spec §1 precisa ser atualizado para alinhar com a impl (psRegex.ts:13-21, 30-34)
- [ ] [m2][T-1009][testkit] Spec §3 inconsistente sobre localização do test (src/ vs tests/); impl resolveu com `tests/psRegex.test.ts` (convenção Vitest, consistente com clock.test.ts, random.test.ts do pacote). Spec §3 precisa referenciar a convenção Vitest do pacote
- [ ] [m1][T-305a][protocol] Defensive handling: extractEntityIds aceita capability com `resource: 'entity:'` (prefixo + ID vazio) e devolve `['']`, o que crasha scopeRBSRTree via BTree.validateId. Fix de 1 linha: `cap.resource.length > ENTITY_PREFIX.length` antes do add (ucanScope.ts:24-32)
- [ ] [m1][T-302a][protocol] Mudança fora do escopo declarado na §3: `packages/protocol/package.json` (devDep `@plataforma/testkit`), `packages/protocol/src/index.ts` (re-export do novo módulo) e `packages/testkit/src/index.ts` (export de `SimNetwork`) — suporte necessário mas deveria constar em [CREATE]/[EDIT] (package.json:16, src/index.ts:24-25, testkit/src/index.ts:3)
- [ ] [m2][T-302a][protocol] `respondNodes` exportado em `index.ts` mas não declarado nos Contratos exatos da §1 (aparece só na impl) — adicionar à spec ou remover do `index.ts` (src/rbsr/exchange.ts:161-175, src/index.ts:24)
- [ ] [m3][T-302a][protocol] Spec §1 declara `ids: ULID[]` em `requestNodes`; impl usa `string[]`. Equivalente em runtime (ULID é string opaca) mas desvia da assinatura nominal — alinhar (src/rbsr/exchange.ts:152-158)
- [ ] [m1][T-021][showcase] Mudança fora do escopo §3 em `apps/design-system-showcase/src/styles.css`: (a) fix de typo pré-existente no `@source` (apontava `apps/design-system` inexistente → `packages/design-system/src`) e (b) `@import` do CSS compilado do DS (`build/react/index.css`) para paridade dev/CI — workaround necessário p/ specs E2E rodarem via `pnpm dev` sem rebuild do DS; follow-up para solução permanente (preset Tailwind compartilhado ou DS sempre buildado) (src/styles.css)
- [ ] [m2][T-021][design-system] `packages/design-system/src/metadata/components.index.json` regenerado com paths absolutos máquina-específicos (`C:\Dev2026\.superapp-worktrees\T-204\…`) — smell pré-existente: o tooling regenera `metadataPath` com path absoluto; refator para path relativo (ou omitir `metadataPath`); não é lógica de runtime (components.index.json)
- [ ] [i1][T-021][showcase] `tsconfig.json` do showcase só inclui `src/` (não `e2e/`), então specs E2E ficam sem type-check no `build` (funcionam em runtime p/ Playwright); follow-up p/ estender include ou criar tsconfig dedicado p/ `e2e/` (tsconfig.json)
- [ ] [i2][T-021][design-system] Carousel `CarouselNext` nasce `disabled`: `checkScroll` roda no mount antes do layout e marca `canScrollNext=false`; spec E2E usa wheel em vez do botão Next — follow-up p/ corrigir `checkScroll` com `ResizeObserver` ou re-check pós-layout (Carousel.tsx)
- [ ] [i3][T-021][ci] Link do run de CI ausente da Seção 8 (§7 exige): workflow dispara em push p/ `master` pós-merge; evidência local cobre os 5 comandos do bloco de Verificação — capturar link do run de CI (x64+arm64) e anexar à Seção 8 (ci.yml)
- [ ] [i4][T-021][testkit] Sondas adversariais do reviewer mutaram `process.env` em runtime p/ testar `CI` toggle; preset lê `process.env.CI` síncrono na chamada. Para concorrência/mutação pós-import, o preset poderia aceitar `ci?: boolean` na `UiE2EOptions` — não é requisito da spec, apenas observação (playwright.ts)
- [ ] [m1][T-303][protocol] `RangeFooter` usa `rangeStartId`/`rangeEndId` em vez de `range: IdRange` conforme spec §1 — consumidores que esperam `footer.range.lo`/`.range.hi` quebram (rangeFooter.ts)
- [ ] [i1][T-303][protocol] `sessionNonce` aceito com qualquer tamanho — spec §1 declara "16 bytes" como contrato; adicionar validação `sessionNonce.length === 16` (rangeFooter.ts)
- [ ] [m1][T-306][protocol] `BTree` importado mas não usado em `waves.ts` — usar `type` na import ou remover (waves.ts:1)
- [ ] [i1][T-306][protocol] `executeWave2` itera `heads` mesmo com `getEntityHeads = null` (cai em `heads = []` e retorna `completed: true`) — comportamento aceitável mas não documentado; vale uma linha de comentário explicitando o fallback (waves.ts:160)
- [ ] [i4][T-306][protocol] `BTree.reconcile` usa XOR aggregation vulnerável a collisions com Fingerprints de baixa entropia — probe P1 do Reviewer 3 demonstrou que `isIdempotent` retorna `true` para trees divergentes quando os XORs colidem (FPs de 1 byte são insuficientes). Em produção FPs são SHA-256 (32 bytes) e colisão é ~0; mas se algum caller em application code gerar FPs com entropia reduzida, a anti-entropy fica silenciosamente quebrada. Documentar "Fingerprints DEVEM ter ≥128 bits de entropia" na doc do `BTree.reconcile` ou trocar XOR por SHA-256 (ADR) (BTree.ts:75-86, waves.ts:69,253)
- [ ] [m1][T-302b][protocol] Lint warning pré-existente em `codec.ts:29` (unused eslint-disable directive, T-203) — NÃO introduzido por T-302b mas re-rodado pelo gate. Housekeeping (codec.ts:29)
- [ ] [m2][T-302b][protocol] `applyNodes` chama `insertNode(storage, node)` SEM `parentId` — caller que precisar de lineage chain deve chamar `insertNode` diretamente. Spec §1 já implica (camada RBSR = sig validation, lineage = T-108a) (exchange.ts:171)
- [ ] [m3][T-302b][protocol] Wire format gap: `respondNodes` (T-302a) envia apenas `{id, fingerprint}`, não o `SignedNode` completo. `applyNodes` fica testada e pronta, mas integração end-to-end com `reconcile` de T-302a depende de decisão arquitetural — T-302c futuro (3 opções: evoluir respondNodes, nova MSG_FULL_NODES, reusar T-802 blob) (T-302b §6)
- [ ] [m1][T-307][transport] Desvio de `SyncCoordinatorOptions` revelado no handover: `transport` e `peerId` adicionados (necessário para `RBSRSession`); spec §1 lista apenas `swarm` + `clock` como deps — formalizar como Decisão 6 do arquiteto na §6 (syncCoordinator.ts:15-25)
- [ ] [m2][T-307][transport] Fragilidade de timing nos tests 3, 6, 8: uso de `for (let i = 0; i < 5; i++) await Promise.resolve();` para flushar microtasks é frágil (depende de contagem exata de awaits na cadeia). Trocar por `await new Promise(r => setTimeout(r, 0))` ou expor helper `flushMicrotasks()` no SyncCoordinator (syncCoordinator.test.ts:101-102, 159)
- [ ] [i1][T-307][transport] `monitorLeader` registra `swarm.onEvent` mas nunca cancela o `unsubscribe` em `stop()` — vazamento de subscription (sem bug hoje, mas armadilha para crescimento futuro). Armazenar o unsubscribe retornado e chamá-lo em `stop()` (syncCoordinator.ts:177-185)
- [ ] [i2][T-307][transport] `triggerFailover` chama `rollbackProgress()` ANTES de computar `lastSubRange` no evento, então `lastSubRange` será sempre `null` na forma atual (válido pelo tipo `string | null` da spec, mas armadilha se `startSession` algum dia persistir sub-range real) (syncCoordinator.ts:295-310)
- [ ] [i3][T-307][transport] Tests 4 e 5 do suite oficial exercitam `startSession → resumeFromCheckpoint` mas NÃO simulam cenário de failover real — spec §4 casos 4 e 5 pedem "após failover, retomar o último sub-range; resultado final é igual ao que seria se não houvesse failover". Cobertura existe (reconcile roda, sizeAfter1 === sizeAfter2), mas não cobre o cenário end-to-end. Aceitável (gate verde, comportamento correto do impl); ADR futuro para teste de failover com `SimNetwork` adversarial (syncCoordinator.test.ts:112-145)
- [ ] [m1][T-022][showcase] Baselines commitadas são `-chromium-win32.png` (geradas em Windows ARM64 local). CI roda em Linux e vai procurar `-chromium-linux.png` — primeiro run do `test:e2e` no CI vai falhar até gerar baseline Linux via `--update-snapshots` e commitar de volta. Documentado no handover §8 da task; precisa virar task de follow-up antes de T-022 ir para produção (handover §8:201-205)
- [ ] [m2][T-022][monorepo] Achado fora do escopo: `pnpm turbo run build --filter='@plataforma/design-system-showcase...'` quebra com **ciclo no grafo** (`@plataforma/protocol ↔ core ↔ testkit#build`); não existia em T-021 (verde); introduzido por merge posterior na master. Workaround atual: `pnpm --filter @plataforma/design-system build` direto (fora do turbo). Se ciclo afeta `pnpm build` da raiz, quebra o CI de build de todos — investigar em task separada (turbo.json / package.json deps)
- [ ] [m3][T-022][showcase] Sem `.gitignore` para `test-results/` e `playwright-report/` na raiz — Playwright gera PNGs de diff (`*-actual.png`, `*-diff.png`) em falhas que podem ir para git em commit acidental. Verificar se já existe `.gitignore` cobrindo; se não, adicionar (T-022 não tocou, é preventivo)
- [ ] [m4][T-022][showcase] Script `lint` ausente em `apps/design-system-showcase/package.json` — não introduzido por T-022 (decisão anterior), mas quebra paridade com outros pacotes que têm `pnpm lint`. Se a equipe quiser padronizar, criar script `lint` (`eslint src/`) — fora do escopo desta task (package.json:6-11)
- [ ] [m1][T-311][bancada] Spec §1 incompleta: `SyncStatus` interface (linhas 49-56) lista 6 campos mas NÃO declara `forceReconcile: () => void`; §5 instrui a usá-lo no botão Forçar Sync. Impl corretamente adicionou; spec precisa ser atualizada para incluir o campo na declaração (T-311 §1; useSyncStatus.ts:12)
- [ ] [m2][T-311][bancada] `useSyncStatus` é stub (retorna null/0/[] em runtime). Spec §5 Decisão 1 autoriza; integração real com RBSRSession fica para T-311a ou glue do WaveCoordinator (T-306). Agarrar em cleanup (useSyncStatus.ts:23-40)
- [ ] [m1][T-312][bancada] Path divergente da spec §3: spec declara `[CREATE] apps/bancada/e2e/dbOwner.test.ts`, impl criou `apps/bancada/tests/e2e/dbOwner.spec.ts` (convenção Playwright/TS do repo: `playwright.config.ts` tem `testDir: ./tests/e2e`). Spec §3 precisa ser atualizada para refletir o path real — housekeeping (spec §3 vs dbOwner.spec.ts)
- [ ] [m2][T-312][bancada] Workaround `__release()` em tests 6/7 (dbOwner.spec.ts:223-228, 264-269) é limitação conhecida do headless Chrome (page.close pode não liberar Web Locks imediatamente). O teste simula "release then close" em vez de true "browser-side crash-release". A semântica do `recovery` counter é real (incrementa porque phase era RECOVERING antes da nova aquisição), mas o trigger é explicit release, não page death. A spec §4 caso 6 ("Matar aba Líder") é atendida funcionalmente mas com semântica relaxada — documentado inline (comentário linhas 223-225)
- [ ] [i1][T-312][bancada] `setInterval(500ms)` re-announce é decisão de design do harness E2E (dbOwner.spec.ts:75-77), não do core. A spec §1 não menciona re-announce para `DbOwner`. Se o core quiser esse comportamento (para suportar followers que join tardio em produção), precisa ser documentado em spec §1 e adicionado ao `DbOwner` core (dbOwner.spec.ts:75-77)
- [ ] [i2][T-312][core] Unit test 5 (`simulateCrashRelease` — dbOwner.test.ts:307-332) verifica que um NOVO `DbOwner` adquire o lock após crash, mas NÃO verifica que `onCrashRecovery` callback é chamado no owner ORIGINAL que estava como follower. O fluxo "follower → leader via crash" (com callback de recovery) está coberto apenas pelo E2E test 7 (recovery === 1). Cobertura razoável mas não redundante (dbOwner.test.ts:307-332)
- [ ] [m1][T-306][protocol] Rework escolheu injetar remote state via parâmetro (`rootRange: IdRange` + `remoteRootXor: Fingerprint` em executeWave0/isIdempotent; `remoteTree?: BTree` em executeWave1/executeWave2). Decisão NÃO foi adicionada ao §6 da spec nem §1 foi atualizada. Quando T-306 for aprovada, formalizar como Decisão 6 do arquiteto (waves.ts:55-90, 94-167, 171-240, 244-252)
- [ ] [i1][T-207][transport] `recordDiscard` chama `this.clock.now()` duas vezes em sequência (uma para `events.push`, outra para `lastBumpMs`). Em clocks com baixa granularidade podem divergir; cachear em const local. Sem impacto funcional (ClockPort determinístico) — micro-otimização (relayTrustModel.ts:63-64)
- [ ] [m1][T-004a][core] Suite oficial não cobre `walCheckpoint()` sem argumento (default mode) — spec §3 declara default `PASSIVE` mas só há teste para `PASSIVE`/`FULL`/`TRUNCATE` explícitos. Adicionar teste 5.0 (sqliteStorage.test.ts)
- [ ] [i1][T-004a][core] Handover §8 do worker afirma "142 tests" mas o suite real tem 132 (12 files) — divergência de 10. Provável erro de digitação; revisar contagem antes de finalizar (tasks/T-004a.md:140)
- [ ] [i2][T-004a][core] `node:sqlite` emite `ExperimentalWarning` em cada abertura; CI/test logs ficam poluídos. Considerar `NODE_NO_WARNINGS=1` no script de test ou suprimir no ctor. Decisão fora do escopo desta task (sqliteStorage.ts)
- [ ] [m1][T-006a][testkit] Mudança fora do escopo declarado em §3: `packages/testkit/src/index.ts` (re-export `LinkConfig`/`PeerConfig`) — sem isto, consumers não conseguem importar os tipos sem caminho interno. Spec §3 deveria ter previsto a linha de re-export (testkit/src/index.ts:5)
- [ ] [m1][T-211][bancada] Mudanças fora do escopo declarado na §3: package.json (+dep @plataforma/transport), src/_stubs/node-zlib.ts (novo), vite.config.ts (alias node:zlib). Stub node-zlib é workaround legítimo para build do browser mas deveria constar em §3 ou §6 (package.json:18, _stubs/node-zlib.ts, vite.config.ts:30)
- [ ] [m2][T-211][bancada] Path do RedeTab.tsx diverge do spec §3 (que diz `apps/bancada/src/tabs/RedeTab.tsx`); impl usou `apps/bancada/src/components/tabs/RedeTab.tsx` consistente com as outras 5 abas. Spec §3 está errada; atualizar para refletir o path real
- [ ] [i1][T-211][bancada] Truncamento de PeerId (`truncatePeerId` em RedeTab.tsx:8-11) é funcionalidade além da spec §1 ("PeerId (truncado)"); documentar o threshold (6 + … + 4) na spec quando revisá-la (RedeTab.tsx:8-11)
- [ ] [i2][T-211][bancada] Spec §1 e §4 caso 4 são internamente inconsistentes: §1 diz "Renderiza tabela/listagem de `swarm.connected()`" mas §4 caso 4 exige mostrar peer em state `connecting`. O worker contornou mudando o test 4 de `connecting` para `connected` no commit 2657d63 — perdeu cobertura do requisito. Decisão arquitetural pendente: trocar para `swarm.all()` (recomendada) ou remover caso 4
- [ ] [i1][ORQ-01][control] Sondas adversariais do reviewer (5 probes via CLI) não são persistidas — spec §4 da task só menciona self-check por CLI, sem framework. Probes ficam registrados no Parecer (Seção 8) como evidência de cobertura, mas cobertura durável precisaria de framework de teste para scripts de tooling (decision: out-of-scope, ver Seção 8)
- [ ] [i1][ORQ-03][tooling] `saldo.mjs:20` — `path.resolve(__dirname, '..', '..')` ancora `.env` no Docs (não isola por CWD). Consistente com headroom-proxies.mjs (decisão de spec §2). Documentar no §6 quando revisado
- [ ] [i2][ORQ-03][tooling] `saldo.mjs:122` — `toFixed(2)` na tabela default pode arredondar saldos muito pequenos (ex.: 0.003 USD → "0.00"). `--json` mantém precisão total; aceitável para tabela humana
- [ ] [m1][ORQ-01][control] Assinatura `depsOk(depIds, allById)` diverge da spec §3 que pede `depsOk(task, allById)`. Comportamento equivalente, mas spec ou impl precisa alinhar (rename impl para `depsOkFor(depIds, allById)` ou atualizar spec). Housekeeping não-bloqueante (tools/scripts/ledger.mjs:171)
- [ ] [m1][T-206][transport] `HeartbeatOptions` declarado `export interface` em `SwarmRegistry.ts:43` mas NÃO re-exportado em `packages/transport/src/index.ts:9-10` (que re-exporta `PeerEntry`, `PeerTier`, `ConnectionState`, `RegistryEvent`, `RegistryEventHandler`). Consumidores externos do package não conseguem tipar o segundo argumento do construtor. Fix: 1 linha em `index.ts` (`export type { HeartbeatOptions } from './SwarmRegistry.js';`). Housekeeping não-bloqueante
- [ ] [i1][ORQ-02][control] `loadConfig` aceita JSONC (strip de `//` e `/* */`) — extensão útil p/ manter comentários na config versionada, não declarada na spec (tools/scripts/orquestrar.mjs:20)
- [ ] [i1][T-701a][core] T-313c (status: ready) referencia T-106 para adicionar a migration `blind_archives` ao array `MIGRATIONS`, mas o destino real é `device_state.db` (T-701a). T-313c usa colunas camelCase (archiveId, blindScopeId, expiresAt, createdAt) enquanto T-701a padronizou snake_case (archive_id, blind_scope_id, expires_at, created_at). Alinhar T-313c com a tabela snake_case de T-701a e mirar DEVICE_STATE_MIGRATIONS (não MIGRATIONS de T-106) — não bloqueante para T-701a, housekeeping para o arquiteto de T-313c
- [ ] [i2][ORQ-02][control] `cwd` para non-work/rework é o path absoluto `root` (`C:\Dev2026\Docs`), spec §5 diz "."; equivalente em runtime já que o script roda do repo (tools/scripts/orquestrar.mjs:142-144)
- [ ] [i3][T-004a][core] `transaction` propaga o valor retornado pelo callback (`Promise<T>` → `Promise<T>`), mas a spec §3 não cita explicitamente a propagação. Verificado pela probe P2. Documentar na doc do `StoragePort` quando a interface for endurecida (sqliteStorage.ts:25-35, T-004a §8 [i2])
- [ ] [m1][T-313a][core] `packArchive` não aceita `hlcRange`/`expiresAt` no signature; ambos hardcoded para `{start:0,end:0}` e `0` em `archiveCargo.ts:135-137`. Spec §1 lista esses campos em `ArchiveCargoPayload` mas o signature de `packArchive` (§1) só tem 5 params — spec gap + impl gap. T-313b (assignCustodian) e T-313c (GC+migration) vão precisar setar `hlcRange`/`expiresAt` reais; hoje só mutando o manifesto pós-pack, o que invalida o `archiveId` self-certifying. Endurecer a spec + impl juntos quando T-313b entrar em execução (archiveCargo.ts:117-141, §1 vs §1 signature)
- [ ] [m2][T-313a][core] `config.compressionLevel` (definido em `ArchiveConfig` §1) nunca é lido — `zstdCompressSync(plaintext)` na linha 125 usa o default do Node. Default do Node zstd = 3 (mesmo da spec), equivalente em runtime, mas o campo é morto. Passar `{ level: config.compressionLevel ?? 3 }` para honrar a spec (archiveCargo.ts:125)
- [ ] [i1][T-313a][core] Spec §1 exemplo de import mostra `import type { EntityId, ULID } from '@plataforma/protocol'`, mas `EntityId`/`ULID` **não são exportados** pelo `@plataforma/protocol` (só `extractEntityIds`/`canAccess`/`scopeRBSRTree` da ucanScope). A impl usa `import type { EntityId } from '../ulid.js'` (fonte canônica no core) — correto, mas spec precisa ser corrigida para refletir o path real (T-313a §1 vs archiveCargo.ts:6)
- [ ] [i2][T-313a][core] `AesGcmResult` em `@plataforma/crypto` é `{ ciphertext, authTag }`, mas `aesGcmEncrypt` retorna `{ nonce, ciphertext, authTag }` (adiciona nonce). Assinatura consistente com T-101 — não bloqueia (archiveCargo.ts:126)
- [ ] [i3][T-313a][core] Ciphertext inclui `nonce(12) + enc + tag(16)`; `manifest.size` reflete o tamanho total pós-assemble (28 bytes de overhead). Spec §1 (`size: number` em `ArchiveCargoPayload`) é ambíguo sobre se é ciphertext puro ou "envelope"; impl escolheu envelope. Documentar a convenção quando §6 for revisitada (archiveCargo.ts:136)
- [ ] [m1][T-304][core] `TOMBSTONE_TYPES = new Set(['ENTITY_TOMBSTONE', 'DELETE', 'TOMBSTONE'])` super-especifica o set — apenas `ENTITY_TOMBSTONE` é exercitado no monorepo (grep: zero usos de `'DELETE'`/`'TOMBSTONE'` como `node_type` em packages/core/src). Resiliência futura aceitável; se quiser apertar, reduzir para `['ENTITY_TOMBSTONE']` (concurrentGuard.ts:22)
- [ ] [m1][ORQ-04][control] Spec §5 step 1 + §7 do DoD referem `crush run --yolo` mas `--yolo` não existe no Crush atual (v0.79.1). Impl usa `--quiet` (tools/scripts/orquestrar.mjs:284). Reendurecer a spec para refletir o flag real (substituir `--yolo` por `--quiet` em §5/§7). Worker já entregou o ajuste, falta atualizar a doc
- [ ] [m1][T-313b][core] `canServeArchive` NÃO usa `scopeRBSRTree` de T-305a como spec §1 pede — impl faz `ucanCapabilities.some(cap => cap.resource === 'scope:archive')`, um magic-string check que ignora `blindScopeId` (PROBE 4 confirma). T-305a expõe `scopeRBSRTree(tree, ucan): BTree` (filter de BTree, não boolean), então a spec pediu algo que a API atual não suporta diretamente. Spec está `triaged` — reendurecer para refletir o check real (assignCustodian.ts:83-89)
- [ ] [m2][T-313b][core] Assinaturas async divergem da spec §1 (que declara sync): `buildHashRing`/`assignCustodian`/`assignCustodianWithRing` retornam `Promise<EntityId>`/`Promise<HashRing>` porque `blake2s256` em `@plataforma/crypto/wrappers.ts:144` é `Promise<Uint8Array>`. Spec está errada; impl forçada a ser async. Reendurecer spec para declarar `Promise<...>` (assignCustodian.ts:22,65,75)
- [ ] [i1][T-313b][core] Mudança fora do escopo declarado em §3: `packages/core/src/index.ts:82-97` re-exporta `buildHashRing`/`assignCustodian`/`assignCustodianWithRing`/`canServeArchive`/`HashRing` — necessário para consumo externo mas não constava em [CREATE]/[EDIT] do escopo. Padrão recorrente (T-302a, T-313a, T-304). Sugestão: adicionar `[UPDATE] packages/core/src/index.ts` ao template de escopo de tasks que criam módulos no `archive/` (index.ts:82-97)
- [ ] [m1][T-209][system-peer] `journal_mode = WAL` aplicado em `Database(':memory:')` é no-op silencioso (SQLite não suporta WAL em memória) — código morto no path padrão de testes (genesis.ts:71)
- [ ] [m2][T-209][system-peer] `multiaddrHost` interpolado cru na URL de onboarding (sem validação de hostname RFC 1123 nem URL-encode) — host com `#`/`?` produz URL ambígua (fragment injection). Validar host com regex ou url-encode (genesis.ts:102, 143)
- [ ] [i1][T-209][system-peer] `getProvisionalFlag()` é test-helper exposto na classe pública sem `@internal` — aceitar como utilitário de teste, mas marcar com sufixo ou decorator (genesis.ts:147)
- [ ] [i2][T-209][system-peer] Zero coupling com T-208 FPP — `genesis.ts` não importa `packages/transport/src/fpp.ts` (interface `GenesisWriter` do FPP não é consumida). Spec lista T-208 como dep. Aceitável para stub v0; registrar para integração futura via ledger (genesis.ts geral)
- [ ] [m1][T-210][core] `PRAGMA foreign_keys = OFF/ON` toggled dentro da transação de `consumeInvite` para contornar FK de `edges.source_id → nodes.id` quando o nó do inviter é remoto. Solução robusta: criar stub `PEER:REMOTE` antes da aresta, ou tornar `source_id` nullable — não bloqueia (invite.ts:168, 198)
- [ ] [m2][T-210][core] `signature: new Uint8Array(64)` é placeholder silencioso para `PROFILE:AUTHENTICATION` e `VOUCHES_FOR` (assinatura real = quem assina o quê, decisão de T-211 ou outra). Spec §1 não fixa. Não bloqueia mas é buraco aberto (invite.ts:179, 194)
- [ ] [i1][T-210][core] Spec §1 menciona "Noise (conexão implícita)" — impl pula handshake Noise (interpretação "implícito" = delegado a T-501/transport). Aceitável; rastrear via integração T-208/T-501 quando aplicável (invite.ts geral)
- [ ] [i2][T-210][core] `migrateSchema(storage, MIGRATIONS)` chamado com 2 args em `invite.test.ts:70`; função `migrateSchema` aceita só 1. Funciona (2º arg ignorado) mas o teste está mentindo — housekeeping, não bloqueia (invite.test.ts:70 vs schema.ts:63)
- [ ] [M1][T-210][core] `bytesToHex` em `invite.ts:71` usa `bytes[i]!.toString(16)` — `@typescript-eslint/no-non-null-assertion` é `error` no monorepo; `pnpm --filter @plataforma/core lint` falha (1 error). Pré-existente do commit `7d546ab` (não introduzido pelo rework). Não bloqueia o Gate declarado na spec §7 (build+test apenas), mas trava CI/lint em PR. Trocar por guard explícito ou `Array.from(bytes).map(...).join('')` (invite.ts:71)
- [ ] [m1][T-313c][core] `packages/core/src/index.ts` (top-level) re-exporta `putBlindArchive`/`getBlindArchive`/`listBlindArchivesByScope`/`garbageCollectArchives`/`BlindArchiveRow` (5 linhas) — NÃO declarado em §3 (spec só lista `[UPDATE] packages/core/src/archive/index.ts`). Padrão recorrente (T-302a, T-313a, T-313b). Adicionar `[UPDATE] packages/core/src/index.ts` ao template de escopo de tasks que criam módulos exportáveis (index.ts:86-99)
- [ ] [i1][T-313c][core] `listBlindArchivesByScope` tem 4º param `now?: number` não declarado na spec §1 (que só lista `(storage, blindScopeId, limit?)`). Impl adicionou para testes determinísticos (mesma motivação de `garbageCollectArchives`). Reendurecer a spec para incluir `now?` (blindArchives.ts:69-81)
- [ ] [i2][T-313c][core] `rowToBlindArchive` faz `new Uint8Array(row['archive_id'] as ArrayBuffer)` — better-sqlite3 retorna `Buffer` (subclasse de `Uint8Array`), não `ArrayBuffer`. Cast incorreto em runtime se StoragePort usar outro driver (ex.: `node:sqlite` real). Funciona hoje, frágil para evolução (blindArchives.ts:29-32)
<!-- END PENDENCIAS -->
