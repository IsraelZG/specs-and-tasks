---
id: C-32
title: "Corrigir StoragePort->GraphStorePort em syncCoordinator (build de transport quebrado na master)"
status: done
complexity: 2
target_agent: logic_agent # perfis: devops_agent, logic_agent, crypto_agent, frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential # parallel | sequential
dependencies: [] # IDs de tarefas que bloqueiam esta
blocks: [] # não bloqueia task específica, mas o build da master está vermelho até isto entrar
capacity_target: haiku # 1 token (this.storage → this.graphStore), verificação por build
# decisions: ["..."]          ← só quando status: draft:pending_decision (espelha a Seção 6)
---

# C-32 · Corrigir StoragePort->GraphStorePort em syncCoordinator (build de transport quebrado na master)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (`pnpm build`, `pnpm test`, `pnpm lint` na raiz afetam todos os pacotes)
- **Test Runner:** `vitest` (pacotes core/protocol) e `playwright` (E2E/Frontend)
- **Capacidade-alvo:** haiku | sonnet | opus-spike *(ver regra "Dimensionamento de Tarefas" no CLAUDE.md: spec sem decisões em aberto, contratos explícitos, sem API externa não-fixada, verificação por comando)*

## 1. Objetivo
Consertar o build **quebrado na master** de `@plataforma/transport`. `src/syncCoordinator.ts:173` passa `this.storage` (tipado `StoragePort`) para `applyNodes()`, que exige `GraphStorePort` → `error TS2345`. A própria classe já tem o campo correto `this.graphStore: GraphStorePort` (linha 69, populado no construtor via `opts.graphStore ?? this.requireGraphStore(storage)`) e a linha 172 logo acima **já o usa**. A linha 173 só usa o campo errado.

É bug de tipo **e** latente de runtime: `this.storage` cru pode não implementar os métodos de grafo; `this.graphStore` é o validado.

**Origem (varredura completa, 2026-07-16):** um `pnpm build` de todo o monorepo (turbo, 31 pacotes) foi rodado como varredura. **15 pacotes verdes**, e a **única** ocorrência real deste padrão (`StoragePort` onde `GraphStorePort` é exigido) é esta linha. O caso irmão em `plugin-workflows/src/durableQueue.ts` **já foi corrigido** na master (commit `e899a0c`). A falha do `transport` **mascarou o build do único dependente, `workers`** (que já usa `StoragePort & GraphStorePort` em `commitCycle.ts`, provavelmente OK) — por isso a verificação exige rebuild completo verde, não só o de `transport`.

## 2. Contexto RAG (Spec-Driven Development)
- `packages/transport/src/syncCoordinator.ts` — o defeito (linha 173) e o padrão correto já presente (linhas 69, 93, 172, 279 `requireGraphStore`).
- [`docs/adr/0015-extensao-graphstore-port.md`](../docs/adr/0015-extensao-graphstore-port.md) — `GraphStorePort` é a porta de grafo; `applyNodes`/`insertNode`/`getLineage` do core exigem-na. Contexto de por que os dois contratos existem.
- Erro literal do tsc: `src/syncCoordinator.ts(173,48): error TS2345: Argument of type 'StoragePort' is not assignable to parameter of type 'GraphStorePort'. Type 'StoragePort' is missing the following properties … putNode, getNode, putEdge, getEdges, and 8 more.`

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[UPDATE]** `packages/transport/src/syncCoordinator.ts` — linha 173: trocar `this.storage` por `this.graphStore` na chamada `applyNodes(nodes, this.storage)`. **Nenhuma outra mudança.**
> Se o rebuild completo (passo de verificação) revelar **novas** ocorrências do mesmo TS2345 em `workers` ou qualquer pacote antes mascarado pela falha do `transport`, corrija-as com o mesmo padrão (usar o binding `GraphStorePort` correto / `& GraphStorePort`) e liste cada arquivo:linha no Handover. Se aparecer algo que NÃO seja esse padrão, **PARE** e registre (Seção 6) — é fora de escopo.

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Framework:** vitest (suítes existentes de `transport` e `workers`).
- [x] **Métricas/Cobertura:** correção de tipo/wiring — sem lógica nova. Não se escreve teste novo (a suíte existente de `syncCoordinator`/`commitCycle` cobre o comportamento). A verificação é o build+test verde.
- [x] **Ambiente do Teste:** Node puro.
- [x] **Fora de Escopo:** a migração ampla de `exec(sql)` → `GraphStorePort` (T-1043/ADR-0015); qualquer refactor de `requireGraphStore`.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** alargar o campo `this.storage` para `StoragePort & GraphStorePort` — a classe já separa os dois campos de propósito (`storage` = SQL local; `graphStore` = grafo). Use o campo certo.
> - **NÃO** remover/alterar o helper `requireGraphStore` nem o campo `graphStore`.
> - **NÃO** tocar em outros arquivos além do necessário para zerar o TS2345 (ver escopo).

### Pegadinhas conhecidas
- O fix é **1 token** (`this.storage` → `this.graphStore`) na linha 173. A linha 172 (`graphStore: this.graphStore`) é o modelo de como referenciar o campo certo.
- `transport` depende de `core`+`protocol` compilados; numa worktree fria rode o build deles antes (P-012). O `pnpm build` da raiz (turbo) já resolve a ordem.
- O único dependente de `transport` é `workers` — só ele pode ter sido mascarado. Rebuild completo confirma.

1. Editar `packages/transport/src/syncCoordinator.ts:173`: `applyNodes(nodes, this.storage)` → `applyNodes(nodes, this.graphStore)`.
2. `pnpm --filter @plataforma/transport build && pnpm --filter @plataforma/transport test`.
3. **Rebuild completo** `pnpm build` (raiz) — precisa ficar **verde em todos os pacotes** (é o que garante que `workers` e qualquer downstream antes mascarado passam). Colar a saída na Seção 8.

## 6. Feedback de Especificação (Spec Feedback Loop)
- *[Sem decisões abertas.]* Se o rebuild completo revelar ocorrências fora do padrão `StoragePort`/`GraphStorePort`, PARE e registre aqui.

## 7. Definition of Done (DoD) & Reviewer Checklist
O agente `agile_reviewer` usará esta checklist para aprovar ou rejeitar o PR:
- [ ] O código segue estritamente os arquivos de Output especificados (sem criar arquivos não solicitados)?
- [ ] O `pnpm test` roda sem erros no ambiente especificado (Node/JSDOM)?
- [ ] Linter (`pnpm lint`) não acusa problemas?
- [ ] A implementação respeita a Regra do Que Não Fazer?
- [ ] **[gate de wiring — se a task entrega primitiva de autorização/privacidade]** existe caller de produção em `src/**` que a consome no caminho real, OU há task de integração linkada? (primitiva só testada = feature NÃO entregue)
- [ ] **[gate de acoplamento — se a task adiciona import cruzando pacote]** o import respeita a direção `protocol ← crypto ← core ← transport` (`visao-arquitetural.md §1`) e NÃO fecha ciclo?

### Verificação automática *(comandos exatos — worker E reviewer rodam e COLAM a saída)*
```bash
pnpm --filter @plataforma/transport build   # tsc — o TS2345 da linha 173 desaparece
pnpm --filter @plataforma/transport test    # suíte de transport verde
pnpm --filter @plataforma/workers build      # dependente antes mascarado — confirma que passa
```
> **REESCOPO (arquiteto, 2026-07-16):** o critério original exigia `pnpm build` (raiz) inteiro verde.
> Isso era **largo demais** — tornava a C-32 refém da saúde de 29 pacotes. Ao consertar o `transport`,
> o rebuild raiz desmascarou uma falha **basal e não relacionada** em `@plataforma/bancada` (WASM
> Automerge / `vite-plugin-pwa`), fora do padrão `StoragePort`/`GraphStorePort` desta task. Essa falha
> foi roteada para **C-33**. O gate da C-32 passa a ser o seu **deliverable real**.
> **Critério de aceite objetivo (reescopado):** `transport` build + test verdes **e** `workers` build
> verde **e** zero **novo** `TS2345` de `StoragePort`/`GraphStorePort` no rebuild. Diff = 1 token em
> `syncCoordinator.ts:173`. O `bancada` (build raiz) NÃO faz parte deste gate — é a C-33.
> **GATE DE EVIDÊNCIA:** nem o `finish` (worker) nem o veredito (reviewer) são válidos sem a
> saída literal desses comandos colada na seção 8. Marcar `[x]` sem evidência é violação.

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- **Implementação preservada:** commit `efc5160` (`fix(C-32): use graph store for sync application`) enviado para `origin/task/C-32`. O diff é exclusivamente `packages/transport/src/syncCoordinator.ts`: `applyNodes(nodes, this.storage)` → `applyNodes(nodes, this.graphStore)`.
- **Evidência verde:**
```
$ pnpm --filter @plataforma/transport build
$ tsc
Exit code: 0

$ pnpm --filter @plataforma/transport test
Test Files  19 passed (19)
     Tests  173 passed (173)
  Duration  6.38s (transform 1.81s, collect 9.43s, tests 6.72s, prepare 3.33s)
Exit code: 0

$ pnpm --filter @plataforma/workers build
$ tsc
Exit code: 0
```
- **Gate raiz — bloqueio fora de escopo:**
```
$ pnpm build
Tasks:    25 successful, 29 total
Cached:    9 cached, 29 total
Time:      27.276s
Failed:    @plataforma/bancada#build

[vite-plugin-pwa:build] Could not load .../@automerge/automerge/.../automerge_wasm_bg.wasm:
"ESM integration proposal for Wasm" is not supported currently.
Use vite-plugin-wasm or `.wasm?init` / `.wasm?url`.
```
O `workers` já compilou no rebuild; não houve nova ocorrência de `TS2345` envolvendo `StoragePort`/`GraphStorePort`. A falha do `bancada` não segue o padrão que a Seção 3 permite corrigir, então a task foi pausada via serviço em vez de ampliar escopo ou finalizar no escuro.
- **Telemetria — processo / chamada de ferramenta:** primeira criação de worktree bloqueada por Docs sujo: 3,4 s / 8,7 s. Drenagem que liberou o Docs: 10,9 s / ~11,5 s (4 commits; 1 intenção C-31 já sem diff descartada). Criação final da worktree: 28,0 s / `not_reliable` por retorno em yields; `pnpm install` interno 18,7 s. Pré-build: crypto 2,4 s / 6,8 s; protocol 2,5 s / 7,1 s; core 2,9 s / ~12,9 s. Gate: transport build 3,2 s / 7,7 s; transport test 6,38 s pelo Vitest / ~18,6 s; workers build 2,9 s / 9,5 s; build raiz 27,276 s / ~30,2 s (falhou). Versionamento: add 0,5 s / 5,8 s; commit 0,9 s / 6,6 s; push 3,2 s / ~13,2 s.
- **Métrica de pensamento/API:** raciocínio interno não é exposto. Intervalos de deliberação estão como `not_measured`; “chamada de ferramenta” mede a ida e volta observável, incluindo runner/scheduler, e não uma latência de API isolada. Em chamadas com yields, a camada atual não entrega um único relógio confiável de ponta a ponta — registrado como limitação, não estimado.

### Parecer do Agente Revisor (Reviewer): `agile_reviewer:claude-opus` (2026-07-16, qa-review --integrar, pós-reescopo)
- [x] **Aprovado**
- [ ] **Requer Refatoração**
- **Escopo:** ✅ `git show efc5160 --stat` = **só `packages/transport/src/syncCoordinator.ts`**, 1 linha.
- **Conteúdo:** ✅ `applyNodes(nodes, this.storage)` → `applyNodes(nodes, this.graphStore)` — exatamente o especificado; usa o campo `GraphStorePort` validado que a classe já mantém (linhas 69/93). Corrige o `TS2345` **e** o risco latente de runtime.
- **Nota de processo:** o gate original (root build verde) era largo demais e foi **reescopado** (ver §7) — a falha do `@plataforma/bancada` (WASM, fora do padrão) foi roteada para **C-33**. O worker (`gpt-5`) pausou corretamente pela §6 antes do reescopo.
- **Evidência de Execução (reviewer, na master JÁ MESCLADA `e6758bc`):**
```
$ pnpm --filter @plataforma/transport build   → $ tsc  (exit 0, TS2345 eliminado)
$ pnpm --filter @plataforma/transport test
  Test Files  19 passed (19)
       Tests  173 passed (173)   Duration 6.23s    → exit 0
$ pnpm --filter @plataforma/workers build     → $ tsc  (exit 0 — dependente antes mascarado, OK)
```
- **Critério de aceite reescopado:** ✅ transport build+test verdes, workers build verde, zero novo `TS2345`. Cumprido.
- **Comentários de Revisão:** master vai de "vermelha no `transport`" para "verde no `transport`+`workers`"; o único vermelho restante do root build é o `bancada` (C-33), basal e pré-existente.
- **Veredito:** APROVADO. Integração executada (merge --no-ff `e6758bc` + gate reescopado verde + `approve`).

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-07-17T00:26]** - *claude-opus* - `[Triado]`: triada: build de transport quebrado na master; 1 ocorrencia confirmada por varredura de build completo
- **[2026-07-17T00:26]** - *claude-opus* - `[Endurecido]`: endurecida: fix de 1 token this.storage->this.graphStore em syncCoordinator:173; verificacao por build completo verde
- **[2026-07-17T00:26]** - *claude-opus* - `[Promovida p/ ready]`: safety-net: deps vazias -> ready
- **[2026-07-17T01:18]** - *gpt-5* - `[Iniciado]`: iniciando: telemetria de processo e chamadas ativa
- **[2026-07-17T01:24]** - *gpt-5* - `[Pausado/Handoff]`: patch efc5160 enviado; transport build+173 testes e workers build verdes; pnpm build raiz bloqueado por WASM Automerge em @plataforma/bancada, fora do padrão permitido
- **[2026-07-17T01:33]** - *claude-opus* - `[Finalizado]`: finish em nome do worker apos reescopo do gate (arquiteto): deliverable real cumprido — transport build + 173 testes e workers build verdes (efc5160); bancada/root-build roteado p/ C-33. Evidencia literal na Secao 8.
- **[2026-07-17T01:33]** - *agile_reviewer:claude-opus* - `[Em revisão]`: revisando C-32 pos-reescopo (qa-review --integrar)
- **[2026-07-17T01:36]** - *agile_reviewer:claude-opus* - `[Aprovado]`: Aprovado + integrado: merge --no-ff na master (e6758bc); gate reescopado verde (transport build + 173 testes, workers build). TS2345 eliminado. bancada roteado p/ C-33. Evidencia na Secao 8.
