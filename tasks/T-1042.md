---
id: T-1042
title: "Migrar o adapter SQLite atual para GraphStorePort"
status: ready
complexity: 3
target_agent: core_agent # perfis: devops_agent, logic_agent, crypto_agent, frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential # parallel | sequential
dependencies: ["T-1041"] # IDs de tarefas que bloqueiam esta
blocks: ["T-1043"] # IDs de tarefas que esta bloqueia
capacity_target: sonnet # haiku | sonnet | opus-spike
---

# T-1042 · Migrar o adapter SQLite atual para GraphStorePort

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (`pnpm build`, `pnpm test`, `pnpm lint` na raiz afetam todos os pacotes)
- **Test Runner:** `vitest` (pacotes core/protocol)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Migrar a implementação atual de armazenamento SQLite (tanto Node quanto WASM/OPFS de browser) para implementar e expor a interface `GraphStorePort` criada em T-1041, confinando o dialeto SQL para dentro dos adapters.

## 2. Contexto RAG (Spec-Driven Development)
- [[adr-005-storage-engine-agnosticism]]
- `packages/core/src/sqliteStorage.ts`
- `packages/core/src/sqliteWasmStorage.ts`
- `packages/protocol/src/ports.ts`

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[UPDATE]** `packages/core/src/sqliteStorage.ts` (Implementar `GraphStorePort` e traduzir as queries do grafo internamente)
- **[UPDATE]** `packages/core/src/sqliteWasmStorage.ts` (Implementar `GraphStorePort` e traduzir as queries do grafo internamente)

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- **Framework:** Vitest (Node puro)
- **Ambiente do Teste:** Node puro
- **Metodologia:** Atualizar os testes de conformidade de persistência SQLite em `@plataforma/core` para exercitar diretamente a nova porta `GraphStorePort` com inserção de nodes, edges, busca e range scans.

## 5. Instruções de Execução (Step-by-Step)
1. Modifique `SqliteStorage` em `packages/core/src/sqliteStorage.ts` para que implemente `GraphStorePort` em vez de `StoragePort` (no que diz respeito às tabelas de nodes/edges).
2. Modifique `SqliteWasmStorage` em `packages/core/src/sqliteWasmStorage.ts` de forma idêntica.
3. Transponha as chamadas e consultas SQL cruas internas de escrita/leitura de nós e arestas para métodos internos de tradução privada nas classes. O SQL de nodes/edges deve ficar isolado dentro desses arquivos.
4. Garanta que a transação (`transaction`) retorne `GraphStoreTx` implementando o isolamento de transações.

## 6. Feedback de Especificação (Spec Feedback Loop)
- *[Nenhum problema identificado]*

## 7. Definition of Done (DoD) & Reviewer Checklist
O agente `agile_reviewer` usará esta checklist para aprovar ou rejeitar o PR:
- [ ] O código segue estritamente os arquivos de Output especificados?
- [ ] O `pnpm test` roda sem erros?
- [ ] Linter (`pnpm lint`) não acusa problemas?
- [ ] A implementação respeita a Regra do Que Não Fazer?

### Verificação automática
```bash
pnpm --filter @plataforma/core build
pnpm --filter @plataforma/core test
```

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- 

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória):**
```
```

## 9. Log de Execução (Agent Execution Log)
- *[Vazio]*
- **[2026-07-02T23:14]** - *sonnet* - `[Promovida p/ ready]`: spec_status hardened — flip draft->ready (arquiteto-decisoes 2026-07-02)
