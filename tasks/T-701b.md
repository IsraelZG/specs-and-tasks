---
id: T-701b
title: "LinkCipher: deriveSessionKey (HKDF)"
status: draft
complexity: 2
target_agent: crypto_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-701a", "T-101c"]
blocks: []
parent: "T-701"
---

# T-701b · LinkCipher: deriveSessionKey (HKDF)

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** haiku

## 1. Objetivo
Implementar `deriveSessionKey` no pacote `@plataforma/transport`: deriva chave AES-256 a partir das chaves pós-handshake Noise_XX via HKDF-SHA256
*(extraído de T-701 §1; T-101c — HKDF)*.

### Contratos exatos
```ts
// --- packages/transport/src/crypto/LinkCipher.ts (extensão) ---
import { AesKey } from '@plataforma/crypto'; // T-101b

/** Deriva chave de sessão AES-256 via HKDF-SHA256(handshakeKey, salt=nonce). */
export function deriveSessionKey(handshakeKey: Uint8Array, nonce: Uint8Array): Promise<AesKey>;
```

## 2. Contexto RAG
- [T-701a · encrypt/decrypt](./T-701a.md)
- [T-101c · HKDF](./T-101c.md)

## 3. Escopo de Arquivos
- **[READ]** `packages/transport/src/crypto/LinkCipher.ts` (T-701a)
- **[CREATE]** `packages/transport/tests/LinkCipher.derive.test.ts`
- **[UPDATE]** `packages/transport/src/crypto/LinkCipher.ts`

## 4. Estratégia de Testes
- [x] **Framework:** Vitest (Node puro).

Casos de teste:
1. `deriveSessionKey` determinístico: mesmas entradas → mesma chave.
2. Chamadas com nonce diferente → chaves diferentes.
3. Chave derivada tem 32 bytes.
4. `deriveSessionKey` + `encryptFrame` roundtrip integrado.

## 5. Instruções de Execução
1. Escreva testes.
2. Implemente via `hkdfSha256` de T-101c.
3. Rode build + test.

## 6. Feedback de Especificação
- *(Nenhuma pendência)*

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/transport build
pnpm --filter @plataforma/transport test
```

### Checklist
- [ ] `deriveSessionKey` determinístico?
- [ ] 32 bytes de saída?
- [ ] 4 casos de teste passando?
- [ ] `pnpm --filter @plataforma/transport build` e `test` verdes?

## 8. Log de Handover e Revisão Agile
### Handover do Executor:
-

### Parecer do Agente Revisor:
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução:**
```
(cole aqui a saída real de pnpm build e pnpm test)
```

## 9. Log de Execução
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
