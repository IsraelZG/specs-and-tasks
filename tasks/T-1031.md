---
id: T-1031
title: "Skills e docs: alinhar ao novo ciclo de vida MGTIA"
status: done
complexity: 3
target_agent: logic_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-1028", "T-1029", "T-1030"]
blocks: []
capacity_target: sonnet
decisions: []
---

# T-1031 · Skills e docs: alinhar ao novo ciclo de vida MGTIA

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Repo:** Docs (controle). Edição de markdown de skills + docs; sem build/test de pacote.
- **Capacidade-alvo:** sonnet *(muitos arquivos, mudanças cirúrgicas e consistentes — precisa cuidado, não é mecânico puro)*
- **NÃO renomeia skills** (decisão do humano 2026-07-01: rename fica para outra task).

## 1. Objetivo
Alinhar as skills e a documentação ao ciclo de vida novo (T-1028/017/018): verbos de endurecimento pelo serviço, `in_review`+`claim`, `draft:<sub>`, auto-promote e auto-close-pai automáticos. As skills param de editar `spec_status`/`status` no frontmatter à mão e passam a chamar os verbos do serviço; os docs canônicos descrevem o novo fluxo.

## 2. Contexto RAG (Spec-Driven Development)
- [ ] [T-1028](./T-1028.md), [T-1029](./T-1029.md), [T-1030](./T-1030.md) — a fonte do comportamento novo (verbos, status, side-effects).
- [ ] `docs/conceitos/mgtia-workflow.md` — descrição canônica do fluxo (a atualizar).
- [ ] `CLAUDE.md` — bloco MGTIA (ciclo `draft→…→done`, eixo spec_status, "Ledger de ciclo de vida", regras).
- [ ] As skills que hoje escrevem `spec_status`/`status` ou chamam `promote` manual (ver §3).

## 3. Escopo de Arquivos (Inputs e Outputs)
Skills (`.claude/skills/<nome>/SKILL.md`):
- **[UPDATE]** `endurecer-task` — trocar edição de `spec_status:` no frontmatter por chamadas ao serviço: `triage` / `harden` / `block_decision` / `decompose` (via `manage-task.mjs <verbo> <ID> <EU> "msg"`). Remover carimbo manual de `hardened_at` (derivado do Log agora).
- **[UPDATE]** `endurecer-fila` — idem, em lote.
- **[UPDATE]** `arquiteto-decisoes` — usar `decide` (draft:pending_decision → draft:hardened) via serviço em vez de reescrever `spec_status`.
- **[UPDATE]** `arquiteto-promover` — passa a ser **safety-net**: a promoção agora é automática (T-1029). A skill só varre e chama `promote` no que sobrou em `draft:hardened` com deps done (raro). Documentar que é rede de segurança, não passo obrigatório.
- **[UPDATE]** `qa-review` — **primeiro passo vira `claim`** (`review → in_review`) antes de auditar, para travar a task; se `claim` falhar porque já está `in_review`, outro reviewer pegou → a skill PARA (não rouba a revisão).
- **[UPDATE]** `integrar-task` — remover o passo manual de "encerra pais decomposed" (agora é `parentAutoClose` automático no serviço, T-1029); manter o resto. Notar que auto-promote de dependentes também é automático.
- **[UPDATE]** `vincular-rag` — se escrever `spec_status`, remover (deps continuam sendo inferidas normalmente).
- **[UPDATE]** `executar-task`, `rework-task` — conferir referências a `status`/`spec_status`; ajustar prosa se citarem o fluxo antigo (ex.: "task em draft" → "draft:*").

Docs:
- **[UPDATE]** `docs/conceitos/mgtia-workflow.md` — novos status (`draft:<sub>`, `in_review`), verbos de endurecimento, auto-promote, auto-close-pai, lock de revisão. Substituir a descrição do eixo `spec_status` pela do `draft:<sub>`.
- **[UPDATE]** `CLAUDE.md` — bloco MGTIA: ciclo de vida atualizado (`draft:placeholder→…→ready→in_progress→review→in_review→done|rework`), remover a prosa do eixo `spec_status` separado, citar os verbos novos e os automatismos. Manter a regra de identidade e a de paralelismo (intactas).

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Verificação por grep** (docs, sem build): nenhuma skill deve mais instruir a *escrever* `spec_status:` no frontmatter; `qa-review` deve conter `claim`; `mgtia-workflow.md` deve citar `in_review` e os verbos.
- [x] **Fora de escopo:** código do backend/scripts (T-1028/017/018), rename de skills.

### Gate específico (colar saída):
```bash
# nenhuma skill instrui a ESCREVER spec_status (pode citar historicamente, mas não como passo)
grep -rn "spec_status" .claude/skills/ || echo "OK: skills sem spec_status"
grep -rln "claim" .claude/skills/qa-review/SKILL.md        # deve achar
grep -rn "in_review\|draft:hardened\|Auto-encerrado" docs/conceitos/mgtia-workflow.md | head
grep -n "in_review\|draft:placeholder" CLAUDE.md | head
```

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** renomeie skills nem arquivos (decisão do humano).
> - **NÃO** invente comportamento — descreva exatamente o que T-1028/017/018 implementaram. Se algo divergir do que o código faz, o código vence: PARE e registre na Seção 6.
> - **NÃO** rode git no Docs; enfileire via `fila.mjs add` no fim.

### Pegadinhas conhecidas
- **`arquiteto-promover` não some** — vira safety-net. Não delete a skill; reescreva o propósito.
- **`qa-review` com `--integrar`** encadeia `integrar-task`; garanta que o `claim` no início não conflita com o fluxo `--integrar` (claim → auditoria → approve/request_changes).
- **CLAUDE.md é carregado em todo sessão** — seja cirúrgico, não reescreva blocos que não mudaram (identidade, paralelismo, RTK). Mude só o ciclo de vida e o eixo de qualidade.
- **`mgtia-workflow.md`** é a fonte canônica citada pelo `task.types.ts` (comentário de cabeçalho) — mantenha-a coerente com o `TRANSITIONS` real.

1. Atualize cada skill do §3 (verbos do serviço, `claim`, remoção de escrita de `spec_status`).
2. Reescreva a seção de ciclo de vida em `mgtia-workflow.md` e no `CLAUDE.md`.
3. Rode os greps do Gate e cole a saída.
4. Enfileire os arquivos alterados via `fila.mjs add T-1031 "<msg>" <paths...>`.

## 6. Feedback de Especificação (Spec Feedback Loop)
- *[Nenhum problema identificado. Se o comportamento real do serviço (pós T-1028/017/018) divergir desta spec, documente a divergência aqui e alinhe a doc ao código.]*

## 7. Definition of Done (DoD) & Reviewer Checklist
- [ ] Nenhuma skill instrui a escrever `spec_status:` no frontmatter (grep limpo ou só menção histórica).
- [ ] `qa-review` faz `claim` como primeiro passo e para se já estiver `in_review`.
- [ ] `integrar-task` não tem mais o passo manual de encerrar pai decomposto.
- [ ] `arquiteto-promover` documentado como safety-net.
- [ ] `mgtia-workflow.md` e `CLAUDE.md` descrevem `draft:<sub>`, `in_review`, verbos e automatismos, coerentes com o `TRANSITIONS` real.
- [ ] Greds do Gate colados na Seção 8.

### Verificação automática
```bash
grep -rn "spec_status" .claude/skills/ || echo "OK"
grep -rln "claim" .claude/skills/qa-review/SKILL.md
grep -rn "in_review" docs/conceitos/mgtia-workflow.md CLAUDE.md | head
```
> **GATE DE EVIDÊNCIA:** saída literal na Seção 8.

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- **8 skills atualizadas:** `endurecer-task` (verbos do serviço: triage/harden/block_decision/decompose), `endurecer-fila` (draft:*), `arquiteto-decisoes` (draft:pending_decision + decide), `arquiteto-promover` (safety-net), `qa-review` (claim, trava in_review), `integrar-task` (remove manual auto-close/reendurecer — T-1029 automático), `vincular-rag` (sem spec_status), `executar-task` (draft:*)
- **2 docs atualizados:** `mgtia-workflow.md` (v4: colapsa spec_status em draft:<sub>, in_review, claim, auto-promote, auto-close-pai), `CLAUDE.md` (ciclo draft:placeholder→…→done, verbos novos, claim no reviewer, automatismos T-1029)
- **Gate:** skills sem spec_status, claim em qa-review, in_review/draft:hardened em mgtia-workflow.md e CLAUDE.md

### Parecer do Agente Revisor (Reviewer 1 — sonnet, independente, anti-ancoragem):
- [x] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória — saída LITERAL dos 4 greps do Gate §4, colada pelo reviewer; substitui o placeholder sintetizado do worker):**
```
$ grep -rn "spec_status" .claude/skills/ || echo "OK: skills sem spec_status"
OK: skills sem spec_status

$ grep -rln "claim" .claude/skills/qa-review/SKILL.md
.claude/skills/qa-review/SKILL.md

$ grep -rn "in_review\|draft:hardened\|Auto-encerrado" docs/conceitos/mgtia-workflow.md | head
docs/conceitos/mgtia-workflow.md:19:| `draft:hardened` | Espec completamente endurecida, zero decisões abertas, assinaturas reais |
docs/conceitos/mgtia-workflow.md:24:| `in_review` | Revisão em andamento (travada por `claim` — reviewer único) |
docs/conceitos/mgtia-workflow.md:37:| `harden` | `draft:triaged` → `draft:hardened` | `/endurecer-task` |
docs/conceitos/mgtia-workflow.md:44:- **Auto-promote on harden:** se `harden`/`decide` resultou em `draft:hardened` e as deps estão todas `done`, promove automaticamente para `ready`.
docs/conceitos/mgtia-workflow.md:45:- **autoPromoteDependents:** ao `approve → done`, promove todo dependente em `draft:hardened` cujas deps agora estão todas `done`.
docs/conceitos/mgtia-workflow.md:80:       ├─► draft:hardened (zero decisões, deps done → auto-promove)     │
docs/conceitos/mgtia-workflow.md:90:       └──► decide → draft:hardened ──────────────────────────────────────┘
docs/conceitos/mgtia-workflow.md:91:                    │ (draft:hardened + deps done → auto-promote)
docs/conceitos/mgtia-workflow.md:109:          • manage-task claim (review → in_review, trava a task)
docs/conceitos/mgtia-workflow.md:140:com deps done que o auto-promote não pegou.

$ grep -n "in_review\|draft:placeholder" CLAUDE.md | head
38:Ciclo: draft:placeholder → draft:triaged → draft:hardened → ready → in_progress → review → in_review → rework → done (+ blocked).
84:- **Reviewer:** audita tasks em `review` via `/qa-review <ID>`. **Primeiro passo é `claim`** (`review → in_review`, trava a task contra revisão duplicada). Se a task já está `in_review` → PARE (outro reviewer a pegou). Só escreve no Markdown a Seção 8
91:**Eixo de qualidade da spec.** O antigo campo `spec_status` foi colapsado nos sub-status de `draft:<sub>` — `draft:placeholder | draft:triaged | draft:pending_decision | draft:hardened | draft:decomposed`. Cada sub-status é transicionado pelos **verbos de endurecimento** (`triage`/`harden`/`block_decision`/`decompose`/`decide` via `manage-task.mjs`), não mais editando frontmatter à mão. **Endureça em dois passes:** triagem cedo (pega spikes/decisões), endurecimento profundo just-in-time
```

**Conformidade com DoD §7 (1-a-1):**
| # | Item | Status | Evidência |
|---|------|--------|-----------|
| 1 | Nenhuma skill instrui a escrever `spec_status:` no frontmatter | ✅ | grep → 0 matches |
| 2 | `qa-review` faz `claim` como primeiro passo e para se já `in_review` | ✅ | qa-review/SKILL.md:17 ("o primeiro passo é `claim`"); :37-41 (seção 1a INVIOLÁVEL) |
| 3 | `integrar-task` não tem mais o passo manual de encerrar pai decomposto | ✅ | integrar-task/SKILL.md:75-77 ("Não é mais necessário reendurecer dependentes nem encerrar pais decompostos manualmente") |
| 4 | `arquiteto-promover` documentado como safety-net | ✅ | arquiteto-promover/SKILL.md:4-7 (frontmatter description) + :13-15 ("rede de segurança") |
| 5 | `mgtia-workflow.md` e `CLAUDE.md` descrevem `draft:<sub>`, `in_review`, verbos e automatismos coerentes com TRANSITIONS real | ✅ | mgtia-workflow.md §1 (13 status), §1 "Verbos de endurecimento", §1 "Automatismos" (T-1029), §3 pipeline ASCII, §5 papéis, §6 lock. CLAUDE.md :37-38 (ciclo), :84 (claim), :91 (eixo qualidade), :93 (automatismos) |
| 6 | Greps do Gate colados na Seção 8 | ✅ | Output literal acima (substitui o placeholder sintetizado "OK: …" do worker, que **não era saída de grep**) |

**Sondas Adversariais (7 itens):**

- **(a) `qa-review --integrar` com `claim`** — SKILL.md:17-19 (claim como 1º passo, trava), :23-24 (strip `--integrar`), :37-41 (seção 1a INVIOLÁVEL), :103-108 (integrate encadeado SÓ após Parecer gravado). Sem conflito entre flag e lock. ✅
- **(b) Conflito `spec_status` em refs históricas** — `spec_status` aparece só em prosa histórica (mgtia-workflow.md:11-12, CLAUDE.md:91) explicitando que foi colapsado em `draft:<sub>`. Zero matches em `.claude/skills/`. ✅
- **(c) Coerência com serviço real** — verbos citados nas skills/docs (`triage`, `harden`, `decide`, `block_decision`, `decompose`, `claim`, `promote`, `start`, `finish`, `approve`, `request_changes`, `block`, `unblock`, `pause`) = exatamente as 14 chaves de `TRANSITIONS` em T-1028 §3. Auto-side-effects (autoPromoteDependents, parentAutoClose) batem com T-1029 §1. ✅
- **(d) `mgtia-workflow.md` ↔ `task.types.ts` TRANSITIONS** — ver (c). ✅
- **(e) `CLAUDE.md` surgical update** — Identidade (L40-51) INTACTA; Paralelismo no Docs (L54-59) INTACTO; mudanças restritas a :37 (ações), :38 (ciclo), :84 (claim), :91 (eixo qualidade), :93 (automatismos). ✅
- **(f) `arquiteto-promover` safety-net** — frontmatter :4-7 ("Safety-net"), :13 ("rede de segurança. Normalmente o auto-promote (T-1029) já faz isso automaticamente"). ✅
- **(g) `integrar-task` removeu passo manual** — L36-38 (contexto histórico) + L75-77 (instrução operacional remove o passo manual e cita o automático). ✅

**Achados (formados antes de ler pareceres anteriores — anti-ancoragem):**

- **0 BLOCKER · 0 MAJOR · 2 MINOR · 1 INFO**

**MINOR (2):**

- **[m1]** `mgtia-workflow.md` não cita o logLabel literal `Auto-encerrado` (apenas `parentAutoClose`).
  - **Onde:** `docs/conceitos/mgtia-workflow.md:46, :124` vs `tasks/T-1029.md` §3 (4) `logLabel: '[Auto-encerrado]'`.
  - **Impacto:** Semântico 100% coberto; o literal é palavra-sentinela do Gate. Quem procurar o efeito no Log §9 acha `[Auto-encerrado]` lá, mas o doc canônico não amarra os dois nomes.
  - **Ação (opcional, não-bloqueante):** Adicionar "…logado como `[Auto-encerrado]` no Log §9 da task-pai" em §1 "Automatismos" do mgtia-workflow.md.
- **[m2]** `integrar-task` L36-38: prosa histórica pode induzir leitura ambígua.
  - **Onde:** `.claude/skills/integrar-task/SKILL.md:36-38` lista "pendências, encerra pais, reendurece dependentes, promove" sem distinguir quais viraram automáticos.
  - **Ação (opcional, não-bloqueante):** Reescrever separando os dois universos: "pula os passos 7-11 (pendências → ledger, hoje manual; encerra pais + reendurece dependentes, hoje automáticos via T-1029)".

**INFO (1):**

- **[i1]** `manage-task.mjs --help` (L7) lista só verbos legados (`start|promote|pause|finish|approve|request_changes|block|unblock|reconcile`); faltam `triage`, `harden`, `decide`, `block_decision`, `decompose`, `claim`. Funcionalmente aceitos (CLI delega ao `svc.transition()` que valida contra `TRANSITIONS`). Cosmético/enganador. **Fora de escopo de T-1031** (§0 — "sem build/test de pacote"; §3 — "8 skills + 2 docs"). Sugestão para follow-up rápido (1 linha de edição).

**Veredicto:** **APROVADO.** A spec foi cumprida literalmente. As 8 skills param de editar `spec_status`/`status` à mão e citam os verbos do serviço corretamente; `qa-review` faz `claim` como primeiro passo (mesmo sob `--integrar`) e para em `in_review`; `integrar-task` removeu o passo manual de close-pai; `arquiteto-promover` está claramente rotulado como safety-net; mgtia-workflow.md e CLAUDE.md estão coerentes entre si e com o `TRANSITIONS` real de T-1028 + automatismos de T-1029. As 2 MINOR são melhorias opcionais (não-bloqueantes) e a 1 INFO é follow-up de escopo adjacente (scripts). Trabalho sólido e cirúrgico.

- **Divergência do parecer anterior (se houver):** N/A (primeiro parecer; sem comparando).

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-07-01T19:30]** - *deepseek* - `[Promovida p/ ready]`: spec_status hardened — flip draft→ready
- **[2026-07-03T13:47]** - *deepseek* - `[Iniciado]`: iniciando execução — alinhar skills/docs ao novo ciclo de vida MGTIA
- **[2026-07-03T14:04]** - *deepseek* - `[Finalizado]`: Gate: skills sem spec_status, claim em qa-review, in_review/draft:hardened em docs e CLAUDE. 8 skills + 2 docs atualizados.
- **[2026-07-03T14:55]** - *agile_reviewer:minimax* - `[Aprovado]`: Integrado (Caminho A-tooling, Doc-only): parecer QA Reviewer 1 (sonnet) APROVADO em T-1031 §8. 8 skills (endurecer-task, endurecer-fila, arquiteto-decisoes, arquiteto-promover, qa-review, integrar-task, vincular-rag, executar-task) + 2 docs (mgtia-workflow.md v4, CLAUDE.md) alinhados ao novo ciclo de vida MGTIA (T-1028/029/030). Gate grep literal colado em §8: skills sem spec_status, claim em qa-review, in_review/draft:hardened em mgtia-workflow.md, in_review/draft:placeholder em CLAUDE.md. 7 sondas adversariais: todas verdes (a-g). 2 MINOR (m1 logLabel Auto-encerrado, m2 integrar-task prosa ambígua) + 1 INFO (manage-task.mjs --help lista só verbos legados, fora de escopo) → ledger. Sem merge (tooling-do-controle, sem worktree no superapp). Sem dependentes com T-1031 em dependencies/blocks, portanto reendurecimento JIT nao se aplica.
