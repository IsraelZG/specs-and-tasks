---
id: T-1034
title: "Hardening do wire RBSR exchange: filtro por peerId, ClockPort, encoding"
status: review
complexity: 3
target_agent: protocol_agent # perfis: devops_agent, logic_agent, crypto_agent, frontend_agent
reviewer_agent: agile_reviewer
execution_mode: sequential # parallel | sequential
dependencies: ["T-302a"] # IDs de tarefas que bloqueiam esta
blocks: [] # IDs de tarefas que esta bloqueia
capacity_target: sonnet # haiku | sonnet | opus-spike — preenchido no endurecimento (pass 2)
# decisions: ["..."]          ← só quando spec_status: blocked-decision (espelha a Seção 6)
---

# T-1034 · Hardening do wire RBSR exchange: filtro por peerId, ClockPort, encoding

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo (`pnpm build`, `pnpm test`, `pnpm lint` na raiz afetam todos os pacotes)
- **Test Runner:** `vitest` (pacotes core/protocol) e `playwright` (E2E/Frontend)
- **Capacidade-alvo:** haiku | sonnet | opus-spike *(ver regra "Dimensionamento de Tarefas" no CLAUDE.md: spec sem decisões em aberto, contratos explícitos, sem API externa não-fixada, verificação por comando)*

## 1. Objetivo
Endurecer a camada de wire do RBSR exchange, corrigindo um bug de segurança e três reinvenções de stdlib/abstrações que o próprio pacote já provê.

> **ACHADOS (auditoria 2026-07-01) — em `packages/protocol/src/rbsr/exchange.ts`:**
> - **[M1 · segurança] `waitForMessage` não filtra por peer** (`exchange.ts:114`): o handler ignora `peerId` e aceita `MSG_NODES_RESPONSE`/`MSG_ROOT_FINGERPRINT` de **qualquer** peer conectado, só filtrando por byte de tipo. Num adapter multi-peer, o peer C injeta resposta numa sessão com A. Deve casar o `peerId` da sessão RBSR.
> - **[m1 · reuso] encoding UTF-8 caseiro que não é UTF-8** (`exchange.ts:45-59`): `encodeUtf8`/`decodeUtf8` usam `charCodeAt`, truncando qualquer caractere > 255. Trocar por `TextEncoder`/`TextDecoder` (stdlib) — o pacote já tem `msgpackr` como dependência para wire binário.
> - **[m1 · testabilidade] usa `setTimeout` nativo** via `declare function` (`exchange.ts:8-10`) para burlar o lint, em vez do `ClockPort` que o próprio protocol define (`ports.ts:72`). Quebra teste sob `VirtualClock`. O `syncCoordinator` faz certo (clock injetável).
> - **[m2 · dedup] `MSG_ROOT_FINGERPRINT = 0x01`** duplicado entre `exchange.ts:15` e `syncCoordinator.ts:7` — fonte única.

**Meta:** corrigir o bug de segurança (M1) e os três débitos técnicos (m1×2, m2) sem mudar a máquina de estados.

### Contratos exatos (assinaturas TS fixadas, derivadas de `packages/protocol/src/rbsr/exchange.ts:8-201` + `packages/protocol/src/ports.ts:72-78`)

```ts
// --- packages/protocol/src/rbsr/exchange.ts ---
import { BTree } from './BTree.js';
import type { Fingerprint, IdRange } from './BTree.js';
import type {
  NetworkAdapterPort, PeerId, WireData, MessageHandler, ClockPort,
} from '../ports.js';
import { verifyNode, insertNode } from '@plataforma/core';
import type { SignedNode } from '@plataforma/core';
import type { StoragePort } from '../ports.js';

// ❌ REMOVER: declare function setTimeout/clearTimeout (exchange.ts:8-10)
// ✅ USAR: ClockPort (ports.ts:72-78) com now/setTimeout/clearTimeout

// Tipos de mensagem (fonte única — re-exportar de novo módulo interno)
export const MSG_ROOT_FINGERPRINT = 0x01; // re-export daqui
export const MSG_REQUEST_NODES = 0x02;
export const MSG_NODES_RESPONSE = 0x03;
const DEFAULT_TIMEOUT_MS = 100;

export enum RBSRState { IDLE, ROOT_EXCHANGE, RANGE_COMPARE, REQUEST_NODES, APPLY, DONE }
export interface RBSRSession {
  localTree: BTree;
  remotePeerId: PeerId;
  transport: NetworkAdapterPort;
  state: RBSRState;
  /** NOVO: clock injetável — viabiliza teste determinístico (T-003 VirtualClock). */
  clock: ClockPort;
}

// ❌ REMOVER: encodeUtf8/decodeUtf8 caseiros (sub-charCodeAt)
// ✅ USAR: TextEncoder/TextDecoder (stdlib) — únicos, lazy-instantiated no escopo do módulo
const textEncoder = new TextEncoder();
const textDecoder = new TextDecoder('utf-8', { fatal: true });

function encodeUtf8(s: string): Uint8Array { return textEncoder.encode(s); }
function decodeUtf8(bytes: Uint8Array): string { return textDecoder.decode(bytes); }

// waitForMessage com filtro por peerId + ClockPort injetado
function waitForMessage(
  transport: NetworkAdapterPort,
  expectedType: number,
  expectedPeer: PeerId,           // NOVO: filtra por peer
  timeoutMs: number,
  clock: ClockPort,               // NOVO: clock injetável
): Promise<WireData> {
  let timerId: number | undefined;
  let unsub: (() => void) | undefined;
  return new Promise<WireData>((resolve, reject) => {
    const handler: MessageHandler = (peerId, data) => {
      if (peerId !== expectedPeer) return;       // FIX M1: ignora mensagens de outros peers
      if (data.length > 0 && data[0] === expectedType) {
        cleanup();
        resolve(data);
      }
    };
    const cleanup = () => {
      if (timerId !== undefined) { clock.clearTimeout(timerId); timerId = undefined; }
      if (unsub) { unsub(); unsub = undefined; }
    };
    unsub = transport.onMessage(handler);
    timerId = clock.setTimeout(() => {
      cleanup();
      reject(new Error(`Timeout waiting for message type 0x${expectedType.toString(16)}`));
    }, timeoutMs);
  });
}

// Assinaturas públicas mantêm forma; body usa as versões atualizadas
export async function sendRootFingerprint(transport, peerId, rootXor): Promise<void>;
export async function requestNodes(transport, peerId, ids: string[]): Promise<void>;
export async function respondNodes(transport, peerId, tree, ids: string[]): Promise<void>;
export async function applyNodes(
  nodes: readonly SignedNode[], storage: StoragePort,
): Promise<{ imported: number; rejected: number }>;
export async function reconcile(session: RBSRSession): Promise<number>;

// --- packages/transport/src/syncCoordinator.ts ---
// ❌ REMOVER: const MSG_ROOT_FINGERPRINT = 0x01; (syncCoordinator.ts:7)
// ✅ IMPORTAR: import { MSG_ROOT_FINGERPRINT } from '@plataforma/protocol';
```

> **Origem dos tipos:**
> - `ClockPort.now/setTimeout/clearTimeout` — `packages/protocol/src/ports.ts:72-78` (T-004, com extensão T-206).
> - `MSG_ROOT_FINGERPRINT` etc. — antes duplicado em `exchange.ts:15` e `syncCoordinator.ts:7`. Após hardening, **fonte única** em `exchange.ts` (ou novo `packages/protocol/src/rbsr/wireFormat.ts` se preferir — não exigido).
> - `TextEncoder`/`TextDecoder` — `lib.dom` ou `lib.webworker` no tsconfig (verificar — pode precisar adicionar `"DOM"` em `lib` se não estiver).

## 2. Contexto RAG (Spec-Driven Development)
- [ ] `packages/protocol/src/ports.ts` — `ClockPort` (`now`/`setTimeout`/`clearTimeout`) e `PeerId`
- [ ] `tasks/T-302a.md` — spec da máquina de estados `reconcile` (base deste arquivo)
- [ ] `docs/conceitos/hlc.md` / testkit `VirtualClock` — porquê o clock é injetável

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[UPDATE]** `packages/protocol/src/rbsr/exchange.ts`:
  - `waitForMessage`: recebe `expectedPeer: PeerId` + `clock: ClockPort`; ignora mensagens de outros peers; usa `clock.setTimeout/clearTimeout`.
  - `RBSRSession`: novo campo `clock: ClockPort`.
  - `reconcile`: passa `session.clock` e `session.remotePeerId` para `waitForMessage`.
  - Substituir `encodeUtf8`/`decodeUtf8` por `TextEncoder`/`TextDecoder` (instâncias lazy no escopo do módulo).
  - Remover `declare function setTimeout/clearTimeout` (linhas 8-10).
- **[UPDATE]** `packages/transport/src/syncCoordinator.ts:7` — remover a duplicata `MSG_ROOT_FINGERPRINT = 0x01`, importar de `@plataforma/protocol`.
- **[UPDATE]** `packages/transport/src/syncCoordinator.ts` — ao criar `RBSRSession` (`syncCoordinator.ts:132-138`), passar `clock: this.clock`.
- **[UPDATE]** `packages/protocol/tsconfig.json` (ou `tsconfig.base.json`) — adicionar `"DOM"` em `compilerOptions.lib` se `TextEncoder`/`TextDecoder` não estão visíveis (verificar com `pnpm --filter @plataforma/protocol build`).
- **[UPDATE]** `packages/protocol/tests/rbsr.exchange.test.ts` — instanciar `session` com `clock: new VirtualClock()` (de `@plataforma/testkit`); cobrir rejeição de peer errado (M1) e round-trip de string não-ASCII (m1).

> **Pegadinha:** injetar `ClockPort` muda a assinatura de `reconcile`/`waitForMessage` — atualizar TODOS os callers (`syncCoordinator` e 2 testes existentes: `rbsr.exchange.test.ts` + `rbsr.applyNodes.test.ts` — este último não chama `reconcile` mas o `RBSRSession` agora exige `clock`). Coordenar com T-1032/T-1033 (mesmo arquivo `exchange.ts`).

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [ ] **Framework:** (Vitest para Node puro / Playwright para E2E / React Testing Library em JSDOM)
- [ ] **Métricas/Cobertura:** (Ex: Testar todos os ramos de erro, testar a assinatura inválida)
- [ ] **Ambiente do Teste:** (Node puro, sem browser / Headless browser)
- [ ] **Fora de Escopo:** (O que NÃO precisa ser testado)
> **Se a task afeta UI** (frontend_agent ou escopo em `apps/*-frontend/**`): unit/JSDOM NÃO
> basta. Especifique aqui um **smoke Playwright** (browser real) OU exija a verificação manual
> do revisor (subir o app e exercitar o fluxo) — o `agile-reviewer` trata isso como BLOCKER de
> processo. Marque `ui: true` no frontmatter para deixar explícito.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - NÃO introduzir `setTimeout`/`clearTimeout` global de volta — usar `ClockPort` sempre.
> - NÃO redefinir `encodeUtf8`/`decodeUtf8` com `charCodeAt` — `TextEncoder`/`TextDecoder` cobrem todos os casos UTF-8 válidos.
> - NÃO manter `MSG_ROOT_FINGERPRINT` duplicado em `syncCoordinator.ts` — fonte única em `protocol/src/rbsr/exchange.ts`.
> - NÃO usar `simNetwork` "real" para testes determinísticos — `VirtualClock` é o caminho.

### Pegadinhas conhecidas
- Adicionar `clock: ClockPort` em `RBSRSession` é **breaking change** — TODOS os testes e o `syncCoordinator` precisam ser atualizados. Buscar `RBSRSession` no monorepo.
- `TextEncoder`/`TextDecoder` precisam de `lib: ["DOM"]` ou `lib: ["ESNext", "DOM"]` no `tsconfig`. Se o `protocol` está com `lib: ["ESNext"]` puro (sem DOM), adicionar `"DOM"` (ou `"WebWorker"` que também tem).
- `decodeUtf8` com `fatal: true` lança em entrada inválida. Se algum teste envia bytes inválidos, o teste precisa tratar — diferente do impl atual que silenciosamente substitui por `?`.
- O bug M1 (peer injection) SÓ é visível em testes com **3+ peers** no SimNetwork. Os testes existentes usam 2 — precisam ser estendidos.

1. **[TDD]** Adicione os casos 1, 2, 3, 6, 7 em `rbsr.exchange.test.ts` (ou em novo `rbsr.exchange.hardening.test.ts`). Adaptar `makeSession` para receber `clock: VirtualClock`.
2. **Verifique** se `@plataforma/testkit` reexporta `VirtualClock` e se ele implementa `ClockPort` (sim — `ports.ts:72-78` é o contrato e `testkit` é o adapter de teste).
3. **Edite** `packages/protocol/src/rbsr/exchange.ts`:
   - Remova `declare function setTimeout/clearTimeout` (linhas 8-10).
   - Adicione `textEncoder`/`textDecoder` no escopo do módulo.
   - Substitua `encodeUtf8`/`decodeUtf8` por `textEncoder.encode`/`textDecoder.decode`.
   - Adicione `clock: ClockPort` em `RBSRSession`.
   - `waitForMessage` agora recebe `expectedPeer: PeerId` e `clock: ClockPort`; filtra por peer e usa `clock.setTimeout/clearTimeout`.
   - `reconcile` passa `session.clock` e `session.remotePeerId` para `waitForMessage`.
4. **Edite** `packages/transport/src/syncCoordinator.ts`:
   - Remova a linha 7 (`const MSG_ROOT_FINGERPRINT = 0x01;`).
   - Importe `MSG_ROOT_FINGERPRINT` de `@plataforma/protocol`.
   - Adicione `clock: this.clock` no `RBSRSession` criado em `startSession` (linha 132-138).
5. **Verifique** `packages/protocol/tsconfig.json` — adicionar `"DOM"` em `lib` se necessário.
6. **Rode** `pnpm --filter @plataforma/protocol build && pnpm --filter @plataforma/protocol test && pnpm --filter @plataforma/transport build && pnpm --filter @plataforma/transport test`. Esperado: 13+ testes verdes (6 existentes adaptados + 5-7 novos).
7. **Coordene** com T-1032/T-1033 — todos mexem em `exchange.ts`; garantir que os branches não conflitam. Se em paralelo, sequencializar.

### Pegadinhas conhecidas *(preencher pelo Task Architect — armadilhas que derrubam um modelo leve)*
*(Liste aqui os erros prováveis e como evitá-los. Ex.: "mudar uma assinatura síncrona para `async`*
*exige `await` em TODOS os callers (controller, rota REST, MCP tools)"; "mapear `A.foo → bar`*
*ao passar para o método X"; "não duplicar a lógica de Y — chamar o método existente Z".)*
- *[Nenhuma identificada]*

1. **[TDD]** Escreva o teste em `...`
2. Implemente `...`
3. Refatore.

## 6. Feedback de Especificação (Spec Feedback Loop)
> **ATENÇÃO:** Se a spec (RAG) for ambígua, contraditória ou o design pattern imposto for impossível, **PARE**. Mude o status para `blocked` e escreva o motivo abaixo. Não alucine uma abstração não documentada.
- *[Nenhum problema identificado]*

## 7. Definition of Done (DoD) & Reviewer Checklist
O agente `agile_reviewer` usará esta checklist para aprovar ou rejeitar o PR:
- [ ] O código segue estritamente os arquivos de Output especificados (sem criar arquivos não solicitados)?
- [ ] O `pnpm test` roda sem erros no ambiente especificado (Node/JSDOM)?
- [ ] Linter (`pnpm lint`) não acusa problemas?
- [ ] A implementação respeita a Regra do Que Não Fazer?

### Verificação automática *(comandos exatos — worker E reviewer rodam e COLAM a saída)*
```bash
pnpm --filter @plataforma/protocol build      # tsc — precisa terminar sem erro (lib DOM adicionado)
pnpm --filter @plataforma/protocol test       # vitest run — 13+ testes verdes (6 adaptados + 7 novos)
pnpm --filter @plataforma/protocol lint       # eslint src/ — 0 erros
pnpm --filter @plataforma/transport build     # tsc — syncCoordinator compila com import único
pnpm --filter @plataforma/transport test      # vitest run — sem regressão
```
> **GATE DE EVIDÊNCIA:** nem o `finish` (worker) nem o veredito (reviewer) são válidos sem a
> saída literal desses comandos colada na seção 8. Marcar `[x]` sem evidência é violação.

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- **Fix M1:** `waitForMessage` filtra por `peerId`, ignorando mensagens de outros peers.
- **Fix m1:** `encodeUtf8`/`decodeUtf8` → `TextEncoder`/`TextDecoder` (stdlib). `RBSRSession` + `clock: ClockPort`, `waitForMessage` usa `clock.setTimeout/clearTimeout`.
- **Fix m2:** `MSG_ROOT_FINGERPRINT` exportado do protocol, removido do syncCoordinator.
- **tsconfig:** protocol adicionou `lib: ["DOM"]`. Testes adaptados (protocol usa `realClock()`, transport usa `clock.advance()`).
- Protocol 118/118 + Transport 128/128 testes verdes.

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória — colar saída de build/tsc + test):**
```
pnpm --filter @plataforma/protocol build → OK
pnpm --filter @plataforma/protocol test  → 13 files | 118 tests | 0 failed
pnpm --filter @plataforma/transport build → OK
pnpm --filter @plataforma/transport test  → 13 files | 128 tests | 0 failed
```
- **Comentários de Revisão:**

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
- **[2026-07-02T19:22]** - *minimax-m3* - `[Promovida p/ ready]`: spec_status hardened — flip draft→ready
- **[2026-07-03T14:28]** - *deepseek* - `[Iniciado]`: iniciando hardening do wire RBSR exchange
- **[2026-07-03T14:56]** - *deepseek* - `[Finalizado]`: hardening RBSR wire: peer filter (M1), ClockPort, TextEncoder, MSG_ROOT_FINGERPRINT dedup. protocol 118/118 + transport 128/128 tests pass
