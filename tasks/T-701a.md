---
id: T-701a
title: "device_state.db: DDL e migrations"
status: done
spec_status: hardened
complexity: 2
target_agent: core_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-004", "T-106"]
blocks: ["T-707"]
capacity_target: haiku
hardened_at: "2026-06-29T19:35:00-03:00"
---

# T-701a · device_state.db: DDL e migrations

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** haiku

## 1. Objetivo
Criar o schema oficial SQLite e as migrations versionadas para o banco de dados de estado local isolado (`device_state.db`) no pacote `@plataforma/core`, aplicando-as via `StoragePort.migrate()`.
*(derivado de `docs/caderno-3-sdk/01-sqlite-and-projections-schema.md` §4.1; `tasks/T-707.md` e `tasks/T-313c.md`)*.

### Contratos exatos (DDL e assinaturas TS fixadas)

```ts
// --- packages/core/src/deviceState/schema.ts ---
import type { StoragePort } from '@plataforma/protocol';

export interface DeviceStateMigration {
  version: number;
  sql: string;
}

export const DEVICE_STATE_MIGRATIONS: DeviceStateMigration[] = [
  {
    version: 1,
    sql: `
      CREATE TABLE IF NOT EXISTS drafts (
        id          TEXT    NOT NULL PRIMARY KEY,
        payload     BLOB    NOT NULL,
        updated_at  INTEGER NOT NULL
      );

      CREATE TABLE IF NOT EXISTS prefetch_cache (
        key         TEXT    NOT NULL PRIMARY KEY,
        blob        BLOB    NOT NULL,
        fetched_at  INTEGER NOT NULL,
        ttl_ms      INTEGER
      );

      CREATE TABLE IF NOT EXISTS ui_preferences (
        key         TEXT    NOT NULL PRIMARY KEY,
        value       TEXT    NOT NULL,
        updated_at  INTEGER NOT NULL
      );

      CREATE TABLE IF NOT EXISTS peer_history (
        peer_id        TEXT    NOT NULL PRIMARY KEY,
        last_multiaddr TEXT,
        last_seen_at   INTEGER NOT NULL,
        success_count  INTEGER NOT NULL DEFAULT 0,
        failure_count  INTEGER NOT NULL DEFAULT 0
      );

      CREATE INDEX IF NOT EXISTS idx_peer_history_seen ON peer_history(last_seen_at);
    `
  },
  {
    version: 2,
    sql: `
      CREATE TABLE IF NOT EXISTS blind_archives (
        archive_id     BLOB    NOT NULL PRIMARY KEY,
        blind_scope_id BLOB    NOT NULL,
        manifest       BLOB    NOT NULL,
        ciphertext     BLOB    NOT NULL,
        expires_at     INTEGER NOT NULL,
        created_at     INTEGER NOT NULL
      );
    `
  }
];

/** Aplica todas as migrations pendentes do device_state via StoragePort. Idempotente. */
export async function migrateDeviceStateSchema(storage: StoragePort): Promise<void>;
```

## 2. Contexto RAG
- [caderno-3-sdk/01-sqlite-and-projections-schema.md §4.1](../docs/caderno-3-sdk/01-sqlite-and-projections-schema.md) — matriz de classificação
- [T-004 · Adaptador de Armazenamento](./T-004.md) — `StoragePort`
- [T-106 · Schema SQLite local (nodes/edges)](./T-106.md) — padrão de migrations
- [T-313c · Archive Cargo: GC + migration blind_archives](./T-313c.md) — blind_archives table definition

## 3. Escopo de Arquivos
- **[READ]** `packages/protocol/src/ports.ts` (StoragePort — T-004)
- **[READ]** `packages/core/src/schema.ts` (T-106)
- **[CREATE]** `packages/core/src/deviceState/schema.ts`
- **[CREATE]** `packages/core/tests/deviceState/schema.test.ts`
- **[UPDATE]** `packages/core/src/index.ts` — re-exportar `migrateDeviceStateSchema` e `DEVICE_STATE_MIGRATIONS`

## 4. Estratégia de Testes
- [x] **Framework:** Vitest (Node puro).
- [x] **Ambiente:** `pnpm --filter @plataforma/core test`. SQLite `:memory:` para migrations.

Casos de teste:
1. `migrateDeviceStateSchema` delega para `storage.migrate` com `DEVICE_STATE_MIGRATIONS`.
2. Rodar DDL contra SQLite real (`better-sqlite3`): tabelas `drafts`, `prefetch_cache`, `ui_preferences`, `peer_history` e `blind_archives` criadas com sucesso.
3. `migrateDeviceStateSchema` é idempotente (rodar 2× não quebra).
4. O índice `idx_peer_history_seen` existe e está ativo.

## 5. Instruções de Execução
1. Escreva os testes unitários em `packages/core/tests/deviceState/schema.test.ts`.
2. Implemente o arquivo `packages/core/src/deviceState/schema.ts`.
3. Exportar tudo no index.ts.
4. Rode build e test.

## 6. Feedback de Especificação
- *(Nenhuma decisão em aberto)*

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/core build
pnpm --filter @plataforma/core test
```

### Checklist
- [ ] `migrateDeviceStateSchema` implementada e exportada?
- [ ] As 5 tabelas (`drafts`, `prefetch_cache`, `ui_preferences`, `peer_history`, `blind_archives`) são criadas nas migrations?
- [ ] Idempotência garantida?
- [ ] 4 casos de teste passando?

## 8. Log de Handover e Revisão Agile
### Handover do Executor:
- 

### Parecer do Agente Revisor:
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução:**
```
$ pnpm --filter @plataforma/core build
$ tsc
✓ build passou sem erros

$ pnpm --filter @plataforma/core test

 RUN  v3.2.6 packages/core

 ✓ tests/ucan.test.ts (18 tests) 212ms
 ✓ tests/ulid.test.ts (13 tests) 9ms
 ✓ tests/signature.test.ts (10 tests) 107ms
 ✓ tests/merge.test.ts (16 tests) 192ms
 ✓ tests/schema.test.ts (7 tests) 14ms
 ✓ tests/lineage.test.ts (14 tests) 377ms
 ✓ tests/snapshot.test.ts (16 tests) 199ms
 ✓ tests/deviceState/schema.test.ts (4 tests) 12ms
 ✓ tests/mock.test.ts (1 test) 2ms

 Test Files  9 passed (9)
      Tests  99 passed (99)
   Duration  1.2s
```

**Checklist:**
- [x] `migrateDeviceStateSchema` implementada e exportada?
- [x] As 5 tabelas (`drafts`, `prefetch_cache`, `ui_preferences`, `peer_history`, `blind_archives`) são criadas nas migrations?
- [x] Idempotência garantida?
- [x] 4 casos de teste passando?
```

### Parecer do Reviewer 2 (minimax-m3, independente):
- [x] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (2ª revisão independente, Gate reexecutado):**
```
$ pnpm --filter @plataforma/core build
$ tsc
(sem erros)

$ pnpm --filter @plataforma/core test
 RUN  v3.2.6 C:/Dev2026/.superapp-worktrees/T-701a/packages/core

 ✓ tests/ucan.test.ts (18 tests) 243ms
 ✓ tests/hlc.test.ts (10 tests) 32ms
 ✓ tests/signature.test.ts (10 tests) 143ms
 ✓ tests/merge.test.ts (16 tests) 271ms
 ✓ tests/snapshot.test.ts (16 tests) 208ms
 ✓ tests/lineage.test.ts (14 tests) 500ms
 ✓ tests/schema.test.ts (7 tests) 13ms
 ✓ tests/deviceState/schema.test.ts (4 tests) 12ms
 ✓ tests/mock.test.ts (1 test) 2ms
 ✓ tests/dbOwner.test.ts (5 tests) 1634ms
 ✓ tests/ulid.test.ts (13 tests) 5ms
 ✓ tests/keyVault.test.ts (11 tests) 5ms

 Test Files  12 passed (12)
      Tests  125 passed (125)
   Duration  2.64s

$ pnpm --filter @plataforma/core lint
$ eslint src/
(sem erros)
```

**Sondas adversariais** (7 escritas em `schema.probe.test.ts`, todas verdes, removidas após):
- P1: ordem das migrations entregues ao `StoragePort.migrate` é `[1, 2]` (cumulatividade v1→v2) — OK
- P2: array `DEVICE_STATE_MIGRATIONS` tem exatamente 2 versões com versões 1 e 2 — OK
- P3: `drafts` tem `id TEXT PK`, `payload BLOB`, `updated_at INTEGER` (PK confirmada via `pk=1`) — OK
- P4: `blind_archives` tem 6 colunas (`archive_id`, `blind_scope_id`, `ciphertext`, `created_at`, `expires_at`, `manifest`); `archive_id` é BLOB PK — OK
- P5: `peer_history.success_count`/`failure_count` têm `DEFAULT 0` — OK
- P6: `prefetch_cache.ttl_ms` aceita `NULL` (sem `NOT NULL` no DDL) — OK
- P7: rodar `migrateDeviceStateSchema` 3× não duplica o índice `idx_peer_history_seen` — OK

**Auditoria de escopo e conformidade:**
- Diff de `master..HEAD`: 12 arquivos. Commits do worker (`fb7c34e` wip + `d58eb3a` feat) tocam **apenas** os 3 arquivos declarados no §3 (`deviceState/schema.ts`, `src/index.ts`, `tests/deviceState/schema.test.ts`). Os outros 9 arquivos (bancada/*, transport/*, lockfile) vêm de merges de base (`T-006a`, `T-207`, `T-312-rework-3`) anteriores ao trabalho do worker — não são alterações de T-701a.
- Assinaturas TS batem literal com §1: `interface DeviceStateMigration { version: number; sql: string }`, `migrateDeviceStateSchema(storage: StoragePort): Promise<void>`, re-export correto de `DEVICE_STATE_MIGRATIONS`/`migrateDeviceStateSchema`/`DeviceStateMigration` no `index.ts:72-73`.
- DDL: as 5 tabelas estão presentes com tipos/colunas batendo §1 (snake_case, INTEGER para timestamps, BLOB para payloads, TEXT para PKs de string); índice `idx_peer_history_seen` em `peer_history(last_seen_at)` ✓; v2 separado da v1 ✓.
- Idempotência: `IF NOT EXISTS` em todas as 5 tabelas + no índice; spec diz "rodar 2× não quebra" — probe P7 valida 3× sem duplicação de índice.
- Ripples: `StoragePort.migrate(migrations: Array<{version, sql}>)` é o contrato de T-004 — `DEVICE_STATE_MIGRATIONS: DeviceStateMigration[]` é estruturalmente compatível (passa o type-check).
- Lint: clean.

**Achados:**

```
BLOCKER (0)
MAJOR  (0)
MINOR  (0)
INFO   (1)
────────────────────────────────────────────────────
[i1] T-313c (status: ready) referencia T-106 para adicionar a migration `blind_archives`,
     mas o destino real é `device_state.db` (T-701a), e T-313c usa colunas camelCase
     (archiveId, blindScopeId, expiresAt, createdAt) enquanto T-701a padroniza snake_case
     (archive_id, blind_scope_id, expires_at, created_at). Não é bloqueante para T-701a
     (a tabela está consistente com §1), mas o arquiteto de T-313c vai precisar alinhar
     antes da execução dela. Sugestão: reescrever a DDL de T-313c para snake_case e
     mirar DEVICE_STATE_MIGRATIONS (não MIGRATIONS de T-106).
```

**VEREDICTO: APROVADO.**
Concordo com o Reviewer 1. A spec foi seguida literalmente, Gate verde, sondas adversariais
não quebraram. Único achado é uma issue de coordenação em T-313c (fora do escopo desta task).

**Divergência do parecer anterior:** Nenhuma. Reviewer 1 já aprovou; minha revisão independente
chega à mesma conclusão com sondas ativas adicionais (P1-P7) que cobrem bordas não tocadas
pelos 4 casos originais (defaults, tipos via PRAGMA, ordem, 3× idempotência).

## 9. Log de Execução
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.

- **[2026-06-29T22:37]** - *devops_agent* - `[Bloqueado]`: Especificacao resolvida, preparando para ready
- **[2026-06-29T22:38]** - *devops_agent* - `[Desbloqueado]`: Especificacoes prontas para execucao
- **[2026-06-30T17:34]** - *deepseek* - `[Iniciado]`: iniciando device_state.db DDL e migrations
- **[2026-06-30T17:37]** - *deepseek* - `[Finalizado]`: device_state.db DDL — 4/4 testes deviceState, 99/99 suite core, build+test verdes
- **[2026-07-01T10:16]** - *agile_reviewer:minimax-m3* - `[Aprovado]`: Integrado: merge na master (commit 2416dfb), worktree removida, Gate verde (pnpm --filter @plataforma/core build: tsc sem erros; test: 12 files / 125 tests passed; lint: clean). 1 INFO nao-bloqueante -> ledger de pendencias. Veredito da 2a revisao independente (minimax-m3) coincide com Reviewer 1.
