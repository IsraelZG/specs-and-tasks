---
id: T-201
title: "Wire format v1"
status: draft
complexity: 3
parent_task: null
subtasks: ["T-201a","T-201b"]
dependencies: ["T-101"]
blocks: ["T-201a","T-201b"]
target_agent: protocol_agent
reviewer_agent: "agile_reviewer"
execution_mode: sequential
---

# T-201 · Wire format v1

> **DECOMPOSTA** — não executar diretamente. O trabalho está nas subtarefas: T-201a, T-201b

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm` (NÃO USE npm ou yarn)
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro, protocol é isomórfico)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar encoder/decoder do wire protocol v1: **MessagePack + Length-Prefixed Framing** com versionamento e quarentena de versão futura
*(derivado de `docs/caderno-2-protocol/05-wire-protocol.md` — origem normativa RFC-005 §A.2; `docs/plano-de-implementacao.md` §3 T-201)*.

### Contratos exatos (assinaturas TS fixadas)

```ts
// --- packages/protocol/src/wireFormat.ts ---

/** Versão corrente do protocolo. Derivado de caderno-2-protocol/05-wire-protocol.md §1. */
export const WIRE_VERSION = 0x01;

/** Tipos de frame — tabela do caderno-2-protocol/05-wire-protocol.md §1. */
export enum FrameType {
  RBSR_ROOT          = 0x01,
  RBSR_RANGE         = 0x02,
  RBSR_REQUEST_NODES = 0x03,
  RBSR_NODES         = 0x04,
  EPHEMERAL          = 0x05,
  HEARTBEAT_PING     = 0x06,
  HEARTBEAT_PONG     = 0x07,
  IDENTITY_CATCHUP   = 0x08,
  KEY_REQUEST        = 0x09,
  KEY_RESPONSE       = 0x0A,
  CONTROL            = 0x0B,
}

/** Teto de tamanho de frame (1 MiB). Caderno §1. */
export const MAX_FRAME_LENGTH = 1_048_576;

/** Frame decodificado. */
export interface WireFrame {
  version: number;
  frameType: FrameType;
  payload: unknown; // MessagePack-decoded (any JSON-compatible)
}

/** Resultado de decode: frame válido ou rejeição/quarentena. */
export type DecodeResult =
  | { ok: true; frame: WireFrame }
  | { ok: false; reason: 'too_large' | 'bad_length' | 'unknown_version' | 'parse_error'; retentionState?: number; raw?: Uint8Array };

/** Codifica payload MessagePack em frame binário. */
export function encodeFrame(frameType: FrameType, payload: unknown): Uint8Array;

/** Decodifica frame binário. Nunca lança — versão futura retorna {ok:false} com retentionState. */
export function decodeFrame(buffer: Uint8Array): DecodeResult;

/** Lê frame de stream incremental (buffer + socket). Retorna {frame, consumed} ou null se incompleto. */
export function tryReadFrame(buffer: Uint8Array): { frame: DecodeResult; consumed: number } | null;
```

### Regras de quarentena (derivado do caderno §3)
- Versão > WIRE_VERSION + frame volátil (`EPHEMERAL`, `HEARTBEAT_*`, `CONTROL`) → descartar (`{ok: false, reason: 'unknown_version'}`).
- Versão > WIRE_VERSION + frame auditável → `{ok: false, reason: 'unknown_version', retentionState: 3, raw}`.
- Versão < 0x01 → `{ok: false, reason: 'unknown_version'}`.
- LENGTH > MAX_FRAME_LENGTH → `{ok: false, reason: 'too_large'}`.

## 2. Contexto RAG (Spec-Driven Development)
- [caderno-2-protocol/05-wire-protocol.md](../docs/caderno-2-protocol/05-wire-protocol.md) (normativo: frame físico, tipos, evolução de versão)
- [plano-de-implementacao.md](../docs/plano-de-implementacao.md) (§3 T-201)

## 3. Escopo de Arquivos (Inputs e Outputs)
- **[READ]** `docs/caderno-2-protocol/05-wire-protocol.md`
- **[CREATE]** `packages/protocol/src/wireFormat.ts` — encodeFrame, decodeFrame, tryReadFrame + tipos
- **[CREATE]** `packages/protocol/tests/wireFormat.test.ts`
- **[UPDATE]** `packages/protocol/src/index.ts` — re-export
- **[UPDATE]** `packages/protocol/package.json` — adicionar dep `msgpackr`

## 4. Estratégia de Testes Estrita (Test-Driven Development)
- [x] **Framework:** Vitest (Node puro).
- [x] **Ambiente do Teste:** Node puro, `pnpm --filter @plataforma/protocol test`.
- [x] **Fora de Escopo:** Socket real, WebSocket, handshake Noise.

Casos de teste (numerados):
1. `encodeFrame(RBSR_ROOT, {key:'val'})` + `decodeFrame` → roundtrip, `frameType` e `payload` preservados.
2. Frame vazio (`Uint8Array(0)`) → `{ok:false, reason:'bad_length'}`.
3. Frame com LENGTH > 1 MiB → `{ok:false, reason:'too_large'}`.
4. Versão `0x00` → `{ok:false, reason:'unknown_version'}`.
5. Versão `0xFF` + tipo `RBSR_NODES` (auditável) → `{ok:false, reason:'unknown_version', retentionState:3}`.
6. Versão `0xFF` + tipo `HEARTBEAT_PING` (volátil) → `{ok:false, reason:'unknown_version'}` (sem retentionState).
7. Payload MessagePack inválido → `{ok:false, reason:'parse_error'}`.
8. `tryReadFrame` com buffer parcial (3 bytes) → `null`; completa com resto → frame decodificado.
9. `tryReadFrame` com buffer contendo 2 frames → primeiro frame decodificado + `consumed` correto.
10. Fuzz: 1000 frames aleatórios (`crypto.getRandomValues`) — `decodeFrame` nunca lança.

## 5. Instruções de Execução (Step-by-Step)
> **⚠️ REGRAS DO QUE NÃO FAZER:**
> - **NÃO** use JSON.stringify. Serialização é MessagePack (`msgpackr`).
> - **NÃO** lance exceção em frame malformado — `decodeFrame` sempre retorna `DecodeResult`.
> - **NÃO** implemente lógica de rede (socket, connect) — apenas encode/decode.

1. **[TDD]** Crie `packages/protocol/tests/wireFormat.test.ts` com os 10 casos da Seção 4.
2. Adicione `msgpackr` em `packages/protocol/package.json`.
3. Implemente `encodeFrame` (LENGTH uint32 BE + VERSION + FRAME_TYPE + MessagePack payload).
4. Implemente `decodeFrame` (parse LENGTH → valida teto → parse VERSION/FRAME_TYPE → MessagePack decode → regras de quarentena).
5. Implemente `tryReadFrame` (buffer incremental: se LENGTH insuficiente → null; senão consume e retorna).
6. Re-exporte em `packages/protocol/src/index.ts`.
7. Rode build + test (Seção 7) e cole saída.

## 6. Feedback de Especificação (Spec Feedback Loop)
- *[Nenhum problema identificado — contratos derivados integralmente de caderno-2-protocol/05-wire-protocol.md]*

## 7. Definition of Done (DoD) & Reviewer Checklist

### Verificação automática (Gate de Evidência)
```bash
pnpm --filter @plataforma/protocol build
pnpm --filter @plataforma/protocol test
```
> **GATE DE EVIDÊNCIA:** Worker cola saída literal na Seção 8.

### Checklist do Reviewer
- [ ] `encodeFrame`/`decodeFrame`/`tryReadFrame` com assinaturas exatas da Seção 1?
- [ ] `FrameType` enum com 11 valores (0x01–0x0B)?
- [ ] Quarentena: versão futura + auditável → retentionState=3; volátil → descartado?
- [ ] 10 casos de teste passando (incluindo fuzz)?
- [ ] `pnpm --filter @plataforma/protocol build` e `test` verdes?

## 8. Log de Handover e Revisão Agile (Code Review)
### Handover do Executor:
- 

### Parecer do Agente Revisor (Reviewer):
- [ ] **Aprovado**
- [ ] **Requer Refatoração**
- **Evidência de Execução (obrigatória):**
```
(cole aqui a saída real de pnpm build e pnpm test)
```
- **Comentários de Revisão:**

## 9. Log de Execução (Agent Execution Log)
> **Agentes de IA:** Registrem aqui cada sessão de trabalho usando `node tools/scripts/manage-task.mjs`.
