---
id: T-305
title: "Sync dirigido por UCAN"
status: done
complexity: 3
target_agent: logic_agent
reviewer_agent: agile_reviewer
execution_mode: sequential
dependencies: ["T-302", "T-501"]
subtasks: ["T-305a","T-305b"]
blocks: ["T-305a","T-305b"]
spec_status: decomposed
capacity_target: sonnet
---

# T-305 · Sync dirigido por UCAN

> **DECOMPOSTA** — não executar diretamente. O trabalho está nas subtarefas: T-305a, T-305b

## 0. Ambiente de Execução Obrigatório
- **Runtime:** Node.js v20+
- **Package Manager:** `pnpm`
- **Monorepo:** Turborepo
- **Test Runner:** `vitest` (Node puro)
- **Capacidade-alvo:** sonnet

## 1. Objetivo
Implementar sync RBSR escopado por UCAN no pacote `@plataforma/protocol`: a B-Tree de fingerprints é filtrada pelo `edge_filter` do UCAN, impedindo vazamento de subgrafos não autorizados
*(derivado de `docs/visao-arquitetural.md` §8 — "sync dirigido por UCAN: peer sem permissão não recebe nem fingerprints do subgrafo"; T-302 — RBSR exchange; T-501 — UCAN)*.

### Contratos
```ts
// --- packages/protocol/src/auth/ucanScope.ts ---
import { SignedUcan, UcanCapability } from '@plataforma/core'; // T-501
import { RBSRBTree, Fingerprint } from '../rbsr/BTree.js'; // T-301

/** Filtra B-Tree de RBSR pelas capacidades do UCAN.
 *  Apenas nós cujo entity_id está no escopo do UCAN são incluídos no fingerprint. */
export function scopeRBSRTree(tree: RBSRBTree, ucan: SignedUcan): RBSRBTree;

/** Verifica se UCAN autoriza acesso a um entity_id específico. */
export function canAccess(ucan: SignedUcan, entityId: string, action: string): boolean;
```
- `scopeRBSRTree`: reconstrói B-Tree apenas com nós dentro dos `with` do UCAN.
- Peer sem UCAN válido → recebe B-Tree vazia (rootXor = 0).

## 2. Contexto RAG
- [visao-arquitetural.md](../docs/visao-arquitetural.md) (§8)
- [T-302 · RBSR](./T-302.md), [T-501 · UCAN](./T-501.md)

## 3. Escopo de Arquivos
- **[CREATE]** `packages/protocol/src/auth/ucanScope.ts`
- **[CREATE]** `packages/protocol/tests/ucanScope.test.ts`

## 4. Estratégia de Testes
- [x] **Framework:** Vitest (Node puro).

Casos de teste:
1. UCAN com escopo `entity:A` → `scopeRBSRTree` inclui apenas nós de A.
2. UCAN com escopo `entity:A` + `entity:B` → inclui A e B.
3. UCAN expirado → `canAccess` retorna false.
4. Sem UCAN → B-Tree vazia (rootXor = 0).
5. `canAccess` com ação não permitida → false.

## 5. Instruções de Execução
1. Escreva testes com UCANs mockados e RBSRBTrees populadas.
2. Implemente `scopeRBSRTree` e `canAccess`.
3. Re-exporte. Rode build + test.

## 6. Feedback de Especificação
- [Nenhum problema identificado]

## 7. DoD & Reviewer Checklist

### Gate de Evidência
```bash
pnpm --filter @plataforma/protocol build
pnpm --filter @plataforma/protocol test
```

### Checklist
- [ ] UCAN sem permissão → B-Tree vazia?
- [ ] `scopeRBSRTree` filtra por entity_id?
- [ ] 5 casos de teste passando?
- [ ] `pnpm --filter @plataforma/protocol build` e `test` verdes?

## 9. Log de Execução (Agent Execution Log)
- **[2026-06-30T00:13]** - *Israel* - `[Promovida p/ ready]`: decomposed — filhas T-305a + T-305b concluídas
- **[2026-06-30T00:13]** - *Israel* - `[Iniciado]`: registrando encerramento por decomposição
- **[2026-06-30T00:13]** - *Israel* - `[Finalizado]`: decomposed: work entregue via T-305a + T-305b (ambas done)
- **[2026-06-30T00:13]** - *agile_reviewer* - `[Aprovado]`: filhas T-305a + T-305b done — pai decomposed encerrado
