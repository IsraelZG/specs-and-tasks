{
  "name": "@plataforma/orchestrator",
  "private": true,
  "type": "module",
  "main": "src/agentAdapter.mjs",
  "version": "0.0.0",
  "description": "AgentAdapter in-process (Vercel AI SDK) — spike ORQ-08. Pacote isolado do Docs dep-free; node_modules próprio.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "smoke": "node --env-file=../../.env smoke.mjs",
    "selftest": "node --env-file=../../.env agent-adapter.poc.mjs --selftest",
    "test": "node --test tests/"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "~3.0.5",
    "@huggingface/transformers": "4.2.0",
    "ai": "~7.0.14",
    "headroom-ai": "0.22.4",
    "onnxruntime-node": "1.27.0",
    "zod": "~4.4.3"
  }
}
