# Triagem — rfc-010 (Protocolo de Plugins e Computação Distribuída)

**Achados:** 10 · **INCORPORAR:** 4 · **JA-COBERTO:** 3 · **UI->INVENTARIO:** 3 · **REJEITAR:** 0 · **REVISAR-HUMANO:** 0

## REVISAR-HUMANO (destaque)
_Nenhum._ Nenhum achado supersede/contradiz canônico ou cria mecânica nova de ontologia; todos cabem como refinamento incremental, cobertura existente ou item de UI.

## Tabela

| id | achado (review §) | veredito | destino (A.N / arquivo) | texto-proposto ou nota | status |
| :-- | :-- | :-- | :-- | :-- | :-- |
| 010-01 | §2 — Resiliência/fallback de SFU: ao cair o nó operador (plugin `infra`), reconexão precisa de sessão de sala persistida efemeramente no grafo para fallback rápido a P2P embutido | INCORPORAR | A.3 (Consequências, após item 3) | "**Sessão de sala é estado efêmero no grafo.** Uma sessão LiveKit publica um nó de sessão efêmero (TTL curto, governado por SPEC) com os participantes e o SFU corrente; na queda do plugin `infra` operador, os peers reconciliam por esse nó para religar — ao SFU substituto quando houver, ou a WebRTC bruto embutido dentro do limite de participantes. Sem nó de sessão vigente não há fallback automático: cai-se em `fato-negativo-verificável` de chamada interrompida." | [x] |
| 010-02 | §2 — Locks de fila assíncrona precisam de keep-alive/heartbeat agressivo para que aba de Browser Worker fechada libere a task rapidamente em vez de travar horas segurando o `ASSET:LOCK` | INCORPORAR | A.5 (item 2, complementar) | "O claim por [[asset-lock]] carrega **lease com heartbeat**: o worker renova o lock dentro de um intervalo curto governado por SPEC; expirado o lease sem renovação (ex.: aba do Browser Worker fechada), o lock é considerado solto e a task volta à fila para re-claim, sem intervenção. A idempotência por chave de requisição garante que re-claim após conclusão tardia seja no-op." | [x] |
| 010-03 | §4 — Aresta de execução `PERFORMED_BY` ligando o resultado de uma task ao worker que o assinou | INCORPORAR | A.5 (item 2, complementar) | "O nó de resultado liga-se ao executor por aresta **`PERFORMED_BY`** (resultado → persona do worker assinante), tornando a procedência da computação consultável e insumo direto do registro auditável de A.7." | [x] |
| 010-04 | §5 — Fim de vida: bundles expurgados de caches locais, mas o `SPEC:PLUGIN` permanece imutavelmente referenciado nas tasks em que atuou, garantindo auditoria retroativa | INCORPORAR | A.5 (item 2, complementar) | "O bundle binário é cacheável e expurgável localmente, mas o `SPEC:PLUGIN` que o produziu permanece imutavelmente referenciado por toda task que o invocou ([[linhagem-de-versoes]]): a procedência ('gerado pelo plugin X-vN') é auditável mesmo após o bundle sair dos caches." | [x] |
| 010-05 | Audit O-04 — A.6.3 atribui "cálculo de folha (RFC-010-consumidora)"; folha pertence ao domínio da RFC-014 (contábil/fiscal/RH) | INCORPORAR | A.6 (item 3, correção de referência) | Corrigir o exemplo normativo em A.6.3: trocar "cálculo de folha (RFC-010-consumidora)" por "cálculo de folha (consumidora — RFC-014)". O exemplo permanece válido (dado de classe restrita jamais elegível a site `external`); apenas a atribuição de domínio é corrigida para a RFC consumidora correta. | [x] |
| 010-06 | §4 — Nó `SPECIFICATION:PLUGIN` (manifesto assinado) | JA-COBERTO | — | A.1.1 define o plugin como nó `SPECIFICATION (kind: PLUGIN)` assinado e versionado; A.1.3 detalha o manifesto. | [x] |
| 010-07 | §4 — Nós transitórios de fila (tasks aguardando/alocadas via `ASSET:LOCK`) | JA-COBERTO | — | A.5.2 já modela task=nó governada por SPEC com claim via [[asset-lock]]. | [x] |
| 010-08 | §5 — Mutação por linhagem/version tracking + supersessão para rollback de patch | JA-COBERTO | — | A.1.1 atribui linhagem ([[linhagem-de-versoes]]) ao plugin de graça; supersessão/rollback é mecânica canônica de linhagem, não norma nova desta RFC. | [x] |
| 010-09 | §3 — `PrivacyClassIndicator` (atom): indica se o plugin acessa plaintext | UI->INVENTARIO | docs/rfcs/inventario-componentes-layouts.md | `PrivacyClassIndicator` — átomo — módulo Marketplace/Plugins (badge da classe-de-privacidade declarada no manifesto; vê plaintext? sim/não). | [x] |
| 010-10 | §3 — `PluginRequirementChecklist` (molecule) + `AsyncJobQueueManager` (organism) + layouts (Dashboard de Ativos Computacionais, App Store Segregada com selos Nativo do Browser × Exige Infra) | UI->INVENTARIO | docs/rfcs/inventario-componentes-layouts.md | `PluginRequirementChecklist` — molécula — Marketplace/Plugins (checa GPU/Worker/Node 20+ vs perfil de recurso do manifesto); `AsyncJobQueueManager` — organismo — Marketplace/Plugins ou Painel de Ativos (tabela de progresso de renditions/embeddings da fila A.5); layouts: Dashboard de Ativos Computacionais (Browser nodes × Server nodes) e App Store Segregada de Plugins (selos de runtime). | [x] |

> Nota sobre 010-10: bullet composto de §3 mantido como um achado de inventário (uma molécula + um organismo + dois layouts correlatos do mesmo módulo). Desdobrar em linhas separadas do inventário ao aplicar.
