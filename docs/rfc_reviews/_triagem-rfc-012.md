# Triagem — rfc-012 (Marketplace e Negociação de Ativos)

**Achados:** 12 · **Vereditos:** INCORPORAR 4 · JA-COBERTO 3 · UI->INVENTARIO 5 · REJEITAR 0 · REVISAR-HUMANO 0

> **REVISAR-HUMANO:** nenhum. (O achado de rateio de perdas SCP — sênior/mezanino vs. pro-rata — foi julgado INCORPORAR como parâmetro Zen do payload da variante, sem criar mecânica de ontologia; não supersede canônico.)

> **Nota de auditoria (O-05):** rfc-012 §A.9.2 rotula a costura de anúncios como "Anúncios (RFC-014)"; o módulo de anúncios é a **RFC-015**. Correção de referência simples — ver linha `012-12`.

| id | achado (review §) | veredito | destino (A.N / arquivo) | texto-proposto ou nota | status |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 012-01 | §2 — TTL `fixed` é destrutivo p/ item escasso de alta concorrência (show); `risk_scaled` deve incluir "TTL por Pulso de Presença" do WebRTC (A.4) | INCORPORAR | A.4.3 (existente) | Acrescentar à política `risk_scaled`: "A política `risk_scaled` pode ancorar o TTL no **pulso de presença** do comprador (heartbeat WebRTC, RFC-018): enquanto há presença viva o lock se mantém; queda de presença encurta o TTL, liberando o head sem punir desconexão de boa-fé. Item escasso de alta concorrência (ingresso) é o caso-motivador — TTL `fixed` longo é destrutivo." | [x] |
| 012-02 | §2 — `risk_scaled` por presença trata desconexão de boa-fé sem travar o último item (A.4) | JA-COBERTO | A.4.3 | Coberto após 012-01: a mecânica de TTL selecionável por SPEC (`risk_scaled`) já é o vetor; 012-01 só especializa a âncora. Sem texto adicional. | [x] |
| 012-03 | §2 — Split multi-moeda: comprador BRL → vendedor USD exige **colar a conversão de câmbio na mesma saga** Tier 1 (A.5) | INCORPORAR | A.6.4 (existente) | Acrescentar: "Quando comprador e vendedor operam em moedas distintas, a conversão A→B (A.5.5) é **uma perna da mesma saga** (A.4.2), não uma operação solta: o lock no saldo de origem (BRL), a perna do conector de câmbio e o `CREDITS` no saldo de destino (USD) compartilham a janela de TTL e compensam juntos. Liquidação parcial cross-moeda é proibida — ou toda a saga finaliza, ou o lock expira e reverte." | [x] |
| 012-04 | §2 — Haircut/propagação de inadimplência SCP: rateio de perda entre cotistas é pro-rata ou por série sênior/mezanino? deve estar no payload da variante (A.8) | INCORPORAR | A.8.4 (existente) | Acrescentar: "A **regra de haircut** — quando a perda excede o fundo de reserva — é declarada Zen no payload da `SPEC:APORTE`: rateio **pro-rata** entre cotistas ou **subordinação por série** (sênior absorve por último, mezanino/júnior primeiro). A ordem de absorção é traversal sobre `RELATES:FINANCE:LASTRO` ponderado pela série declarada em cada cota; não há regra global de subordinação embutida no core." | [x] |
| 012-05 | §3 layout — CartDrawer Universal serve itens físicos e títulos de recebível (Shell RFC-026) | UI->INVENTARIO | inventario-componentes-layouts.md | `CartDrawer` — organismo — Marketplace (RFC-012) / Shell (RFC-026). Gaveta lateral de carrinho universal; renderiza qualquer classe de liquidação (bem, assinatura, instrumento financeiro). | [x] |
| 012-06 | §3 layout — Tabela Financeira Expansível (Tree-View: Venda → Split → Imposto → Comissão) | UI->INVENTARIO | inventario-componentes-layouts.md | `FinancialTreeTable` — organismo — Marketplace (RFC-012). Data-grid com tree-view expansível para decompor uma op de partida dobrada (`SPENDS`→N `CREDITS`). | [x] |
| 012-07 | §3 atoms — `CurrencyMoneyView` respeitando locale (RFC-009) | UI->INVENTARIO | inventario-componentes-layouts.md | `Money` / `CurrencyMoneyView` — átomo — Marketplace (RFC-012) / Jurisdição (RFC-009). Exibe valor monetário formatado por jurisdição/locale; já citado como atom `Money` em A.5.5. | [x] |
| 012-08 | §3 molecules — `CartItemRow` (renderiza `bem_serializado` ou `assinatura`) | UI->INVENTARIO | inventario-componentes-layouts.md | `CartItemRow` — molécula — Marketplace (RFC-012). Linha de carrinho polimórfica por classe de liquidação. | [x] |
| 012-09 | §3 molecules — `SagaLegStatus` (tickers visuais do checkout pending) | UI->INVENTARIO | inventario-componentes-layouts.md | `SagaLegStatus` — molécula — Marketplace (RFC-012). Indicador visual de estado de cada perna da saga (pending/confirmada/revertida). | [x] |
| 012-10 | §3 organisms — `CheckoutSagaWizard` (progressão das pernas do orquestrador local) | UI->INVENTARIO | inventario-componentes-layouts.md | `CheckoutSagaWizard` — organismo — Marketplace (RFC-012). Acompanha a progressão das pernas da saga de checkout no orquestrador local (A.4). | [x] |
| 012-11 | §4/§5 — nós/arestas (`INTENT`, `BALANCE_STATE`, `INVENTORY`/`PERMISSION`/`ROLE`, `SPENDS`/`CREDITS`, `BELONGS_TO`) e ciclo de vida (nascimento via comando, finalização na head, expiração de lock) | JA-COBERTO | A.1, A.2, A.4, A.5, A.9.1 | §4/§5 apenas descrevem a ontologia que a RFC já fixa: nós e arestas em A.1–A.5; ciclo de vida do lock/TTL em A.4.2; `BELONGS_TO` listing→produto em A.9.1/A.1.1. Nenhuma norma nova. | [x] |
| 012-12 | Auditoria O-05 — §A.9.2 rotula a costura de anúncios como "Anúncios (RFC-014)"; o módulo de anúncios é a **RFC-015** | INCORPORAR | A.9.2 (existente) | Corrigir a referência: trocar "**Anúncios (RFC-014):**" por "**Anúncios (RFC-015):**" no título do item A.9.2 (RFC-014 é contábil/fiscal/RH; anúncios é RFC-015). | [x] |

<!-- Σ vereditos = 12 = nº de achados. JA-COBERTO confirmados por grep/leitura da RFC. -->
