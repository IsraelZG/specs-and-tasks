# Triagem do review — `rfc-026` (Shell de Aplicação e Composição de Módulos)

**Contagens por veredito:** INCORPORAR 3 · JA-COBERTO 3 · UI->INVENTARIO 6 · REJEITAR 0 · REVISAR-HUMANO 0
**Total de achados:** 12 (Σ vereditos = 12)

### REVISAR-HUMANO em destaque
> **Nenhum.** Os três refinamentos normativos (render-sleep, drag-intent safety, estado multi-dispositivo) cabem no escopo da RFC sem contradizer canônico ou outra RFC; foram redigidos como adições/emendas diretas.

---

| id | achado (review §) | veredito | destino (A.N / arquivo) | texto-proposto ou nota | status |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 026-01 | §2 Render-sleep: colunas na pilha de colapsados devem entrar em modo Sleep/Unmount, liberando GPU/RAM, preservando só o estado em memória (sessão) | INCORPORAR | A.9 (emenda A.9.2) | "Suspender significa **render-sleep**: o componente do painel é **desmontado** (libera GPU/RAM — relevante para painéis pesados: mapas WebGL, vídeo, gráficos), enquanto o **estado de sessão permanece em memória** (efêmero local). Painéis na **pilha de colapsados** (A.4) sofrem a mesma suspensão; reabri-los **remonta** a partir do estado preservado, sem reinicializar a sessão." | [x] |
| 026-02 | §2 Drag-intent safety: drag que vira intent durável irreversível (ex.: arrastar para "Deletado") exige drop zones seguras + time-delay/undo antes de emitir o comando | INCORPORAR | A.6 (novo item A.6.3) | "Para comandos **irreversíveis** (deleção, transição de estado destrutiva), o gesto de drag/share **não** emite o intent imediatamente: a UI exige confirmação via **drop zones explícitas e destacadas** e oferece **time-delay** ou **undo** antes de materializar a mensagem de comando no protocolo. Gestos acidentais são recuperáveis; o intent durável só é emitido após a janela de desfazer." | [x] |
| 026-03 | §2 Estado multi-dispositivo: auto-save de `SPEC:WORKSPACE` não deve emitir broadcast de re-render para outros terminais ativos do mesmo perfil; serve como estado estático inicial da próxima sessão | INCORPORAR | A.1 (novo item A.1.4) | "O auto-save de `SPEC:WORKSPACE` **não** propaga broadcasts de re-render a outros dispositivos/terminais ativos do mesmo perfil: redimensionar ou rearranjar no dispositivo A **não** rearranja a sessão viva do dispositivo B (preservar foco). O workspace salvo é o **estado estático inicial** da **próxima** sessão a abri-lo — nunca um stream de mutações ao vivo entre dispositivos." | [x] |
| 026-04 | §1 Validação: UI como árvore FlexLayout + restrições declarativas + solver determinístico + command palette ("SO de janelas no browser") | JA-COBERTO | A.1, A.3, A.8 | RFC já define shell como árvore FlexLayout (A.1), restrições no manifesto + solver determinístico (A.3) e command palette + overlay (A.8). Validação positiva, sem objeção. | [x] |
| 026-05 | §4 Nós: `SPEC:WORKSPACE` salvo como snapshot no grafo de preferências do usuário | JA-COBERTO | A.1.2, A.1.3 | RFC já trata `SPEC:WORKSPACE` como layout salvo durável por usuário/perfil e o snapshot do estado vivo materializável (A.1.2/A.1.3). Sem norma nova. | [x] |
| 026-06 | §5 Ciclo de vida: nascimento no workspace default, mutações voláteis até intent "Salvar Workspace", deleção de perfis; sessões suspensas perdem tree local se não salvas | JA-COBERTO | A.1.2, A.9.2, A.11.3 | RFC já cobre: estado vivo efêmero vs. workspace salvo (A.1.2), suspensão preserva sessão efêmera local (A.9.2), sessões efêmeras não-persistidas não voltam (A.11.3). Descreve o que a RFC já diz. | [x] |
| 026-07 | §3 Resizer bars com snap points magnéticos (splitters entre colunas) | UI->INVENTARIO | `inventario-componentes-layouts.md` | `ResizerBar` (átomo · shell/composição) — splitter entre colunas com snap points magnéticos. | [x] |
| 026-08 | §3 Mobile footer transformável: rampa que sobe revelando menus modulares expansíveis | UI->INVENTARIO | `inventario-componentes-layouts.md` | `TransformableMobileFooter` (organismo · shell/composição) — footer-rampa que expande para menu modal full-screen (regime mobile, A.5). | [x] |
| 026-09 | §3 Atom `PanelGrabHandle` | UI->INVENTARIO | `inventario-componentes-layouts.md` | `PanelGrabHandle` (átomo · shell/composição) — alça de arraste de painel. | [x] |
| 026-10 | §3 Atom `CollapsedPanelTab` + `ShortcutChip` | UI->INVENTARIO | `inventario-componentes-layouts.md` | `CollapsedPanelTab` (átomo · shell/composição) — aba reabrível na pilha de colapsados (A.4); `ShortcutChip` (átomo · shell/composição) — chip de atalho na command palette. | [x] |
| 026-11 | §3 Molecule `OmnibarOmniSearch` (interface da command palette) | UI->INVENTARIO | `inventario-componentes-layouts.md` | `OmnibarOmniSearch` (molécula · shell/composição) — superfície de busca/intenção da command palette (A.8). | [x] |
| 026-12 | §3 Organism `FlexLayoutTreeSolver` (container que executa a matemática do layout) | UI->INVENTARIO | `inventario-componentes-layouts.md` | `FlexLayoutTreeSolver` (organismo · shell/composição) — container raiz que renderiza a árvore e executa o gerenciador determinístico de espaço (A.3). | [x] |
